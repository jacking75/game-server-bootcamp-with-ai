# Phase 2. 네트워크 프로그래밍 기초 (4~7주, 160h)

> 공통 70h / 트랙 90h. 이 Phase가 끝나면 학습자는 소켓 수준에서 TCP를 다루고, 패킷 프로토콜을 스스로 설계·구현하며, 수백 접속을 Windows에서 안정적으로 유지하는 서버를 만든다.

## 0. 이 문서 사용법

Phase 1과 같은 규칙이다. 매일 §2의 해당 일자 블록을 펼쳐 놓고 진행하고, 일자 블록이 호출하는 실습(`L2-C-xx`, `L2-CS-xx`, `L2-CPP-xx`)은 §3에서 찾아 그대로 수행한다. 과제(2-C, 2-1, 2-2, 2-3, 2-4)의 상세 명세는 §7에 있다.

| 표기 | 뜻 |
|---|---|
| `L2-C-xx` | Phase 2 공통 실습 |
| `L2-CS-xx` / `L2-CPP-xx` | C# / C++ 트랙 실습 |
| 🔴 | 건너뛰면 이후가 막히는 필수 |
| 🟡 | 시간 부족 시 미뤄도 되는 항목 |
| **DoD** | 그날의 완료 조건 |

이 Phase의 핵심 원칙 세 가지

1. **패킷 캡처 도구는 쓰지 않는다.** TCP의 동작은 (a) 서버 애플리케이션 로그(수신 바이트 수·타임스탬프), (b) `netstat -an` / `Get-NetTCPConnection`의 연결 상태, (c) 코드 기반 테스트로 확인한다. 이 방식이 오히려 "내 코드가 무엇을 보는가"에 집중하게 만든다.
2. **에코 → 프레이밍 → 채팅 순서를 지킨다.** 채팅 기능을 먼저 만들면 프레이밍 버그가 기능 버그로 위장된다.
3. **모든 조건은 수치로 증명한다.** "잘 돈다"가 아니라 "500접속·10분·유실 0·메모리 증가 10% 이내"를 로그로 남긴다.

---

## 1. 개요

### 1.1 목표

1. **TCP를 코드 관점에서 설명한다.** 3-way handshake, 4-way 종료, TIME_WAIT/CLOSE_WAIT, 흐름 제어, Nagle이 내 서버 코드의 어떤 증상으로 나타나는지 연결해서 말할 수 있다.
2. **프레이밍을 직접 구현한다.** 길이 접두 방식으로 분할·병합·손상 입력을 모두 방어하는 파서를 만들고 20개 이상의 테스트로 증명한다.
3. **트랙 API로 비동기 서버를 만든다.** C#은 `SocketAsyncEventArgs`, C++은 IOCP를 **직접** 다뤄 수백~천 접속을 유지한다.
4. **세션 수명을 안전하게 관리한다.** 수신 중 종료, 송신 중 종료, 종료 폭풍에서 크래시·누수 0을 만든다.
5. **부하를 스스로 만든다.** 테스트 클라이언트를 만들어 500접속·10분 시나리오를 재현하고 통계를 낸다.

### 1.2 선수 조건 자가 진단 (Phase 2 시작 전 20분)

| # | 질문 | 통과 기준 |
|---|---|---|
| 1 | Phase 1의 로그 큐를 60분 내 재구현할 수 있는가 | 실제로 해 봤고 통과함 |
| 2 | 오브젝트 풀(1-2)이 독립 라이브러리로 분리되어 있는가 | 다른 솔루션에서 참조 가능 |
| 3 | 데드락을 디버거 병렬 스택으로 찾을 수 있는가 | 스크린샷 보유 |
| 4 | Release 빌드로 벤치마크를 돌리고 분산을 표기할 수 있는가 | 리포트 1건 보유 |
| 5 | `netstat -an`의 출력에서 LISTENING/ESTABLISHED/TIME_WAIT를 구분할 수 있는가 | 즉답 |

3개 미만이면 Phase 1의 미완 과제부터 처리한다. 특히 2번이 안 되어 있으면 이번 Phase의 버퍼 관리가 전부 막힌다.

### 1.3 이 Phase가 끝나면 할 수 있는 것 (검증 가능한 형태)

- 빈 프로젝트에서 **에코 서버를 30분 안에** 작성한다(동기 버전)
- **프레이밍 파서를 90분 안에** 재구현하고 제공 테스트 10개를 통과시킨다
- "패킷이 잘려 온다"를 서버 로그로 재현하고, 파서가 어떻게 처리하는지 코드 경로로 설명한다
- 세션 종료 경쟁(수신 완료 콜백 중 close)이 왜 크래시로 이어지는지 시간 순서로 설명하고, 자기 코드의 방어를 짚는다
- 500접속·10분 부하 결과표(유실·지연 p50/p99·메모리 추세)를 제시한다

### 1.4 이 Phase의 산출물

```
phase2/
├─ PROTOCOL.md                  과제 2-C: 패킷 프로토콜 명세
├─ adr/0001-serialization.md    직렬화 선택 ADR
├─ PacketLib/                   과제 2-2: 프레이밍·직렬화 라이브러리 (독립 프로젝트)
│  ├─ src/  tests/(20개+)
├─ EchoServer/                  과제 2-1a(동기) / 2-1b(비동기)
├─ ChatServer/                  과제 2-1c: 채팅 서버
│  ├─ Net/                      네트워크 계층(세션·송수신)
│  ├─ Logic/                    채팅 로직(방·유저)  ← Net을 참조하지 않는다
│  ├─ tests/
│  └─ appsettings.json / config.toml
├─ LoadClient/                  과제 2-3: 테스트 클라이언트
├─ LOAD-2-1.md                  부하 결과 리포트
├─ COMPARE-2-1.md               동기 vs 비동기 비교표
└─ labs/                        실습 결과물
```

### 1.5 4주 로드맵 한눈에 보기

| 주차 | 큰 주제 | 공통 | C# 트랙 | C++ 트랙 | 과제 |
|---|---|---|---|---|---|
| 4주 | TCP와 동기 소켓 | 계층·TCP 동작·상태 관찰·소켓 API | `Socket` 동기 에코, 스레드-퍼-커넥션 | Winsock 동기 에코, 스레드-퍼-커넥션 | 2-C 초안, 2-1a |
| 5주 | 프레이밍과 비동기 I/O | 스트림 경계·프레이밍 설계·직렬화 비교 | TAP → SAEA, 송신 큐 | 논블로킹 → select → Overlapped → IOCP | 2-2, 2-1b |
| 6주 | 세션과 채팅 서버 | 세션 상태 머신·타임아웃·하트비트·종료 | MemoryPack 패킷, 방 로직 | 참조 카운트, AcceptEx, 방 로직 | 2-1c |
| 7주 | 부하와 안정화 | 부하 설계·통계·결과 해석 | 500접속 튜닝, 라이브러리 비교 읽기 | 500접속 튜닝, ASan 정리 | 2-3, 2-4🟡, 평가 |

---

## 2. 주차별 상세 계획 (일 단위)

일자 번호는 과정 전체 기준으로 이어진다(Phase 1이 1~15일차, Phase 2는 16~35일차).

### 2.1 4주차 — TCP와 동기 소켓

#### 16일차 (월) — 네트워크 계층과 용어 🔴

**오전 (2.5h)**
- 교재 "게임 서버 개발, 네트워크부터 이해하기" 1~3장: 용어, OSI/TCP-IP 계층, 패킷의 여행
- 핵심 정리: 캡슐화, IP·포트·NAT, MTU/MSS, 이더넷 프레임과 TCP 세그먼트의 관계
- 질문 3개를 스스로 만들고 답한다: "내 서버가 받은 100바이트는 어느 계층에서 어떻게 조립된 것인가?" 등

**오후 (2.5h)**
- `L2-C-01` 연결 상태 관찰 준비: 간단한 리스너를 띄우고 `netstat -an | findstr 9000`으로 LISTENING 확인
- `L2-C-02` 포트·주소 개념 실습: 같은 포트 두 번 바인드 → 오류 코드 확인, `SO_REUSEADDR` 동작 확인
- 과제 2-C 착수: `PROTOCOL.md` 뼈대(헤더 표 자리, 패킷 ID 체계 자리) 만들기

**AI 없는 1시간**
- 계층별로 "내 채팅 메시지 `hello`가 랜 카드까지 가는 동안 무엇이 붙는가"를 그림으로 그린다

**DoD**
- [ ] 계층 그림 1장(노트)
- [ ] 리스너가 뜨고 `netstat`에서 LISTENING 확인
- [ ] `PROTOCOL.md` 뼈대 커밋

#### 17일차 (화) — TCP 동작과 상태 전이 🔴

**오전 (2.5h)**
- 교재 "게임 서버 개발자가 알아야할 네트워크 이론" 전체 속독 후 다음을 정리: 3-way handshake, 4-way 종료, TIME_WAIT/CLOSE_WAIT, 시퀀스/ACK, 흐름 제어(윈도우), 혼잡 제어 개요, Nagle과 지연 ACK, keep-alive, RST
- 공식 문서(RFC 9293 요약 또는 learn.microsoft.com)와 대조해 책과 다른 곳 1개 이상 찾기

**오후 (2.5h)**
- `L2-C-03` 상태 전이 관찰: 접속→데이터→종료를 하면서 1초 간격으로 `Get-NetTCPConnection`을 찍어 상태 변화 표 만들기
- `L2-C-04` 누가 먼저 닫는가 실험: 서버 먼저 종료 / 클라이언트 먼저 종료 두 경우의 TIME_WAIT 위치 비교
- `L2-C-05` RST 만들기: 수신 버퍼에 데이터가 남은 채 `closesocket`(또는 `Socket.Close(0)`) → 상대가 받는 오류 확인

**AI 없는 1시간**
- "서버가 먼저 끊으면 왜 서버에 TIME_WAIT이 쌓이고, 그것이 왜 문제인가"를 5문장으로

**DoD**
- [ ] 상태 전이 표(최소 6행: SYN_SENT/ESTABLISHED/FIN_WAIT_1/FIN_WAIT_2/TIME_WAIT/CLOSE_WAIT)
- [ ] 서버·클라이언트 선종료 비교 표
- [ ] RST 재현 로그

#### 18일차 (수) — 동기 소켓 에코 서버 (과제 2-1a) 🔴

**오전 (2.5h)**
- 소켓 API 개념: 블로킹/논블로킹, `bind/listen/accept/connect/send/recv/shutdown/close`
- **부분 송신**의 존재: `send`가 요청보다 적게 보낼 수 있다 → 루프 필요
- 트랙 교재: C#은 "C# Socket 프로그래밍" 1~3장 / C++은 "TCP/IP Windows 소켓" 1~4장

**오후 (2.5h)**
- `L2-CS-01` 또는 `L2-CPP-01`: 동기 에코 서버 + 클라이언트 (스레드-퍼-커넥션)
- 요구: 접속마다 스레드 1개, `recv` 0바이트 = 정상 종료 처리, 부분 송신 루프, 예외/오류 코드 로그

**AI 없는 1시간**
- 에코 서버를 **빈 파일에서 30분 안에** 다시 작성한다(이 Phase의 기준 능력)

**DoD**
- [ ] 동기 에코 서버·클라이언트 동작, 100접속에서 정상
- [ ] 0바이트 수신 처리, 부분 송신 루프가 코드에 있다

#### 19일차 (목) — 동기 서버의 한계 측정 🔴

**오전 (2.5h)**
- 스레드-퍼-커넥션의 비용: 스레드 스택(기본 1MB), 컨텍스트 스위칭, 커널 객체
- 서버 I/O 모델 개론 시작: 준비 통지 vs 완료 통지

**오후 (2.5h)**
- `L2-C-06` 동기 서버 한계 측정: 접속 100 / 500 / 1,000을 시도하며 **스레드 수·작업 집합(메모리)·CPU·접속 성공률** 기록
  - 스레드 수: `Get-Process -Id <pid> | Select Threads` 또는 작업 관리자
  - 실패 지점(접속 거부, 응답 지연 급증)을 찾아 기록
- 결과를 `COMPARE-2-1.md`의 "동기" 열에 채운다

**AI 없는 1시간**
- "왜 1,000 스레드는 1,000 접속을 감당하지 못하는가"를 측정치로 3문단

**DoD**
- [ ] 접속 수 3구간 측정 표(스레드·메모리·CPU·성공률)
- [ ] `COMPARE-2-1.md` 동기 열 완료

#### 20일차 (금) — 프로토콜 명세 초안 + 주간 점검

**오전 (3h)**
- 과제 2-C 본격 작성(§7.1): 헤더 바이트 표, 패킷 ID 체계, 채팅 패킷 20종 목록, 세션 상태 머신, 하트비트 규칙
- 직렬화 후보 비교(수동/MemoryPack/MessagePack/Protobuf/FlatBuffers) → ADR 초안

**오후 (4h) — 주간 점검**
1. AI 없이 재구현(60분): 동기 에코 서버
2. 설명 시험(45분): handshake 4단계, TIME_WAIT, 부분 송신
3. 코드 리뷰(45분): 루브릭 4번(에러 처리) 중심
4. 주간 회고(30분): `notes/weekly/W04.md`

**DoD**
- [ ] `PROTOCOL.md` 초안(헤더·ID 체계·패킷 10종 이상)
- [ ] ADR 초안, 주간 회고

### 2.2 5주차 — 프레이밍과 비동기 I/O

#### 21일차 (월) — 스트림 경계 문제 재현 🔴

**오전 (2.5h)**
- TCP는 바이트 스트림이다: 메시지 경계가 없다. 길이 접두 vs 구분자 vs 고정 길이
- 헤더 설계 원칙: 길이 필드 위치·크기, 최대 패킷 크기, 엔디안(네트워크 바이트 오더), 정렬, 버전/플래그
- 링 버퍼 개념

**오후 (2.5h)**
- `L2-C-07` 분할 수신 재현: 64KB를 한 번에 `send` → 서버 수신 콜백마다 "받은 바이트 수·누적"을 로그 → **한 번에 다 안 온다**는 것을 로그로 확인
- `L2-C-08` 병합 수신 재현: 작은 메시지 100개를 연속 `send`(Nagle 켠 상태) → 한 번의 `recv`에 여러 개가 붙어 오는 로그 확인
- `L2-C-09` Nagle on/off 비교: `TCP_NODELAY` 유무로 작은 메시지 100개의 송신→수신 타임스탬프 차이 비교 표

**AI 없는 1시간**
- 위 두 현상을 처리하는 파서의 **의사코드**를 손으로 작성(상태: 헤더 대기 / 바디 대기)

**DoD**
- [ ] 분할·병합 로그 각각 저장(`labs/L2-C-07`, `08`)
- [ ] Nagle on/off 비교 표
- [ ] 파서 의사코드

#### 22일차 (화) — 프레이밍 라이브러리 구현 (과제 2-2) 🔴

**오전 (2.5h)** — 설계 먼저
- `PacketReader`/`PacketWriter` API 확정(§7.3의 시그니처 사용)
- 오류 처리 방침: **예외가 아니라 오류 코드 반환** → 호출자가 세션 종료 결정
- 버퍼 전략: 1-2 오브젝트 풀 사용, 최대 패킷 크기(예: 64KB) 상한

**오후 (2.5h)** — 구현·테스트
- `L2-C-10` `PacketReader` 구현: 누적 버퍼 + 헤더 파싱 + 부분 패킷 대기
- 테스트 8개 먼저 작성(1바이트씩 입력, 경계 정확히 걸치기, 2패킷 병합, 길이 0, 최대 초과, 헤더 미완, 바디 미완, 연속 호출)

**AI 없는 1시간**
- `PacketReader`를 빈 파일에서 다시 구현(테스트는 그대로 사용)

**DoD**
- [ ] `PacketReader` 테스트 8개 통과
- [ ] 오류 코드 체계(`Ok/NeedMore/TooLarge/BadLength/BadVersion`) 정의

#### 23일차 (수) — 직렬화와 패킷 정의 🔴

**오전 (2.5h)**
- 직렬화 비교: 수동 바이너리(`BinaryPrimitives`/`memcpy`), MemoryPack, MessagePack, Protobuf, FlatBuffers — 크기·속도·스키마 진화·언어 지원
- 교재 "네트워크 학습 로드맵" Chapter 7(응용 프로토콜·직렬화)

**오후 (2.5h)**
- `L2-C-11` 직렬화 비교 실험: 같은 패킷 5종을 2가지 방식으로 직렬화 → **바이트 크기·직렬화 시간·역직렬화 시간** 표
- 과제 2-2 계속: `PacketWriter` 구현 + 채팅 패킷 20종 정의(C#: MemoryPack / C++: 수동 또는 FlatBuffers)
- ADR 완성: 왜 이 직렬화를 택했는가(대안·기각 이유 포함)

**AI 없는 1시간**
- 자기 패킷 5종의 바이트 레이아웃을 손으로 그린다(오프셋·크기 표기)

**DoD**
- [ ] 직렬화 비교 표
- [ ] 패킷 20종 정의 완료, 라운드트립 테스트 통과
- [ ] `adr/0001-serialization.md` 완성

#### 24일차 (목) — 비동기 I/O 입문 (과제 2-1b 착수) 🔴

**C# 트랙 (5h)**
- `L2-CS-02` TAP 비동기 에코: `AcceptAsync/ReceiveAsync/SendAsync`(Task 기반), `Memory<byte>` 오버로드, 취소 토큰, `SocketException` 코드 처리
- `L2-CS-03` SAEA 입문: `SocketAsyncEventArgs` 1개로 에코 → **`ReceiveAsync`가 `false`를 반환하면 동기 완료**라는 것을 로그로 확인(이게 이 실습의 핵심)
- 교재 "C# Socket 프로그래밍" 4·10장

**C++ 트랙 (5h)**
- `L2-CPP-02` 논블로킹 + `select` 에코: `ioctlsocket(FIONBIO)`, `WSAEWOULDBLOCK` 처리, `fd_set` 64개 한계 확인
- `L2-CPP-03` Overlapped I/O 에코: `WSARecv/WSASend` + `OVERLAPPED` + 이벤트 방식, **버퍼 수명 규칙**(완료 전 해제 금지)
- 교재 "TCP/IP Windows 소켓" 9장(입출력 모델), `first_IOCP` 코드 읽기

**AI 없는 1시간**
- C#: SAEA 동기 완료 경로를 처리하지 않으면 무슨 일이 생기는지 코드로 설명
- C++: OVERLAPPED를 스택에 두면 왜 위험한지 3문장

**DoD**
- [ ] 비동기 에코가 동작(C#: TAP+SAEA / C++: select+Overlapped)
- [ ] 동기 완료(또는 즉시 완료) 경로를 로그로 확인

#### 25일차 (금) — 비동기 서버 완성 + 주간 점검

**오전 (3h)**
- C#: `L2-CS-04` SAEA 풀링 + 세션 클래스(수신 SAEA 1, 송신 SAEA 1) → 접속 1,000개 유지
- C++: `L2-CPP-04` IOCP 기본형: `CreateIoCompletionPort`, 워커 스레드 N개, `GetQueuedCompletionStatus`, Per-Handle/Per-I/O 데이터 → 접속 1,000개 유지
- 측정 후 `COMPARE-2-1.md`의 "비동기" 열 채우기(스레드 수·메모리·CPU·성공률)

**오후 (4h) — 주간 점검**
1. AI 없이 재구현(90분): 프레이밍 파서(§8.2 시험과 동일 형식)
2. 설명 시험(45분): 스트림 경계, 길이 접두의 취약점, 동기 완료/즉시 완료
3. 코드 리뷰(45분): 루브릭 3번(리소스) 중심 — 버퍼·SAEA·OVERLAPPED 수명
4. 회고(30분): `W05.md`

**DoD**
- [ ] 1,000 접속 유지, `COMPARE-2-1.md` 완성(동기 vs 비동기)
- [ ] 과제 2-2 테스트 15개 이상 통과

### 2.3 6주차 — 세션 관리와 채팅 서버

#### 26일차 (월) — 세션 상태 머신과 수명 🔴

**오전 (2.5h)**
- 세션 상태: `Connected → Named(인증 전/후) → Active → Closing → Closed`
- 종료 경쟁: 수신 완료 콜백 실행 중 다른 스레드가 close → 이중 해제·use-after-free
- 방어: C#은 참조 유지 + `Interlocked` 상태 전이, C++은 **참조 카운트**(진행 중 I/O 수) + `shared_ptr`
- `shutdown` vs `closesocket`, `SO_LINGER`

**오후 (2.5h)**
- `L2-C-12` 세션 상태 머신 구현: 상태 enum + 허용 전이표 + 불법 전이 로그
- `L2-C-13` 종료 경쟁 재현: 수신 처리 중 강제 close를 100회 반복 → 크래시/누수 관찰 → 방어 적용 후 재실행
- C++ 추가: `L2-CPP-05` 참조 카운트 세션(진행 중 I/O 카운터, 0이 될 때만 해제)

**AI 없는 1시간**
- "recv 완료와 close가 동시에 일어나는 시간 순서"를 다이어그램으로 그리고, 각 시점에서 필요한 방어를 적는다

**DoD**
- [ ] 상태 전이표와 불법 전이 테스트 5개
- [ ] 종료 경쟁 100회 반복에서 크래시 0

#### 27일차 (화) — 송신 큐와 백프레셔 🔴

**오전 (2.5h)**
- **동시 Send 금지**: 같은 소켓에 두 스레드가 동시에 보내면 데이터가 섞인다
- 세션별 송신 큐 + "진행 중 송신 1개" 플래그, 송신 완료 콜백에서 다음 항목 전송
- 송신 병합(여러 작은 패킷을 한 번에), 백프레셔(큐 상한 초과 시 정책: 끊기 vs 드롭)

**오후 (2.5h)**
- `L2-C-14` 송신 큐 구현 + 테스트: 100개 스레드가 같은 세션에 동시에 `Send` → 수신 측에서 **메시지 경계가 깨지지 않음**을 검증
- `L2-C-15` 백프레셔 테스트: 수신을 안 읽는 느린 클라이언트에 대량 송신 → 큐 상한 도달 시 정책대로 동작하는지 확인(메모리 폭증 없음)

**AI 없는 1시간**
- 송신 큐가 무한히 자라는 시나리오 3개를 적고 각각의 방어를 쓴다

**DoD**
- [ ] 동시 Send 테스트 통과(데이터 섞임 0)
- [ ] 느린 클라이언트에서 메모리 상한 유지 확인

#### 28일차 (수) — 채팅 로직 (과제 2-1c) 🔴

**오전 (2.5h)**
- 계층 분리 설계: `Net`(세션·송수신) ↔ `Logic`(방·유저). **Logic은 소켓을 모른다**(인터페이스로 송신)
- 방 관리 자료구조, 브로드캐스트 비용(N명에게 N번 직렬화하지 않기 — 한 번 직렬화 후 공유)

**오후 (2.5h)**
- `L2-C-16` 채팅 로직 구현: 방 생성/입장/퇴장/목록, 브로드캐스트, 귓속말, 인원 상한 100
- 로직 단위 테스트(네트워크 없이): 페이크 세션으로 방 입퇴장·브로드캐스트 검증 10개

**AI 없는 1시간**
- 방 목록 조회가 O(N)인 부분을 찾아 개선안을 적는다

**DoD**
- [ ] 방 기능 5종 동작, 로직 테스트 10개 통과
- [ ] Logic 프로젝트가 Net 프로젝트를 참조하지 않는다(빌드로 증명)

#### 29일차 (목) — 타임아웃·하트비트·종료 🔴

**오전 (2.5h)**
- 하트비트 설계: 클라이언트 10초 주기, 서버 30초 무응답 시 종료. 타이머 주기와 검사 비용
- 닉네임 미설정 30초 강제 종료(미인증 세션 정리 = 플러딩 방어의 기초)
- graceful shutdown 순서: 신규 접속 차단 → 전체 세션에 종료 통지 → 송신 완료 대기(상한) → 소켓 정리 → 스레드 조인

**오후 (2.5h)**
- `L2-C-17` 타임아웃 검사 타이머 구현(전체 세션 순회 vs 만료 시각 힙 비교)
- `L2-C-18` graceful shutdown 구현 + 테스트: 종료 신호 후 5초 내 전 스레드 종료, 로그에 순서 기록
- `L2-C-19` 잘못된 패킷 방어: 헤더 손상/크기 초과/상태 불일치 패킷 수신 → **해당 세션만** 종료되고 서버는 생존

**AI 없는 1시간**
- 종료 시퀀스를 순서도로 그리고 "이 순서가 바뀌면 생기는 문제"를 각 단계마다 1줄

**DoD**
- [ ] 하트비트 타임아웃 테스트(무응답 세션이 30초 내 정리)
- [ ] graceful shutdown 5초 내 완료 로그
- [ ] 손상 패킷 3종 테스트 통과

#### 30일차 (금) — 채팅 서버 통합 + 주간 점검

**오전 (3h)**
- 2-1c 통합: 프레이밍(2-2) + 세션 + 송신 큐 + 채팅 로직 + 설정 파일(포트·워커 수·버퍼 크기·타임아웃)
- 설정 파일로 포트를 바꿔 두 인스턴스를 동시에 띄워 본다(Phase 6 예습)

**오후 (4h) — 주간 점검**
1. AI 없이 재구현(90분): 세션 종료 경쟁 방어가 포함된 최소 세션 클래스
2. 설명 시험(45분): 종료 경쟁, 송신 큐, 하트비트 설계 근거
3. 코드 리뷰(45분): 루브릭 2·3번 중심 → 상위 3개 수정
4. 회고(30분): `W06.md`

**DoD**
- [ ] 채팅 서버가 설정 파일로 기동, 기능 6종 동작
- [ ] 회고 작성

### 2.4 7주차 — 부하 테스트와 안정화

#### 31일차 (월) — 테스트 클라이언트 (과제 2-3) 🔴

**오전 (2.5h)**
- 부하 테스트 설계: 램프업(초당 N접속), 정상 상태 유지, 종료 정리
- 지표: 접속 성공/실패, 송수신 메시지 수, **왕복 지연 p50/p95/p99**, 초당 처리량
- 로컬 루프백의 한계: 동적 포트 고갈, 클라이언트 자체 병목(CPU)

**오후 (2.5h)**
- `L2-C-20` 클라이언트 골격: 인자 파싱(`--host --port --conn --rampup --scenario --duration`), 접속 풀, 통계 수집기
- 시나리오 `chat` 구현: 접속 → 닉네임 → 방 입장(N/50 분산) → 1초마다 메시지 → 종료
- 지연 측정: 자기 메시지가 브로드캐스트로 돌아오는 시간(시퀀스 번호로 매칭)

**AI 없는 1시간**
- p99를 정확히 계산하는 방법(정렬 vs 히스토그램)을 비교하고 자기 구현을 선택·정당화

**DoD**
- [ ] 100접속으로 `chat` 시나리오 5분 동작, 통계 출력
- [ ] p99 계산이 검증됨(알려진 데이터로 단위 테스트)

#### 32일차 (화) — 500접속 부하와 1차 튜닝 🔴

**오전 (2.5h)**
- 500접속·10분 1차 실행 → 실패·병목 기록(접속 실패, 지연 급증, 메모리 증가, CPU 100%)
- 동적 포트 범위 확인: `netsh int ipv4 show dynamicport tcp`, 필요 시 확대

**오후 (2.5h)**
- 병목 가설 3개 세우고 각각 확인 방법 정하기(카운터·로그)
- 수정 후 재측정. 흔한 원인: 매 수신마다 할당(풀 미사용), 브로드캐스트 시 N번 직렬화, 로그 동기 쓰기, 송신 큐 락 경합
- 결과를 `LOAD-2-1.md`에 기록(1차/2차 비교)

**AI 없는 1시간**
- "지연 p99가 튀는 원인 후보"를 5개 적고 각각의 관찰 방법을 쓴다

**DoD**
- [ ] 500접속 10분 1회 이상 완주(실패해도 기록)
- [ ] 가설-수정-재측정 사이클 1회 이상 기록

#### 33일차 (수) — 종료 폭풍과 리소스 누수 🔴

**오전 (2.5h)**
- 종료 폭풍: 500개가 동시에 끊어질 때 발생하는 문제(참조 카운트 오류, 핸들 누수, 딕셔너리 잔존)
- 리소스 추적: 핸들 수(작업 관리자), 세션 수 카운터, 풀 미반환 수를 5초마다 로그

**오후 (2.5h)**
- `L2-C-21` `storm` 시나리오 구현(클라이언트) + 실행: 500접속 후 동시 종료 × 5회 반복
- 통과 조건: 크래시 0, 핸들 수가 시작 수준으로 복귀, 세션 수 0, 풀 미반환 0
- C++: ASan 빌드로 1회 통과(성능은 Release로 별도 측정)

**AI 없는 1시간**
- 누수 추적 카운터를 어디에 두어야 하는지(세션 생성/소멸, 버퍼 대여/반환) 설계를 적는다

**DoD**
- [ ] 종료 폭풍 5회 반복 후 핸들·세션·풀 카운터 정상 복귀
- [ ] C++는 ASan 통과 로그

#### 34일차 (목) — 안정화와 비교 읽기

**오전 (2.5h)**
- 500접속·10분 **최종 측정**: 유실 0, 메모리 증가 10% 이내(1분 시점 vs 10분 시점), p99 기록
- `LOAD-2-1.md` 완성(§7.2 리포트 형식)

**오후 (2.5h)**
- C#: `L2-CS-05` SuperSocketLite / FreeNetLite 구조 읽기 → 자기 서버와 **스레드 모델·버퍼 관리·세션 수명** 비교표
- C++: `L2-CPP-06` `IocpNetLib` 구조 읽기 → 같은 비교표. 🟡 Boost.Asio 샘플 빌드해 코드량 비교
- 🟡 과제 2-4(교차 통신) 수행

**AI 없는 1시간**
- 비교표를 보고 "내 서버에서 당장 고칠 것 3개"를 뽑아 이슈로 등록

**DoD**
- [ ] `LOAD-2-1.md` 완성(500접속·10분·종료 폭풍·메모리 추세)
- [ ] 라이브러리 비교표 1장

#### 35일차 (금) — Phase 2 평가 🔴

**오전 (3h)**
1. **재구현 시험(90분, AI 없음)**: §8.2 — 프레이밍 파서 + 제공 테스트 10개
2. **설명 시험(60분)**: §8.3 질문 은행에서 10문항
3. **결함 찾기(45분)**: §8.4 — 프레이밍·세션 종료·송신 큐 결함 5개 중 4개

**오후 (3h)**
- 체크리스트(§8.1) 점검, 미완 보충
- `W07.md` + Phase 2 회고
- §10 Phase 3 준비(공용 라이브러리 정리)

**DoD**
- [ ] §8.1 전 항목 완료, 설명 시험 평균 4.0 이상
- [ ] `PacketLib`·`LoadClient`가 다른 솔루션에서 참조 가능한 상태

---

## 3. 실습 카탈로그

결과물은 `phase2/labs/<실습번호>/`에 저장한다. 로그·표·스크린샷은 반드시 파일로 남긴다(설명 시험과 리포트의 근거가 된다).

### 3.1 공통 실습 (L2-C)

#### L2-C-01 리스너와 연결 상태 확인 (30분) 🔴
- **목표**: 서버 소켓의 상태를 OS 관점에서 본다
- **단계**: 포트 9000에 리스너 → `netstat -an | findstr 9000` 및 `Get-NetTCPConnection -LocalPort 9000` → 클라이언트 1개 접속 후 다시 확인
- **기대**: 접속 전 `LISTENING` 1행, 접속 후 `ESTABLISHED` 2행(서버측·클라이언트측)
- **확인 기준**: 두 시점의 출력을 파일로 저장, 각 행이 무엇인지 주석
- **노트**: 왜 ESTABLISHED가 2행인가(같은 머신 루프백)

#### L2-C-02 바인드 충돌과 SO_REUSEADDR (30분)
- **단계**: 같은 포트에 리스너 2개 → 오류 코드 확인(`WSAEADDRINUSE`/`SocketException 10048`) → `SO_REUSEADDR` 설정 후 재시도 → 서버 종료 직후 재바인드 시도(TIME_WAIT 영향)
- **확인 기준**: 오류 코드와 옵션 유무별 동작을 표로
- **주의**: Windows의 `SO_REUSEADDR`는 Linux와 의미가 다르다(하이재킹 가능). 노트에 차이를 적는다

#### L2-C-03 상태 전이 관찰 (45분) 🔴
- **단계**: 접속 → 데이터 송수신 → 정상 종료를 수행하면서 백그라운드에서 1초 간격으로 `Get-NetTCPConnection -LocalPort 9000 | Select State,LocalPort,RemotePort` 기록
- **기대**: `LISTEN → ESTABLISHED → FIN_WAIT_1/2 또는 CLOSE_WAIT → TIME_WAIT → 소멸`
- **확인 기준**: 최소 6개 상태가 담긴 시간순 표
- **노트**: TIME_WAIT의 기본 유지 시간과 그 이유

#### L2-C-04 누가 먼저 닫는가 (40분) 🔴
- **단계**: (a) 서버가 먼저 `close` (b) 클라이언트가 먼저 `close` 두 시나리오에서 TIME_WAIT이 **어느 쪽에** 생기는지 관찰. 각 100회 반복 후 `netstat -an | findstr TIME_WAIT | Measure-Object`로 개수 비교
- **확인 기준**: 두 경우의 TIME_WAIT 개수 표
- **노트**: 서버가 먼저 닫으면 왜 포트 고갈 위험이 커지는가

#### L2-C-05 RST 만들기 (30분)
- **단계**: 수신 버퍼에 데이터가 남은 상태에서 소켓을 닫거나 `SO_LINGER(0)` 설정 후 close → 상대 측 오류 확인(`WSAECONNRESET` / `SocketException 10054`)
- **확인 기준**: 정상 종료(FIN)와 강제 종료(RST)에서 상대가 받는 결과의 차이를 로그로

#### L2-C-06 동기 서버 한계 측정 (90분) 🔴
- **단계**: 접속 수 100 / 500 / 1,000으로 늘리며 스레드 수·작업 집합·CPU·접속 성공률·평균 응답 시간 측정(각 3회)
- **확인 기준**: 4열 표 + "어느 지점에서 무엇이 먼저 깨졌는가" 3줄
- **자주 나는 오류**: 클라이언트가 먼저 죽는다 → 클라이언트를 2개 프로세스로 분산

#### L2-C-07 분할 수신 재현 (40분) 🔴
- **단계**: 64KB 한 번 `send` → 서버는 수신 콜백마다 `[t, bytes, total]` 로그
- **확인 기준**: 로그에 recv 호출이 2회 이상, 합계가 65,536
- **노트**: "왜 한 번에 안 오는가"를 MSS·윈도우·커널 버퍼로 설명

#### L2-C-08 병합 수신 재현 (30분) 🔴
- **단계**: 20바이트 메시지 100개를 연속 `send`(Nagle 켬) → 서버 로그에서 한 번의 recv에 여러 메시지가 들어오는 것 확인
- **확인 기준**: 한 recv에 2개 이상 메시지가 담긴 사례 로그
- **노트**: 프레이밍 없이 "1 recv = 1 메시지"로 짜면 어떤 버그가 나는가

#### L2-C-09 Nagle on/off 비교 (40분)
- **단계**: `TCP_NODELAY` on/off 각각에서 20바이트 메시지 100개 전송, 클라이언트가 각 메시지의 송신 시각과 서버 수신 시각을 기록(같은 머신이므로 시계 공유)
- **확인 기준**: 평균/최대 지연 비교 표
- **노트**: 게임 서버에서 Nagle을 끄는 이유와, 끄면 손해인 경우

#### L2-C-10 PacketReader 구현 (120분) 🔴
- **목표**: 과제 2-2의 핵심. 어떤 조각으로 나뉘어 들어와도 정확히 패킷을 복원
- **단계**: ① 누적 버퍼 설계(링 버퍼 또는 확장 배열) ② 헤더 파싱 ③ 바디 대기 ④ 완성 시 슬라이스 반환 ⑤ 오류 코드 반환
- **확인 기준**: §7.3 테스트 20개 중 프레이밍 관련 12개 통과
- **자주 나는 오류**: 오프셋 계산 실수 → 경계 테스트(정확히 헤더 크기만큼, 헤더+1바이트)로 잡는다

#### L2-C-11 직렬화 비교 실험 (60분)
- **단계**: 패킷 5종(로그인 요청/응답, 채팅, 방 목록, 하트비트)을 두 방식으로 직렬화 → 바이트 크기·직렬화/역직렬화 시간(1만 회) 측정
- **확인 기준**: 표 + 선택 근거 3줄 → ADR에 반영

#### L2-C-12 세션 상태 머신 (60분) 🔴
- **단계**: 상태 enum, 전이표(현재 상태 × 이벤트 → 다음 상태), 불법 전이 시 로그+세션 종료
- **확인 기준**: 불법 전이 5종 테스트 통과(인증 전 채팅, 종료 중 입장 등)

#### L2-C-13 종료 경쟁 재현·방어 (90분) 🔴
- **단계**: 수신 처리 중 다른 스레드가 세션을 닫는 상황을 100회 반복 → 크래시/이중 해제 관찰 → 방어(참조 카운트 또는 상태 CAS) 적용 → 1,000회 재실행
- **확인 기준**: 방어 후 1,000회 무사고, C++은 ASan 통과

#### L2-C-14 송신 큐와 동시 Send 방지 (90분) 🔴
- **단계**: 100개 스레드가 같은 세션에 서로 다른 메시지를 동시에 `Send` → 수신 측에서 모든 메시지가 **온전한 형태로** 도착하는지 검증(경계 깨짐 0)
- **확인 기준**: 10회 반복 무결성 100%

#### L2-C-15 백프레셔 테스트 (60분)
- **단계**: 수신하지 않는 클라이언트를 만들고 서버가 대량 송신 → 송신 큐 상한 도달 시 정책(연결 종료 또는 드롭) 동작 확인, 서버 메모리 상한 유지
- **확인 기준**: 메모리 증가가 상한 내, 정책 로그 출력

#### L2-C-16 채팅 로직 구현 (120분) 🔴
- **단계**: 방 생성/입장/퇴장/목록/브로드캐스트/귓속말, 인원 상한. **네트워크 없이** 테스트 가능한 구조로
- **확인 기준**: 로직 테스트 10개 통과, Logic 프로젝트가 소켓 API를 참조하지 않음

#### L2-C-17 타임아웃 검사 (60분)
- **단계**: (a) 전체 세션 순회 방식 (b) 만료 시각 힙 방식 두 가지 구현 후 세션 5,000개 기준 검사 비용 비교
- **확인 기준**: 두 방식 비용 표 + 선택 근거

#### L2-C-18 graceful shutdown (60분) 🔴
- **단계**: 종료 신호(Ctrl+C 핸들러) → 신규 접속 차단 → 종료 통지 브로드캐스트 → 송신 완료 대기(최대 2초) → 소켓 정리 → 스레드 조인. 각 단계 로그
- **확인 기준**: 5초 내 완전 종료, 로그에 단계 순서가 남음

#### L2-C-19 손상 패킷 방어 (45분) 🔴
- **단계**: 클라이언트가 (a) 길이 필드에 0xFFFFFFFF (b) 길이 0 (c) 정의되지 않은 패킷 ID (d) 상태에 맞지 않는 패킷 전송
- **확인 기준**: 각 경우 해당 세션만 종료, 서버 생존, 로그에 사유 기록

#### L2-C-20 테스트 클라이언트 골격 (120분) 🔴
- **단계**: 인자 파싱 → 램프업 접속 → 시나리오 실행 → 5초마다 통계 출력 → 종료 시 파일 저장
- **확인 기준**: 100접속 5분 실행, 통계 파일 생성

#### L2-C-21 종료 폭풍 시나리오 (60분) 🔴
- **단계**: N접속 확보 후 전원 동시 종료를 5회 반복. 서버 측 핸들 수·세션 수·풀 미반환 수를 각 회차 전후로 기록
- **확인 기준**: 5회 후 카운터가 시작값으로 복귀

### 3.2 C# 트랙 실습 (L2-CS)

#### L2-CS-01 동기 에코 서버 (90분) 🔴
- **골격**
  ```csharp
  var listener = new Socket(AddressFamily.InterNetwork, SocketType.Stream, ProtocolType.Tcp);
  listener.Bind(new IPEndPoint(IPAddress.Any, 9000));
  listener.Listen(backlog: 512);
  while (true) {
      var client = listener.Accept();
      new Thread(() => HandleClient(client)) { IsBackground = true }.Start();
  }
  // HandleClient: Receive 루프, 0바이트면 종료, Send는 부분 송신 루프로
  ```
- **확인 기준**: 100접속 정상, 0바이트 처리, 부분 송신 루프 존재
- **노트**: `Listen(backlog)`의 의미와 값이 작으면 생기는 일

#### L2-CS-02 TAP 비동기 에코 (90분)
- **단계**: `AcceptAsync/ReceiveAsync/SendAsync`(Task 기반) + `Memory<byte>` 오버로드 + `CancellationToken`
- **확인 기준**: 1,000접속에서 스레드 수가 동기 버전보다 현저히 적음(표로 비교)

#### L2-CS-03 SAEA 동기 완료 경로 (60분) 🔴
- **단계**: `SocketAsyncEventArgs`로 수신하고 `ReceiveAsync(e)`의 **반환값이 false면 즉시(동기) 완료**임을 로그로 확인 → 그 경우 `Completed` 이벤트가 오지 않으므로 직접 처리 루프를 돌린다
- **확인 기준**: 동기 완료 발생 횟수 로그, 처리 누락 0
- **노트**: 동기 완료를 재귀 호출로 처리하면 왜 스택 오버플로 위험이 있는가(루프로 바꾸는 이유)

#### L2-CS-04 SAEA 풀링 세션 (150분) 🔴
- **단계**: 세션당 수신 SAEA 1 + 송신 SAEA 1, SAEA 풀에서 대여/반환, 버퍼는 1-2 풀 사용, `UserToken`에 세션 참조
- **확인 기준**: 1,000접속 유지, 접속·종료 반복 후 SAEA·버퍼 미반환 0

#### L2-CS-05 라이브러리 비교 읽기 (90분)
- **단계**: `FreeNetLiteRe`와 SuperSocketLite의 수신 흐름·송신 흐름·세션 수명을 함수 호출 순서로 정리
- **확인 기준**: 자기 서버와의 비교표(스레드 모델 / 버퍼 관리 / 세션 수명 / 백프레셔) 4행

### 3.3 C++ 트랙 실습 (L2-CPP)

#### L2-CPP-01 Winsock 동기 에코 (90분) 🔴
- **골격**: `WSAStartup` → `socket/bind/listen` → `accept` 루프 → 접속마다 `std::thread` → `recv/send` 루프 → `shutdown/closesocket` → `WSACleanup`
- **확인 기준**: 100접속 정상, `recv` 0 처리, `send` 부분 송신 루프, 모든 오류에서 `WSAGetLastError()` 로그
- **노트**: `sockaddr_in`과 `getaddrinfo`의 차이, 바이트 오더 변환이 필요한 필드

#### L2-CPP-02 논블로킹 + select (90분)
- **단계**: `ioctlsocket(FIONBIO)`, `select`로 다중 소켓 처리, `WSAEWOULDBLOCK` 처리, `fd_set` 64개 한계 확인(초과 시 동작)
- **확인 기준**: 60접속 정상 동작, 64 초과 시 한계를 로그로 확인
- **노트**: select의 O(N) 스캔 비용

#### L2-CPP-03 Overlapped I/O 에코 (90분) 🔴
- **단계**: `WSARecv/WSASend` + `OVERLAPPED` + 이벤트 방식. 버퍼와 OVERLAPPED를 **완료 통지 전까지 살려 두는** 구조(힙 할당 + 소유자 명시)
- **확인 기준**: 정상 에코, 의도적으로 스택 OVERLAPPED를 써서 깨지는 것을 1회 확인 후 수정
- **노트**: 커널이 버퍼를 소유하는 구간

#### L2-CPP-04 IOCP 기본형 (180분) 🔴
- **단계**: `CreateIoCompletionPort` → 워커 스레드 N개(코어 수 기준) → `GetQueuedCompletionStatus` 루프 → Per-Handle(세션) / Per-I/O(OVERLAPPED 확장) 데이터 구분 → `AcceptEx`로 비동기 accept
- **확인 기준**: 1,000접속 유지, 워커 스레드 수 고정, 접속·종료 반복에서 핸들 누수 0
- **노트**: `GetQueuedCompletionStatus` 실패 시에도 `lpOverlapped`가 유효할 수 있다 → 정리 코드 경로

#### L2-CPP-05 참조 카운트 세션 (120분) 🔴
- **단계**: 세션에 진행 중 I/O 카운터(atomic) → I/O 시작 시 증가, 완료 시 감소 → 0이 되고 종료 플래그가 서면 해제. `shared_ptr` 사용 시 `enable_shared_from_this` 주의
- **확인 기준**: 종료 폭풍 500개에서 크래시·ASan 오류 0

#### L2-CPP-06 IocpNetLib 구조 읽기 (90분)
- **단계**: 교재 코드의 수신 경로를 함수 호출 순서로 정리, 자기 서버와 비교
- **확인 기준**: 비교표 4행(스레드 모델 / 버퍼 / 세션 수명 / 송신 큐)

---

## 4. 학습 항목 상세

### 4.1 공통 (70h)

**네트워크 기초 이론 (12h)**
- 무엇: OSI/TCP-IP 계층, 캡슐화, IP·포트·NAT, MTU/MSS, ARP/ICMP 개요
- 왜: "왜 패킷이 잘리는가", "왜 로컬에서 되던 게 외부에서 안 되는가"를 계층으로 설명하기 위해
- 어떻게: 교재 통독 → 용어 퀴즈(매일 5개) → `L2-C-01`로 실제 연결 상태와 대응시키기
- 확인: 자기 서버 연결을 `netstat -an`/`Get-NetTCPConnection`으로 관찰하고 각 상태가 계층 개념과 어떻게 대응되는지 노트에 정리

**TCP 동작 (14h)**
- 무엇: 3-way handshake, 4-way 종료, TIME_WAIT/CLOSE_WAIT, 시퀀스/ACK, 흐름 제어(윈도우), 혼잡 제어 개요, Nagle과 지연 ACK, keep-alive, RST
- 왜: "접속 끊김 감지", "지연 원인", "포트 고갈"이 전부 여기서 나온다
- 어떻게: 교재 → 공식 문서 대조 → `L2-C-03/04/05/09`로 재현
- 확인: ① 상태 전이 표 ② 서버/클라이언트 선종료 비교 표 ③ Nagle on/off 지연 비교 표

**스트림 특성과 프레이밍 (12h)**
- 무엇: 메시지 경계 없음, 길이 접두/구분자/고정 길이, 헤더 설계, 최대 크기 제한, 링 버퍼, 엔디안
- 왜: 프레이밍이 틀리면 서버가 임의 시점에 죽거나 메모리를 폭발시킨다
- 어떻게: `L2-C-07`(분할) → `L2-C-08`(병합) → `L2-C-10`(파서 구현) 순서. **현상을 먼저 보고 파서를 만든다**
- 확인: 분할·병합 로그와 파서 테스트 12개

**직렬화 (8h)**
- 무엇: 수동 바이너리, MemoryPack, MessagePack, Protobuf, FlatBuffers의 크기·속도·스키마 진화·언어 지원
- 왜: 프로토콜은 클라이언트와 공유되므로 선택 근거를 설명할 수 있어야 한다
- 어떻게: `L2-C-11` 비교 실험 → ADR 작성
- 확인: 5종 패킷 2방식 비교 표와 ADR

**세션 관리 (10h)**
- 무엇: 상태 머신, 타임아웃, 하트비트, 중복 접속, 강제 종료, 종료 중 수신/송신 경쟁, 재접속(Phase 3 예습)
- 왜: Phase 3 게임 서버의 유저 관리가 이 위에 올라간다
- 어떻게: `L2-C-12`(상태 머신) → `L2-C-13`(종료 경쟁) → `L2-C-17`(타임아웃)
- 확인: 불법 전이 5종 테스트 + 종료 경쟁 1,000회 무사고

**서버 I/O 모델 개론 (6h)**
- 무엇: 스레드-퍼-커넥션 → select → 완료 통지(IOCP) → epoll/io_uring(개념), C10K, 준비 통지 vs 완료 통지
- 왜: 자기 트랙 API가 어느 모델인지 알고 비교할 수 있어야 면접에서 설명이 된다
- 어떻게: `L2-C-06`(동기 한계 측정) → 트랙 실습 → 비교표 작성
- 확인: 4모델을 "접속 1만 개일 때 스레드 수·시스템 콜 수·메모리" 관점으로 비교한 표

**부하 테스트 기초 (8h)**
- 무엇: 램프업, 동시 접속 vs 초당 접속, 메시지율, p50/p95/p99, 루프백 한계(포트 고갈·클라이언트 병목)
- 왜: 2-1의 "500접속·10분" 조건을 스스로 증명해야 한다
- 어떻게: `L2-C-20` 클라이언트 → `L2-C-21` 종료 폭풍 → 결과 표
- 확인: `LOAD-2-1.md`

### 4.2 C# 트랙 (90h)

| 항목 | 시간 | 무엇을 | 학습 순서 | 확인 방법 |
|---|---|---|---|---|
| 동기 소켓 | 8h | `Socket`, `TcpListener/TcpClient`, `NetworkStream`, 블로킹 Send/Receive, 스레드-퍼-커넥션 | L2-CS-01 → L2-C-06 | 100접속에서 스레드 수 관찰 |
| 비동기 소켓(TAP) | 12h | `AcceptAsync/ReceiveAsync/SendAsync`, `Memory<byte>`, 취소, `SocketException` 코드 | L2-CS-02 | 1,000접속 스레드 수 비교 |
| SocketAsyncEventArgs | 16h | SAEA 풀링, **동기 완료 처리**, 버퍼 고정, `Completed` 이벤트, 세션당 수신/송신 SAEA | L2-CS-03 → L2-CS-04 | 동기 완료 경로 테스트, 미반환 0 |
| 송신 큐 | 8h | 동시 Send 금지, 세션별 큐 + 진행 플래그, 송신 병합, 백프레셔 | L2-C-14 → L2-C-15 | 100스레드 동시 Send 무결성 |
| 프레이밍 구현 | 10h | 누적 버퍼, 헤더 파싱, 부분 패킷, 큰 패킷 방어, 1-2 풀 사용 | L2-C-10 | 파서 테스트 20개 |
| System.IO.Pipelines 🟡 | 8h | `PipeReader/Writer`, `SequenceReader`, 백프레셔 옵션 | 2-2 완료 후 | Pipelines 버전과 코드량·성능 비교 |
| MemoryPack 패킷 | 8h | `[MemoryPackable]`, 소스 제너레이터, 버전 관리, 공용 프로젝트 분리 | L2-C-11 이후 | 패킷 20종 라운드트립 |
| 채팅 서버 로직 | 12h | 방 관리, 브로드캐스트(1회 직렬화 공유), 귓속말, 타임아웃 타이머 | L2-C-16 → L2-C-17 | 로직 테스트 10개 |
| 라이브러리 비교 | 8h | SuperSocketLite·FreeNetLite 구조 읽기 | L2-CS-05 | 비교표 4행 |

### 4.3 C++ 트랙 (90h)

| 항목 | 시간 | 무엇을 | 학습 순서 | 확인 방법 |
|---|---|---|---|---|
| Winsock 기초 | 8h | `WSAStartup`, `socket/bind/listen/accept/connect`, `sockaddr_in`, `getaddrinfo`, `WSAGetLastError` | L2-CPP-01 | 동기 에코 + 오류 로그 |
| 데이터 전송 | 8h | 부분 송신 루프, 바이트 오더, 소켓 옵션(`SO_RCVBUF`, `TCP_NODELAY`, `SO_LINGER`) | L2-C-07 → L2-C-09 | 64KB 전송에서 send 반환값 처리 |
| 논블로킹과 select | 10h | `FIONBIO`, `select`, `fd_set` 64 한계, `WSAEWOULDBLOCK` | L2-CPP-02 | select 서버 60접속 |
| Overlapped I/O | 8h | `WSARecv/WSASend` + `OVERLAPPED`, 버퍼 수명 규칙 | L2-CPP-03 | 스택 OVERLAPPED로 깨뜨려 보고 수정 |
| IOCP | 20h | `CreateIoCompletionPort`, `GetQueuedCompletionStatus`, 워커 수, Per-Handle/Per-I/O, `AcceptEx`, Zero-byte recv | L2-CPP-04 | 1,000접속 유지, 핸들 누수 0 |
| 세션 수명 관리 | 12h | 참조 카운트, 종료 경쟁, `shutdown` vs `closesocket`, 세션 풀 | L2-CPP-05 → L2-C-13 | 종료 폭풍 ASan 통과 |
| 송신 버퍼 체이닝 | 8h | 세션별 송신 큐, 진행 중 1개 보장, `WSABUF` 배열, 백프레셔 | L2-C-14 | 동시 송신 무결성 |
| 프로토콜·직렬화 | 8h | 헤더 파싱, 링 버퍼, FlatBuffers/Protobuf(vcpkg) 또는 수동, 핸들러 디스패치 | L2-C-10 → L2-C-11 | 파서 테스트 20개 |
| 비교 읽기 🟡 | 8h | Boost.Asio io_context/strand를 IOCP와 대응 | L2-CPP-06 이후 | "IOCP 직접 vs Asio" 비교표 |

---

## 5. 교재 활용 가이드 (Phase 2)

### 5.1 공통

| 교재 | 읽을 장 | 언제 | 어떻게 활용 |
|---|---|---|---|
| 게임 서버 개발, 네트워크부터 이해하기 | 전체(5장) | 16~17일차 | 용어집처럼 쓴다. 1장 용어를 매일 AI 퀴즈 5개로 점검. 5장(블로킹/논블로킹·I/O 멀티플렉싱·동기/비동기)은 "서버 I/O 모델 개론"의 기초 |
| 게임 서버 개발자가 알아야할 네트워크 이론 | 전체 | 17일차 | 각 항목을 공식 문서(RFC 등)와 대조하고, 가능한 범위는 서버 로그·`netstat`/`Get-NetTCPConnection`으로 재현해 노트에 적는다. **책과 다른 점 1개 이상 찾기**가 이 책의 과제 |
| 온라인 게임 개발자를 위한 네트워크 학습 로드맵 | Ch.1(네트워킹 기초), Ch.2(전송 프로토콜 심화·TCP/UDP 선택·Reliable UDP), Ch.7(응용 프로토콜·직렬화) | 21~23일차 | 지정 장만. Ch.2의 "TCP vs UDP 선택 기준"은 설명 시험 단골. Ch.7 직렬화 비교를 2-C ADR 근거로. 3~6·8장은 Phase 3·5·6 |

### 5.2 C# 트랙

| 교재 | 읽을 장 | 언제 | 어떻게 활용 |
|---|---|---|---|
| 게임 서버 개발을 위한 C# Socket 프로그래밍 | 1~4장(개요·기초·동기·비동기), 5장(고성능), 6장(데이터 처리·프레이밍), 7장(라이브러리 설계), 9장(채팅 서버), 10장(SAEA 심화) | 18~29일차 | **주 교재.** `codes/7장`, `codes/9장`, `codes/10장`을 .NET 10으로 올려 빌드하고, NetServerLib 구조와 자기 2-1 설계를 비교표로. 8장은 Phase 5, 11장(오목)은 Phase 3 |
| C# Socket 프로그래밍 — FreeNetLite.md | 전체 | 32~34일차 | `codes/FreeNetLiteRe`의 EchoServer/test_client를 빌드해 자기 2-3 클라이언트와 접속시켜 본다 |
| SuperSocketLite를 이용한 C# 게임 서버 프로그래밍 | 1~6장 | 34일차 | "직접 만든 서버 vs 라이브러리 위 서버" 비교용. 5장 테스트 클라이언트는 2-3 설계 참고, 6장 MemoryPack 패킷 정의는 2-2에 적용 |
| C# async/await 라이브러리 만들기 | 7장(Socket 비동기 패턴) | 24일차 | SAEA를 `ValueTask`로 감싸는 방식을 읽고, TAP과 EAP 중 무엇을 왜 택했는지 ADR 1개 작성 |

### 5.3 C++ 트랙

| 교재 | 읽을 장 | 언제 | 어떻게 활용 |
|---|---|---|---|
| 게임 서버 개발자가 알아야할 TCP/IP Windows 소켓 프로그래밍 | 1~5장(소켓·주소·서버/클라이언트·데이터 전송), 6장(멀티스레드), 8장(소켓 옵션), 9장(입출력 모델), 10장(select 채팅), 11장(IOCP 채팅), 13장(버퍼 풀링) | 18~30일차 | **주 교재.** `codes/tcp_server_01~03`, `first_IOCP`, `SelectChatServer`, `IocpNetLib`를 순서대로 빌드·실행하고 puml 시퀀스 다이어그램과 코드를 대조. 5~6장 "실습" 절은 전부 수행. **책의 IOCP 구조를 이해한 뒤 자기 서버는 빈 프로젝트에서 다시 만든다**(복사 금지) |
| 모던 Windows 멀티스레딩 | 6장(Windows Thread Pool API) | 29일차 | IOCP 워커를 직접 만드는 방식과 스레드 풀에 맡기는 방식 비교. 11장은 Phase 3 |
| C++ Boost.Asio로 만드는 온라인 게임 서버 🟡 | 1~2장, 4~6장, 7장 | 34일차 | IOCP를 직접 만든 뒤에 읽어야 Asio가 무엇을 추상화하는지 보인다. 필수 경로는 IOCP 직접 구현 |
| 실전 WinRT: 네트워크와 멀티스레드 프로그래밍 (참고) | 17장(프로토콜 설계), 19장(프레이밍), 20장(세션 관리) | 21~26일차 참고 | API(WinRT)는 쓰지 않지만 설계 장은 API 독립적. 2-C 명세 체크리스트로 |

### 5.4 부 트랙 읽기 과제

- C# 주 트랙: TCP/IP Windows 소켓 9장(입출력 모델) → **"SAEA는 IOCP 위에서 어떻게 동작하는가" 1페이지**
- C++ 주 트랙: C# Socket 프로그래밍 10장(SAEA) → **"IOCP를 .NET은 어떻게 감쌌는가" 1페이지**

---

## 6. AI 협업 가이드 (Phase 2 특화)

### 6.1 프롬프트

**서버 로그 해석**
```
다음은 내 서버가 64KB 패킷을 받을 때의 수신 콜백 로그다: (recv별 바이트 수·타임스탬프)
1) 이 로그에서 애플리케이션이 한 번의 recv로 전부 받지 못한 이유를 설명해 달라.
2) 내 처리 방식은 이렇다: "(내 설명)". 이 방식의 허점을 지적해 달라.
3) 이 현상을 재현하는 테스트 케이스를 3개 제안해 달라(코드 말고 케이스 설명만).
```

**단계별 구현 (C++ IOCP)**
```
IOCP 에코 서버를 만들려 한다. 전체 코드를 한 번에 주지 말고 단계로 나누자.
단계 1: 리슨 소켓 + IOCP 생성 + 워커 스레드 N개 골격 (accept는 아직 동기)
각 단계 코드는 60줄 이내, 새로 등장하는 Win32 API마다 "왜 필요한지" 한 줄 주석.
내가 "다음"이라고 말하기 전에는 단계 2로 넘어가지 말라.
단계 1 코드에 대해 내가 먼저 설명을 시도하면 틀린 부분만 지적해 달라.
```

**세션 종료 경쟁 리뷰**
```
다음은 세션 종료 코드다. (붙여넣기)
"수신 완료 콜백 실행 중 다른 스레드가 이 세션을 닫는" 시나리오를 시간 순서(T1, T2, ...)로 시뮬레이션해 보고,
크래시·이중 해제·누수가 가능한 순서가 있으면 그 순서를 표로 보여 달라. 수정 코드는 아직 주지 말라.
```

**병목 가설**
```
접속을 100에서 1,000으로 늘리면 내 서버에서 무엇이 먼저 깨질 것 같은가?
가설 3개와 각 가설을 확인할 관찰 방법(카운터·로그·도구)을 제시해 달라.
결론을 먼저 내리지 말라. 나는 실측 후 결과를 가져오겠다.
```

**프로토콜 명세 리뷰**
```
다음은 내 채팅 프로토콜 명세다. (PROTOCOL.md 붙여넣기)
너는 이 명세만 보고 클라이언트를 구현해야 하는 다른 팀 개발자다.
1) 구현하려 할 때 추측해야 하는 부분(모호한 곳)을 전부 지적해 달라.
2) 악의적 클라이언트가 악용할 수 있는 필드가 있으면 짚어 달라.
```

### 6.2 결함 주입 주제 (이 Phase)

| 영역 | 심을 결함 |
|---|---|
| 프레이밍 | 길이 필드 검증 누락(음수/거대 값), 부분 패킷 처리 누락, 오프셋 계산 오류(off-by-one) |
| 세션 | 종료 후 송신, 참조 카운트 불일치, 소켓 핸들 누수, 상태 검사 누락 |
| 송신 | 동시 Send 허용, 송신 큐 무한 성장, 송신 완료 콜백에서 다음 항목 미전송 |
| C# | SAEA 동기 완료 미처리, `ArrayPool` 반환 누락, 이벤트 핸들러 중복 등록 |
| C++ | 완료 전 OVERLAPPED/버퍼 해제, `WSABUF` 수명, 워커 스레드 종료 신호 누락 |

### 6.3 AI가 자주 틀리는 지점

- `Socket.ReceiveAsync(SAEA)`가 `false`를 반환하면 **동기 완료**이며 `Completed` 이벤트가 오지 않는다 — 이 처리를 빠뜨린 코드가 흔하다
- IOCP에서 `GetQueuedCompletionStatus`가 실패해도 `lpOverlapped`가 유효할 수 있다(정리 필요)
- "TCP는 패킷 단위로 전달된다"는 잘못된 설명 → `L2-C-07/08` 로그로 반박
- 루프백 500접속 실패를 서버 문제로 오진 → 클라이언트 동적 포트 고갈 확인 먼저
- `Channel`/큐 용량을 무제한으로 제안 → 백프레셔 정책 요구
- 브로드캐스트에서 N명에게 N번 직렬화하는 코드 → 1회 직렬화 후 버퍼 공유(참조 카운트) 요구

---

## 7. 과제 명세

### 7.1 공통 과제 2-C. 패킷 프로토콜 명세서 (필수, 8h, 16·20·23일차)

**목표**: 이후 모든 Phase에서 확장해 쓸 프로토콜 문서를 만든다. Phase 3에서 게임 패킷 30종이 여기에 더해진다.

**요구사항 1 — 헤더 구조** (아래 표를 채운 형태로 `PROTOCOL.md`에 넣는다. 값은 자기 설계대로 바꿔도 되지만 근거를 적는다)

| 오프셋 | 크기 | 필드 | 타입 | 설명 |
|---|---|---|---|---|
| 0 | 2 | `TotalLength` | uint16 | 헤더 포함 전체 길이. 최대 65,535 |
| 2 | 2 | `PacketId` | uint16 | 패킷 종류 |
| 4 | 1 | `Version` | uint8 | 프로토콜 버전. 불일치 시 거부 |
| 5 | 1 | `Flags` | uint8 | bit0 압축, bit1 암호화(이번 Phase 미사용) |
| 6 | 2 | `Reserved` | uint16 | 체크섬·향후 확장용, 현재는 0 고정 |
| 8 | N | `Body` | bytes | 직렬화된 페이로드 |

- 엔디안: **리틀 엔디안 고정**(x86 기준, 변환 비용 제거) 또는 네트워크 바이트 오더 — 택일하고 근거를 적는다
- 최대 패킷 크기: **65,535바이트**(uint16으로 표현 가능한 최댓값, 초과 시 즉시 세션 종료)
- 최소 패킷 크기: 헤더 크기(8). `TotalLength < 8`이면 오류
- 문자열은 UTF-8, 길이는 `uint16` 바이트 수 접두사로 표현한다. 배열 원소 수는 `uint16`으로 표현하고 상한을 패킷별로 명시한다

**요구사항 2 — 패킷 ID 체계**

| 범위 | 용도 | 예시 |
|---|---|---|
| 1000~1999 | 시스템 | 하트비트, 버전 체크, 오류 통지 |
| 2000~2999 | 인증·세션 | 닉네임 설정, 로그인(Phase 4에서 확장) |
| 3000~3999 | 로비·방 | 방 생성/입장/퇴장/목록 |
| 4000~4999 | 채팅 | 방 채팅, 귓속말, 공지 |
| 5000~5999 | 게임 | Phase 3에서 사용 |

- 요청은 짝수, 응답은 요청+1 같은 규칙을 정하면 디버깅이 쉬워진다(선택)

**요구사항 3 — 패킷 목록 20종 이상** (아래는 최소 목록. 각 패킷에 필드 표를 붙인다)

| ID | 이름 | 방향 | 필드 | 허용 상태 |
|---|---|---|---|---|
| 1000 | HeartbeatReq | C→S | (없음) | Named, Active |
| 1001 | HeartbeatRes | S→C | serverTimeMs:int64 | - |
| 1002 | VersionCheckReq | C→S | clientVersion:uint8 | Connected |
| 1003 | VersionCheckRes | S→C | result:uint16 | - |
| 1010 | ErrorNotify | S→C | errorCode:uint16, message:string | - |
| 2000 | SetNameReq | C→S | name:string(2~16) | Connected |
| 2001 | SetNameRes | S→C | result:uint16, userId:int32 | - |
| 2010 | DisconnectNotify | S→C | reason:uint16 | - |
| 3000 | RoomListReq | C→S | page:uint16 | Named |
| 3001 | RoomListRes | S→C | rooms:[{id,name,count,max}] | - |
| 3002 | RoomCreateReq | C→S | name:string, maxCount:uint16 | Named |
| 3003 | RoomCreateRes | S→C | result:uint16, roomId:int32 | - |
| 3004 | RoomEnterReq | C→S | roomId:int32 | Named |
| 3005 | RoomEnterRes | S→C | result:uint16, members:[{userId,name}] | - |
| 3006 | RoomEnterNotify | S→C | userId:int32, name:string | - |
| 3007 | RoomLeaveReq | C→S | (없음) | InRoom |
| 3008 | RoomLeaveRes | S→C | result:uint16 | - |
| 3009 | RoomLeaveNotify | S→C | userId:int32 | - |
| 4000 | ChatReq | C→S | text:string(≤200) | InRoom |
| 4001 | ChatRes | S→C | result:uint16 | - |
| 4002 | ChatNotify | S→C | userId:int32, name:string, text:string, seq:int64 | - |
| 4010 | WhisperReq | C→S | targetName:string, text:string | Named |
| 4011 | WhisperRes | S→C | result:uint16 | - |
| 4012 | WhisperNotify | S→C | fromName:string, text:string | - |

**요구사항 4 — 세션 상태 머신** (Mermaid로 그린다)

```
Connected --SetNameReq(성공)--> Named
Named --RoomEnterReq(성공)--> InRoom
InRoom --RoomLeaveReq--> Named
any --하트비트 타임아웃/오류/종료 요청--> Closing --> Closed
```
- 각 상태에서 **허용되는 패킷 ID 목록**을 표로 적는다. 허용되지 않은 패킷 수신 시: 로그 + `ErrorNotify` + 세션 종료(정책 명시)

**요구사항 5 — 규칙**
- 하트비트: 클라이언트 10초 주기 송신, 서버는 마지막 수신 후 30초 무응답 시 종료
- 버전 불일치: `VersionCheckRes(result=VersionMismatch)` 후 즉시 종료
- 에러 코드 표: `0=Success, 1=InvalidPacket, 2=InvalidState, 3=NameDuplicated, 4=RoomFull, 5=RoomNotFound, 6=VersionMismatch, 7=RateLimited, 8=TargetOffline, 9=ServerFull, ...`
- `ChatNotify.seq`는 방별 단조 증가 `uint64`다. 귓속말 대상이 오프라인이면 `TargetOffline`을 반환한다. `RateLimited`가 3회 누적되면 세션을 종료한다
- `DisconnectNotify`는 송신 완료 콜백 또는 2초 타임아웃 뒤에 소켓을 닫는다

**요구사항 6 — ADR**: `adr/0001-serialization.md` — 직렬화 방식 선택(맥락·결정·대안 3개·기각 이유·결과). 템플릿은 `08-templates.md` T5

**제출물**: `PROTOCOL.md`, `adr/0001-serialization.md`

**채점**

| 항목 | 배점 | 기준 |
|---|---|---|
| 완결성 | 40 | 헤더 표·ID 체계·패킷 20종·상태 머신·에러 코드·하트비트 규칙 전부 존재 |
| 모호성 없음 | 40 | AI "다른 팀 개발자" 리뷰(§6.1)에서 지적된 모호 항목을 전부 해소 |
| 상태 머신 | 20 | 상태별 허용 패킷 표와 불법 전이 처리 정책이 명시 |

### 7.2 트랙 과제 2-1. 비동기 에코 서버 → 채팅 서버 (필수, 48h)

**단계 구성**

| 단계 | 시기 | 내용 | 산출물 |
|---|---|---|---|
| 2-1a | 18~19일차 | 동기 에코(스레드-퍼-커넥션), 100/500/1,000접속 한계 측정 | `EchoServer/`(sync), 측정 표 |
| 2-1b | 24~25일차 | 비동기 에코(C# SAEA / C++ IOCP), 1,000접속 유지 | `EchoServer/`(async), `COMPARE-2-1.md` |
| 2-1c | 26~30일차 | 채팅 서버(방·귓속말·하트비트·graceful shutdown) | `ChatServer/` |

**2-1c 기능 요구사항**

1. **닉네임**: 접속 후 30초 내 `SetNameReq` 없으면 강제 종료. 중복 닉네임 거부(`NameDuplicated`)
2. **방**: 생성/입장/퇴장/목록(페이지네이션), 방 인원 상한 100, 방장 없음(단순화), 빈 방 자동 소멸
3. **채팅**: 방 브로드캐스트(발신자 포함), 귓속말(닉네임 대상), 메시지 길이 200자 제한, **초당 5회 초과 시 `RateLimited`**
4. **하트비트**: 서버는 30초 무응답 세션 종료. 검사 주기 5초
5. **오류 처리**: 손상 패킷(길이/ID/상태 불일치) 수신 시 로그 + 해당 세션만 종료. 서버는 생존
6. **graceful shutdown**: Ctrl+C 시 ① 신규 접속 차단 ② `DisconnectNotify` 브로드캐스트 ③ 송신 완료 대기(최대 2초) ④ 소켓 정리 ⑤ 스레드 조인. 5초 내 완료
7. **설정 파일**: 포트, 워커 스레드 수, 수신 버퍼 크기, 최대 접속 수, 타임아웃, 로그 레벨. C#은 `appsettings.json`, C++은 TOML/JSON
8. **접속 상한**: 최대 접속 수를 넘으면 `ErrorNotify(ServerFull)` 송신 완료 후 종료한다. 동일 IP 상한과 세션별 패킷 속도 상한을 설정으로 둔다

**비기능 요구사항 (측정으로 증명)**

| 조건 | 기준 |
|---|---|
| 500 동시 접속, 접속당 1초에 1메시지(50B), 10분 | 메시지 유실 0, 크래시 0 |
| 메모리 | 1분 시점 대비 10분 시점 증가 10% 이내 |
| 지연 | 채팅 왕복(자기 메시지가 브로드캐스트로 돌아오기까지) p99 100ms 이하 |
| 종료 폭풍 | 500접속 동시 종료 × 5회, 크래시 0, 핸들·세션·풀 카운터 원복 |
| C++ | ASan 빌드로 위 시나리오 1회 통과(성능 측정은 Release) |

**구조 요구사항**

```
ChatServer/
├─ Net/      세션, 수신/송신, 프레이밍 연동, 세션 매니저   ← PacketLib 참조
├─ Logic/    유저, 방, 채팅 규칙                          ← Net을 참조하지 않음(인터페이스로 송신)
├─ Host/     진입점, 설정 로딩, 종료 처리
└─ tests/    Logic 단위 테스트 + Net 통합 테스트
```
- `Logic`은 `ISessionSender { void Send(int sessionId, ReadOnlySpan<byte>); void Close(int sessionId); }` 같은 인터페이스만 안다 → 테스트에서 페이크로 대체
- IOCP 워커/SAEA 콜백은 `Logic`을 동시에 호출할 수 있으므로 **Logic 전역 락 1개**로 방·유저 상태를 보호한다. 직렬화와 실제 `Send` 호출은 락 밖에서 수행한다
- 수신/송신 버퍼는 **1-2 오브젝트 풀** 사용
- 세션별 송신 큐로 동시 송신 방지

**테스트 요구사항**

- 단위(Logic, 네트워크 없음): 방 입퇴장 5, 브로드캐스트 3, 귓속말 3, 상태 전이 5, 레이트 리밋 2 — 최소 18개
- 통합(2-3 클라이언트 사용): chat 시나리오 100접속 5분, storm 시나리오, 손상 패킷 3종

**LOAD-2-1.md 템플릿**

```markdown
# 2-1 부하 리포트
## 1. 환경
CPU/RAM/OS, 서버·클라이언트 동일 머신 여부, 빌드 구성, 커밋 해시, 동적 포트 범위 설정
## 2. 시나리오
접속 수 / 램프업 / 메시지 주기 / 지속 시간 / 반복 횟수
## 3. 결과
| 회차 | 접속 성공 | 유실 | p50 | p95 | p99 | CPU% | 메모리(1분/10분) |
## 4. 종료 폭풍
| 회차 | 크래시 | 핸들 수(전/후) | 세션 수(후) | 풀 미반환 |
## 5. 병목 분석
가설 → 관찰 방법 → 결과 → 채택/기각 (최소 3개, 기각 1개 이상 포함)
## 6. 수정 내역과 재측정 결과 (전/후 표)
## 7. 남은 한계와 다음 Phase 과제
```

**채점**

| 항목 | 배점 | 기준 |
|---|---|---|
| 기능 | 25 | 요구사항 1~7 전부, 손상 패킷 방어 포함 |
| 안정성 | 30 | 500접속·10분 무유실, 메모리 10% 이내, 종료 폭풍 5회 |
| 동시성·리소스 | 20 | 루브릭 2·3번 AI 리뷰 4점 이상, C++ ASan 통과, 미반환 0 |
| 구조 | 15 | Logic이 Net을 참조하지 않음(빌드로 증명), 풀 사용, 송신 큐 |
| 문서 | 10 | `LOAD-2-1.md`·`COMPARE-2-1.md`·설정 설명 |

**자주 하는 실수**
- 브로드캐스트에서 인원수만큼 직렬화 → 1회 직렬화 후 버퍼 공유(참조 카운트/복사)로
- 방 목록을 매번 전체 순회 → 페이지네이션 + 캐시
- 로그를 동기 파일 쓰기로 → Phase 1의 로그 큐를 재사용
- 세션 딕셔너리에서 제거만 하고 소켓 close 누락 → 핸들 누수

### 7.3 트랙 과제 2-2. 패킷 프레이밍·직렬화 라이브러리 (필수, 24h, 22~23일차)

**목표**: 2-C 명세를 라이브러리로 구현해 2-1과 Phase 3~6에서 재사용한다. **독립 프로젝트**여야 한다.

**API (C#)**
```csharp
public enum ParseResult { Ok, NeedMore, TooLarge, BadLength, BadVersion }

public sealed class PacketReader
{
    public PacketReader(int maxPacketSize = 65535, BufferPool? pool = null);
    // 소켓에서 읽은 조각을 누적
    public void Feed(ReadOnlySpan<byte> chunk);
    // 완성된 패킷을 하나 꺼낸다. Ok면 packet에 헤더+바디 슬라이스
    public ParseResult TryRead(out PacketView packet);
    public int BufferedBytes { get; }
    public void Reset();
}

public readonly ref struct PacketView
{
    public ushort Id { get; }
    public byte Version { get; }
    public ReadOnlySpan<byte> Body { get; }
}

public static class PacketWriter
{
    // 헤더를 채우고 body 직렬화 결과를 담은 풀 버퍼를 반환(호출자가 반환 책임)
    public static PooledBuffer Write<T>(ushort id, in T body) where T : IPacketBody;
}
```

**API (C++)**
```cpp
enum class ParseResult { Ok, NeedMore, TooLarge, BadLength, BadVersion };

struct PacketView {
    std::uint16_t id;
    std::uint8_t  version;
    std::span<const std::byte> body;
};

class PacketReader {
public:
    explicit PacketReader(std::size_t max_packet_size = 65535);
    void Feed(std::span<const std::byte> chunk);
    ParseResult TryRead(PacketView& out);
    std::size_t BufferedBytes() const noexcept;
    void Reset();
};
```

**요구사항**
1. **오류는 예외가 아니라 반환값**으로 알린다(호출자가 세션 종료를 결정)
2. `NeedMore`는 정상 흐름이다(부분 패킷). 로그를 남기지 않는다
3. 최대 크기 초과·길이 필드 이상은 즉시 `TooLarge`/`BadLength` → 호출자는 세션 종료
4. 버퍼는 1-2 풀에서 대여, 반환 책임을 문서화
5. 직렬화: C# MemoryPack(또는 MessagePack) / C++ 수동 또는 FlatBuffers. **2-C의 패킷 20종**을 전부 정의
6. 할당 최소화: 정상 경로에서 패킷당 힙 할당 0을 목표(C#은 `MemoryDiagnoser`로 증명)

**테스트 목록(20개, 전부 필수)**

| # | 이름 | 입력 | 기대 |
|---|---|---|---|
| 1 | 완전한패킷1개 | 정확히 1패킷 | Ok, 잔여 0 |
| 2 | 1바이트씩분할 | 1바이트씩 Feed | 마지막 바이트에서 Ok |
| 3 | 헤더만도착 | 8바이트 | NeedMore |
| 4 | 헤더-1바이트 | 7바이트 | NeedMore |
| 5 | 바디부족 | 헤더+바디절반 | NeedMore |
| 6 | 두패킷병합 | 2패킷 연속 | Ok 두 번, 잔여 0 |
| 7 | 세패킷과절반 | 3.5패킷 | Ok 3번 후 NeedMore |
| 8 | 경계정확히 | 버퍼 크기와 동일 | Ok |
| 9 | 길이0 | TotalLength=0 | BadLength |
| 10 | 길이<헤더 | TotalLength=5 | BadLength |
| 11 | 설정상한초과 | `maxPacketSize=1024`, TotalLength=2000 | TooLarge |
| 12 | 버전불일치 | Version=99 | BadVersion |
| 13 | 미정의ID | Id=9999 | Ok(파서는 통과, 상위에서 판단) |
| 14 | 최대크기패킷 | 정확히 65,535바이트 | Ok |
| 15 | 연속1000패킷 | 1,000개 연속 | 전부 Ok, 순서 유지 |
| 16 | Reset후재사용 | 오류 후 Reset | 정상 파싱 재개 |
| 17 | 라운드트립_채팅 | ChatNotify 직렬화→역직렬화 | 필드 일치 |
| 18 | 라운드트립_방목록 | 가변 배열 포함 | 필드 일치 |
| 19 | 문자열_UTF8 | 한글 200자 | 손상 없음, 길이 검증 |
| 20 | 할당0_정상경로 | 1만 패킷 파싱 | (C#) 할당 0 또는 상수 |
| 21 | 무작위퍼징 | 무작위 1MB를 1~100B 조각으로 Feed | 예외·크래시 0, 오류 코드 또는 NeedMore만 |

**제출물**: 라이브러리 프로젝트, 테스트 20개, 사용 예제(10줄), README(사용법·버퍼 반환 규칙)

**채점**: 정확성(테스트 20개) 50 / 방어 로직(오류 코드·상한) 20 / API 설계(사용 편의·할당) 30

### 7.4 공통 과제 2-3. 테스트 클라이언트 (필수, 16h, 31·33일차)

**목표**: 부하와 시나리오를 재현하는 도구. Phase 3에서 게임 시나리오, Phase 5에서 운영급 도구로 확장한다.

**CLI 인터페이스**
```
LoadClient --host 127.0.0.1 --port 9000 --conn 500 --rampup 50 \
           --scenario chat --duration 600 --out results/run1.json
```

| 인자 | 의미 | 기본값 |
|---|---|---|
| `--conn` | 목표 동시 접속 수 | 100 |
| `--rampup` | 초당 접속 생성 수 | 20 |
| `--scenario` | `chat` \| `storm` \| `garbage` | chat |
| `--duration` | 정상 상태 유지 시간(초) | 300 |
| `--msg-interval` | 메시지 주기(ms) | 1000 |
| `--msg-size` | 메시지 바이트 크기 | 50 |
| `--graceful-ratio` | storm 정상 종료 비율(0~1) | 0.5 |
| `--seed` | 재현용 난수 시드 | 1 |
| `--rooms` | 분산할 방 개수 | conn/50 |
| `--out` | 결과 파일 경로(JSON) | stdout |

**시나리오**
- `chat`: 접속 → `SetNameReq` → 방 입장(분산) → 주기적 채팅 → 종료 시 퇴장·정상 종료
- `storm`: 목표 접속 확보 후 **동시에** 전원 종료(정상 종료/강제 종료 비율 옵션)
- `garbage`: 손상 패킷 전송(길이 이상·미정의 ID·상태 위반) 후 서버 생존 확인

**통계 요구사항**
- 접속: 시도/성공/실패(오류 코드별)
- 메시지: 송신/수신/유실(시퀀스 기반), 초당 처리량
- 지연: 자기 메시지가 브로드캐스트로 돌아오기까지의 시간 p50/p95/p99/최대 — **히스토그램 기반 계산 권장**
- 서버 종료 감지 수, 예외 종류별 카운트
- 5초마다 콘솔 요약, 종료 시 JSON + 마크다운 표 파일 저장

**정확성 요구**: p99 계산을 알려진 데이터셋(1~1000)으로 검증하는 단위 테스트 필수

클라이언트 I/O는 비동기 이벤트 루프/IOCP/SAEA 중 하나를 사용하며 스레드-퍼-커넥션을 금지한다. JSON에는 입력 인자·시드·환경·접속/메시지/지연/종료 사유 카운터를 고정 필드로 기록한다.

**제출물**: 코드, 500접속 실행 결과 파일 2건(chat/storm), 사용법 README

**채점**: 시나리오 동작 40 / 통계 정확성(p99 검증 테스트 포함) 30 / 사용성(인자·출력·재현성) 30

### 7.5 교차 과제 2-4. 언어 간 상호 통신 (심화 🟡, 8h, 34일차)

- 상대 언어의 참고 에코 서버(C# 트랙은 `IocpNetLib` Echo, C++ 트랙은 `FreeNetLiteRe` EchoServer)와 내 2-3 클라이언트를 통신시킨다
- 헤더 형식이 다르면 클라이언트에 **프로토콜 어댑터**를 만든다(내 프로토콜 ↔ 상대 프로토콜 변환)
- 제출물: 통신 로그(요청/응답 타임스탬프), 어댑터 설명 1페이지, "두 구현의 헤더 설계 차이 3가지"

---

## 8. 학습 완료 판정 (35일차 금요일)

### 8.1 체크리스트

**과제**
- [ ] 2-C: `PROTOCOL.md`(패킷 20종·상태 머신·에러 코드) + ADR 1개
- [ ] 2-1a/b/c 완료, 채팅 기능 7종 동작
- [ ] 2-2: 테스트 20개 통과, 독립 라이브러리로 분리
- [ ] 2-3: chat/storm/garbage 3종 시나리오 동작, p99 검증 테스트 통과
- [ ] 🟡 2-4 또는 미수행 사유 기록

**측정**
- [ ] `LOAD-2-1.md`: 500접속·10분 무유실, 메모리 10% 이내, p99 기록
- [ ] 종료 폭풍 5회 결과(핸들·세션·풀 카운터 원복)
- [ ] `COMPARE-2-1.md`: 동기 vs 비동기(스레드 수·메모리·성공률)

**관찰 기록**
- [ ] TCP 상태 전이 표, 서버/클라이언트 선종료 비교 표
- [ ] 분할 수신 로그, 병합 수신 로그, Nagle on/off 비교 표

**기록**
- [ ] 학습 노트 20일치, 주간 회고 4개

### 8.2 재구현 시험 (AI 없음, 90분)

**문제**: 빈 프로젝트에서 프레이밍 파서를 구현하고 아래 제공 테스트 10개를 통과시킨다.
헤더는 `[TotalLength:uint16][PacketId:uint16][Version:uint8][Flags:uint8][Reserved:uint16]` = 8바이트, 리틀 엔디안, 최대 64KB.

제공 테스트: §7.3 테스트 목록의 #1~#12 중 10개(1,2,3,5,6,7,9,10,11,12)

**통과 기준**: 90분 내 10개 전부 통과.

### 8.3 설명 시험 질문 은행 (10문항 무작위, 평균 4.0 이상)

**공통**
1. 3-way handshake의 각 단계에서 서버 소켓 상태는. SYN 플러딩은 어느 단계를 공격하는가
2. TIME_WAIT은 누가 왜 가지는가. 서버가 먼저 끊으면 어떤 문제가 생기는가
3. TCP에 메시지 경계가 없다는 것을 코드에서 어떻게 다루는가. 길이 접두 방식의 취약점은
4. Nagle을 끄는 이유와 끄지 말아야 하는 경우
5. IOCP(완료 통지)와 epoll(준비 통지)의 차이를 recv 흐름으로 설명하라
6. 스레드-퍼-커넥션이 1,000접속에서 무너지는 이유를 자기 측정치로 설명하라
7. 백프레셔가 없으면 무슨 일이 생기는가. 자기 서버의 정책은
8. 브로드캐스트를 100명에게 보낼 때 무엇을 조심해야 하는가
9. graceful shutdown의 단계와 순서가 바뀌면 생기는 문제
10. 손상 패킷을 받았을 때 서버 전체가 아니라 해당 세션만 끊는 이유
11. 하트비트 주기와 타임아웃 값을 어떻게 정했는가
12. 루프백 부하 테스트의 한계 3가지

**C#**
13. SAEA를 재사용하는 이유. `ReceiveAsync`가 false를 반환하면 무엇을 해야 하는가
14. 세션별 송신 큐가 필요한 이유와 큐 무한 성장을 막는 방법
15. `ArrayPool`을 소켓 버퍼에 쓸 때 주의점(반환 시점, 크기)
16. TAP과 EAP(SAEA) 중 무엇을 왜 택했는가
17. `Memory<byte>`와 `Span<byte>`를 비동기 코드에서 어떻게 나눠 쓰는가

**C++**
18. IOCP 워커 스레드 수를 어떻게 정하는가. 코어 수보다 많으면 무슨 일이
19. OVERLAPPED와 버퍼의 수명 규칙. 완료 전에 해제하면 어떻게 되는가
20. 세션 참조 카운트가 왜 필요한가. recv 완료와 close가 동시에 일어나는 순서를 설명하라
21. `shutdown(SD_SEND)` 후 `closesocket`을 하는 이유
22. `AcceptEx`가 `accept`보다 나은 점과 주의점

### 8.4 결함 찾기 (45분)

프레이밍·세션 종료·송신 큐 주제로 결함 5개를 심은 코드에서 4개 이상 발견 + 수정안 제시(§6.2 주제 목록 사용).

### 8.5 미달 시 대응

| 미달 항목 | 대응 |
|---|---|
| 500접속 조건 미달 | Phase 3 첫 주 8h 보강 후 재측정. 그래도 미달이면 300접속으로 기준을 낮추되 **원인 분석 문서 필수 제출** |
| 재구현 90분 초과 | Phase 3 1주차에 파서만 3회 반복 연습(각 60분) |
| 설명 시험 3.5 미만 | 약한 주제 2개를 Phase 3 첫 주에 8h 보강 후 재시험 |
| 2-2 테스트 미달 | **통과 불가**. Phase 3 서버가 이 라이브러리를 쓰므로 최우선 완료 |
| 종료 폭풍 크래시 | **통과 불가**. 세션 수명 설계를 다시(참조 카운트 도입) |

---

## 9. 흔한 막힘 포인트

| 증상 | 원인 | 대처 |
|---|---|---|
| 루프백 500접속에서 클라이언트 접속 실패 | 동적 포트 고갈, TIME_WAIT 누적 | `netsh int ipv4 set dynamicport tcp start=10000 num=55000`, 클라이언트를 2개 프로세스로 분산, 테스트 간 30초 대기 |
| 방화벽 팝업/외부 접속 불가 | Windows Defender 방화벽 | 인바운드 규칙 추가(포트 지정), 사설망 프로필에서 테스트 |
| C# 서버 CPU 100% | SAEA 동기 완료를 루프가 아닌 재귀로 처리하거나, 0바이트 수신을 종료로 처리하지 않음 | 0바이트 = 정상 종료, 동기 완료는 while 루프로 |
| C++ IOCP 간헐적 크래시 | 완료 전 세션/버퍼 해제, 참조 카운트 오류 | 참조 카운트 로그 추가, ASan 빌드로 재현, 종료 경로 단일화 |
| 메모리가 서서히 증가 | SAEA/버퍼 반환 누락, 세션 딕셔너리 잔존, 송신 큐 누적 | 풀 미반환 수·세션 수·큐 길이를 5초마다 로그 |
| 메시지가 섞여서 도착 | 동시 Send | 송신 큐 + 진행 중 1개 플래그 |
| 특정 클라이언트만 응답 없음 | 송신 큐가 막힘(느린 수신자), 백프레셔 미구현 | 큐 상한과 정책 적용, 상한 초과 시 연결 종료 |
| 채팅 지연 p99만 튄다 | 로그 동기 쓰기, GC, 브로드캐스트 직렬화 반복 | 로그 큐 사용, 1회 직렬화 공유, 할당 프로파일 |
| 종료 시 프로세스가 안 죽는다 | 워커 스레드가 블로킹 대기 중 | IOCP는 `PostQueuedCompletionStatus`로 워커 수만큼 종료 신호, C#은 취소 토큰 + Complete |
| 파서가 가끔 이상한 패킷을 뱉음 | 오프셋 계산 오류, 버퍼 재사용 시 초기화 누락 | 경계 테스트(#4, #8) 추가, 슬라이스 범위 검증 |
| 테스트 클라이언트가 서버보다 먼저 죽음 | 클라이언트 CPU·포트 한계 | 2대(또는 2프로세스) 분산, 메시지 주기 조정 |
| `Get-NetTCPConnection`이 느리다 | 연결 수천 개 | `-LocalPort` 필터 사용, 샘플링 주기 늘리기 |

---

## 10. Phase 3로 넘어가기 전 준비

- [ ] `PacketLib`(2-2)와 `LoadClient`(2-3)를 공용 프로젝트로 정리하고 버전 태그(`v0.2`)를 단다
- [ ] `PROTOCOL.md`에 Phase 3용 5000번대(게임) 영역을 예약 표시해 둔다
- [ ] 교재 "게임 개발자를 위한 C# 디자인 패턴" 7~12장(Command·Observer·State·Strategy·Game Loop·Update Method)을 클론
- [ ] `08-templates.md`의 T4(설계 문서)·T5(ADR) 템플릿을 미리 읽고, Phase 3에서 쓸 `DESIGN.md` 빈 문서를 만들어 둔다
- [ ] 채팅 서버의 "방" 코드를 Phase 3의 "룸 액터"로 바꿀 때 무엇이 달라져야 할지 3줄 메모(스레드 소유권 관점)
- [ ] Phase 2 회고: 가장 오래 막힌 버그와 그 원인, AI가 틀렸던 사례 1개, Phase 3에 가져갈 습관 1개

---

## 11. 2026-09-05 보강 사항 (앞 절과 충돌하면 이 절 우선)

### 11.1 일정 재배치와 완충

- 2-2는 22~24일차 3일로 확장한다. C++은 `BinaryReader/Writer` 헬퍼를 먼저 만들고 20종 패킷을 작성한다. `L2-CPP-05`는 27일차, 2-3의 `garbage`와 대규모 접속 함정은 33일차 오전으로 이동한다
- 25~26일차 초과 실습을 나누고, 31일차와 33일차 오전을 2-3 전용으로 확보한다. 매주 반일 완충 슬롯에서 미완료 네트워크 실습을 소진한다
- 과제 시간은 개념·교재·AI 없는 재구현·리뷰를 포함한 상한이다. 실제 구현 슬롯은 2-C 6h, 2-1 42h, 2-2 18h, 2-3 10h, 2-4 6h로 계획한다

### 11.2 대규모 접속·TCP 의미론

#### L2-CS-06 대규모 SAEA 접속 함정 (90분) 🔴
- **단계**: pinned 64KB×1,000 대기 수신과 recv 2개를 재현하고, 풀 슬라이스·소켓당 recv 1개로 수정한다
- **확인 기준**: 10055/완료 순서 로그와 수정 전후 pinned 메모리 표

#### L2-CPP-07 대규모 IOCP 접속 함정 (90분) 🔴
- **단계**: OVERLAPPED 버퍼 수명, 10055, zero-byte recv, 소켓당 recv 1개를 재현·수정한다
- **확인 기준**: 1,000접속 유지, 세션·버퍼 카운터 원복, 완료 순서 로그

- `L2-CS-06/L2-CPP-07 대규모 접속 함정`(90분): 소켓당 미해결 recv는 1개만 둔다. 64KB×1,000 대기 recv로 `WSAENOBUFS(10055)`를 관찰한 뒤 작은 풀 버퍼/zero-byte recv로 수정하고, recv 2개를 걸어 완료 순서 붕괴 로그를 남긴다
- C#은 pinned object heap(`GC.AllocateArray(..., pinned:true)`), SAEA 버퍼 슬라이스, `BufferList`를 다룬다. C++은 OVERLAPPED 완료까지 버퍼 수명을 보장한다
- keep-alive와 앱 하트비트, `shutdown(SD_SEND)` half-close, backlog 초과, `SO_EXCLUSIVEADDRUSE`, 송수신 버퍼, CLOSE_WAIT 진단을 30분 실습 3개로 수행한다
- TLS/`SslStream`, 평문 TCP 위험, IPv6 `DualMode`, 프로토콜 하위 호환, 포트 0 in-process 테스트와 주입 가능한 시계를 개념·테스트 항목에 포함한다

### 11.3 평가·AI 검증

- `L2-C-16`은 워커 4개가 입장/퇴장 1만 회를 동시에 수행해 방·유저 카운터가 일치해야 통과한다. 5초 주기 카운터 로그와 `COMPARE-2-1.md`를 필수 제출물로 둔다
- 질문 은행에 IOCP 완료 순서, 미해결 recv 1개, 10055, half-close, TIME_WAIT, Nagle, 프레이밍 퍼징, Logic 락 범위를 추가한다. 결함 주입에는 락 없는 방 딕셔너리를 포함한다
- AI가 콜백 1개=스레드 1개, `ReceiveAsync=true`=성공, TCP 메시지 경계 보장, 무제한 pinned 버퍼를 제안하면 반례 로그와 공식 API 계약으로 검증한다
