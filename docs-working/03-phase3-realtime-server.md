# Phase 3. 실시간 게임 서버 아키텍처 (8~11주, 160h)

> 공통 90h / 트랙 70h. 이 Phase가 끝나면 학습자는 게임 로직이 들어간 실시간 서버를 **스레드 모델부터 설계**하고, 설계 문서와 코드가 일치하는 상태로 완성한다.

## 0. 이 문서 사용법

Phase 1·2와 같은 구조다. §2의 일자 블록을 매일 펼쳐 놓고 진행하며, 실습(`L3-C-xx`, `L3-CS-xx`, `L3-CPP-xx`)은 §3에서, 과제 상세는 §7에서 찾는다.

이 Phase의 3대 원칙

1. **설계를 먼저 글로 쓴다.** 코드보다 `DESIGN.md`가 먼저다. 8주차에 설계 문서를 쓰고, 11주차에 "설계와 달라진 점"을 갱신한다. 실무에서 신입이 가장 못 하는 것이 이것이다.
2. **룸 로직에 락을 쓰지 않는다.** 모든 룸 접근은 Job 큐를 통과한다. 락이 하나라도 남으면 이 Phase는 통과 불가다.
3. **판정은 서버가 한다.** 클라이언트는 "요청"만 보낸다. 승패·좌표·턴은 전부 서버가 검증한다. 치트 시나리오 5종이 전부 거부되어야 한다.

| 표기 | 뜻 |
|---|---|
| `L3-C-xx` / `L3-CS-xx` / `L3-CPP-xx` | 공통 / C# / C++ 실습 |
| 🔴 필수 · 🟡 여유 시 | 우선순위 |
| **DoD** | 그날의 완료 조건 |

---

## 1. 개요

### 1.1 목표

1. **스레드 모델을 선택하고 근거를 문서로 남긴다.** 4가지 모델을 비교해 오목 서버에 룸 액터를 택한 이유를 ADR로 쓴다.
2. **룸 액터를 구현한다.** 룸마다 Job 큐를 두고 한 번에 한 스레드만 룸을 실행한다. 타이머·패킷·룸 간 메시지가 전부 Job으로 들어간다.
3. **상태 머신을 설계·구현한다.** 세션·룸·매칭 각각의 상태와 전이, 불법 전이 처리.
4. **서버 권위를 구현한다.** 착수 유효성·턴 검증·속도 제한·재현 가능한 랜덤.
5. **계층을 분리한다.** 규칙(순수 함수) / 로직(룸·매칭) / 네트워크(세션) 3계층. 규칙 테스트 30개가 네트워크 없이 1초 안에 돈다.
6. **100룸·300접속에서 착수 지연 p99 50ms**를 측정으로 증명한다.

### 1.2 선수 조건 자가 진단 (20분)

| # | 질문 | 통과 기준 |
|---|---|---|
| 1 | 2-2 패킷 라이브러리가 독립 프로젝트로 참조 가능한가 | 다른 솔루션에서 빌드됨 |
| 2 | 500접속 채팅 서버가 10분 무유실로 돌았는가 | `LOAD-2-1.md` 보유 |
| 3 | 세션 종료 경쟁을 어떻게 막았는지 30초 안에 설명할 수 있는가 | 즉답 |
| 4 | 2-3 테스트 클라이언트로 시나리오를 추가할 수 있는가 | 구조를 안다 |
| 5 | Phase 1의 로그 큐/오브젝트 풀을 서버에 붙여 쓰고 있는가 | 코드에 참조 존재 |

3개 미만이면 Phase 2 미완 항목부터 처리한다. 특히 1번·2번이 안 되면 이번 Phase의 부하 조건을 만족할 수 없다.

### 1.3 이 Phase가 끝나면 할 수 있는 것

- "왜 룸을 단일 스레드로 처리하는가, 대안은 무엇인가"를 4모델 비교로 설명한다
- 룸 액터를 **120분 안에 재구현**한다(Job 큐 + 단일 소비 루프 + 타이머 Job + 룸 간 메시지)
- 100룸이 동시에 도는 서버에서 착수 p99를 측정하고 병목을 지목한다
- 설계 문서만 보고 다른 사람이 서버 구조를 설명할 수 있게 쓴다
- 치트 시나리오 5종을 서버가 전부 거부함을 테스트로 보인다

### 1.4 이 Phase의 산출물

```
phase3/
├─ DESIGN.md                  과제 3-C: 설계 문서(+ "설계와 달라진 점" 절)
├─ adr/
│  ├─ 0002-thread-model.md    스레드 모델 선택
│  ├─ 0003-room-session-ref.md 룸-세션 참조 방식
│  └─ 0004-matching.md         매칭 방식
├─ OmokServer/
│  ├─ Rules/                  오목 규칙 — 외부 의존 0 (테스트 30개)
│  ├─ Logic/                  룸 액터·유저·매칭·로비 (테스트 25개)
│  ├─ Net/                    세션·디스패치 (PacketLib 참조)
│  ├─ Host/                   진입점·설정·로깅·종료
│  └─ tests/
├─ LoadClient/                2-3 확장: omok/spectate/reconnect/cheat 시나리오
├─ LOAD-3-1.md                100룸 부하 리포트
└─ labs/
```

### 1.5 4주 로드맵

| 주차 | 큰 주제 | 공통 | C# 트랙 | C++ 트랙 | 과제 |
|---|---|---|---|---|---|
| 8주 | 설계와 룸 액터 | 스레드 모델 4종, 룸 액터, 틱·타이머, 설계 문서·ADR | `Channel<IJob>` 액터, `PeriodicTimer` | 룸 Job 큐 + 워커 배정, 타이머 스레드 | 3-C 초안, 액터 프로토타입 |
| 9주 | 상태 머신과 규칙 | 세션·룸·매칭 상태, 서버 권위, 재현 랜덤 | 핸들러 디스패치, 규칙 테스트 | 함수 테이블 디스패치, 규칙 테스트 | 3-1 로비·매칭·입장 |
| 10주 | 게임 진행과 운영 | 타임아웃·재접속·관전, 구조화 로깅, 종료 | Serilog, Generic Host | spdlog, 종료 시퀀스 | 3-1 게임 완성, 3-2 |
| 11주 | 부하와 정합 | 통합 테스트, 100룸 부하, 설계 갱신 | 병목 수정, 라이브러리 분석 | 병목 수정, 🟡 Asio 비교 | 3-1 성능 통과, 평가 |

---

## 2. 주차별 상세 계획 (일 단위)

일자 번호는 과정 전체 기준(Phase 3은 36~55일차).

### 2.1 8주차 — 설계와 룸 액터

#### 36일차 (월) — 스레드 모델 4종 비교 🔴

**오전 (2.5h)**
- 4모델 정리
  1. **세션당 스레드**: 단순하지만 1,000접속에서 붕괴(Phase 2에서 측정 완료)
  2. **I/O 스레드 + 로직 단일 스레드**: 락 없음, 구현 단순, 코어 1개만 씀 → 룸이 많으면 한계
  3. **워커 풀 + 공유 상태 락**: 코어를 다 쓰지만 락 경합·데드락·디버깅 지옥
  4. **룸/엔티티 단위 액터**: 룸마다 큐, 룸 로직은 단일 스레드 → 락 없이 병렬. 이 과정의 선택
- 각 모델의 처리량·지연·복잡도·확장성·디버깅 난이도를 5점 척도로 평가하는 표를 직접 만든다

**오후 (2.5h)**
- `L3-C-01` 4모델 비교표 작성: "오목 100룸", "MMO 존 서버", "FPS 매치 서버" 세 케이스에 각각 어떤 모델을 택할지와 근거
- `adr/0002-thread-model.md` 초안 작성(맥락·결정·대안 3개·기각 이유·결과)

**AI 없는 1시간**
- 룸 액터의 단점 3가지를 스스로 적는다(룸 간 통신 비용, 룸 하나가 무거울 때, 디버깅 시 흐름 추적)

**DoD**
- [ ] 4모델 × 3케이스 비교표
- [ ] ADR-0002 초안(대안 3개와 기각 이유 포함)

#### 37일차 (화) — 룸 액터 프로토타입 🔴

**오전 (2.5h)**
- 액터 원칙: ① 룸 상태는 룸 스레드만 만진다 ② 외부는 Job으로만 요청 ③ 룸 간 통신도 Job ④ 타이머도 Job
- Job 설계: `IJob { void Execute(Room room); }` 또는 태그드 유니온. 할당 최소화(구조체 Job + 풀)
- 큐 선택: 무제한 vs 상한. **상한 + 초과 정책**(가장 오래된 것 폐기 / 세션 끊기)을 정한다

**오후 (2.5h)**
- `L3-CS-01`(C#) `Channel<IJob>` 기반 룸 액터 프로토타입 / `L3-CPP-01`(C++) MPSC 큐 + 실행 중 플래그(atomic)
- 요구: 룸 100개를 만들고 각 룸에 Job 1만 개를 넣어 **모든 Job이 순서대로, 한 번에 한 스레드에서만** 실행됨을 검증(어서션 + 카운터)

**AI 없는 1시간**
- "두 워커가 같은 룸을 동시에 실행하지 않는다"를 어떻게 코드로 보장했는지 3문장으로 쓰고, 깨질 수 있는 시나리오를 1개 상상

**DoD**
- [ ] 룸 100개 × Job 1만 개 실행, 순서 보장·동시 실행 0 검증
- [ ] 큐 상한과 초과 정책이 코드에 있다

#### 38일차 (수) — 틱·타이머·게임 루프 🔴

**오전 (2.5h)**
- 틱 기반 vs 이벤트 기반. 오목은 이벤트 기반 + 타임아웃 타이머로 충분하다(왜인지 설명할 수 있어야 함)
- Windows 타이머 정밀도: 기본 15.6ms, `timeBeginPeriod`의 영향과 부작용
- 타이머 Job 원칙: **타이머 콜백에서 룸 상태를 직접 만지지 않는다.** 룸 큐에 Job을 넣는다

**오후 (2.5h)**
- `L3-C-02` 타이머 정밀도 측정: 1ms/10ms/100ms 주기 타이머의 실제 발화 간격 분포(1,000회) 표
- `L3-C-03` 타이머 → 룸 Job 투입 구현: 턴 타임아웃(30초), 하트비트 검사(5초), 재접속 유예(60초) 3종
- 타이머 취소: 룸 파괴 시 예약된 타이머가 남지 않도록(취소 토큰 또는 세대 번호)

**AI 없는 1시간**
- 타이머 콜백에서 룸 상태를 직접 바꿨을 때 생길 수 있는 버그를 시간 순서로 적는다

**DoD**
- [ ] 타이머 정밀도 표(3구간 × 평균/최대/표준편차)
- [ ] 타이머 Job 3종 동작, 룸 파괴 시 타이머 정리 확인

#### 39일차 (목) — 상태 머신 설계 🔴

**오전 (2.5h)**
- 세션 상태: `Connected → Authenticated(닉네임) → Lobby → Matching → InGame → Result → Lobby`
- 룸 상태: `Created → Waiting → Playing → Ended → Destroying`
- 매칭 상태: `Requested → Queued → Matched / Cancelled / TimedOut`
- 각 상태 × 이벤트 → 다음 상태 표를 만들고, **불법 전이 처리 정책**(무시 / 오류 응답 / 세션 종료)을 정한다

**오후 (2.5h)**
- `L3-C-04` 상태 전이표 3종 작성(세션·룸·매칭) → `DESIGN.md`에 삽입
- `L3-C-05` 패킷 목록 설계: 2-C의 5000번대 게임 패킷 확장(§7.1 표 참고, 30종 이상)
- 시퀀스 다이어그램 3개 작성: 매칭 성사 / 착수→판정→통지 / 끊김→재접속

**AI 없는 1시간**
- "로비 상태에서 착수 패킷이 오면?" 같은 불법 전이 5개를 나열하고 각각의 처리 방침을 정한다

**DoD**
- [ ] 상태 전이표 3종, 시퀀스 다이어그램 3개
- [ ] 게임 패킷 30종 목록

#### 40일차 (금) — 설계 문서 완성 + 주간 점검

**오전 (3h)**
- `DESIGN.md` 완성(§7.1 항목 전부): 목표·비목표, 제약, 컴포넌트 다이어그램, 스레드 모델 다이어그램, 데이터 소유 표, 상태 전이표, 패킷 목록, 시퀀스 3개, 리스크
- ADR 3개 완성(스레드 모델 / 룸-세션 참조 / 매칭 방식)
- **AI "처음 보는 개발자" 리뷰**(§6.1 프롬프트): 문서만 읽고 구조를 설명하게 한 뒤, 틀리게 이해한 부분을 문서 결함으로 간주해 수정

**오후 (4h) — 주간 점검**
1. AI 없이 재구현(60분): 룸 액터 최소 버전(Job 큐 + 단일 소비 루프)
2. 설명 시험(45분): 4모델 비교, 액터 원칙, 타이머 규칙
3. 코드 리뷰(45분): 루브릭 6번(구조) 중심
4. 회고(30분): `W08.md`

**DoD**
- [ ] `DESIGN.md` 완성 + AI 리뷰 지적사항 반영
- [ ] ADR 3개 커밋

### 2.2 9주차 — 상태 머신과 게임 규칙

#### 41일차 (월) — 오목 규칙 라이브러리 🔴

**오전 (2.5h)**
- 규칙 확정: 15×15, 흑 선공, 정확히 5목 승리 여부(6목 이상=승리 인정 or 장목 무효) **택일하고 문서화**
- 자료 구조 선택: `byte[225]` 1차원 vs 2차원, 비트보드(선택). 승리 판정 알고리즘(마지막 착수 기준 4방향 스캔)
- 규칙 프로젝트는 **외부 의존 0**(소켓·로그·시간 없음). 순수 함수만

**오후 (2.5h)**
- `L3-C-06` 규칙 구현: `PlaceResult Place(Board b, int x, int y, Stone s)`, `WinCheck(Board, lastX, lastY)`, `IsDraw(Board)`
- 테스트 30개 작성(§7.2 규칙 테스트 목록 그대로)

**AI 없는 1시간**
- 승리 판정을 AI 없이 다시 구현하고 테스트 30개를 통과시킨다

**DoD**
- [ ] 규칙 테스트 30개 통과, 실행 시간 1초 이내
- [ ] Rules 프로젝트가 다른 프로젝트를 참조하지 않음(빌드로 증명)

#### 42일차 (화) — 매칭 시스템 🔴

**오전 (2.5h)**
- 매칭 큐 설계: 선착순 2인. 대기 60초 초과 시 취소 통지
- 매칭 주기: 이벤트 즉시 매칭 vs 주기적 배치(100ms) — 선택과 근거를 ADR에
- 🟡 레이팅 범위 매칭(교재 "FPS 게임 매칭 시스템" 3장): 허용 범위를 시간에 따라 확장

**오후 (2.5h)**
- `L3-C-07` 매칭 큐 구현: 요청/취소/타임아웃, 2인 성사 시 룸 생성 + 양쪽 통지
- 매칭 로직 테스트 8개: 2인 성사, 취소 후 미성사, 타임아웃 통지, 동시 요청 중복 방지, 이미 매칭 중 재요청 거부, 성사 직후 취소 처리, 룸 생성 실패 시 롤백, 대기열 순서(선착순)

**AI 없는 1시간**
- "매칭 성사와 취소 요청이 동시에 오면?"의 시간 순서를 그리고 방어를 설계

**DoD**
- [ ] 매칭 테스트 8개 통과
- [ ] 매칭 통계 로그(대기 인원, 평균 대기 시간)

#### 43일차 (수) — 로비와 룸 입장 흐름 🔴

**오전 (2.5h)**
- 로비: 닉네임 로그인(Phase 4에서 인증으로 교체), 로비 유저 목록, 로비 채팅(2-1 재사용)
- 룸 입장 흐름: 매칭 성사 → 룸 생성 → 양쪽에 `GameStartNotify`(룸ID·상대 정보·선공) → 세션 상태 `InGame`

**오후 (2.5h)**
- `L3-C-08` 핸들러 디스패치 구현: 패킷 ID → 핸들러 맵, **세션 상태별 허용 검사**, 패킷 → Job 변환
- `L3-C-09` 로비·룸 입장 구현 + 불법 전이 테스트 5개(로비에서 착수, 게임 중 매칭 요청 등)

**AI 없는 1시간**
- 디스패치 테이블을 손으로 그리고 각 패킷의 허용 상태를 채운다

**DoD**
- [ ] 로그인~매칭~룸 입장까지 클라이언트로 성공
- [ ] 불법 전이 5종 테스트 통과

#### 44일차 (목) — 서버 권위와 치트 방어 🔴

**오전 (2.5h)**
- 서버 권위 원칙: 클라이언트는 "요청"만. 서버가 턴·좌표·규칙·시간을 전부 검증
- 검증 목록: 내 턴인가 / 좌표 범위 / 빈 칸인가 / 게임 진행 중인가 / 이 룸의 참가자인가 / 속도 제한(초당 10회)
- 재현 가능한 랜덤: 선공 결정 등에 시드 고정(로그에 시드 기록 → 재현 가능)

**오후 (2.5h)**
- `L3-C-10` 착수 검증 구현 + 치트 테스트 5종: ① 내 턴 아닌 착수 ② 범위 밖 좌표 ③ 이미 놓인 자리 ④ 다른 룸 대상 패킷 ⑤ 초당 50회 폭주
- 누적 3회 위반 시 세션 종료 정책 구현

**AI 없는 1시간**
- "클라이언트가 승리 결과를 보내면 서버는 무엇을 해야 하는가"를 3문장으로(절대 신뢰하지 않는다)

**DoD**
- [ ] 치트 5종 전부 거부, 로그에 사유·세션ID 기록
- [ ] 누적 위반 3회 시 종료 동작 확인

#### 45일차 (금) — 게임 진행 완성 + 주간 점검

**오전 (3h)**
- 착수 → 판정 → 양쪽 통지 → 턴 교대 → 승패/무승부 판정 → 결과 통지 → 룸 파괴
- 턴 타임아웃 30초: 타이머 Job으로 자동 패 처리
- 기권 처리, 재매칭(결과 후 로비 복귀)

**오후 (4h) — 주간 점검**
1. AI 없이 재구현(90분): 매칭 큐(2인 성사·취소·타임아웃)
2. 설명 시험(45분): 서버 권위, 상태 전이, 매칭 경쟁
3. 코드 리뷰(45분): 루브릭 1·6번 중심
4. 회고(30분): `W09.md`

**DoD**
- [ ] 게임 1판이 로비→매칭→플레이→결과→로비까지 완주
- [ ] 턴 타임아웃·기권 동작

### 2.3 10주차 — 재접속·관전·운영 기능

#### 46일차 (월) — 세션과 유저 분리, 재접속 🔴

**오전 (2.5h)**
- **핵심 개념**: 세션(연결)과 유저(게임 상태)를 분리한다. 연결이 끊겨도 유저는 룸에 남는다
- 재접속 흐름: 끊김 감지 → 유저를 `Disconnected` 표시 + 60초 유예 타이머 → 같은 닉네임 재접속 시 유저에 새 세션 연결 → **판 상태 스냅샷 전송** → 게임 계속
- 유예 초과: 자동 패 처리 후 룸 종료

**오후 (2.5h)**
- `L3-C-11` 세션-유저 분리 리팩터링: 유저 객체가 세션 참조를 교체 가능하게
- `L3-C-12` 재접속 구현 + 테스트 6개: 정상 재접속, 유예 초과 패, 재접속 중 상대 착수, 이중 재접속 시도, 다른 룸으로 재접속 시도, 게임 종료 후 재접속

**AI 없는 1시간**
- 재접속 시 전송할 "판 상태 스냅샷"에 무엇이 들어가야 하는지 목록을 만든다(판·턴·남은 시간·상대 정보)

**DoD**
- [ ] 재접속 6종 테스트 통과, 판 상태 복구 100%
- [ ] 유예 타이머가 룸 파괴 시 정리됨

#### 47일차 (화) — 관전 기능 🔴

**오전 (2.5h)**
- 관전 설계: 진행 중 룸 목록, 관전 입장(룸당 최대 10명), 착수 통지 수신, 관전자 채팅
- 관전자는 착수 불가(권한 분리), 관전자 수가 룸 성능에 주는 영향(브로드캐스트 대상 증가)

**오후 (2.5h)**
- `L3-C-13` 관전 구현 + 테스트 5개: 입장/퇴장, 상한 초과 거부, 관전자 착수 시도 거부, 게임 종료 시 관전자 정리, 관전 중 룸 파괴
- `L3-C-14` 브로드캐스트 최적화: 한 번 직렬화한 버퍼를 참가자+관전자에게 공유(참조 카운트)

**AI 없는 1시간**
- 관전자 100명일 때 룸 스레드가 하는 일을 계산하고 병목 가능성을 적는다

**DoD**
- [ ] 관전 5종 테스트 통과
- [ ] 브로드캐스트 1회 직렬화 공유 구현(할당 측정으로 증명)

#### 48일차 (수) — 구조화 로깅·설정·종료 🔴

**오전 (2.5h)**
- 구조화 로그 필드 표준: `timestamp, level, sessionId, userId, roomId, packetId, event, durationMs`
- 로그 레벨 정책: Debug(개발), Info(상태 전이·매칭·게임 결과), Warn(치트·타임아웃), Error(예외)
- C#은 **Serilog**(구조화 로그 + 비동기 싱크 + 일 단위 롤링), C++은 spdlog(비동기 싱크)

**오후 (2.5h)**
- `L3-CS-02`/`L3-CPP-02` 로깅 구성: Serilog/spdlog 설정, 필드 표준 적용, 로그 파일 롤링
- `L3-C-15` 설정 파일: 포트·워커 수·룸 상한·턴 시간·타임아웃·로그 레벨. 환경별 파일 분리
- `L3-C-16` graceful shutdown: 신규 접속 차단 → 모든 룸에 종료 Job → 진행 중 게임 "무효" 통지 → 세션 정리 → 스레드 조인(5초 내)

**AI 없는 1시간**
- 로그만 보고 "특정 게임 1판의 전 과정"을 추적할 수 있는지 확인하고, 부족한 필드를 보강

**DoD**
- [ ] 게임 1판의 전 과정이 로그로 추적 가능(roomId 필터)
- [ ] 종료 5초 내 완료, 단계 순서가 로그에 남음

#### 49일차 (목) — 시나리오 클라이언트 확장 (과제 3-2) 🔴

**오전 (2.5h)**
- 2-3 클라이언트에 봇 로직 추가: 규칙 라이브러리를 참조해 **합법 수만** 두는 봇(무작위 빈 칸)
- 시나리오 `omok`: 접속 → 로비 → 매칭 → 게임(턴당 0.5~2초) → 결과 → 재매칭 반복

**오후 (2.5h)**
- `L3-C-17` 시나리오 3종 추가: `spectate`(관전 입장 후 통지 카운트), `reconnect`(N초 후 강제 끊김 → 30초 후 재접속 → 판 일치 확인), `cheat`(치트 4종 시도 후 거부율 집계)
- 통계 확장: 룸 수, 게임 완료 수, 착수→통지 p50/p95/p99, 재접속 성공률, 치트 거부율

**AI 없는 1시간**
- 봇이 "합법 수만 둔다"를 어떻게 보장했는지 확인하고, 규칙 라이브러리 재사용의 이점을 3줄

**DoD**
- [ ] 시나리오 4종(omok/spectate/reconnect/cheat) 동작
- [ ] 통계 출력에 p99·재접속 성공률·치트 거부율 포함

#### 50일차 (금) — 통합과 주간 점검

**오전 (3h)**
- 통합 실행: 10룸 규모로 전 기능(매칭·플레이·관전·재접속·치트 방어) 동시 검증
- 발견된 버그 수정, 로직 테스트 25개 채우기

**오후 (4h) — 주간 점검**
1. AI 없이 재구현(90분): 세션-유저 분리 + 재접속 스냅샷
2. 설명 시험(45분): 재접속 설계, 관전 브로드캐스트, 종료 시퀀스
3. 코드 리뷰(45분): 루브릭 2·3번 중심(룸 파괴와 타이머 정리)
4. 회고(30분): `W10.md`

**DoD**
- [ ] 10룸 통합 시나리오 무사고
- [ ] 로직 테스트 25개 통과

### 2.4 11주차 — 부하와 정합성

#### 51일차 (월) — 100룸 1차 부하 🔴

**오전 (2.5h)**
- 부하 조건 설정: 100룸(플레이 200 + 관전 100 = 300접속), 10분, 착수 p99 50ms 목표
- 측정 항목: 착수→통지 지연 p50/p95/p99, 룸 큐 최대 길이, 메모리(1분 vs 10분), CPU, 게임 완료 수

**오후 (2.5h)**
- 1차 실행 및 결과 기록. 병목 가설 3개 수립(§6.1 프롬프트로 AI 예측도 받아 비교)
- 흔한 병목: 룸당 Task/스레드 과다, 브로드캐스트 직렬화 반복, 로그 동기 쓰기, 타이머 스레드 과부하, GC(C#)

**AI 없는 1시간**
- 측정 결과를 보고 "가장 의심스러운 3곳"을 스스로 지목하고 확인 방법을 적는다

**DoD**
- [ ] 100룸 10분 1회 완주, 결과 표 기록
- [ ] 병목 가설 3개 + 각 확인 방법

#### 52일차 (화) — 병목 수정과 재측정 🔴

**오전 (2.5h)**
- 가설 검증: 카운터·로그·프로파일러(C#: `dotnet-counters`, C++: VS Profiler)로 확인
- 채택된 가설만 수정. **기각된 가설도 리포트에 남긴다**(중요)

**오후 (2.5h)**
- 수정 후 재측정, 전/후 비교표 작성
- 대표 최적화: 브로드캐스트 버퍼 공유, Job 객체 풀링, 로그 비동기화, 룸 배정 방식 변경(라운드 로빈 → 실행 가능 룸 큐)

**AI 없는 1시간**
- 전/후 수치를 근거로 "무엇이 병목이었는가"를 3문단으로

**DoD**
- [ ] 가설 3개 중 최소 1개 채택·수정·재측정
- [ ] 기각 가설 1개 이상 기록

#### 53일차 (수) — 대규모 재접속·룸 파괴 시나리오 🔴

**오전 (2.5h)**
- 시나리오: 게임 중 100세션 동시 끊김 → 30초 후 전원 재접속 → 판 상태 복구율 확인
- 시나리오: 룸 파괴 중 패킷 도착(착수/채팅/관전 입장) → 크래시 0, 로그에 무시 기록

**오후 (2.5h)**
- `L3-C-18` 위 두 시나리오 자동화 스크립트 작성 및 5회 반복
- C++: ASan 빌드로 1회 통과. C#: 미반환 풀 버퍼 0 확인

**AI 없는 1시간**
- "룸이 파괴된 뒤 큐에 남은 Job은 어떻게 되는가"를 코드 경로로 추적해 적는다

**DoD**
- [ ] 재접속 복구율 100%, 룸 파괴 경쟁 크래시 0
- [ ] C++ ASan 통과 로그

#### 54일차 (목) — 설계-코드 정합성 갱신 🔴

**오전 (2.5h)**
- `DESIGN.md`에 **"구현 후 달라진 점"** 절 작성: 항목 / 설계 / 실제 / 이유 표
- 다이어그램 갱신(스레드 모델·컴포넌트가 바뀌었으면 반영), 변경 이력 커밋

**오후 (2.5h)**
- `LOAD-3-1.md` 완성(§7.2 형식)
- C#: `L3-CS-03` 라이브러리 분석 읽기(FastSocketLite·LiteNetwork·laster40Net) → 자기 서버와 스레드 모델 비교표
- C++: `L3-CPP-03` `IocpNetLib` 채팅 서버에서 "로직 스레드가 어디인가" 찾기 → 비교표. 🟡 Asio strand 비교

**AI 없는 1시간**
- 설계와 실제가 달라진 이유 중 "설계가 틀렸던 것"과 "구현이 타협한 것"을 구분해 적는다

**DoD**
- [ ] `DESIGN.md` 갱신본(달라진 점 표 5행 이상)
- [ ] `LOAD-3-1.md` 완성

#### 55일차 (금) — Phase 3 평가 🔴

**오전 (3h)**
1. **재구현 시험(120분, AI 없음)**: §8.2 — 룸 액터(Job 큐 + 단일 소비 + 타이머 Job + 룸 간 메시지) + 제공 테스트 4개
2. **설명 시험(45분)**: §8.3에서 10문항
3. **결함 찾기(45분)**: §8.4 — 룸 로직 결함 5개 중 4개

**오후 (3h)**
- 체크리스트(§8.1) 점검, 룸 로직 락 0 검색 결과 캡처(`grep -r "lock\|mutex" Logic/Room*`)
- `W11.md` + Phase 3 회고
- §10 Phase 4 준비

**DoD**
- [ ] §8.1 전 항목, 설명 시험 평균 4.0 이상
- [ ] 룸 로직 락 0 증명 캡처

---

## 3. 실습 카탈로그

### 3.1 공통 실습 (L3-C)

#### L3-C-01 스레드 모델 비교표 (60분) 🔴
- **목표**: 설계 결정을 "취향"이 아니라 "근거"로 만든다
- **단계**: 4모델 × 5기준(처리량·지연·복잡도·확장성·디버깅) 5점 척도 표 → 3케이스(오목 100룸 / MMO 존 / FPS 매치)별 선택과 근거 3줄씩
- **확인 기준**: 각 선택에 "이 케이스의 어떤 특성 때문인가"가 명시됨
- **노트**: 룸 액터의 단점 3가지

#### L3-C-02 타이머 정밀도 측정 (45분)
- **단계**: 1ms/10ms/100ms 주기로 1,000회 발화시켜 실제 간격의 평균·최대·표준편차 측정. `timeBeginPeriod(1)` 적용 전후 비교(적용 시 전력 소모 증가도 언급)
- **확인 기준**: 3구간 × 전/후 표
- **노트**: 턴 타임아웃 30초에 이 정밀도가 문제가 되는가(안 된다면 왜)

#### L3-C-03 타이머 → 룸 Job 투입 (60분) 🔴
- **단계**: 타이머 콜백이 **룸 상태를 만지지 않고** 큐에 Job만 넣도록 구현. 턴 타임아웃/하트비트/재접속 유예 3종
- **확인 기준**: 룸 파괴 후 타이머가 발화해도 크래시 없이 무시됨(세대 번호 또는 취소 토큰)
- **자주 나는 오류**: 타이머가 룸 객체를 강하게 참조해 파괴가 지연됨 → ID 참조로

#### L3-C-04 상태 전이표 3종 (60분) 🔴
- **단계**: 세션·룸·매칭 각각 (현재 상태 × 이벤트 → 다음 상태 + 동작 + 불법 시 처리) 표 작성
- **확인 기준**: 모든 셀이 채워짐(불가능한 조합은 "불법-무시" 등으로 명시)

#### L3-C-05 게임 패킷 설계 (60분) 🔴
- **단계**: 5000번대 게임 패킷 30종 이상 정의(§7.1 표 참고). 각 패킷의 필드·허용 상태·응답 관계
- **확인 기준**: `PROTOCOL.md`에 추가되고 2-2 라이브러리로 직렬화 가능

#### L3-C-06 오목 규칙 구현 (150분) 🔴
- **API**
  ```
  enum class PlaceResult { Ok, OutOfRange, Occupied, NotYourTurn, GameOver }
  PlaceResult Place(Board&, int x, int y, Stone);
  WinState  CheckWin(const Board&, int lastX, int lastY);  // None/BlackWin/WhiteWin
  bool      IsDraw(const Board&);
  ```
- **확인 기준**: §7.2 규칙 테스트 30개 통과, 1초 이내 완료, 외부 의존 0

#### L3-C-07 매칭 큐 (120분) 🔴
- **단계**: 선착순 2인 매칭, 취소, 60초 타임아웃, 성사 시 룸 생성·통지
- **확인 기준**: 매칭 테스트 8개 통과(§2 42일차 목록)
- **자주 나는 오류**: 성사 순간 한쪽이 끊김 → 상대를 큐로 되돌리는 처리 누락

#### L3-C-08 핸들러 디스패치 (90분) 🔴
- **단계**: 패킷 ID → 핸들러 테이블, 상태별 허용 검사, 패킷 → Job 변환(버퍼 소유권 이동)
- **확인 기준**: 불법 전이 5종이 전부 거부되고 로그에 사유가 남음

#### L3-C-09 로비·룸 입장 (120분) 🔴
- **단계**: 닉네임 로그인, 로비 유저 목록, 로비 채팅, 매칭 성사 → 룸 입장 → `GameStartNotify`
- **확인 기준**: 클라이언트로 로그인~게임 시작까지 성공

#### L3-C-10 착수 검증과 치트 방어 (90분) 🔴
- **단계**: 6가지 검증(턴·범위·빈 칸·게임 상태·룸 소속·속도 제한) 구현, 위반 시 거부 응답 + 로그, 누적 3회 시 종료
- **확인 기준**: 치트 5종 테스트 전부 거부

#### L3-C-11 세션-유저 분리 (90분) 🔴
- **단계**: `User`(게임 상태·룸 소속) 와 `Session`(연결) 분리, 유저의 세션 참조를 교체 가능하게
- **확인 기준**: 세션이 끊겨도 유저 객체와 룸 상태가 유지됨

#### L3-C-12 재접속 (120분) 🔴
- **단계**: 끊김 감지 → `Disconnected` 표시 + 60초 유예 → 재접속 시 세션 교체 → 스냅샷 전송 → 계속
- **확인 기준**: 재접속 6종 테스트 통과, 판 상태 100% 일치

#### L3-C-13 관전 (90분)
- **단계**: 진행 중 룸 목록, 관전 입장(상한 10), 통지 수신, 관전자 채팅, 관전자 착수 거부
- **확인 기준**: 관전 5종 테스트 통과

#### L3-C-14 브로드캐스트 최적화 (60분) 🔴
- **단계**: 한 번 직렬화한 버퍼를 참가자·관전자에게 공유(참조 카운트 또는 복사 최소화)
- **확인 기준**: 관전자 10명일 때 직렬화 횟수 1회(카운터로 증명), 할당량 전/후 비교

#### L3-C-15 설정 파일 (45분)
- **단계**: 포트·워커 수·룸 상한·턴 시간·타임아웃·로그 레벨, 환경별 파일 분리, 시작 시 유효성 검사
- **확인 기준**: 잘못된 설정(음수 포트 등)에서 명확한 오류로 기동 실패

#### L3-C-16 graceful shutdown (60분) 🔴
- **단계**: 신규 접속 차단 → 모든 룸에 종료 Job → 진행 중 게임 "무효" 통지 → 세션 정리 → 스레드 조인
- **확인 기준**: 5초 내 완료, 단계별 로그, 종료 중 도착한 패킷 무시

#### L3-C-17 시나리오 클라이언트 확장 (150분) 🔴
- **단계**: `omok`/`spectate`/`reconnect`/`cheat` 4종 + 통계(p99·복구율·거부율)
- **확인 기준**: 100룸 실행 결과 파일 생성

#### L3-C-18 대규모 재접속·룸 파괴 경쟁 (90분) 🔴
- **단계**: 100세션 동시 끊김 → 30초 후 재접속 5회 반복 / 룸 파괴 중 패킷 폭탄
- **확인 기준**: 복구율 100%, 크래시 0, (C++) ASan 통과

### 3.2 C# 트랙 실습 (L3-CS)

#### L3-CS-01 Channel 기반 룸 액터 (150분) 🔴
- **골격**
  ```csharp
  public interface IJob { void Execute(Room room); }

  public sealed class Room
  {
      private readonly Channel<IJob> _jobs =
          Channel.CreateBounded<IJob>(new BoundedChannelOptions(1024)
          { FullMode = BoundedChannelFullMode.DropWrite, SingleReader = true });

      public bool Post(IJob job) => _jobs.Writer.TryWrite(job);   // 실패 시 정책 적용

      public async Task RunAsync(CancellationToken ct)            // 룸당 1개 루프
      {
          await foreach (var job in _jobs.Reader.ReadAllAsync(ct))
              try { job.Execute(this); } catch (Exception ex) { Log.Error(ex, "job failed"); }
      }
  }
  ```
- **확인 기준**: 룸 100개 동시 실행, 각 룸의 Job 순서 보장, 룸 로직에 `lock` 0개
- **노트**: `SingleReader=true`가 주는 이점, Bounded 초과 정책 선택 근거

#### L3-CS-02 Serilog 구조화 로깅 (60분) 🔴
- **단계**: `Log.Logger = new LoggerConfiguration().Enrich.WithProperty("app","omok").WriteTo.Async(a => a.File("logs/log-.txt", rollingInterval: RollingInterval.Day)).CreateLogger();`
  로그 호출은 `Log.Information("place {RoomId} {UserId} {X} {Y} {DurationMs}", ...)` 형태로 **필드화**
- **확인 기준**: 로그 파일에서 `RoomId`로 한 게임 전체 추적 가능, 비동기 싱크로 지연 영향 최소

#### L3-CS-03 네트워크 라이브러리 분석 (90분)
- **단계**: FastSocketLite·LiteNetwork·laster40Net 분석서에서 수신 흐름·Send 흐름·스레드 모델만 읽고 자기 서버와 비교표 4행

### 3.3 C++ 트랙 실습 (L3-CPP)

#### L3-CPP-01 룸 Job 큐 + 워커 배정 (180분) 🔴
- **골격**
  ```cpp
  class Room {
  public:
      bool Post(Job job);           // MPSC 큐에 투입, 상한 초과 시 정책
      void Drain();                 // 워커가 호출: 큐를 비우며 Job 실행
      std::atomic<bool> running_{false};   // 한 룸은 한 워커만
  };
  // 스케줄러: Post 후 running_ CAS 성공 시 "실행 가능 룸 큐"에 넣고, 워커가 꺼내 Drain
  ```
- **확인 기준**: 두 워커가 같은 룸을 동시에 Drain 하지 않음(assert + 카운터), 룸 로직에 `mutex` 0개
- **노트**: `running_` CAS 실패 시 어떻게 되는가(다른 워커가 이미 처리 중 → 그대로 반환)

#### L3-CPP-02 spdlog 비동기 로깅 (60분) 🔴
- **단계**: `spdlog::init_thread_pool`, 비동기 파일 싱크, 일 단위 롤링, 필드 형식 통일(`[roomId=..][userId=..]`)
- **확인 기준**: 부하 중 로그 때문에 p99가 튀지 않음(로그 on/off 비교)

#### L3-CPP-03 IocpNetLib 로직 스레드 분석 (90분)
- **단계**: 교재 채팅 서버에서 "로직이 어느 스레드에서 도는가"를 코드로 추적 → 자기 룸 액터와 비교표

---

## 4. 학습 항목 상세

### 4.1 공통 (90h)

**스레드 모델 (14h)**
- 무엇: 4모델(세션당 스레드 / I/O + 로직 단일 / 워커 풀 + 락 / 룸 액터)의 처리량·지연·복잡도·확장성·디버깅 난이도
- 왜: 게임 서버 설계의 첫 결정이고 면접 단골이며, 잘못 고르면 전부 다시 짠다
- 어떻게: `L3-C-01` 비교표 → ADR-0002 작성 → 프로토타입으로 검증
- 확인: 3케이스 선택 근거표, ADR

**룸 액터 패턴 (12h)**
- 무엇: 룸별 Job 큐, 단일 소비, 룸 간 메시지, 스케줄링(워커 배정), 타이머 Job, 큐 상한 정책
- 왜: 락 없이 게임 로직을 쓸 수 있고 데이터 경쟁이 구조적으로 사라진다
- 어떻게: `L3-CS-01`/`L3-CPP-01` 프로토타입 → 100룸 검증 → 실제 서버에 적용
- 확인: 룸 로직 락 0(코드 검색), 동시 실행 0(assert), 100룸 Job 순서 보장

**게임 루프·틱·타이머 (8h)**
- 무엇: 틱 vs 이벤트 기반, 타이머 정밀도(기본 15.6ms), 지연 Job, 취소
- 왜: 턴 타임아웃·하트비트·재접속 유예가 전부 타이머다
- 어떻게: `L3-C-02` 측정 → `L3-C-03` Job 투입 구조
- 확인: 정밀도 표, 룸 파괴 후 타이머 무해성

**상태 머신 (10h)**
- 무엇: 세션·룸·매칭 상태와 전이, 허용 패킷, 불법 전이 처리
- 왜: 명시하지 않으면 "로비에서 착수" 같은 입력에 서버가 이상 동작한다
- 어떻게: `L3-C-04` 표 작성 → `L3-C-08` 디스패치에 반영 → 테스트로 고정
- 확인: 전이표 3종 + 불법 전이 5종 테스트

**서버 권위와 검증 (8h)**
- 무엇: 6가지 검증(턴·범위·빈 칸·게임 상태·룸 소속·속도), 재현 가능한 랜덤, 치트 시나리오
- 왜: 온라인 게임 신뢰 모델의 기초
- 어떻게: `L3-C-10` → 치트 5종 테스트 → 누적 위반 정책
- 확인: 치트 5종 전부 거부 + 로그

**동기화 개념 (8h)**
- 무엇: 상태 동기화 vs 입력 동기화, 스냅샷·델타, 보간, 예측·되감기(개념), 틱 레이트와 대역폭, AOI
- 왜: 오목에는 불필요하지만 Phase 6 확장과 면접에 필요
- 어떻게: 교재 로드맵 Ch.4 → **"오목에 필요한가/불필요한가" 판정표**를 만드는 것이 핵심 훈련
- 확인: "30Hz·100명·위치 16B" 대역폭 계산과 절감 방법 3가지

**설계 문서와 ADR (10h)**
- 무엇: 설계 문서 구조, ADR 형식, 다이어그램(컴포넌트·시퀀스·상태), 구현 후 갱신
- 왜: 실무에서 코드보다 먼저 검토받는 산출물이고 캡스톤 평가의 축
- 어떻게: 39~40일차에 작성 → AI "처음 보는 개발자" 리뷰 → 54일차에 갱신
- 확인: AI가 문서만으로 구조를 정확히 재설명, "달라진 점" 표 5행 이상

**계층 분리와 테스트 (10h)**
- 무엇: 규칙(순수) / 로직(룸·매칭) / 네트워크(세션) 3계층, 인터페이스로 네트워크 추상화, 페이크 세션
- 왜: 규칙 버그를 서버 없이 1초 만에 잡기 위해
- 어떻게: Rules 프로젝트를 의존성 0으로 유지 → Logic은 `ISessionSender`만 안다
- 확인: 규칙 테스트 30개가 네트워크 참조 없이 1초 내 실행

**로깅·설정·종료 (10h)**
- 무엇: 구조화 로그 필드 표준, 레벨 정책, 설정 파일·환경 분리, 종료 순서
- 왜: Phase 5 관측·운영의 기반
- 어떻게: `L3-CS-02`/`L3-CPP-02` → `L3-C-15` → `L3-C-16`
- 확인: roomId로 한 게임 추적 가능, 종료 5초 내 완료

### 4.2 C# 트랙 (70h)

| 항목 | 시간 | 무엇을 | 학습 순서 | 확인 방법 |
|---|---|---|---|---|
| Channel 기반 룸 액터 | 16h | 룸당 `Channel<IJob>`(Bounded), 단일 소비 루프, 룸 Task 수명, 취소, 룸 간 메시지 | L3-CS-01 → 3-1 적용 | 100룸 독립 실행, 룸 로직 lock 0 |
| 타이머 | 6h | `PeriodicTimer`, 타이머 Job 투입, 취소 토큰, 룸 파괴 시 정리 | L3-C-02 → L3-C-03 | 턴 타임아웃 테스트 |
| 핸들러 디스패치 | 8h | 패킷 ID → 핸들러(딕셔너리/소스 제너레이터), 상태별 허용 검사, 패킷 → Job | L3-C-08 | 불법 전이 5종 |
| 로직-네트워크 분리 | 8h | `ISessionSender`, 로직이 소켓 미참조, 페이크 세션 | L3-C-09 이후 리팩터 | 로직 테스트가 Net 참조 없이 컴파일 |
| 호스팅·설정·로깅 | 10h | Generic Host, `IHostedService`, `IOptions<T>`, **Serilog** 구조화 로그, `IHostApplicationLifetime` | L3-CS-02 → L3-C-15/16 | 종료 시퀀스 로그 |
| 오목 규칙 | 6h | 판 표현, 승리 판정(4방향), 무승부, 6목 정책 | L3-C-06 | 규칙 테스트 30개 |
| 라이브러리 분석 | 8h | FastSocketLite/LiteNetwork/laster40Net의 스레드 모델·흐름 | L3-CS-03 | 비교표 |
| 부하·수정 | 8h | 100룸 시나리오, 병목 가설·수정·재측정 | 51~52일차 | `LOAD-3-1.md` |

### 4.3 C++ 트랙 (70h)

| 항목 | 시간 | 무엇을 | 학습 순서 | 확인 방법 |
|---|---|---|---|---|
| 룸 Job 큐와 스케줄링 | 16h | 룸별 MPSC 큐(1-1 재사용), 실행 중 플래그(atomic CAS), 실행 가능 룸 큐, IOCP 워커와 로직 워커 분리 여부 | L3-CPP-01 | 동시 실행 0 assert, mutex 0 |
| 타이머 | 8h | 타이머 휠 또는 `priority_queue` 스레드, 만료 시 룸 큐 투입, 고해상도 | L3-C-02 → L3-C-03 | 정밀도 표, 타임아웃 테스트 |
| 핸들러 디스패치 | 6h | 패킷 ID → 함수 테이블, 상태 검사, 버퍼 소유권 이동 | L3-C-08 | 불법 전이 테스트 |
| 객체 수명 | 10h | 세션·유저·룸 참조 방식(ID 참조 권장), 룸 파괴 시점, 정리 순서, 메모리 풀 적용 | L3-C-11 → L3-C-18 | 룸 파괴 경쟁 ASan 통과 |
| 로깅·설정·종료 | 8h | spdlog 비동기, toml++/nlohmann 설정, `SetConsoleCtrlHandler` 종료 | L3-CPP-02 → L3-C-16 | 종료 시퀀스 로그 |
| 오목 규칙 | 6h | 고정 배열 판, 승리 판정, 무승부 | L3-C-06 | GoogleTest 30개 |
| 비교 읽기 🟡 | 8h | Asio strand ↔ 룸 액터 대응 | L3-CPP-03 이후 | 비교표 |
| 부하·수정 | 8h | 100룸, 병목 수정 | 51~52일차 | `LOAD-3-1.md` |

---

## 5. 교재 활용 가이드 (Phase 3)

### 5.1 공통

| 교재 | 읽을 장 | 언제 | 어떻게 활용 |
|---|---|---|---|
| 온라인 게임 개발자를 위한 네트워크 학습 로드맵 | Ch.3(게임 네트워크 아키텍처·Authority), Ch.4(동기화와 상태 관리·Tick Rate·Lag Compensation), Ch.5(성능 최적화) | 36~47일차 | Ch.3 Server Authoritative 절은 §4.1 "서버 권위"의 교재. **읽은 뒤 "오목 서버에 필요한가/불필요한가" 항목별 판정표를 만드는 것이 과제**(적용 판단 훈련). Ch.4는 3-3에서 적용 |
| 게임 개발자를 위한 C# 디자인 패턴 | 7장(Command), 8장(Observer), 9장(State), 10장(Strategy), 11장(Game Loop), 12장(Update Method), 16장(안티패턴) | 36~39일차 | Command = Job, State = 상태 머신, Game Loop/Update = 틱 처리에 대응. C++ 트랙도 개념 교재로 읽고 옮긴다. **각 패턴을 자기 서버 어디에 썼는지 `DESIGN.md`에 표기** |
| FPS 게임 매칭 시스템 만들기 | 1장(기본 개념), 3장(매칭 알고리즘 설계) | 42일차 | 오목은 "2인 선착순"으로 충분하나 3장을 읽고 🟡 레이팅 범위 매칭을 선택 기능으로. 4~7장은 Phase 6 |
| 온라인 게임 클라이언트 개발자를 위한 네트워크 지식 | 4장(주요 이슈와 해결), 5장(실시간 게임플레이) | 46~47일차 | "서버가 이렇게 보내면 클라이언트는 무엇을 하는가"를 재접속 스냅샷·관전 통지 설계에 반영 |
| ECS 기반 온라인 게임 서버 🟡 | 1장(ECS 소개), 3장(기본 서버 아키텍처), `omok_ecs.md` | 54일차 | 룸 액터/OOP와 다른 아키텍처 비교. 같은 오목을 ECS로 만든 예를 보고 "내 설계와 차이 5개" |

### 5.2 C# 트랙

| 교재 | 읽을 장 | 언제 | 어떻게 활용 |
|---|---|---|---|
| C# async/await 라이브러리 만들기 | 16장(Channel 기반 액터 모델), 17장(Room/Session 비동기 패턴), 18장(백프레셔와 부하 격리) | 37일차 | **룸 액터의 직접 교재.** `samples/src/Chapter16~18` 빌드 후 자기 액터와 비교. 18장 백프레셔는 룸 큐 상한 정책 근거 |
| 게임 서버 개발을 위한 C# Socket 프로그래밍 | 11장(오목 게임 서버) | 41~43일차 | 참고 설계. **베끼지 말고** "책의 스레드 모델 vs 내 룸 액터" 차이를 ADR로 |
| SuperSocketLite를 이용한 C# 게임 서버 프로그래밍 | 7장(온라인 오목 Server/Client) | 43~49일차 | `codes/OnlineOmok`을 .NET 10으로 올려 빌드해 플레이. WinForms 클라이언트를 3-3 대신 개조해 쓰는 것도 허용 |
| C# Async/Await 완전 정복 | 10장(실전 패턴: 틱 루프·백프레셔·그레이스풀 셧다운) | 48일차 | 종료 시퀀스 구현 체크리스트 |
| 학습을 위한 C# 네트워크 라이브러리 분석 | 세 라이브러리의 수신/송신 흐름·전략 절 | 54일차 | `*.txt`를 AI에 주고 "이 라이브러리에 룸 액터에 해당하는 것이 있는가" 질문 |

### 5.3 C++ 트랙

| 교재 | 읽을 장 | 언제 | 어떻게 활용 |
|---|---|---|---|
| 모던 Windows 멀티스레딩 | 6장(Thread Pool API), 11장(IOCP + Thread Pool 아키텍처), 12장(C++23 시너지) 🟡 | 36~38일차 | **11장이 이 Phase의 핵심 교재.** 책 아키텍처를 그대로 쓰지 말고 "룸 액터를 어디에 얹을지"를 결정해 ADR |
| 게임 서버 개발자가 알아야할 TCP/IP Windows 소켓 프로그래밍 | 11장(IOCP 채팅 재독), 14장(지연 처리·배치 처리) | 37~47일차 | 14장 지연 처리는 타이머 Job·배치 브로드캐스트의 근거 |
| 게임 서버 개발자를 위한 최신 Win32 API 프로그래밍 | 2장(메모리 풀 재독), 4장(스레드 우선순위·affinity) | 51~52일차 | 로직 워커의 affinity/우선순위 실험 후 효과 측정(효과 없으면 그것도 결론) |
| C++ Boost.Asio로 만드는 온라인 게임 서버 🟡 | 11장(채팅), 12장(strand), 13장(타이머), 17장(확장 설계) | 54일차 | strand = 룸 액터의 직렬화 보장. 비교용이며 경로를 갈아타지 않는다 |

### 5.4 부 트랙 읽기 과제

- C# 주 트랙: 모던 Windows 멀티스레딩 11장 → "네이티브 IOCP 서버와 .NET 서버의 스레드 구성 차이" 1페이지
- C++ 주 트랙: async/await 라이브러리 만들기 16장 → "Channel 액터를 C++로 옮기면 무엇이 필요한가" 1페이지

---

## 6. AI 협업 가이드 (Phase 3 특화)

### 6.1 프롬프트

**설계 리뷰(문서를 먼저 쓴 뒤)**
```
다음은 내 오목 서버 설계 문서다. (DESIGN.md 붙여넣기)
시니어 게임 서버 개발자 관점에서 리뷰해 달라.
1) 장애 관점(세션 끊김·룸 파괴·타임아웃이 동시에 발생)에서 취약한 지점 2개
2) 확장 관점(룸 1만 개, 서버 2대)에서 막히는 지점 2개
3) 운영 관점(로그·설정·종료)에서 빠진 것 2개
각 지점마다 내가 반박할 기회를 달라. 내 반박이 타당하면 인정하고, 아니면 근거를 대라.
```

**"처음 보는 개발자" 문서 테스트**
```
너는 이 프로젝트를 처음 보는 개발자다. 첨부한 설계 문서만 읽고
① 서버의 스레드 구성 ② 룸 상태를 누가 변경하는가 ③ 착수 요청이 처리되는 경로
를 네 말로 설명해 보라. 문서에 없어서 추측한 부분은 "추측"이라고 표시하라.
```
→ AI가 추측한 부분이 곧 문서의 결함이다. 그 부분을 문서에 채운다.

**병목 예측 → 검증**
```
이 서버(설계 문서 첨부)를 100룸·300접속으로 돌리면 병목이 어디일지 상위 3개를 예측해 달라.
각 예측에 확인 방법(카운터·로그·프로파일러 뷰)을 붙여 달라.
결론을 먼저 내리지 말라. 나는 실측 후 결과를 가져와 예측과 비교하겠다.
```

**결함 주입(룸 로직)**
```
내 룸 액터 코드(첨부)를 기반으로 경쟁 조건 2개·타이머 누수 1개·재접속 상태 오류 1개·서버 권위 우회 1개를 숨긴 버전을 만들어 달라.
결함은 컴파일되고 대부분의 실행에서 정상 동작해야 한다.
정답은 내가 "정답 공개"라고 하기 전까지 비공개.
```

**규칙 테스트 케이스 뽑기**
```
오목 승리 판정 함수의 테스트 케이스 목록을 뽑아 달라(경계·모서리, 6목 이상, 대각선, 무승부, 종료된 판에 착수 등).
나는 이 목록에서 빠진 케이스를 최소 5개 추가하겠다. 내가 추가한 케이스가 타당한지 평가해 달라.
```

### 6.2 맡길 것 / 맡기지 않을 것

| 맡긴다 | 맡기지 않는다 |
|---|---|
| 핸들러 디스패치 보일러플레이트, 설정 로딩 | 스레드 모델 선택, 룸 액터 핵심 루프 |
| 규칙 테스트 케이스 목록 초안 | 설계 문서 본문(리뷰만 받는다) |
| 로그 필드 표준화 코드, 스냅샷 직렬화 보일러플레이트 | 종료 시퀀스 순서 결정, 재접속 정책 |
| 결함 주입 코드 | 병목 판정의 최종 결론 |

### 6.3 AI가 자주 틀리는 지점

- **타이머 콜백에서 룸 상태를 직접 수정**하는 코드를 "간단하니까"라며 제안 → 액터 원칙 위반, 반드시 Job으로
- `Channel<T>` **Unbounded**를 기본 제안 → 백프레셔 없음. 상한과 초과 정책을 요구
- C++에서 룸을 `shared_ptr`로 사방에서 참조하게 만들어 파괴 시점이 불명확 → **ID 참조 + 소유자 명시**
- "서버 권위"를 말하면서 클라이언트가 보낸 승패 결과를 저장하는 예제
- 재접속을 세션 재사용으로 구현(세션과 유저를 같은 객체로) → 판 상태 유실
- 관전자 브로드캐스트에서 인원수만큼 직렬화

---

## 7. 과제 명세

### 7.1 공통 과제 3-C. 설계 문서 + ADR (필수, 16h, 36~40·54일차)

**목표**: 구현 전에 설계를 글로 고정하고, 구현 후 차이를 갱신한다. **이 과제가 Phase 6 캡스톤 문서의 원형이다.**

**`DESIGN.md` 필수 항목** (템플릿: `08-templates.md` T4)

1. **목표와 비목표**: 이번에 하는 것 / 하지 않는 것(예: 인증은 Phase 4, 다중 서버는 Phase 6)
2. **제약**: Windows 단일 프로세스, 목표 접속 300, 룸 100, 착수 p99 50ms, 메모리 상한
3. **컴포넌트 다이어그램**: 네트워크·세션 매니저·로비·매칭·룸·규칙·타이머·로그·설정
4. **스레드 모델 다이어그램**: 어떤 스레드가 무엇을 실행하는지, 스레드 간 큐를 화살표로. 스레드 개수 산정 근거
5. **데이터 소유 표**

   | 데이터 | 소유 컴포넌트 | 변경 가능 스레드 | 읽기 허용 | 비고 |
   |---|---|---|---|---|
   | 세션 목록 | SessionManager | I/O 스레드 | 로직(스냅샷) | 동시 수정 금지 |
   | 룸 상태(판·턴) | Room | 해당 룸 실행 워커 1개 | 없음(Job 경유) | 락 없음 |
   | 매칭 큐 | Matcher | 매칭 스레드 1개 | 없음 | |
   | 유저 목록 | UserManager | 로직 스레드 | | 재접속용 |

6. **상태 전이표 3종**(세션·룸·매칭): 상태 × 이벤트 → 다음 상태 + 동작 + 불법 시 처리
7. **패킷 목록**: 2-C 확장, 게임 패킷 30종 이상(아래 표 참고)
8. **시퀀스 다이어그램 3개**: ① 매칭 성사 ② 착수→판정→통지 ③ 끊김→재접속
9. **리스크와 대응**: 룸 파괴 중 패킷 도착, 타이머와 종료 경쟁, 관전자 폭증, 큐 포화
10. **(구현 후 추가) 설계와 달라진 점**: 항목 / 설계 / 실제 / 이유 표 — 5행 이상

**게임 패킷 목록(5000번대, 최소 30종 중 핵심 발췌)**

| ID | 이름 | 방향 | 필드 | 허용 상태 |
|---|---|---|---|---|
| 5000 | MatchReq | C→S | mode:uint8 | Lobby |
| 5001 | MatchRes | S→C | result:uint16 | - |
| 5002 | MatchCancelReq | C→S | - | Matching |
| 5003 | MatchCancelRes | S→C | result:uint16 | - |
| 5004 | MatchTimeoutNotify | S→C | waitedMs:int32 | - |
| 5010 | GameStartNotify | S→C | roomId:int32, opponent:{userId,name}, myStone:uint8, turnTimeoutMs:int32 | - |
| 5020 | PlaceReq | C→S | x:uint8, y:uint8, turnSeq:int32 | InGame |
| 5021 | PlaceRes | S→C | result:uint16 | - |
| 5022 | PlaceNotify | S→C | userId:int32, x:uint8, y:uint8, nextTurnUserId:int32, turnSeq:int32, remainMs:int32 | - |
| 5030 | TurnTimeoutNotify | S→C | loserUserId:int32 | - |
| 5031 | ResignReq | C→S | - | InGame |
| 5040 | GameEndNotify | S→C | winnerUserId:int32, reason:uint8(5목/타임아웃/기권/무승부/연결끊김), boardHash:int64 | - |
| 5050 | ReconnectReq | C→S | name:string, roomId:int32 | Connected |
| 5051 | ReconnectRes | S→C | result:uint16, snapshot:{board:bytes(225), turnUserId, remainMs, myStone, moves:[{x,y,seq}]} | - |
| 5052 | OpponentDisconnectNotify | S→C | userId:int32, graceMs:int32 | - |
| 5053 | OpponentReconnectNotify | S→C | userId:int32 | - |
| 5060 | SpectateListReq | C→S | page:uint16 | Lobby |
| 5061 | SpectateListRes | S→C | rooms:[{roomId, players, spectators, movesCount}] | - |
| 5062 | SpectateEnterReq | C→S | roomId:int32 | Lobby |
| 5063 | SpectateEnterRes | S→C | result:uint16, snapshot:{...} | - |
| 5064 | SpectateLeaveReq | C→S | - | Spectating |
| 5070 | RematchReq | C→S | - | Result |
| 5080 | GameChatReq | C→S | text:string(≤100) | InGame, Spectating |
| 5081 | GameChatNotify | S→C | userId:int32, name:string, text:string, isSpectator:bool | - |

**ADR 3개 이상**

- `0002-thread-model.md`: 룸 액터 선택 — 대안(단일 로직 스레드 / 워커 풀+락)과 기각 이유, 되돌리는 비용
- `0003-room-session-ref.md`: 룸-세션 참조 방식 — ID 참조 vs 포인터/shared_ptr, 파괴 시점 문제
- `0004-matching.md`: 매칭 방식 — 선착순 vs 레이팅 범위, 매칭 주기(즉시 vs 배치)

**채점**

| 항목 | 배점 | 기준 |
|---|---|---|
| 완결성 | 30 | 10개 필수 항목 전부, 패킷 30종, 다이어그램 3종 |
| 명확성 | 30 | AI "처음 보는 개발자" 테스트에서 ①②③을 정확히 재설명(추측 항목 0~1개) |
| ADR 근거 | 20 | 각 ADR에 대안 2개 이상과 기각 이유, 되돌리기 비용 |
| 갱신 이력 | 20 | "설계와 달라진 점" 5행 이상 + 다이어그램 갱신 커밋 |

### 7.2 트랙 과제 3-1. 멀티 룸 실시간 대전 게임 서버 (필수, 80h, 41~53일차)

**게임 사양**: 오목. 15×15, 흑 선공, 5목 승리. **6목 이상 처리 규칙은 택일해 문서화**(권장: 6목도 승리 인정 — 단순). 턴 제한 30초.

**기능 요구사항**

1. **로비**: 닉네임 로그인(Phase 4에서 인증 연동), 로비 유저 목록, 로비 채팅(2-1 재사용)
2. **매칭**: 요청/취소, 2인 성사 시 룸 생성·양쪽 통지, 60초 초과 시 취소 통지, 성사 직전 이탈 처리
3. **게임**: 착수 요청 → 서버 판정(6검증) → 양쪽 통지 → 턴 교대, 턴 타임아웃 30초 자동 패, 기권, 승패·무승부 판정, 결과 통지 후 룸 파괴
4. **재접속**: 끊김 시 60초 유예, 같은 닉네임 재접속 시 **판 상태 스냅샷** 복구, 유예 초과 시 패
5. **관전**: 진행 중 룸 목록, 관전 입장(룸당 10명), 착수 통지 수신, 관전자 채팅, 관전자 착수 거부
6. **재매칭**: 결과 후 로비 복귀, 즉시 재매칭
7. **치트 방어**: 내 턴 아닌 착수 / 다른 룸 대상 패킷 / 초당 10회 초과 / 범위 밖 좌표 / 이미 놓인 자리 → 거부 + 로그, 누적 3회 시 세션 종료

**구조 요구사항**

- 룸 로직은 **단일 스레드에서만** 실행(락 0). 패킷·타이머·룸 간 메시지 전부 Job
- 3계층 분리: `Rules`(외부 의존 0) / `Logic`(룸·매칭·유저) / `Net`(세션·디스패치). Logic은 소켓을 모른다
- 2-2 패킷 라이브러리, 1-2 오브젝트 풀 재사용
- 구조화 로그(sessionId·userId·roomId·packetId·durationMs), 설정 파일, graceful shutdown

**비기능 요구사항**

| 조건 | 기준 |
|---|---|
| 100룸(플레이 200 + 관전 100 = 300접속), 10분 | 착수 요청→통지 p99 **50ms 이하**, 크래시 0 |
| 메모리 | 1분 시점 대비 10분 시점 증가 10% 이내 |
| 재접속 | 100세션 동시 끊김 → 30초 후 재접속, 판 상태 복구율 100% |
| 룸 파괴 경쟁 | 파괴 중 패킷 도착 100회, 크래시 0 |
| C++ | ASan 빌드로 위 시나리오 1회 통과 |

**규칙 테스트 30개(§7.2 필수 목록)**

| # | 케이스 | 기대 |
|---|---|---|
| 1~5 | 가로/세로/우하향 대각/우상향 대각 5목, 정확히 5목 | 승리 |
| 6~8 | 판 좌상단·우하단·모서리에서의 5목 | 승리(경계 처리) |
| 9~10 | 6목(연속 6개) | 문서화한 정책대로 |
| 11~12 | 4목 + 막힌 양끝 | 승리 아님 |
| 13 | 5목이 상대 돌로 끊긴 경우 | 승리 아님 |
| 14~15 | 무승부(판 가득) | Draw |
| 16 | 이미 끝난 판에 착수 | GameOver 오류 |
| 17 | 이미 놓인 자리 | Occupied |
| 18~19 | 좌표 범위 밖(-1, 15) | OutOfRange |
| 20 | 상대 턴에 착수 | NotYourTurn |
| 21~24 | 각 방향 5목 판정의 시작/끝 인덱스 경계 | 정확 |
| 25 | 첫 수는 항상 흑 | 강제 |
| 26 | 착수 순서 기록(리플레이용) | 순서 보존 |
| 27 | 보드 해시가 같은 상태에서 동일 | 일치 |
| 28~29 | 승리 직후 추가 착수 시도 | 거부 |
| 30 | 224수까지 진행 후 마지막 수로 승리 | 정상 판정 |

**로직 테스트 25개(페이크 세션 사용)**: 상태 전이 10, 불법 전이 5, 치트 5, 재접속 5

**부하 리포트 `LOAD-3-1.md`**

```markdown
## 1. 환경 / 2. 시나리오(100룸 구성·램프업·지속) / 3. 결과 표
| 회차 | 룸 수 | 접속 | 게임 완료 | 착수 p50 | p95 | p99 | 룸 큐 최대 | CPU | 메모리(1분/10분) |
## 4. 병목 가설 표 (가설/확인 방법/결과/채택·기각)  ← 기각 1개 이상 필수
## 5. 수정 내역과 전/후 비교
## 6. 재접속 시나리오 결과 (복구율, 소요 시간)
## 7. 룸 파괴 경쟁 결과
## 8. 남은 병목과 Phase 5 과제
```

**채점**

| 항목 | 배점 | 기준 |
|---|---|---|
| 기능 | 25 | 요구사항 1~7 전부 |
| 구조 | 20 | 룸 로직 락 0(검색 증명), 3계층 분리(빌드 증명), 규칙 테스트 독립 실행 |
| 안정성·성능 | 25 | 100룸 p99 50ms, 10분 무사고, 재접속 100%, 메모리 10% 이내 |
| 테스트 | 15 | 규칙 30 + 로직 25 + 통합 시나리오 |
| 설계-코드 일치 | 15 | `DESIGN.md` 갱신본과 코드 대조에서 불일치 0~1건 |

**자주 하는 실수**
- 룸 파괴 후 큐에 남은 Job이 실행되어 크래시 → 룸 상태 검사 + 세대 번호
- 재접속 시 유저를 새로 만들어 판 상태 유실 → 세션/유저 분리
- 타이머가 룸을 강참조해 파괴 지연 → ID 참조
- 관전자 브로드캐스트에서 N번 직렬화 → 1회 직렬화 공유
- 매칭 성사와 취소가 동시에 → 매칭 스레드에서 단일 처리(또는 CAS)

### 7.3 공통 과제 3-2. 시나리오 테스트 클라이언트 확장 (필수, 16h, 49일차)

**요구사항**

1. 2-3 클라이언트에 시나리오 4종 추가
   - `omok`: 접속 → 로비 → 매칭 → 게임(무작위 **합법** 착수, 턴당 0.5~2초) → 결과 → 재매칭 반복
   - `spectate`: 진행 중 룸 관전 입장 → 통지 수신 카운트 → 퇴장
   - `reconnect`: 게임 중 N초 후 강제 끊김 → 30초 후 재접속 → **판 상태 일치 검증**(보드 해시 비교)
   - `cheat`: 내 턴 아닌 착수 / 범위 밖 좌표 / 이미 놓인 자리 / 초당 50회 요청 → 거부 응답 카운트
2. 봇은 규칙 라이브러리를 참조해 합법 수만 둔다(서버와 같은 코드 재사용)
3. 통계: 룸 수, 게임 완료 수, 착수→통지 p50/p95/p99, 재접속 성공률, 치트 거부율, 평균 게임 길이(수)
4. 결과를 JSON + 마크다운 표로 저장, 같은 조건 3회 실행 편차 계산

**제출물**: 코드, 100룸 실행 결과 3회분, 편차 표

**채점**: 시나리오 4종 동작 50 / 통계 정확성 30 / 재현성(3회 편차 10% 이내 표기) 20

### 7.4 심화 과제 3-3. 미니 클라이언트 또는 2D 액션 (선택 🟡, 24h)

- **3-3a GUI 클라이언트**: Unity/Godot(C#)로 오목 클라이언트, 또는 교재 SuperSocketLite 7장 WinForms 클라이언트를 내 프로토콜에 맞게 개조. 요구: 로그인·매칭·착수·결과·재접속 UI
- **3-3b 2D 액션 프로토타입**: 플레이어 4명, 이동·공격, 20Hz 틱, 위치 동기화 + 보간. 로드맵 교재 Ch.4 적용. 요구: 틱 기반 룸 액터(오목의 이벤트 기반과 비교), 대역폭 측정
- **제출물**: 실행 영상 1분, 적용한 동기화 방식 설명 1페이지, 오목(이벤트 기반)과 액션(틱 기반)의 룸 액터 차이 비교표

---

## 8. 학습 완료 판정 (55일차 금요일)

### 8.1 체크리스트

**과제**
- [ ] 3-C: `DESIGN.md`(10개 항목 + 달라진 점 표) + ADR 3개
- [ ] 3-1: 기능 7종, 규칙 테스트 30개, 로직 테스트 25개 통과
- [ ] 3-2: 시나리오 4종 동작, 3회 실행 편차 표
- [ ] 🟡 3-3 또는 미수행 사유

**측정**
- [ ] `LOAD-3-1.md`: 100룸·300접속·10분, 착수 p99 50ms 이하(또는 미달 시 원인 분석)
- [ ] 재접속 100세션 복구율 100%
- [ ] 룸 파괴 경쟁 100회 크래시 0, (C++) ASan 통과

**구조**
- [ ] 룸 로직에 lock/mutex 0개 — 코드 검색 결과 캡처
- [ ] Rules 프로젝트 의존성 0, Logic이 Net 미참조 (빌드로 증명)

**기록**
- [ ] 학습 노트 20일치, 주간 회고 4개

### 8.2 재구현 시험 (AI 없음, 120분)

**문제**: 빈 프로젝트에서 룸 액터를 구현하고 제공 테스트 4개를 통과시킨다.

요구 API
```
Room.Post(job) -> bool        // 큐 상한 초과 시 false
Room.RunLoop(token)           // 단일 소비 루프
Scheduler.PostTimer(roomId, delayMs, job)   // 타이머 → 룸 큐
Room.SendTo(otherRoomId, job) // 룸 간 메시지
```

제공 테스트
1. `순서보장`: 한 룸에 Job 10,000개를 4스레드가 투입 → 실행 순서가 투입 순서와 일치(투입 스레드별)
2. `동시실행금지`: 룸 1개에 Job 1,000개, 워커 8개 → 동시에 두 워커가 실행한 적 0(카운터 검증)
3. `타이머발화`: 100ms 지연 Job이 룸 큐에서 실행되고, 룸 파괴 후 발화한 타이머는 무시
4. `룸간메시지`: 룸 A가 룸 B에 메시지 → B가 처리 후 A에 응답, 왕복 성공

**통과 기준**: 120분 내 4개 통과.

### 8.3 설명 시험 질문 은행 (10문항 무작위, 평균 4.0)

1. 스레드 모델 4가지의 장단점. 오목 서버와 MMO 존 서버에서 각각 무엇을 고르는가
2. 룸을 단일 스레드로 처리하는 이유. 룸 하나가 무거워지면 어떻게 하는가
3. 타이머 콜백에서 룸 상태를 직접 바꾸면 왜 안 되는가. 어떻게 우회했는가
4. 세션이 끊겼을 때 룸 상태를 정리하는 순서와, 순서가 바뀌면 생기는 문제
5. 서버 권위 판정이 필요한 이유. 클라이언트 예측과 어떻게 공존하는가
6. 재접속 유예를 구현할 때 세션과 유저를 왜 분리하는가
7. 룸 큐가 가득 차면 어떤 정책을 쓰는가. 그 선택의 근거는
8. 룸이 파괴된 뒤 큐에 남은 Job은 어떻게 처리되는가
9. 관전자가 100명이면 룸 스레드에 무슨 일이 생기는가. 어떻게 완화했는가
10. 착수 p99가 튀는 원인 3가지와 각각의 확인 방법
11. 매칭 성사와 취소가 동시에 오면 어떻게 처리되는가
12. 상태 머신을 문서화하지 않으면 어떤 버그가 생기는가(실제 예)
13. 규칙을 별도 프로젝트로 분리한 이유와 이득
14. 설계와 구현이 달라진 지점 하나와 그 이유
15. (C#) `Channel<T>` Bounded가 가득 차면 어떤 옵션이 있는가. 무엇을 골랐는가
16. (C#) `SingleReader=true`가 주는 이점
17. (C++) 룸 실행 중 플래그를 atomic CAS로 처리하는 방식과 기아 가능성
18. (C++) 룸을 ID로 참조하는 이유. `shared_ptr`로 하면 무엇이 문제인가

### 8.4 결함 찾기 (45분)

룸 로직 경쟁 조건 2 / 타이머 누수 1 / 재접속 상태 오류 1 / 권위 우회 1 — 5개 중 4개 발견 + 수정안.

### 8.5 미달 시 대응

| 미달 항목 | 대응 |
|---|---|
| 착수 p99 50ms 미달 | 원인 분석(프로파일 결과 포함) 제출 시 Phase 4 진행 허용, Phase 5 성능 과제에서 재측정 |
| 룸 로직에 락 잔존 | **통과 불가**. 최대 1주 연장해 제거(액터 원칙은 이 과정의 핵심) |
| 재접속 복구율 90% 미만 | 세션-유저 분리 재설계 후 재시험 |
| 설계 문서 AI 테스트 실패 | 추측 항목을 전부 문서에 채우고 재테스트 |
| 재구현 120분 초과 | Phase 4 첫 주에 룸 액터만 2회 반복 연습 |

---

## 9. 흔한 막힘 포인트

| 증상 | 원인 | 대처 |
|---|---|---|
| 룸이 종료됐는데 Job이 계속 실행됨 | 파괴 후 큐 잔존, 타이머 미취소 | 룸 상태 `Destroyed` 검사 후 무시, 타이머에 세대 번호 |
| 재접속 시 판 상태가 어긋남 | 세션과 유저를 같은 객체로 관리 | 유저(게임 상태)와 세션(연결) 분리, 세션 포인터 교체 |
| 착수 p99가 가끔 튄다 | GC(C#)/할당 폭주, 로그 동기 쓰기, 타이머 해상도, 룸 큐 대기 | 할당 프로파일, 비동기 로그 싱크, 룸 큐 길이 로그 |
| 관전자 많은 룸만 느림 | 룸 스레드에서 N번 직렬화 | 1회 직렬화 후 버퍼 공유(참조 카운트) |
| 봇끼리 매칭이 안 됨 | 매칭 큐 타이밍, 봇이 동시에 요청 안 함 | 램프업 후 동시 매칭 요청, 매칭 주기 로그 확인 |
| C++ 룸 파괴 시 크래시 | 세션이 룸 포인터 보유 | 룸 ID 참조 + 매니저 조회, 파괴는 룸 스레드의 마지막 Job |
| 두 워커가 같은 룸을 실행 | CAS 순서 오류(큐 투입과 플래그 설정 순서) | Post → CAS → 스케줄 순서 고정, assert로 검증 |
| 룸 큐가 계속 참 | 소비보다 생산이 빠름(브로드캐스트 폭주) | 큐 상한 정책, 배치 처리, 불필요 통지 제거 |
| 타이머가 대량으로 쌓임 | 매 착수마다 타이머 새로 등록하고 취소 안 함 | 기존 타이머 취소 후 등록, 세대 번호로 무효화 |
| 로직 테스트가 네트워크를 요구 | 계층 분리 실패 | `ISessionSender` 인터페이스 도입, 페이크 세션 |
| 게임 결과가 가끔 두 번 처리됨 | 타임아웃과 착수 승리가 동시 | 룸 상태 `Ended` 전이를 단일 지점에서만 |
| 로그로 게임 추적이 안 됨 | 필드 누락(roomId 없음) | 로그 필드 표준 강제, 구조화 로깅 |

---

## 10. Phase 4로 넘어가기 전 준비

- [ ] SQLite는 별도 설치 불필요(기본 DB). 🟡 MySQL을 선택했다면 MySQL 8이 Windows 서비스로 기동되는지 확인
- [ ] Redis(redis-windows)가 기동되고 `redis-cli ping` → `PONG` 확인
- [ ] C++ 트랙: Phase 4 경로 결정(순수 C++ vs C# 혼합). 혼합이면 "ASP.NET Core Web API를 위한 필수 C# 가이드" 통독 20h를 12주 첫 주에 배정
- [ ] 3-1 서버에 **"게임 결과" 이벤트 훅**(인터페이스)을 만들어 둔다 — Phase 4에서 API 서버 호출로 연결된다
  ```
  interface IGameResultSink { void OnGameEnded(GameResult r); }   // 지금은 로그만, Phase 4에서 HTTP 호출로
  ```
- [ ] 유저 식별자를 닉네임에서 **userId(정수)** 로 다룰 수 있게 준비(Phase 4 인증 연동 대비)
- [ ] Phase 3 회고: 설계와 구현이 달라진 가장 큰 이유, 액터 원칙을 어긴 유혹의 순간, AI가 틀린 사례 1개
