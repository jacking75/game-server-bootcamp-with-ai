# Phase 4. 데이터·API 서버 (12~15주, 160h)

> 공통 100h / 트랙 60h. 이 Phase가 끝나면 학습자는 계정·자산 데이터를 다루는 HTTP API 서버와 DB를 실무 구조로 만들고, 동시성 문제(재화 복제 등)를 트랜잭션으로 방어한다.

## 0. 이 문서 사용법

Phase 1~3과 같은 구조다. §2 일자 블록을 따라가고, 실습(`L4-C-xx`, `L4-CS-xx`, `L4-CPP-xx`)은 §3에서, 과제 상세는 §7에서 찾는다.

이 Phase의 3대 원칙

1. **DB는 인터페이스 뒤에 둔다.** Repository/DAO 인터페이스 + DI로 구현체를 주입한다. 개발 기본값은 **SQLite**(설치 불필요, 파일 기반), 원하면 설정 한 줄로 **MySQL**로 전환된다. "DB를 바꿀 수 있게 짜는 것" 자체가 이번 학습 목표다.
2. **정합성은 테스트로 증명한다.** "동시에 100번 사더라도 재화는 한 번만 빠진다"를 말이 아니라 **동시성 테스트 통과 로그**로 낸다.
3. **공격자 관점을 항상 켠다.** 모든 API에 "이걸 어떻게 악용할까"를 먼저 묻고, 악용 시나리오 5개 이상을 테스트로 재현한 뒤 막는다.

| 표기 | 뜻 |
|---|---|
| `L4-C-xx` / `L4-CS-xx` / `L4-CPP-xx` | 공통 / C# / C++ 실습 |
| 🔴 필수 · 🟡 여유 시 | 우선순위 |
| **(SQLite)** / **(MySQL)** | DB 경로별로 다른 지시 |

---

## 1. 개요

### 1.1 목표

1. **게임 API 서버의 흐름을 만든다**: 버전 체크 → 로그인 → 유저 데이터 로드 → 로비 행동(인벤토리·우편·출석·상점·랭킹) → 로그아웃
2. **DB 접근을 추상화한다**: Repository/DAO 인터페이스 + DI. 기본 SQLite, 선택 MySQL. **설정만 바꿔 전환**되어야 한다
3. **트랜잭션과 격리 수준을 이해한다**: 이중 지출·중복 수령을 조건부 UPDATE / 멱등성 키 / 락으로 방어
4. **Redis를 용도별로 쓴다**: 세션(TTL), 랭킹(ZSET), 캐시, 분산 락의 한계
5. **게임 서버(3-1)와 연동한다**: 게임 결과 저장 → 랭킹 갱신, 서버 간 인증
6. **악용 시나리오를 도출하고 막는다**: 재화 복제·중복 수령·토큰 탈취·리플레이·레이스

### 1.2 DB 경로 (트랙과 독립)

| 구분 | 기본 (권장) | 선택 |
|---|---|---|
| DB | **SQLite** — 파일 기반, 설치 불필요, 테스트가 빠름 | **MySQL 8** — 서버 기반, 실서비스에 가까움 |
| C# 클라이언트 | `Microsoft.Data.Sqlite` 또는 EF Core Sqlite | `MySqlConnector` + SqlKata/Dapper |
| C++ 클라이언트 | vcpkg `sqlite3` 또는 `SQLiteCpp` | MySQL Connector/C++ |
| 성능 실습 규모 | 1만 유저 / 10만 인벤토리 | 100만 유저 / 1,000만 인벤토리 |
| 실행 계획 | `EXPLAIN QUERY PLAN` | `EXPLAIN` |

**핵심 규칙**: 어느 쪽을 쓰든 **애플리케이션 코드는 인터페이스만 안다.** DB 전용 SQL은 구현체 안에만 존재한다. Phase 4를 SQLite로 완주한 뒤 🟡 과제 4-3c에서 MySQL로 갈아 끼워 보면 이 설계의 가치를 체감한다.

### 1.3 선수 조건 자가 진단 (20분)

| # | 질문 | 통과 기준 |
|---|---|---|
| 1 | SQL로 3개 테이블 JOIN + 집계를 쓸 수 있는가 | 즉석에서 작성 가능 |
| 2 | 트랜잭션이 무엇을 보장하는지 ACID로 말할 수 있는가 | 즉답 |
| 3 | 3-1 서버에 "게임 결과 훅"이 있는가 | 인터페이스 존재 |
| 4 | HTTP 상태 코드 200/400/401/403/404/409/429/500의 의미를 아는가 | 즉답 |
| 5 | (C# 트랙) DI 수명 3종(Singleton/Scoped/Transient)의 차이를 아는가 | 즉답 |
| 6 | Redis를 실행하고 `redis-cli`로 키를 넣고 뺄 수 있는가 | 실행 확인 |

3개 미만이면 12주 첫 이틀에 SQL·HTTP 기초 보강을 배치한다(교재 MySQL 책 Day 1~2, 로드맵 Ch.7).

### 1.4 이 Phase가 끝나면 할 수 있는 것

- "동시 100회 구매에서 재화가 정확히 한 번만 차감"을 **테스트로 증명**한다
- 느린 쿼리를 실행 계획으로 찾아 인덱스로 고치고 전/후 시간을 제시한다
- 격리 수준별 현상(더티 리드·논리피터블 리드·팬텀)을 스크립트로 재현한다
- "게임 서버가 왜 DB를 직접 치지 않고 API 서버를 거치는가"를 설명한다
- SQLite로 만든 서버를 설정만 바꿔 MySQL로 돌린다(🟡 4-3c 수행 시)

### 1.5 이 Phase의 산출물

```
phase4/
├─ SCHEMA.md                   ERD + DDL + 인덱스 근거 + DB/Redis 배치 판정표
├─ API.md (또는 openapi.yaml)  엔드포인트 20개+ 명세, 에러 코드 표
├─ adr/0005-db-abstraction.md  Repository+DI, SQLite 기본/MySQL 선택 결정
├─ GameApi/
│  ├─ Contracts/               DTO, 에러 코드 (외부 의존 0)
│  ├─ Domain/                  서비스 로직 (트랜잭션 경계)
│  ├─ Data/                    IRepository 인터페이스
│  ├─ Data.Sqlite/             SQLite 구현체        ← 기본
│  ├─ Data.MySql/              MySQL 구현체 🟡      ← 선택
│  ├─ Cache/                   Redis 접근(세션·랭킹)
│  ├─ Api/                     HTTP 진입점, 미들웨어, 인증
│  ├─ Worker/                  배치(우편 만료·원장/재고 정합 검사)
│  └─ tests/                   통합 30+ / 동시성 3 / 인증 5
├─ migrations/                 스키마 버전 스크립트
├─ tools/seed-data.ps1         더미 데이터 생성
├─ PERF-4-1.md                 실행 계획 전/후, p99 측정
├─ SECURITY-SCENARIOS.md       과제 4-2
└─ labs/
```

### 1.6 4주 로드맵

| 주차 | 큰 주제 | 공통 | 트랙 | 과제 |
|---|---|---|---|---|
| 12주 | API·스키마·DB 추상화 | HTTP/REST, 게임 API 흐름, 스키마 설계, 해싱 | Repository+DI, SQLite 구현체 | 4-C, 가입/로그인 |
| 13주 | 인증·Redis·트랜잭션 | 토큰 vs 세션, Redis 자료구조, 격리 수준, 인덱스 | 인증 미들웨어, Redis 클라이언트 | 4-1 계정·인벤토리 |
| 14주 | 동시성·콘텐츠·연동 | 이중 지출, 멱등성, 랭킹, 서버 간 인증, 배치 | 상점 트랜잭션, ZSET 랭킹 | 4-1 상점·우편·출석·랭킹·연동 |
| 15주 | 보안·성능·평가 | 악용 시나리오, 통합 테스트, 성능 검증 | 실행 계획 튜닝 | 4-2, 🟡4-3, 평가 |

---

## 2. 주차별 상세 계획 (일 단위)

일자 번호는 과정 전체 기준(Phase 4는 56~75일차).

### 2.1 12주차 — API 설계와 DB 추상화

#### 56일차 (월) — HTTP/REST와 게임 API 흐름 🔴

**오전 (2.5h)**
- HTTP 메서드·상태 코드·헤더, JSON, 멱등성의 정의(같은 요청을 여러 번 해도 결과가 같다)
- **게임 API의 특징**: REST보다 커맨드형(POST 통일)이 흔하다 — 이유는 "행동 단위"가 리소스와 안 맞기 때문. 장단점을 정리한다
- 표준 흐름: `버전 체크 → 로그인(토큰 발급) → 유저 데이터 로드(1회 대량) → 로비 행동 → 로그아웃`
- 에러 설계: HTTP 상태 + **애플리케이션 에러 코드**(예: 200 OK + `{errorCode: 3001}`) 방식과 순수 HTTP 방식의 트레이드오프

**오후 (2.5h)**
- `L4-C-01` 최소 API 서버 기동: 헬스 체크 + 버전 체크 엔드포인트, `.http` 파일로 호출 테스트
- 과제 4-C 착수: `API.md` 뼈대(엔드포인트 목록 자리, 에러 코드 표 자리)

**AI 없는 1시간**
- 오목 로비 게임에 필요한 API 20개를 이름만 나열해 본다(나중에 4-C와 비교)

**DoD**
- [ ] 헬스 체크·버전 체크 API 응답 확인(`.http` 파일 커밋)
- [ ] `API.md` 뼈대

#### 57일차 (화) — 스키마 설계 🔴

**오전 (2.5h)**
- 정규화와 게임에서의 비정규화 판단(인벤토리를 행으로 vs JSON 컬럼)
- 키 설계: AUTO_INCREMENT vs UUID/ULID(분산·추측 방지·인덱스 지역성)
- 필수 테이블: `account`, `user`, `user_currency`, `item_master`, `user_item`, `mail`, `mail_attachment`, `attendance`, `shop_product`, `purchase_log`, `game_result`, `idempotency_key`
- 각 테이블에 `created_at`, `updated_at`을 넣는 이유

**오후 (2.5h)**
- `L4-C-02` ERD 작성(Mermaid) + DDL 초안 (SQLite 기준, 타입은 MySQL 대응도 병기)
- `L4-C-03` 인덱스 설계: 각 인덱스에 **"어떤 쿼리를 위한 것인지" 주석**을 단다. 인덱스가 없으면 느려질 쿼리 5개를 먼저 적고 그에 맞춰 설계

**AI 없는 1시간**
- "유저 1억, 인벤토리 10억일 때 느려질 쿼리"를 3개 예측해 적는다(15주차에 검증)

**DoD**
- [ ] ERD + DDL 12테이블 이상
- [ ] 인덱스마다 목적 주석, 핵심 쿼리 5개 목록

#### 58일차 (수) — Repository + DI (이 Phase의 뼈대) 🔴

**오전 (2.5h)**
- 의존성 역전: 도메인 서비스는 `IUserRepository`만 알고, SQLite/MySQL 구현은 바깥에 둔다
- 트랜잭션 경계를 어디에 둘 것인가: 서비스 계층(권장) vs 리포지토리. **여러 리포지토리를 한 트랜잭션으로 묶는 방법**(UnitOfWork 또는 커넥션 전달)
- SQLite 특성: 파일 하나, 쓰기 직렬화, **WAL 모드**로 읽기 동시성 확보, `busy_timeout` 설정

**오후 (2.5h)**
- `L4-C-04` 인터페이스 정의(§7.2 골격 사용): `IUserRepository`, `IItemRepository`, `IMailRepository`, `IShopRepository`, `IUnitOfWork`
- `L4-CS-01`/`L4-CPP-01` SQLite 구현체 + DI 등록. 연결 문자열·WAL·busy_timeout 설정
- 마이그레이션 스크립트(`migrations/001_init.sql`)와 적용 도구(간단한 러너)

**AI 없는 1시간**
- "왜 서비스가 SQL을 직접 쓰면 안 되는가"를 3가지 이유로 적는다(테스트·교체·중복)

**DoD**
- [ ] 인터페이스 5개 + SQLite 구현체, DI 등록
- [ ] 마이그레이션으로 빈 DB가 생성되고 테스트가 이를 사용

#### 59일차 (목) — 가입·로그인·해싱 🔴

**오전 (2.5h)**
- 비밀번호 해싱: **Argon2id 또는 bcrypt/PBKDF2**. SHA256 단독 금지(이유: 빠른 연산 = 무차별 대입 유리)
- 솔트·반복 횟수·메모리 파라미터, 저장 형식
- 계정 정책: ID 규칙, 비밀번호 최소 요건, 중복 가입 방지(유니크 인덱스 + 예외 처리)

**오후 (2.5h)**
- `L4-C-05` 가입 API: 검증 → 해싱 → 저장(유니크 위반 처리) → 기본 유저 데이터 생성(재화·출석 레코드)를 **한 트랜잭션**으로
- `L4-C-06` 로그인 API: 계정 조회 → 해시 검증 → 토큰 발급(다음 날 Redis 세션과 연결) → 마지막 로그인 갱신
- 통합 테스트 6개: 정상 가입, 중복 ID, 약한 비밀번호, 정상 로그인, 잘못된 비밀번호, 없는 계정

**AI 없는 1시간**
- 해시 결과를 로그에 찍지 않았는지, 에러 메시지가 "아이디 없음/비밀번호 틀림"을 구분해 노출하지 않는지 점검(계정 열거 방지)

**DoD**
- [ ] 가입·로그인 동작, 통합 테스트 6개 통과
- [ ] 비밀번호가 평문/약한 해시로 저장되지 않음

#### 60일차 (금) — 4-C 명세 완성 + 주간 점검

**오전 (3h)**
- `SCHEMA.md` 완성: ERD, DDL, 인덱스 근거, **DB vs Redis 배치 판정표**(§7.1)
- `API.md` 완성: 엔드포인트 20개+ 각각 요청/응답/인증 필요 여부/에러 코드/멱등성 여부
- `adr/0005-db-abstraction.md`: Repository+DI, SQLite 기본·MySQL 선택 결정과 대안

**오후 (4h) — 주간 점검**
1. AI 없이 재구현(60분): 가입 API(검증·해싱·트랜잭션)
2. 설명 시험(45분): 해싱 요구사항, 트랜잭션 경계, DB 추상화 이유
3. 코드 리뷰(45분): 루브릭 6번(구조) — 계층 침범 여부
4. 회고(30분): `W12.md`

**DoD**
- [ ] 4-C 제출 상태(`SCHEMA.md`, `API.md`, ADR)
- [ ] AI "다른 팀 개발자" 리뷰에서 모호 항목 해소

### 2.2 13주차 — 인증·Redis·트랜잭션

#### 61일차 (월) — 토큰과 세션 🔴

**오전 (2.5h)**
- 토큰 방식 비교: **불투명 토큰 + Redis 세션**(무효화 쉬움) vs **JWT**(검증 빠름, 무효화 어려움)
- 이 과정의 선택: 불투명 토큰(랜덤 32B) → Redis에 `session:{token} → userId`, TTL 1시간, 요청마다 슬라이딩 갱신
- 중복 로그인 처리: `user_session:{userId} → token` 을 함께 두고 새 로그인 시 이전 토큰 삭제

**오후 (2.5h)**
- `L4-C-07` Redis 세션 구현: 발급·검증·갱신·삭제, 중복 로그인 시 이전 세션 무효화
- `L4-C-08` 인증 미들웨어/필터: 헤더에서 토큰 → 검증 → 요청 컨텍스트에 userId 주입, 실패 시 401
- 인증 테스트 5개: 만료 토큰, 위조 토큰, 타인 토큰으로 내 데이터 접근, 미인증 접근, 중복 로그인 후 이전 토큰

**AI 없는 1시간**
- "JWT만 쓰면 로그아웃이 왜 어려운가"와 그 우회책(짧은 만료 + 리프레시, 블랙리스트)의 비용을 적는다

**DoD**
- [ ] 인증 테스트 5개 통과
- [ ] 토큰이 로그에 남지 않음(마스킹 확인)

#### 62일차 (화) — Redis 자료구조와 용도 🔴

**오전 (2.5h)**
- 자료구조별 게임 용도: String(세션·플래그), Hash(프로필 캐시), List(큐), Set(온라인 목록), **ZSet(랭킹)**
- TTL 전략, 파이프라인, **Lua 스크립트의 원자성**, 영속성(RDB/AOF) 개념
- "Redis에 두면 안 되는 것": 재화 원장 같은 **잃으면 안 되는 데이터**(휘발 위험 + 트랜잭션 결합 어려움)

**오후 (2.5h)**
- `L4-C-09` 랭킹 ZSET: 점수 갱신, 상위 100 조회, 내 순위 조회(`ZREVRANK`), 동점 처리 규칙
- `L4-C-10` 캐시 패턴: 유저 프로필 캐시(읽기 시 채우고 쓰기 시 무효화), 캐시 스탬피드 방지(짧은 락 또는 확률적 만료)
- `L4-C-11` Lua 원자 연산: 상점 재고 차감을 Lua로 원자화

**AI 없는 1시간**
- 데이터 10종에 대해 "DB인가 Redis인가 둘 다인가" 판정표를 작성(§7.1에 포함)

**DoD**
- [ ] 랭킹 3기능 동작, Lua 재고 차감 테스트
- [ ] 데이터 배치 판정표 완성

#### 63일차 (수) — 트랜잭션과 격리 수준 🔴

**오전 (2.5h)**
- ACID, 격리 수준 4단계와 각 단계의 현상(더티 리드/논리피터블 리드/팬텀)
- **(SQLite)** 기본은 직렬화에 가깝다(쓰기 1개). WAL 모드에서 읽기-쓰기 동시성, `SQLITE_BUSY`와 `busy_timeout`
- **(MySQL)** InnoDB 기본 REPEATABLE READ, 레코드/갭/넥스트키 락, 데드락과 재시도
- 트랜잭션 경계 설계: 짧게 유지, 외부 호출(HTTP/Redis)을 트랜잭션 안에 넣지 않는다

**오후 (2.5h)**
- `L4-C-12` 격리 수준 현상 재현 스크립트: 두 세션을 띄워 논리피터블 리드/팬텀을 관찰(**(SQLite)** 는 잠금 동작 관찰로 대체)
- `L4-C-13` 데드락 또는 잠금 충돌 재현 + 재시도 로직(지수 백오프, 최대 3회)

**AI 없는 1시간**
- "트랜잭션 안에서 HTTP 호출을 하면 무슨 일이 생기는가"를 3문장으로

**DoD**
- [ ] 격리/잠금 현상 재현 로그
- [ ] 재시도 로직과 그 테스트

#### 64일차 (목) — 인벤토리와 유저 데이터 로드 🔴

**오전 (2.5h)**
- 로그인 후 1회 호출로 필요한 데이터를 모두 주는 "데이터 로드" API 설계(왕복 줄이기)
- 페이징 설계: offset vs keyset(커서). 인벤토리가 큰 경우 keyset이 유리한 이유

**오후 (2.5h)**
- `L4-C-14` 유저 데이터 로드 API: 기본 정보 + 재화 + 인벤토리 요약 + 미수령 우편 수
- `L4-C-15` 인벤토리 API: 조회(페이징), 아이템 사용(수량 차감, 0이면 삭제) — **조건부 UPDATE로 음수 방지**
- 통합 테스트 8개: 페이징 경계, 없는 아이템 사용, 수량 초과 사용, 동시 사용 2회 등

**AI 없는 1시간**
- N+1 쿼리가 생길 수 있는 지점을 찾아 표시하고 해결책을 적는다

**DoD**
- [ ] 데이터 로드·인벤토리 API 동작, 테스트 8개
- [ ] N+1 없음(쿼리 로그로 확인)

#### 65일차 (금) — 실행 계획과 인덱스 + 주간 점검

**오전 (3h)**
- `L4-C-16` 더미 데이터 생성 스크립트: **(SQLite)** 1만 유저·10만 인벤토리 / **(MySQL)** 100만·1,000만. 배치 INSERT로
- `L4-C-17` 실행 계획 확인: 핵심 쿼리 5개를 `EXPLAIN QUERY PLAN`(또는 `EXPLAIN`)으로 보고 풀 스캔 제거. 인덱스 추가 전/후 실행 시간 표

**오후 (4h) — 주간 점검**
1. AI 없이 재구현(60분): Redis 세션 발급·검증·중복 로그인 무효화
2. 설명 시험(45분): 격리 수준, 인덱스 컬럼 순서, 토큰 vs 세션
3. 코드 리뷰(45분): 루브릭 3번(리소스) — 커넥션 수명·트랜잭션 누수
4. 회고(30분): `W13.md`

**DoD**
- [ ] 실행 계획 전/후 표(쿼리 5개)
- [ ] 더미 데이터 스크립트 커밋

### 2.3 14주차 — 동시성·콘텐츠·서버 연동

#### 66일차 (월) — 이중 지출 방어 (이 Phase의 핵심) 🔴

**오전 (2.5h)**
- 잘못된 패턴: `SELECT 잔액 → 애플리케이션에서 검사 → UPDATE` (레이스)
- 올바른 패턴 3가지
  1. **조건부 UPDATE**: `UPDATE user_currency SET gold = gold - :price WHERE user_id = :id AND gold >= :price` → **영향 행 수 확인**
  2. **비관적 락**: `SELECT ... FOR UPDATE`(MySQL) / 즉시 쓰기 트랜잭션(SQLite)
  3. **낙관적 락**: 버전 컬럼 + `WHERE version = :v`, 실패 시 재시도
- 교재 "가챠 확률 구현 가이드" 11·12장이 이 주제의 최고 교재다

**오후 (2.5h)**
- `L4-C-18` 상점 구매 트랜잭션 구현: 재화 차감 + 아이템 지급 + 구매 이력 기록을 **하나의 트랜잭션**으로. 재고 있는 상품은 Lua 또는 조건부 UPDATE로 차감
- `L4-C-19` **동시 100회 구매 테스트**: 같은 계정으로 100개 요청 동시 발사 → 성공 1건, 재화 1회분만 차감, 아이템 1개만 지급

**AI 없는 1시간**
- 세 가지 방어 패턴의 비용(락 대기·재시도·구현 복잡도)을 표로 정리

**DoD**
- [ ] 동시 100회 구매 테스트 통과(10회 반복해도 안정)
- [ ] 영향 행 수 검사가 코드에 있다

#### 67일차 (화) — 멱등성: 우편·출석 🔴

**오전 (2.5h)**
- 멱등성 키 설계: 요청 ID를 클라이언트가 생성 → 서버가 저장 → 중복이면 **이전 결과를 그대로 반환**
- 저장 위치와 만료(테이블 + TTL 인덱스 또는 Redis), 키 충돌 시 처리
- 우편 수령의 "1회만" 보장: 상태 컬럼 + 조건부 UPDATE(`WHERE status = 'unread'`)

**오후 (2.5h)**
- `L4-C-20` 우편함 API: 목록(페이징), 수령(첨부 재화/아이템 지급 + 상태 변경, 한 트랜잭션), 만료 처리(배치용 플래그)
- `L4-C-21` 출석 API: 일 1회, **자정(KST) 기준** 리셋, 보상 지급. 날짜 경계 테스트(23:59:59 / 00:00:01)
- 동시성 테스트 2개: 우편 동시 50회 수령 → 1회만, 출석 동시 50회 → 1회만

**AI 없는 1시간**
- "멱등성 키를 언제 지우는가"와 "지우기 전에 재시도가 오면?"을 정리

**DoD**
- [ ] 우편·출석 동시성 테스트 통과
- [ ] 날짜 경계 테스트 통과(타임존 명시)

#### 68일차 (수) — 랭킹과 게임 결과 저장 🔴

**오전 (2.5h)**
- 랭킹 설계: Redis ZSET(실시간) + DB(원장). **둘의 정합성**을 어떻게 맞출 것인가(쓰기 순서, 복구 절차)
- 시즌 개념: 키에 시즌 포함(`rank:season:{n}`), 시즌 종료 시 스냅샷을 DB에 저장
- 동점 처리: 점수 같으면 먼저 도달한 사람이 위(타임스탬프를 소수부로 섞는 기법)

**오후 (2.5h)**
- `L4-C-22` 랭킹 API: 점수 갱신(게임 결과 반영), 상위 100, 내 순위·내 주변 순위
- `L4-C-23` 게임 결과 저장 API(내부용): `game_result` 테이블 기록 + 랭킹 갱신을 함께. **멱등성 키 = gameId**

**AI 없는 1시간**
- Redis가 재시작해 랭킹이 날아갔을 때 DB에서 복구하는 절차를 쓴다

**DoD**
- [ ] 랭킹 3기능 + 게임 결과 저장(멱등) 동작
- [ ] 랭킹 복구 절차 문서화

#### 69일차 (목) — 서버 간 연동 🔴

**오전 (2.5h)**
- 게임 서버(3-1) → API 서버 호출: 내부 전용 엔드포인트, **서버 간 인증**(공유 시크릿 헤더 또는 내부 토큰), 외부 노출 차단
- 실패 처리: 재시도 큐(게임 서버 측), 지수 백오프, 중복 방지(gameId 멱등성)
- 게임 서버의 로그인 토큰 검증: API 호출 vs Redis 직접 조회 — 트레이드오프 비교

**오후 (2.5h)**
- `L4-C-24` 3-1 서버에 HTTP 클라이언트 붙이기: 게임 종료 시 결과 전송, 실패 시 로컬 큐에 보관 후 재시도
- `L4-C-25` 연동 시연: 게임 1판 → 결과 저장 → 랭킹 반영까지 로그로 추적
- 내부 API 인증 테스트: 시크릿 없이 호출 → 401

**AI 없는 1시간**
- "게임 서버가 DB를 직접 치면 무엇이 문제인가"를 4가지로 정리

**DoD**
- [ ] 게임 결과가 API를 거쳐 DB·랭킹에 반영되는 로그
- [ ] 내부 API가 외부에서 호출되지 않음(인증 테스트)

#### 70일차 (금) — 배치 워커 + 주간 점검

**오전 (3h)**
- `L4-C-26` 배치 워커: 우편 만료 정리, 원장 합=잔액·재고 불일치 검사, 시즌 종료 스냅샷
- 스케줄링: 별도 콘솔 앱 + 타이머(운영체제 스케줄러 의존 없이), 중복 실행 방지(락)

**오후 (4h) — 주간 점검**
1. AI 없이 재구현(90분): 상점 구매 트랜잭션 + 동시성 테스트 통과
2. 설명 시험(45분): 이중 지출 방어 3가지, 멱등성, 랭킹 정합성
3. 코드 리뷰(45분): 루브릭 1·4번(정확성·에러 처리)
4. 회고(30분): `W14.md`

**DoD**
- [ ] 배치 워커 동작(중복 실행 방지 포함)
- [ ] 4-1 기능 10종 모두 동작

### 2.4 15주차 — 보안·성능·평가

#### 71일차 (월) — 악용 시나리오 도출 (과제 4-2) 🔴

**오전 (2.5h)**
- 공격자 관점 체크리스트: 재화 복제, 중복 수령, 토큰 탈취/재사용, 리플레이, 파라미터 조작(음수 수량·타인 ID), 레이스, 열거(계정 존재 확인), 대량 요청
- 교재 "가챠 확률 구현 가이드" 16장(사고 사례), API 게임 서버 실습 부록 G(보안 체크리스트)

**오후 (2.5h)**
- `L4-C-27` AI와 함께 악용 시나리오 **8개 이상 도출**(§6.1 프롬프트) → 각각 재현 방법·방어 방법 정리
- 그중 5개를 **테스트로 재현**: 방어 전 실패(취약 확인) → 방어 후 통과 커밋 이력을 남긴다

**AI 없는 1시간**
- 내 API 중 "인증 없이 호출 가능한 것"을 전부 나열하고 각각 왜 안전한지 근거를 적는다

**DoD**
- [ ] 시나리오 8개 목록, 5개 이상 테스트 재현
- [ ] 방어 전/후 커밋 이력

#### 72일차 (화) — 방어 구현과 레이트 리밋 🔴

**오전 (2.5h)**
- 입력 검증: 길이·범위·타입·상태(음수 수량, 타인 userId, 존재하지 않는 상품)
- SQL 인젝션: **파라미터 바인딩 전수 점검**(문자열 결합 0건)
- 레이트 리밋: 계정당·IP당 요청 수 제한(Redis 카운터 + TTL), 429 응답

**오후 (2.5h)**
- `L4-C-28` 방어 구현 및 재테스트: 5개 시나리오가 전부 막히는지 확인
- `L4-C-29` 레이트 리밋 구현 + 테스트: 정상 사용자는 영향 없고 폭주 요청은 429

**AI 없는 1시간**
- 로그에 민감 정보(비밀번호·토큰·주민 정보 등)가 남지 않는지 전수 확인

**DoD**
- [ ] `SECURITY-SCENARIOS.md` 완성
- [ ] 파라미터 바인딩 100%, 레이트 리밋 동작

#### 73일차 (수) — 성능 검증 🔴

**오전 (2.5h)**
- 로그인 API p99 측정: 동시 100 요청, **(SQLite)** 1만 유저 / **(MySQL)** 100만 유저 상태
- 병목 확인: 해싱 비용(의도적으로 비싸다), 커넥션 풀, 인덱스, 직렬화

**오후 (2.5h)**
- `L4-C-30` 성능 측정·튜닝: 커넥션 풀 크기, 캐시 적용, 쿼리 개선. 전/후 표를 `PERF-4-1.md`에
- 주의: 해싱 파라미터를 낮춰 p99를 개선하는 것은 **보안 후퇴**다. 이런 트레이드오프를 문서에 명시

**AI 없는 1시간**
- "p99를 낮추기 위해 하면 안 되는 최적화" 3가지를 적는다

**DoD**
- [ ] 총 100·동시 10 로그인 p99 ≤ 해시 1회 실측×2+30ms, `/user/data` 동시 100 p99 ≤ 50ms
- [ ] `PERF-4-1.md` 완성(실행 계획 전/후 + p99)

#### 74일차 (목) — 통합 테스트 정리와 🟡 심화

**오전 (2.5h)**
- 통합 테스트 30개 채우기: 실제 DB/Redis 사용, 테스트 전용 스키마·키 prefix, 테스트 간 격리(각 테스트 전 정리)
- 테스트 데이터 픽스처 정리, 실행 시간 단축(SQLite in-memory 옵션 활용)

**오후 (2.5h)**
- 🟡 `L4-CS-02`/`L4-CPP-02` 과제 4-3 심화 중 택1: 관리자 CLI / 가챠 / **MySQL 전환**
- **MySQL 전환(4-3c)을 권장**: `Data.MySql` 구현체를 추가하고 설정만 바꿔 전체 테스트를 통과시킨다 → 추상화의 가치를 체감

**AI 없는 1시간**
- 통합 테스트가 서로 오염되는 지점을 찾아 격리 방식을 개선

**DoD**
- [ ] 통합 30개 + 동시성 3개 + 인증 5개 통과
- [ ] 🟡 4-3 택1 진행 상황 기록

#### 75일차 (금) — Phase 4 평가 🔴

**오전 (3h)**
1. **재구현 시험(90분, AI 없음)**: §8.2 — "재화 차감 + 아이템 지급" 트랜잭션 API + 동시성 테스트(동시 50회 → 1회만)
2. **설명 시험(60분)**: §8.3에서 10문항
3. **결함 찾기(45분)**: §8.4 — 트랜잭션 누락·N+1·인덱스 미사용·토큰 검증 누락·레이스 5개 중 4개

**오후 (3h)**
- 체크리스트(§8.1) 점검, 미완 보충
- `W15.md` + Phase 4 회고
- §10 Phase 5 준비(로그 필드 표준·메트릭 준비)

**DoD**
- [ ] §8.1 전 항목, 설명 시험 평균 4.0 이상
- [ ] 3-1 연동 시연 로그 제출

---

## 3. 실습 카탈로그

### 3.1 공통 실습 (L4-C)

#### L4-C-01 최소 API 서버 기동 (45분) 🔴
- **단계**: 헬스 체크(`GET /health`) + 버전 체크(`POST /version` → 최소 지원 버전 비교) 구현, `.http` 파일로 호출
- **확인 기준**: 두 엔드포인트가 200을 반환하고 `.http` 파일이 저장소에 있다
- **노트**: 헬스 체크는 왜 인증을 요구하면 안 되는가

#### L4-C-02 ERD와 DDL (90분) 🔴
- **단계**: 12테이블 이상 ERD(Mermaid) → DDL 작성. SQLite 타입(`INTEGER/TEXT/REAL/BLOB`)과 MySQL 대응 타입을 병기
- **확인 기준**: 마이그레이션 실행으로 빈 DB가 만들어진다
- **자주 나는 오류**: SQLite는 타입 친화성이 느슨하다 → `CHECK` 제약으로 보완

#### L4-C-03 인덱스 설계 (60분) 🔴
- **단계**: 핵심 쿼리 5개를 먼저 적고 → 각 쿼리를 위한 인덱스 설계 → DDL에 목적 주석
- **확인 기준**: 모든 인덱스에 "이 쿼리를 위한 것" 주석. 불필요 인덱스 0
- **노트**: 복합 인덱스 컬럼 순서 결정 기준(선택도·범위 조건 위치)

#### L4-C-04 Repository 인터페이스 정의 (60분) 🔴
- **단계**: §7.2 골격대로 `IUserRepository` 등 5개 + `IUnitOfWork` 정의. **SQL 타입이 인터페이스에 노출되지 않게**
- **확인 기준**: `Contracts`/`Data` 프로젝트가 SQLite/MySQL 패키지를 참조하지 않음(빌드로 증명)

#### L4-C-05 가입 API (90분) 🔴
- **단계**: 입력 검증 → 해싱(Argon2id/bcrypt) → `account` 삽입 → `user`·`user_currency`·`attendance` 초기 레코드 생성 (**한 트랜잭션**)
- **확인 기준**: 중복 ID 시 409/에러 코드, 부분 생성 없음(롤백 확인)
- **노트**: 유니크 위반을 "먼저 SELECT로 확인"하지 않고 예외로 처리해야 하는 이유(레이스)

#### L4-C-06 로그인 API (60분) 🔴
- **단계**: 계정 조회 → 해시 검증 → 토큰 발급 → 마지막 로그인 시각 갱신
- **확인 기준**: 잘못된 비밀번호와 없는 계정의 응답이 **구분되지 않음**(계정 열거 방지), 응답 시간도 비슷하게

#### L4-C-07 Redis 세션 (75분) 🔴
- **단계**: `session:{token} → userId`(TTL 3600), `user_session:{userId} → token`. 로그인 시 이전 토큰 삭제
- **확인 기준**: 중복 로그인 후 이전 토큰으로 요청 시 401

#### L4-C-08 인증 미들웨어 (60분) 🔴
- **단계**: 헤더 파싱 → Redis 조회 → TTL 슬라이딩 갱신 → 컨텍스트에 userId 주입. 인증 제외 경로 목록 관리
- **확인 기준**: 인증 테스트 5개 통과, 인증 제외 경로가 명시적 목록으로 관리됨

#### L4-C-09 랭킹 ZSET (60분) 🔴
- **단계**: `ZADD rank:season:1 score userId`, 상위 100(`ZREVRANGE ... WITHSCORES`), 내 순위(`ZREVRANK`), 내 주변 ±5
- **확인 기준**: 1만 명 더미 데이터에서 조회 응답 10ms 이하
- **노트**: 동점 처리 규칙(먼저 도달한 사람 우선)을 어떻게 구현했는가

#### L4-C-10 캐시 패턴 (60분)
- **단계**: 프로필 조회 시 캐시 → 미스면 DB 조회 후 채움 → 프로필 변경 시 무효화. 캐시 스탬피드 완화(짧은 락 또는 지터 TTL)
- **확인 기준**: 캐시 히트율 로그, 무효화 누락 없음(변경 후 즉시 반영 테스트)

#### L4-C-11 Lua 원자 재고 차감 (45분)
- **단계**: `EVAL`로 "재고 확인 + 차감"을 원자적으로. 재고 부족 시 0 반환
- **확인 기준**: 동시 100요청에서 재고 10개면 정확히 10명만 성공

#### L4-C-12 격리 수준·잠금 현상 재현 (60분) 🔴
- **단계 (MySQL)**: 두 세션으로 논리피터블 리드/팬텀 재현, 격리 수준 변경 후 차이 관찰
- **단계 (SQLite)**: 쓰기 트랜잭션 중 다른 쓰기 시도 → `SQLITE_BUSY` 관찰 → WAL 모드에서 읽기는 가능함을 확인
- **확인 기준**: 재현 로그 + "내 API에서 이 현상이 문제가 되는 지점" 1개 지목

#### L4-C-13 잠금 충돌 재시도 (45분)
- **단계**: 충돌 발생 시 지수 백오프(50ms→100ms→200ms) 최대 3회 재시도, 초과 시 명확한 에러 코드
- **확인 기준**: 동시 부하에서 재시도로 성공률 회복, 무한 재시도 없음

#### L4-C-14 유저 데이터 로드 API (60분) 🔴
- **단계**: 1회 호출로 기본 정보·재화·인벤토리 요약·미수령 우편 수 반환. 쿼리 수를 로그로 세어 N+1 없음 확인
- **확인 기준**: 쿼리 4회 이하, 응답 시간 기록

#### L4-C-15 인벤토리 API (75분) 🔴
- **단계**: 조회(페이징), 사용(수량 차감 — **조건부 UPDATE**로 음수 방지, 0이면 삭제)
- **확인 기준**: 수량 초과 사용 거부, 동시 사용 2회에서 음수 불가

#### L4-C-16 더미 데이터 생성 (60분) 🔴
- **단계**: 배치 INSERT(1,000행 단위), 트랜잭션 묶음. **(SQLite)** 1만 유저·10만 인벤토리 / **(MySQL)** 100만·1,000만
- **확인 기준**: 생성 시간 기록, 재실행 가능(멱등 또는 초기화 옵션)
- **노트**: 인덱스를 나중에 만들면 왜 빠른가

#### L4-C-17 실행 계획 분석 (75분) 🔴
- **단계**: 핵심 쿼리 5개에 `EXPLAIN QUERY PLAN`(SQLite) / `EXPLAIN`(MySQL) → 풀 스캔 확인 → 인덱스 추가 → 전/후 시간 비교
- **확인 기준**: 풀 스캔 0, 전/후 표(쿼리·전 시간·후 시간·사용 인덱스)

#### L4-C-18 상점 구매 트랜잭션 (90분) 🔴
- **단계**: 상품 조회 → 재화 조건부 차감 → 아이템 지급 → 구매 이력 기록(한 트랜잭션). 재고 상품은 재고 차감 포함
- **확인 기준**: 영향 행 수 검사, 실패 시 전체 롤백, 이력에 결제 전/후 잔액 기록

#### L4-C-19 동시 100회 구매 테스트 (60분) 🔴
- **단계**: 같은 계정으로 100개 동시 요청(`Task.WhenAll` 또는 스레드 100개) → 성공 1건 확인
- **확인 기준**: 10회 반복해도 항상 1건, 재화 정확히 1회분, 아이템 1개
- **자주 나는 오류**: 테스트가 순차 실행이라 통과한 것처럼 보임 → 실제 동시 시작 배리어 사용

#### L4-C-20 우편함 API (75분) 🔴
- **단계**: 목록(페이징), 수령(첨부 지급 + 상태 변경, 한 트랜잭션, `WHERE status='unread'`), 만료 플래그
- **확인 기준**: 동시 50회 수령에서 1회만 성공

#### L4-C-21 출석 API (60분) 🔴
- **단계**: 자정(KST) 기준 일 1회, 보상 지급, 연속 출석 카운트
- **확인 기준**: 23:59:59와 00:00:01 경계 테스트 통과, 동시 50회에서 1회만

#### L4-C-22 랭킹 API (60분) 🔴
- **단계**: 점수 갱신·상위 100·내 순위·내 주변 순위. 시즌 키 사용
- **확인 기준**: 게임 결과 반영 후 즉시 순위 변동 확인

#### L4-C-23 게임 결과 저장(내부 API) (60분) 🔴
- **단계**: `game_result` 기록 + 랭킹 갱신. **gameId를 멱등성 키로** 사용해 중복 저장 방지
- **확인 기준**: 같은 gameId를 3번 보내도 1건만 저장되고 랭킹도 1회만 반영

#### L4-C-24 게임 서버에 HTTP 클라이언트 붙이기 (75분) 🔴
- **단계**: 3-1의 `IGameResultSink` 구현체를 HTTP 호출로. 실패 시 로컬 큐 보관 후 재시도(지수 백오프)
- **확인 기준**: API 서버를 잠시 중지해도 게임 서버가 죽지 않고, 재시작 후 밀린 결과가 전송됨

#### L4-C-25 연동 시연 (45분) 🔴
- **단계**: 게임 1판 진행 → 결과 저장 → 랭킹 반영을 로그로 추적(gameId로 필터)
- **확인 기준**: 게임 서버 로그 → API 로그 → DB/Redis 결과가 한 줄로 연결됨

#### L4-C-26 배치 워커 (75분)
- **단계**: 우편 만료 정리, 원장 합=잔액·재고 불일치 검사, 🟡 시즌 스냅샷. 중복 실행 방지(Redis 락 또는 DB 플래그)
- **확인 기준**: 두 인스턴스를 동시에 띄워도 작업이 1회만 수행됨

#### L4-C-27 악용 시나리오 도출 (90분) 🔴
- **단계**: §6.1 프롬프트로 8개 이상 도출 → 각각 재현 방법·방어·심각도 정리 → 5개를 테스트로 재현(방어 전 실패 확인)
- **확인 기준**: 방어 전 실패 → 방어 후 통과 커밋 이력

#### L4-C-28 방어 구현 (90분) 🔴
- **단계**: 입력 검증 강화, 파라미터 바인딩 전수 점검, 권한 검사(타인 리소스 접근 차단), 멱등성 적용
- **확인 기준**: 5개 시나리오 전부 차단, 문자열 결합 SQL 0건

#### L4-C-29 레이트 리밋 (60분)
- **단계**: Redis 카운터(`ratelimit:{userId}:{초}` INCR + TTL) 또는 토큰 버킷. 초과 시 429 + `Retry-After`
- **확인 기준**: 정상 사용자 영향 없음, 폭주 계정만 차단

#### L4-C-30 성능 측정·튜닝 (90분) 🔴
- **단계**: 로그인 API 동시 100요청 p99 측정 → 병목(해싱/커넥션 풀/쿼리) 확인 → 튜닝 → 재측정
- **확인 기준**: 전/후 표, "보안을 낮춰 성능을 얻지 않았음"을 문서에 명시

### 3.2 C# 트랙 실습 (L4-CS)

#### L4-CS-01 SQLite 구현체 + DI 등록 (90분) 🔴
- **골격**
  ```csharp
  // Program.cs
  builder.Services.AddSingleton<IDbConnectionFactory>(_ =>
      new SqliteConnectionFactory(cfg.GetConnectionString("Db")!));   // 기본
  // 🟡 MySQL로 전환: new MySqlConnectionFactory(...) 로 교체만
  builder.Services.AddScoped<IUnitOfWork, UnitOfWork>();
  builder.Services.AddScoped<IUserRepository, UserRepository>();
  ```
  연결 문자열은 공유 캐시를 사용하지 않는다. 시작 시 `PRAGMA journal_mode=WAL; PRAGMA busy_timeout=3000;`을 적용하고 요청/스레드별 연결을 팩토리에서 생성한다
- **확인 기준**: 통합 테스트가 실제 SQLite 파일(또는 임시 파일)로 통과
- **노트**: `IDbConnection` 수명을 Scoped로 두면 생기는 문제와 팩토리 패턴이 푸는 문제

#### L4-CS-02 MySQL 구현체 추가 (🟡 4-3c, 120분)
- **단계**: `Data.MySql` 프로젝트 추가 → 같은 인터페이스 구현(SqlKata/Dapper) → DI 등록만 교체 → **전체 테스트를 그대로 통과**시킨다
- **확인 기준**: 애플리케이션 코드 변경 0줄, 테스트 100% 통과, 두 DB의 실행 계획 비교표

#### L4-CS-03 WebApplicationFactory 통합 테스트 (75분) 🔴
- **단계**: 테스트 서버 기동 + 테스트 전용 DB 파일/스키마 + Redis 키 prefix + 각 테스트 전 초기화
- **확인 기준**: 테스트 30개가 서로 오염 없이 병렬 실행 가능(또는 순차라도 격리 보장)

#### L4-CS-04 동시성 테스트 작성법 (45분) 🔴
- **단계**: `Barrier` 또는 `TaskCompletionSource`로 100개 요청을 **정확히 동시에** 출발시키기
- **확인 기준**: 방어 코드를 잠시 제거하면 테스트가 실패함(테스트가 진짜로 경쟁을 만든다는 증거)

### 3.3 C++ 트랙 실습 (L4-CPP)

#### L4-CPP-01 SQLite 접근 계층 (120분) 🔴
- **단계**: vcpkg `sqlite3`(또는 `SQLiteCpp`) 연결, 프리페어드 스테이트먼트, 트랜잭션 RAII 래퍼, WAL·busy_timeout 설정
- **골격**
  ```cpp
  class SqliteConnection {
  public:
      explicit SqliteConnection(const std::string& path);   // WAL, busy_timeout 설정
      Statement Prepare(std::string_view sql);
      Transaction Begin();                                   // RAII: 소멸 시 미커밋이면 롤백
  };
  ```
- **확인 기준**: 트랜잭션 RAII가 예외 경로에서 롤백함을 테스트로 증명

#### L4-CPP-02 MySQL 구현체 추가 (🟡, 150분)
- **단계**: MySQL Connector/C++로 같은 인터페이스 구현 + 연결 풀(스레드 안전·유효성 검사)
- **확인 기준**: 인터페이스 뒤에서 교체되어 테스트 통과

#### L4-CPP-03 HTTP 서버와 라우팅 (120분) 🔴
- **단계**: cpp-httplib로 라우팅·JSON(nlohmann)·에러 응답 규격화·요청 컨텍스트(userId 주입)
- **확인 기준**: 4-C 엔드포인트 20개 라우팅, 인증 필터 동작

#### L4-CPP-04 통합 테스트 (90분) 🔴
- **단계**: GoogleTest + 실제 SQLite/Redis + cpp-httplib client로 호출, 스레드 100개 동시 요청
- **확인 기준**: 동시성 테스트 3개 통과

---

## 4. 학습 항목 상세

### 4.1 공통 (100h)

**HTTP/REST와 게임 API 설계 (12h)**
- 무엇: 메서드·상태 코드·헤더, JSON, REST vs 커맨드형, 게임 API 흐름, 에러 코드 체계, 버전 관리, DTO
- 왜: 게임 API는 "로그인 후 대량 로드", "재화 차감" 같은 특유 패턴이 있다
- 어떻게: `L4-C-01` → 4-C 명세 작성 → 구현하며 명세 갱신
- 확인: 엔드포인트 20개 명세 + 에러 코드 표

**인증·인가 (10h)**
- 무엇: 해싱(Argon2/bcrypt/PBKDF2), 토큰 vs 세션, 발급·검증·만료·갱신, 중복 로그인, 서버 간 인증
- 왜: 계정 탈취·재화 복제의 첫 방어선
- 어떻게: `L4-C-05/06` → `L4-C-07/08` → 인증 테스트 5개
- 확인: 만료·위조·타인·미인증·중복 로그인 테스트 전부 통과

**DB 설계와 쿼리 (24h)**
- 무엇: 정규화/비정규화 판단, 키 설계, 인덱스(B-tree·복합·커버링), 실행 계획, JOIN, 페이징, 마이그레이션, 샤딩 개념, **SQLite와 MySQL의 차이**(파일 vs 서버, 쓰기 직렬화, 타입 친화성)
- 왜: 라이브 서비스 장애의 최다 원인이 느린 쿼리다
- 어떻게: `L4-C-02/03` → `L4-C-16` 더미 데이터 → `L4-C-17` 실행 계획
- 확인: 실행 계획 전/후 표, 풀 스캔 0

**트랜잭션·동시성 (20h)**
- 무엇: ACID, 격리 수준과 현상, 락(레코드·갭·넥스트키/SQLite 쓰기 락), 데드락·재시도, 조건부 UPDATE, 낙관적 락, 멱등성 키, Redis 분산 락의 한계
- 왜: "동시 100회 구매" 조건이 이 Phase의 핵심 평가다
- 어떻게: `L4-C-12/13` 재현 → `L4-C-18/19` 구매 → `L4-C-20/21` 멱등성
- 확인: 동시성 테스트 3개(구매·우편·출석) 통과

**Redis 활용 (12h)**
- 무엇: 자료구조별 용도, TTL, 파이프라인, Lua 원자성, 영속성(RDB/AOF), 캐시 무효화, "두면 안 되는 것"
- 왜: 게임 서버 실시간 데이터의 상당수가 Redis에 있다
- 어떻게: `L4-C-07`(세션) → `L4-C-09`(랭킹) → `L4-C-10`(캐시) → `L4-C-11`(Lua)
- 확인: 랭킹 3기능, 세션 TTL 갱신, Lua 재고 차감

**서버 연동·배치 (8h)**
- 무엇: 내부 API와 서버 간 인증, 실패 재시도·중복 방지, 배치 작업과 중복 실행 방지
- 왜: 실시간 서버와 데이터 서버의 경계를 정하는 훈련
- 어떻게: `L4-C-23/24/25` → `L4-C-26`
- 확인: 게임 결과가 API를 거쳐 DB·랭킹에 반영되는 로그

**통합 테스트 (8h)**
- 무엇: 실제 DB/Redis 사용, 테스트 격리(전용 스키마·파일·키 prefix), 동시성 테스트 작성법, 픽스처
- 왜: 트랜잭션·락은 목(mock)으로 검증할 수 없다
- 어떻게: `L4-CS-03`/`L4-CPP-04` → `L4-CS-04`(동시 출발 배리어)
- 확인: 통합 30개 + 동시성 3개

**보안·악용 시나리오 (6h)**
- 무엇: 입력 검증, SQL 인젝션 방어, 레이트 리밋, 재화 복제·중복 수령·토큰 탈취·리플레이, 로그 민감 정보
- 어떻게: `L4-C-27/28/29`
- 확인: `SECURITY-SCENARIOS.md`, 시나리오 5개 방어 증명

### 4.2 C# 트랙 / 혼합 경로 (60h)

| 항목 | 시간 | 무엇을 | 학습 순서 | 확인 방법 |
|---|---|---|---|---|
| ASP.NET Core 구조 | 12h | 미들웨어 파이프라인, DI 수명, 컨트롤러 vs Minimal API, 모델 바인딩·검증, 필터, 설정, 헬스 체크 | L4-C-01 → L4-C-08 | 미들웨어 순서를 바꿔 인증이 깨지는 실험 |
| DB 접근 | 14h | Repository 인터페이스, **SQLite 기본 구현**(Microsoft.Data.Sqlite), 트랜잭션·UnitOfWork, 🟡 MySQL 구현(MySqlConnector+SqlKata)으로 교체 | L4-CS-01 → L4-C-18 → 🟡L4-CS-02 | 상점 구매 트랜잭션, DB 교체 시 앱 코드 0줄 변경 |
| Redis 접근 | 8h | StackExchange.Redis 또는 CloudStructures, ZSet·Hash, Lua 실행, 연결 관리 | L4-C-07/09/11 | 랭킹·세션 |
| 인증 미들웨어 | 8h | 커스텀 토큰 검증, 컨텍스트 주입, 인증 제외 경로 | L4-C-08 | 인증 테스트 5개 |
| 테스트 | 10h | `WebApplicationFactory`, 픽스처, 동시성 테스트(Barrier) | L4-CS-03 → L4-CS-04 | 통합 30 + 동시성 3 |
| 로깅·설정 | 8h | **Serilog** 요청 로그(요청ID·userId·소요 시간·결과 코드), 환경 분리, 민감 정보 마스킹 | 59일차 이후 상시 | 로그 샘플, 토큰 마스킹 확인 |

### 4.3 C++ 트랙 (a) 순수 C++ (60h)

| 항목 | 시간 | 무엇을 | 학습 순서 | 확인 방법 |
|---|---|---|---|---|
| HTTP 서버 | 14h | cpp-httplib(또는 Drogon), 라우팅, JSON, 요청 컨텍스트, 에러 규격 | L4-CPP-03 | 엔드포인트 20개 |
| DB 접근 | 16h | **SQLite 기본**(sqlite3/SQLiteCpp), 프리페어드, 트랜잭션 RAII, 🟡 MySQL Connector/C++ + 연결 풀 | L4-CPP-01 → 🟡L4-CPP-02 | 구매 트랜잭션, 예외 시 롤백 |
| Redis | 8h | redis-plus-plus, 파이프라인, Lua, 연결 풀 | L4-C-07/09/11 | 랭킹·세션 |
| 인증 | 6h | 토큰 생성(난수+HMAC), 검증 필터, libsodium argon2 | L4-C-05~08 | 인증 테스트 |
| 테스트 | 10h | GoogleTest + 실제 DB/Redis, HTTP 클라이언트, 스레드 100 동시 | L4-CPP-04 | 동시성 3개 |
| 빌드·설정 | 6h | vcpkg 매니페스트, 설정 파일, spdlog 요청 로그 | 상시 | 로컬 빌드·테스트 스크립트 통과 |

### 4.4 C++ 트랙 (b) 혼합 경로

- 12주 첫 주 20h: "ASP.NET Core Web API를 위한 필수 C# 가이드" 통독 + C# 프로젝트 구성·async 기초
- 이후 §4.2와 동일(실질 40h로 압축, 로깅·설정 항목 최소화)
- 추가 요구: C++ 게임 서버(3-1)에서 API를 호출하는 HTTP 클라이언트(cpp-httplib client 또는 WinHTTP) 직접 구현 — `L4-C-24`

---

## 5. 교재 활용 가이드 (Phase 4)

### 5.1 공통

| 교재 | 읽을 장 | 언제 | 어떻게 활용 |
|---|---|---|---|
| 1주일만에 배우는 MySQL C# 프로그래밍 | Day 1(DBMS·InnoDB), Day 2(SQL·테이블 설계), Day 5(고급 쿼리·인덱스) | 56~65일차 | 이 과정은 SQLite가 기본이므로 **SQL 문법·설계 개념만 취하고 연결 코드는 SQLite로 옮겨 실습**한다. Day 1의 MySQL 설치·Workbench 절은 MySQL을 선택했을 때만. Day 5 인덱스는 `EXPLAIN QUERY PLAN`으로 실습. 부록 A 치트시트는 인쇄 |
| 1주일만에 배우는 Redis 프로그래밍 | Ch.1(설치 — redis-windows 절), Ch.2(내부 구조·데이터 타입) | 62일차 | Docker/WSL2/Memurai 절은 건너뛰고 redis-windows로 설치. Ch.2 자료구조 표를 "이 데이터는 DB인가 Redis인가" 판정표로 확장 |
| ASP.NET Core Web API를 이용한 API 게임 서버 실습 | 부록 A(기획서), B(API 명세 가이드), C(스키마), F(커스텀 토큰), G(보안 체크리스트) | 56~60일차 | **4-C 명세 작성의 직접 참고.** 부록 B 형식을 따르되 오목 로비 기준으로 다시 쓴다. 부록 C의 MySQL 스키마는 SQLite DDL로 옮긴다. 부록 G는 4-2 체크리스트 |
| 신입 게임 프로그래머를 위한 가챠 확률 구현 실전 가이드 | 9장(서버 추첨 원칙), 10장(데이터 설계), **11장(재화 차감 원자성)**, **12장(Race Condition)**, 16장(사고 사례) | 66~67일차 | **11·12장은 이중 지출 방어의 최고 교재.** 상점 구매 설계 직전에 읽는다. 16장은 4-2 악용 시나리오 소재 |
| 온라인 게임 개발자를 위한 네트워크 학습 로드맵 | Ch.6(보안) 중 인증 절 | 61일차 | 토큰 설계 근거 |

### 5.2 C# 트랙 / 혼합

| 교재 | 읽을 장 | 언제 | 어떻게 활용 |
|---|---|---|---|
| ASP.NET Core Web API를 위한 필수 C# 가이드 | 전체 | 12주 첫 주(혼합 필수, C#은 복습) | 12절·27절 예제를 직접 타이핑해 빌드 |
| ASP.NET Core Web API를 이용한 API 게임 서버 실습 | 1~3장, 4~7장(DB 설계·연동·CRUD), 8~11장(토큰·로그인·미들웨어), 12~14장(Redis), 16~18장(인벤토리·우편·상점), 20장(출석), 22장(랭킹) | 56~70일차 | **주 교재.** 코드 폴더가 없으니 본문을 직접 타이핑하며 진행하고, 수집형 RPG 도메인을 **오목 로비로 바꿔** 구현한다. 4~7장의 MySQL 연동 코드는 Repository 뒤에서 SQLite로 대체, MySQL 선택 시 원문대로 |
| ASP.NET Core Web API로 만드는 게임 서버 | 5장(DB 연동), 6장(Redis), 7장(인증), 8장(데이터 API), 13장(비동기 처리), 16장(보안) | 61~72일차 | 보충용. `codes/GameAPIServer_Template_01`을 net10.0으로 올려 빌드해 Controllers/Repository/Services 구조를 보고 프로젝트 구조 ADR. 17장(배포)은 이 과정 범위 밖 |
| 1주일만에 배우는 MySQL C# 프로그래밍 | Day 3(C# 연동), Day 4(SqlKata), Day 6(게임 콘텐츠), Day 7(성능·운영) | 58~70일차 | Day 6은 4-1 요구사항과 거의 일치. 연결 코드는 SQLite로 옮기고, Day 7 성능·운영은 MySQL 선택 시 적용 |
| 1주일만에 배우는 Redis 프로그래밍 | Ch.3(세션·중복 로그인), 4(캐시), 5(ZSet 랭킹), 6(Pub/Sub — Phase 6 예습), 7(Pipeline·모니터링) | 61~68일차 | Ch.3·5는 4-1 직접 참고 |
| 2D MMORPG 게임 서버 개발 | 2주차(API 서버 & 계정) | 69일차 | 게임 서버 + API 서버 분리 구조의 실제 코드(net10.0). "게임 서버가 API를 어떻게 호출하는가"를 3-1 연동에 반영 |

### 5.3 C++ 트랙 (a)

저장소에 C++ API 서버 책은 없다. 공식 문서를 쓴다.
- cpp-httplib README, Drogon 문서 🟡
- **SQLite 공식 문서**(sqlite.org: WAL, busy_timeout, 트랜잭션) / 🟡 MySQL Connector/C++ 가이드
- redis-plus-plus README
- 공통 교재의 SQL·자료구조 부분은 동일하게 읽고, C# 코드 부분은 "C++ 라이브러리로는 어떻게 하는가"로 번역하는 것이 과제

### 5.4 부 트랙 읽기 과제

- C# 주 트랙: 가챠 가이드 11~12장 → "재화 차감 원자성 패턴 3가지" 정리(이미 공통 과제)
- C++ 주 트랙(경로 a): API 게임 서버 실습 8장(커스텀 토큰 설계) → 자기 C++ 토큰 구현과 비교 1페이지

---

## 6. AI 협업 가이드 (Phase 4 특화)

### 6.1 프롬프트

**스키마 리뷰**
```
다음은 내 오목 로비 API 서버의 스키마다(기본 DB: SQLite, 선택: MySQL). (DDL 붙여넣기)
1) 유저 1억·인벤토리 10억·우편 5억 규모에서 느려질 쿼리를 예측해 달라(API 명세 첨부).
2) 각 예측을 실행 계획으로 확인하는 방법을 알려 달라.
3) 인덱스를 제안하되 왜 그 컬럼 순서인지 설명해 달라.
나는 더미 데이터를 넣고 전/후를 측정한 뒤 결과를 가져오겠다. 결론을 먼저 내리지 말라.
```

**악용 시나리오 도출**
```
다음 API 명세를 공격자 관점에서 본다. (첨부)
재화 복제, 중복 수령, 토큰 탈취, 리플레이, 파라미터 조작, 레이스 컨디션, 계정 열거, 대량 요청 관점에서
악용 시나리오를 최소 8개 열거해 달라.
각 시나리오마다 "재현 방법"과 "방어 방법"을 나눠 적고, 심각도(높음/중간/낮음)를 매겨 달라.
나는 5개 이상을 테스트로 재현하고 방어를 구현하겠다.
```

**트랜잭션 경계 리뷰**
```
상점 구매 서비스 코드다. (붙여넣기)
동시 100회 요청 시 재화가 정확히 1회분만 차감되는지 두 요청의 시간 순서로 시뮬레이션해 검토해 달라.
SQLite(쓰기 직렬화)와 MySQL(REPEATABLE READ)에서 결과가 달라지는 지점이 있으면 짚어 달라.
트랜잭션 안에 넣으면 안 되는 작업이 포함되어 있는지도 확인해 달라.
```

**데이터 배치 판정**
```
다음 항목을 DB(SQLite/MySQL)·Redis·둘 다 중 어디에 두어야 하는지 근거와 함께 판정해 달라:
골드 잔액, 세션 토큰, 랭킹, 우편함, 출석 기록, 온라인 유저 목록, 상점 재고, 게임 결과 이력, 멱등성 키.
내 판정은 이렇다: (내 표). 다른 부분만 지적하고, 내가 맞은 항목은 넘어가라.
```

**DB 추상화 리뷰**
```
내 Repository 인터페이스와 SQLite 구현이다. (붙여넣기)
이 인터페이스로 MySQL 구현을 추가할 때 문제가 될 지점(SQLite 전용 문법·타입·트랜잭션 동작 차이)을 지적해 달라.
인터페이스가 특정 DB에 새고 있는 부분이 있으면 짚어 달라.
```

### 6.2 맡길 것 / 맡기지 않을 것

| 맡긴다 | 맡기지 않는다 |
|---|---|
| 컨트롤러·DTO·리포지토리 보일러플레이트 | 트랜잭션 경계와 격리 수준 결정 |
| 더미 데이터 생성 스크립트 | 스키마·인덱스의 최종 결정(측정으로 검증) |
| 악용 시나리오 목록 초안 | 방어 구현과 테스트 |
| `.http` 테스트 파일, 마이그레이션 스크립트 초안 | 에러 코드 체계, 데이터 배치 판정 |

### 6.3 AI가 자주 틀리는 지점

- **`SELECT` 후 잔액 확인 → `UPDATE`** 두 쿼리로 재화를 차감하는 코드(레이스) → 조건부 UPDATE 또는 `FOR UPDATE`
- 비밀번호를 **SHA256+salt**로 해싱하는 예제 → Argon2id/bcrypt/PBKDF2
- **JWT를 Redis 세션 없이** 쓰면서 "로그아웃 구현했다"고 하는 예제(무효화 불가)
- 인덱스를 모든 컬럼에 제안 → 쓰기 비용과 인덱스 유지 비용을 되묻는다
- Redis 분산 락을 만능처럼 제시 → 락 만료·클라이언트 사망·시계 문제를 되묻는다
- **SQLite에 MySQL 문법**(예: `ON DUPLICATE KEY UPDATE`)을 섞어 제안 → SQLite는 `ON CONFLICT DO UPDATE`
- 트랜잭션 안에 HTTP 호출·Redis 호출을 넣는 코드 → 트랜잭션 시간이 외부 지연에 묶인다

---

## 7. 과제 명세

### 7.1 공통 과제 4-C. 스키마·API 명세 (필수, 16h, 57~60일차)

**산출물 1 — `SCHEMA.md`**

1. **ERD**(Mermaid)
2. **테이블별 DDL**(SQLite 기준, MySQL 타입 병기). 최소 테이블:

| 테이블 | 핵심 컬럼 | 비고 |
|---|---|---|
| `account` | account_id(PK), login_id(UNIQUE), password_hash, created_at, last_login_at | 인증 전용. 솔트는 해시 문자열에 포함 |
| `user` | user_id(PK), account_id(FK,UNIQUE), nickname(UNIQUE), level, exp, created_at | 게임 프로필 |
| `user_currency` | user_id(PK), gold, gem, updated_at | 재화 원장 |
| `item_master` | item_id(PK), name, type, stackable, max_stack | 마스터 데이터 |
| `user_item` | user_item_id(PK), user_id, item_id, count, acquired_at | (user_id, item_id) 인덱스 |
| `mail` | mail_id(PK), user_id, title, body, status, expire_at, created_at | (user_id, status) 인덱스 |
| `mail_attachment` | mail_id, slot, item_id, count, currency_type, currency_amount | 복합 PK |
| `attendance` | user_id(PK), last_check_date, streak_count | 일 1회 |
| `shop_product` | product_id(PK), item_id, price_currency, price_amount, stock, is_active | 재고 nullable |
| `purchase_log` | purchase_id(PK), user_id, product_id, price_amount, gold_before, gold_after, created_at | 감사 추적 |
| `currency_ledger` | ledger_id(PK), user_id, delta, reason, ref_id, balance_after, created_at | 모든 재화 변동 원장 |
| `game_result` | game_id(PK, 외부 생성), room_id, winner_user_id, loser_user_id, reason, played_at | 멱등성 키 |
| `idempotency_key` | key(PK), user_id, endpoint, status, request_hash, response_json, created_at | TTL 정리 대상 |
| `notice` | notice_id(PK), title, body, starts_at, ends_at | 공지 조회 |

3. **인덱스 목록**: 각 인덱스에 "이 쿼리를 위한 것" 주석
4. **DB vs Redis 배치 판정표**

| 데이터 | 저장소 | 근거 |
|---|---|---|
| 골드 잔액 | DB | 잃으면 안 되는 원장, 트랜잭션 필요 |
| 세션 토큰 | Redis | 휘발 가능, TTL 필요, 조회 빈번 |
| 랭킹 | Redis(ZSET) + DB 스냅샷 | 실시간 정렬 + 복구 가능성 |
| 우편함 | DB | 영속성 필요 |
| 출석 기록 | DB | 영속성·감사 |
| 온라인 유저 목록 | Redis | 휘발성 |
| 상점 재고 | DB 조건부 UPDATE | 단일 진실 원장. Redis Lua는 원자성 데모에만 사용하며 DB 실패 시 보상 INCR·배치 불일치 검출 필요 |
| 멱등성 키 | Redis(TTL) 또는 DB | 만료 관리 편의 |

**산출물 2 — `API.md`** (엔드포인트 20개 이상)

| # | 엔드포인트 | 인증 | 멱등 | 설명 |
|---|---|---|---|---|
| 1 | `GET /health` | X | - | 헬스 체크 |
| 2 | `POST /version` | X | O | 클라이언트 버전 확인 |
| 3 | `POST /account/signup` | X | X | 가입 |
| 4 | `POST /account/login` | X | X | 로그인, 토큰 발급 |
| 5 | `POST /account/logout` | O | O | 세션 삭제 |
| 6 | `POST /account/token/refresh` | O | X | 토큰 갱신 |
| 7 | `GET /user/data` | O | O | 로그인 후 1회 대량 로드 |
| 8 | `GET /user/profile` | O | O | 프로필 |
| 9 | `GET /inventory?cursor=&limit=` | O | O | 인벤토리 페이징 |
| 10 | `POST /inventory/use` | O | X | 아이템 사용(수량 차감) |
| 11 | `GET /mail?cursor=&limit=` | O | O | 우편 목록 |
| 12 | `POST /mail/receive` | O | **O(키 필요)** | 우편 수령 |
| 13 | `POST /attendance/check` | O | **O** | 출석 체크 |
| 14 | `GET /shop/products` | O | O | 상품 목록 |
| 15 | `POST /shop/purchase` | O | **O(키 필요)** | 구매(트랜잭션) |
| 16 | `GET /rank/top?count=100` | O | O | 상위 랭킹 |
| 17 | `GET /rank/me` | O | O | 내 순위·주변 |
| 18 | `GET /notice` | X | O | 공지 |
| 19 | `POST /internal/game/result` | 서버 간 | **O(gameId)** | 게임 결과 저장 |
| 20 | `POST /internal/token/verify` | 서버 간 | O | 게임 서버의 토큰 검증 |
| 21 | `GET /internal/health/deep` | 서버 간 | O | DB·Redis 연결 확인 |

각 엔드포인트마다 **요청 스키마 / 응답 스키마 / 가능한 에러 코드 / 멱등성 여부**를 표로 적는다.

**산출물 3 — 에러 코드 표**

| 코드 | HTTP | 이름 | 클라이언트 대응 |
|---|---|---|---|
| 0 | 200 | Success | - |
| 1001 | 400 | InvalidRequest | 입력 확인 |
| 1002 | 401 | Unauthenticated | 재로그인 |
| 1003 | 403 | Forbidden | 접근 불가 안내 |
| 1004 | 404 | NotFound | - |
| 1005 | 429 | RateLimited | 재시도 대기 |
| 1006 | 500 | InternalError | 재시도 |
| 2001 | 409 | DuplicateLoginId | 다른 ID 사용 |
| 2002 | 401 | InvalidCredential | 아이디/비번 확인(구분하지 않음) |
| 2003 | 401 | SessionExpired | 재로그인 |
| 3001 | 400 | InsufficientCurrency | 재화 부족 안내 |
| 3002 | 400 | ItemNotFound | - |
| 3003 | 409 | AlreadyReceived | 이미 수령 |
| 3004 | 409 | AlreadyCheckedToday | 이미 출석 |
| 3005 | 409 | OutOfStock | 재고 없음 |
| 4001 | 409 | GameResultDuplicated | 무시(정상) |

**산출물 4 — 시퀀스 다이어그램 2개**: ① 로그인~데이터 로드 ② 상점 구매(트랜잭션 내부 단계 표시)

**산출물 5 — `adr/0005-db-abstraction.md`**: Repository+DI, SQLite 기본/MySQL 선택. 대안(ORM 전면 사용 / DB 직접 호출)과 기각 이유

**채점**: 완결성 40 / 데이터 배치 판정 근거 30 / 명확성(AI "다른 팀 개발자" 리뷰 통과) 30

### 7.2 트랙 과제 4-1. 게임 API 서버 (필수, 88h, 58~74일차)

**기능 요구사항**

0. **DB 접근은 Repository/DAO 인터페이스 + DI**. 기본 SQLite, 🟡 MySQL 구현체 추가 시 **설정만 바꿔 전환**
1. **계정**: 가입(검증·해싱), 로그인(토큰+Redis 세션 TTL 1h), 토큰 갱신, 로그아웃, 중복 로그인 시 이전 세션 무효화
2. **버전 체크·공지·헬스 체크**(인증 불필요)
3. **유저 데이터 로드**: 1회 호출로 기본 정보·재화·인벤토리 요약·미수령 우편 수
4. **인벤토리**: 조회(페이징), 사용(수량 차감, 0이면 삭제)
5. **우편함**: 목록, 수령(첨부 지급, 1회만), 만료(배치)
6. **출석**: KST 날짜 기준 일 1회, 연속 출석 카운트. 별도 자정 리셋 작업은 두지 않는다
7. **상점**: 상품 목록, 구매(재화 차감 + 아이템 지급 + 이력, **하나의 트랜잭션**), 재고 상품 1종
8. **랭킹**: 게임 결과로 점수 갱신(ZSET), 상위 100, 내 순위·주변
9. **내부 API**: 게임 결과 저장(gameId 멱등), 토큰 검증. 서버 간 인증 필수
10. **배치 워커**: 우편 만료 정리, 재화 원장·재고 정합 검사, 중복 실행 방지

**Repository 인터페이스 골격**

```csharp
public interface IUnitOfWork : IAsyncDisposable
{
    Task<ITransaction> BeginAsync(CancellationToken ct = default);
}

public interface IUserRepository
{
    Task<UserRow?> FindByIdAsync(long userId, ITransaction? tx = null, CancellationToken ct = default);
    Task<bool>     TryConsumeGoldAsync(long userId, long amount, ITransaction tx, CancellationToken ct = default);
    //  ↑ 조건부 UPDATE + 영향 행 수로 성공 여부를 돌려준다. "잔액 조회" API는 별도.
}

public interface IItemRepository
{
    Task AddItemAsync(long userId, int itemId, int count, ITransaction tx, CancellationToken ct = default);
    Task<bool> TryConsumeItemAsync(long userId, int itemId, int count, ITransaction tx, CancellationToken ct = default);
    Task<IReadOnlyList<UserItemRow>> ListAsync(long userId, long cursor, int limit, CancellationToken ct = default);
}

public interface IMailRepository
{
    Task<bool> TryMarkReceivedAsync(long mailId, long userId, ITransaction tx, CancellationToken ct = default);
    // WHERE mail_id=? AND user_id=? AND status='unread'  → 영향 행 수 1이어야 성공
}
```
(C++는 같은 의미의 순수 가상 인터페이스 + `Transaction&` 전달)

**비기능 요구사항**

| 조건 | 기본(SQLite) | 선택(MySQL) |
|---|---|---|
| 동시 100회 상점 구매 | 재화 1회분만 차감, 아이템 1개만 지급 | 동일 |
| 우편 수령·출석 동시 50회 | 1회만 처리 | 동일 |
| 로그인 해시 경로 | 총 100요청·동시 10, p99 ≤ 해시 1회 실측×2+30ms | 동일 |
| `GET /user/data` | 동시 100, p99 50ms 이하 | 동일 |
| 실행 계획 | 1만/10만 규모, 핵심 쿼리 5개 풀 스캔 0 | 100만/1,000만 규모, 동일 |
| SQL | 파라미터 바인딩 100%(문자열 결합 0) | 동일 |
| 로그 | 요청ID·userId·소요 시간·결과 코드, 토큰·비밀번호 마스킹 | 동일 |

**테스트 요구사항**

- **통합 30개 이상**: 계정 6 / 인벤토리 5 / 우편 5 / 출석 4 / 상점 5 / 랭킹 3 / 내부 API 3
- **동시성 3개**: 구매 100, 우편 수령 50, 출석 50 — 각각 방어 코드를 제거하면 실패해야 한다(테스트의 유효성 증명)
- **인증 5개**: 만료·위조·타인 토큰·미인증·중복 로그인

**제출물**: 코드, 테스트, 마이그레이션 스크립트, 더미 데이터 스크립트, `PERF-4-1.md`, 3-1 연동 시연 로그

**`PERF-4-1.md` 템플릿**
```markdown
## 1. 환경(DB 종류·버전, 데이터 규모, 하드웨어)
## 2. 핵심 쿼리 5개 실행 계획 전/후
| 쿼리 | 전(스캔 종류/시간) | 인덱스 | 후(스캔 종류/시간) |
## 3. 해시 비용 실측과 로그인 API p99 (총 100·동시 10)
| 해시 파라미터 | 해시 1회 | 로그인 p50 | p95 | p99 | 실패율 |
## 4. /user/data p99 (동시 100, 목표 50ms)
## 5. 병목과 조치 (해싱 비용은 낮추지 않는다 — 이유 명시)
## 6. 남은 한계
```

**채점**

| 항목 | 배점 | 기준 |
|---|---|---|
| 기능 | 25 | 요구사항 0~10 |
| 정합성·멱등성 | 25 | 동시성 테스트 3개 통과 + 방어 제거 시 실패 증명 |
| 데이터 설계·성능 | 20 | 실행 계획 풀 스캔 0, 로그인 p99 기준 충족 |
| 보안 | 15 | 인증 5개, 파라미터 바인딩 100%, 해싱 정책, 로그 마스킹 |
| 테스트·문서 | 15 | 통합 30개, `PERF-4-1.md`, 연동 로그 |

**자주 하는 실수**
- 트랜잭션 안에서 Redis·HTTP 호출 → 지연이 트랜잭션을 잡아먹는다
- 동시성 테스트가 실제로 동시가 아님 → 배리어로 동시 출발
- 멱등성 키를 저장만 하고 **이전 응답을 돌려주지 않음** → 두 번째 요청이 실패로 보임
- 인벤토리 사용에서 `count`가 음수가 될 수 있음 → 조건부 UPDATE + 영향 행 수
- SQLite에서 장시간 트랜잭션을 열어 두어 `SQLITE_BUSY` 폭증 → 트랜잭션 짧게

### 7.3 공통 과제 4-2. 악용 시나리오 방어 리포트 (필수, 12h, 71~72일차)

**요구사항**
1. AI와 함께 시나리오 **8개 이상** 도출(재화 복제·중복 수령·토큰 탈취·리플레이·파라미터 조작·레이스·계정 열거·대량 요청)
2. 그중 **5개 이상을 테스트로 재현**: 방어 전 실패(취약함 증명) → 방어 구현 → 통과. 커밋 이력으로 남긴다
3. `SECURITY-SCENARIOS.md`: 시나리오 / 재현 방법 / 심각도 / 방어 방법 / 테스트 링크 / 남은 리스크

**필수 포함 시나리오 예시**
- 음수 수량으로 아이템 사용 → 수량 증가
- 타인 `userId`를 요청 바디에 넣어 남의 우편 수령
- 같은 우편 수령 요청을 동시에 50번
- 만료된 토큰 재사용 / 로그아웃 후 토큰 재사용
- 상점 구매 요청을 캡처해 그대로 재전송(리플레이)
- 존재하는 아이디인지 응답으로 구분(계정 열거)

**채점**: 재현 증거 40 / 방어 타당성 40 / 남은 리스크 인식 20

### 7.4 심화 과제 4-3 (선택 🟡)

- **4-3a 관리자 도구(16h)**: 유저 조회, 재화 지급, 우편 발송, 공지 등록, **감사 로그**(누가 언제 무엇을). CLI 또는 최소 웹
- **4-3b 가챠(16h)**: 교재 4~6장의 룰렛·등급제·천장 중 하나를 상점에 추가. 14장(확률 검정)으로 10만 회 시뮬레이션 후 기대 확률과 비교
- **4-3c MySQL 전환(8h, 권장)**: `Data.MySql` 구현체 추가 → **DI 등록만 교체** → 전체 테스트 통과. `MYSQL-MIGRATION.md`에 (1) 바꾼 파일 목록 (2) SQL 방언 차이 (3) 동시성 모델 차이가 실제로 준 영향 (4) SQLite/MySQL 성능 비교표

---

## 8. 학습 완료 판정 (75일차 금요일)

### 8.1 체크리스트

**과제**
- [ ] 4-C: `SCHEMA.md`(12테이블·인덱스 근거·배치 판정표), `API.md`(20개+·에러 코드), ADR
- [ ] 4-1: 기능 0~10 동작, 통합 30 + 동시성 3 + 인증 5 통과
- [ ] 4-2: 시나리오 8개, 5개 테스트 재현·방어
- [ ] 🟡 4-3 택1 또는 미수행 사유

**측정**
- [ ] `PERF-4-1.md`: 실행 계획 전/후, 로그인 p99
- [ ] 동시 100회 구매에서 1건만 성공(10회 반복 안정)
- [ ] 3-1 게임 결과 → API → 랭킹 반영 로그

**구조**
- [ ] 도메인·계약 프로젝트가 DB 패키지를 참조하지 않음(빌드 증명)
- [ ] SQL 파라미터 바인딩 100%(문자열 결합 검색 결과 0건)

**기록**
- [ ] 학습 노트 20일치, 주간 회고 4개

### 8.2 재구현 시험 (AI 없음, 90분)

**문제**: 빈 프로젝트(DB 포함)에서 "재화 차감 + 아이템 지급" API 하나를 구현하고 제공 테스트를 통과시킨다.

1. `POST /purchase` — 요청: `{userId, productId, idempotencyKey}`
2. 성공 시 재화 차감·아이템 지급·이력 기록(한 트랜잭션)
3. 제공 테스트
   - 정상 구매 1건
   - 잔액 부족 시 거부(변경 없음)
   - **동시 50회 요청 → 정확히 1건 성공**
   - 같은 멱등성 키로 재요청 → 이전 결과 반환(중복 차감 없음)

**통과 기준**: 90분 내 4개 통과.

### 8.3 설명 시험 질문 은행 (10문항 무작위, 평균 4.0)

1. 격리 수준 4단계와 각 단계의 현상. 내가 쓰는 DB의 기본값과 그 의미는
2. 이중 지출을 막는 방법 3가지와 각각의 비용
3. 복합 인덱스 컬럼 순서를 정하는 기준. 커버링 인덱스란
4. 토큰 vs 세션. JWT만 쓰면 로그아웃이 왜 어려운가
5. Redis에 재화 잔액을 두면 안 되는 이유. 예외가 있다면
6. 게임 서버가 DB를 직접 치지 않고 API를 거치게 한 이유. 직접 치는 게 맞는 경우는
7. 멱등성 키를 어디에 저장하고 언제 지우는가. 지우기 전 재시도가 오면
8. Repository + DI로 DB를 추상화한 이유. 추상화가 새는 지점은 어디였는가
9. SQLite와 MySQL의 동시성 모델 차이가 내 코드에 준 영향
10. 트랜잭션 안에 넣으면 안 되는 작업과 그 이유
11. 비밀번호 해싱에 SHA256을 쓰면 안 되는 이유. 파라미터는 어떻게 정했는가
12. 계정 열거를 막기 위해 무엇을 했는가
13. 랭킹을 Redis에 두면서 DB와 어떻게 정합성을 맞췄는가
14. N+1 문제가 생겼던 지점과 해결 방법
15. 레이트 리밋을 계정 단위와 IP 단위 중 무엇으로 했는가. 왜
16. (C#) DI 수명 Scoped인 `DbConnection`을 Singleton 서비스에 주입하면 무슨 일이 생기는가
17. (C++) 연결 풀을 직접 만들 때 스레드 안전성과 연결 유효성 검사를 어떻게 하는가

### 8.4 결함 찾기 (45분)

트랜잭션 누락 / N+1 / 인덱스 미사용 / 토큰 검증 누락 / 레이스 — 5개 중 4개 발견 + 수정안.

### 8.5 미달 시 대응

| 미달 항목 | 대응 |
|---|---|
| 동시성 테스트 미통과 | **통과 불가**. 최대 1주 연장(이 Phase의 핵심 역량) |
| 로그인 p99 미달 | 원인 분석 제출 시 진행, Phase 5에서 재측정 |
| 실행 계획 풀 스캔 잔존 | 인덱스 재설계 후 재측정, 남는 것은 근거 문서화 |
| 통합 테스트 20개 미만 | Phase 5 1주차에 보강(테스트 없이는 이후 리팩터가 불가능) |
| 보안 시나리오 3개 미만 | 4-2 재수행 |

---

## 9. 흔한 막힘 포인트

| 증상 | 원인 | 대처 |
|---|---|---|
| 동시 구매 테스트에서 가끔 2회 차감 | SELECT 후 UPDATE 분리 | `UPDATE ... SET gold = gold - ? WHERE user_id = ? AND gold >= ?` + 영향 행 수 확인 |
| (SQLite) `database is locked` 빈발 | 긴 쓰기 트랜잭션, WAL 미설정 | `PRAGMA journal_mode=WAL`, `busy_timeout`, 트랜잭션 짧게, 읽기/쓰기 분리 |
| (MySQL) 데드락 간헐 발생 | 두 트랜잭션이 다른 순서로 행 락 | 락 획득 순서 통일(유저 → 아이템), 재시도 |
| Redis 세션이 사라짐 | TTL 갱신 누락, 서비스 재시작 | 요청마다 TTL 연장, 영속성(RDB/AOF) 설정 확인 |
| 더미 데이터 삽입이 너무 느림 | 행 단위 INSERT, 인덱스 존재 | 배치 INSERT(1,000행), 트랜잭션 묶기, 인덱스는 나중에 생성 |
| 실행 계획이 인덱스를 안 탐 | 컬럼 순서, 함수 적용, 타입 불일치 | 조건 컬럼을 선두로, `WHERE DATE(col)` 금지, 타입 맞춤 |
| 테스트가 서로 데이터를 오염 | 공용 DB/키 | 테스트 전용 파일·스키마 + 키 prefix + 각 테스트 전 초기화 |
| 통합 테스트가 느려서 안 돌린다 | 매 테스트마다 전체 마이그레이션 | 템플릿 DB 복사 또는 in-memory, 픽스처 재사용 |
| 멱등성 키를 넣었는데 중복 처리됨 | 키 저장과 처리 사이의 레이스 | 키 삽입을 유니크 제약으로 두고 **삽입 성공한 요청만** 처리 |
| 토큰이 로그에 남음 | 요청 전체 로깅 | 민감 필드 마스킹 필터 |
| (C#) `WebApplicationFactory`에서 설정이 안 바뀜 | 환경 이름 | `ASPNETCORE_ENVIRONMENT=Test`, `appsettings.Test.json` |
| 게임 서버 연동이 API 장애 때 같이 죽음 | 동기 호출·재시도 없음 | 로컬 큐 + 지수 백오프, 타임아웃 설정 |

---

## 10. Phase 5로 넘어가기 전 준비

- [ ] Prometheus·Grafana·windows_exporter Windows 바이너리 다운로드(설치는 Phase 5 17주차)
- [ ] 3-1·4-1의 로그 필드가 표준화되어 있는지 점검(Phase 5 관측의 입력이 된다): `requestId/sessionId/userId/roomId/gameId/durationMs/resultCode`
- [ ] 4-1에 `/metrics` 엔드포인트를 붙일 자리(HTTP 리스너)를 확인해 둔다
- [ ] 부하 도구(2-3/3-2 클라이언트)에 **API 호출 시나리오**를 넣을 준비(로그인 → 게임 → 랭킹 조회)
- [ ] 성능 기준선 기록: 현재 로그인 p99, 구매 p99, 게임 결과 저장 p99 — Phase 5에서 개선 대상이 된다
- [ ] Phase 4 회고: 가장 어려웠던 정합성 문제, AI가 제안한 위험한 코드 1개, DB 추상화가 실제로 도움이 된 순간

---

## 11. 2026-09-05 보강 사항 (앞 절과 충돌하면 이 절 우선)

### 11.1 구현 계약

- 비밀번호 해시는 내장 `Rfc2898DeriveBytes.Pbkdf2`를 기본으로 하고 알고리즘·반복·salt를 PHC형 단일 문자열에 저장한다. 대안은 `BCrypt.Net-Next` 하나로 고정한다. C++은 libsodium을 사용한다
- C++ SQLite는 요청 스레드별/thread-local 연결 또는 풀을 사용한다. 단일 연결 공유 시 `SQLITE_OPEN_FULLMUTEX`와 외부 직렬화를 명시한다. 공통 SQL 접근은 Dapper/준비된 문장으로 통일한다
- `TimeProvider`/`IClock`을 주입하고 저장은 UTC, 출석 판정만 KST 날짜로 한다. `mail.status`는 `Unread/Received/Expired`, 토큰은 절대 만료 24h·슬라이딩 1h이며 refresh 시 회전하고 구 토큰을 즉시 폐기한다
- `session:{token}`과 `user_session:{userId}` 갱신은 Lua/MULTI로 묶는다. 멱등 필터는 신규 키 삽입, 처리 중 409, 완료 응답 재생, 같은 키·다른 request hash 422를 구현한다
- 모든 재화 변동은 `currency_ledger`와 잔액을 같은 트랜잭션에 기록한다. 배치가 원장 합=잔액과 Redis/DB 재고 불일치를 검사한다
- 게임 서버 HTTP 호출은 룸 Job 밖의 영속 아웃박스 큐·전용 송신 스레드에서 수행하며 최대 보관 수·타임아웃·재시도/폐기 정책을 ADR로 남긴다

### 11.2 일정·시험·마이그레이션

#### L4-C-20a 멱등성 필터 (90분) 🔴
- **단계**: key 삽입→처리 중 409→완료 응답 저장/재생→request hash 불일치 422를 구현한다
- **확인 기준**: 동일 요청 50회가 1회만 반영되고 변조 재사용은 거부된다

#### L4-C-24b 게임 서버 토큰 검증 핸드오프 (60분) 🔴
- **단계**: `LoginReq{token}`을 내부 verify API로 확인해 userId를 세션에 바인딩하고 만료·위조 토큰을 거부한다
- **확인 기준**: Phase 3 닉네임 로그인 경로 제거, 성공/만료/위조 통합 테스트

- 58일차 오후를 테스트 인프라 데이로 두어 `L4-CS-03/04` 또는 `L4-CPP-03/04`, 테스트 DB 격리, `IClock`, `CLAUDE.md`를 완료한다. 67일차에 `L4-C-20a 멱등성 필터` 90분, 69일차에 `L4-C-24b 토큰 검증 핸드오프` 60분을 수행한다
- 64일차에 nickname `version` 낙관적 락, 68일차에 `schema_version`과 `002_add_season.sql`, 70일차에 원장 합 검사를 추가한다. 4-3c MySQL 전환은 Phase 5 완충 시간으로 이동한다
- 4-C/4-1/4-2 계획 시간은 12h/62h/10h다. 재구현 시험은 골격을 제공하고 120분으로 하며 요청 본문 `userId`는 테스트 편의용이고 실제 구현은 인증 컨텍스트만 신뢰한다
- 통합 테스트 30개를 계정6/인벤토리5/우편5/출석4/상점5/랭킹3/내부API3으로 명시하고, 정합성·멱등성 25점 만점을 통과 게이트로 둔다

### 11.3 공격·성능·AI 검증

- 4-2 replay는 키 없이 재전송하거나 같은 키로 바디를 변조한다. 73일차 부하는 3-2 클라이언트 HTTP 시나리오 또는 `HttpClient` 병렬 스크립트로 실행한다
- 질문 은행에 처리 중 멱등 재시도, 원장, 낙관적 락, UTC/KST, 토큰 회전, 아웃박스를 추가한다. AI가 SQLite 공유 캐시, DB+Redis 선차감 이중 쓰기, 본문 userId 신뢰를 제안하면 동시성·실패 주입 테스트로 반박한다
