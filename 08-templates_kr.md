# 템플릿 모음

> 과정 전반에서 반복 사용하는 문서 템플릿이다. 각 템플릿은 그대로 복사해 쓴다. 채우는 데 5분 이상 걸리는 항목은 과감히 줄여도 되지만, "왜"를 적는 칸은 비우지 않는다.

---

## T1. 일일 학습 노트 — `notes/daily/YYYY-MM-DD.md`

```markdown
# 2026-MM-DD (Phase N, W주차, 요일)

## 오늘 목표 (아침에 작성)
- [ ] 
- [ ] 

## 아침 퀴즈 (AI 출제, 전날 범위)
- 점수: /5
- 틀린 문제와 이유:

## 배운 것 (핵심 3개 이내, 자기 말로)
1. 
2. 
3. 

## 몰랐던 것 / 헷갈리는 것
- 

## AI(또는 교재)가 틀린 것과 발견 방법
- 예: "AI가 ReceiveAsync가 항상 비동기 완료된다고 했으나 공식 문서상 false 반환 시 동기 완료" — learn.microsoft.com 확인

## AI 없는 1시간에 한 것
- 무엇을 재구현했고 결과(성공/부분/실패):
- 코딩 테스트 문제 수(해당 시):

## 시간 사용
- 계획 h / 실제 h / 차이와 이월 항목:

## 과제 진행
- 과제 번호: 
- 한 것 / 막힌 것 / 다음에 할 것:

## 내일 계획 (저녁에 작성)
- 
- 아침 퀴즈 범위:
```

---

## T2. 주간 회고 — `notes/weekly/W##.md`

```markdown
# W## 회고 (Phase N)

## 주간 점검 결과
| 항목 | 결과 |
|---|---|
| AI 없이 재구현 (N분) | 컴포넌트: / 통과 테스트: /N |
| 설명 시험 (주제 3개) | 평균 점수: / 가장 약한 주제: |
| 코드 리뷰 상위 3개 문제 | 1. 2. 3. (수정 커밋: ) |

## 잘된 것
- 

## 막힌 것과 원인
- 

## AI 사용에서 배운 것
- 잘 쓴 사례:
- 잘못 쓴 사례(AI에 너무 의존/검증 누락):
- 다음 주 AI 사용 규칙 조정:

## 시간 사용
- 계획 40h 중 실제 h. 미이행 항목과 보충 계획:

## 다음 주 조정
- 
```

---

## T3. 학습용 CLAUDE.md (C# 버전)

> 이 템플릿은 Claude Code 기준 예시다. Codex CLI 등 다른 코딩 에이전트를 쓴다면 해당 도구의 지침 파일(예: `AGENTS.md`)에 동일한 내용을 작성하면 된다.

```markdown
# 프로젝트: <이름> (학습용)

## 목적
이 프로젝트는 학습 과제다. 코드의 완성보다 학습자(나)가 모든 코드를 이해하는 것이 우선이다.

## 환경
- Windows 11, .NET 10 SDK, Visual Studio 2026 / VS Code
- 테스트: xUnit. 벤치마크: BenchmarkDotNet (Release만)
- Docker 사용 안 함. CI 사용 안 함(로컬 스크립트로 빌드·테스트). 기본 DB는 SQLite(파일 기반), 선택 시 MySQL 8(Repository 인터페이스 + DI로 구현체 교체). Redis는 redis-windows (https://github.com/redis-windows/redis-windows)를 로컬에서 실행

## 구조
- src/<Project>.Net : 네트워크 계층 (세션, 송수신)
- src/<Project>.Logic : 게임 로직 (룸, 유저, 매칭) — 소켓 참조 금지
- src/<Project>.Rules : 게임 규칙 — 외부 의존 없음
- tests/ : 단위·통합 테스트
- docs/ : DESIGN.md, PROTOCOL.md, adr/

## 코딩 규칙
1. nullable 활성화, 경고는 오류로 취급
2. async 메서드는 Async 접미사, async void 금지
3. 공유 상태는 소유자를 주석으로 명시. 룸 로직에는 lock 금지(Job 큐로)
4. 소켓 버퍼는 반드시 풀에서 빌리고 반환
5. 예외는 경계(핸들러 진입점)에서만 잡고 Serilog로 로그
6. 로그에 `traceId/requestId/service/instance`를 포함하고 토큰·비밀번호·시크릿은 기록하지 않는다
7. 포트와 serverId는 설정/인자로 분리하며 코드에 고정하지 않는다

## 에이전트 행동 규칙 (학습 목적)
- 코드를 생성할 때 각 클래스/주요 함수에 "왜 이렇게 설계했는지" 한 줄 주석을 단다
- 한 번에 파일 하나만 만든다. 다음 파일로 넘어가기 전에 멈춘다
- 내가 이전 작업에 대한 설명 없이 다음 작업을 지시하면, 이전 작업의 핵심을 1문장 질문으로 확인한 뒤 진행한다
- 새 NuGet 패키지를 추가할 때 선택 이유와 대안 1개를 함께 적는다
- 테스트 없이 기능 코드를 완료로 보고하지 않는다
- 내가 "결함 주입" 모드를 요청하면 정답 위치는 "정답 공개" 전까지 절대 말하지 않는다
- 내가 설계 문서를 첨부하면 문서와 다른 구조를 제안하기 전에 먼저 차이를 지적한다
```

## T3'. 학습용 CLAUDE.md (C++ 버전) — 차이 나는 부분만

```markdown
## 환경
- Windows 11, Visual Studio 2026 (MSVC, C++20/23), CMake 3.28+, vcpkg 매니페스트 모드
- 테스트: GoogleTest. 벤치마크: Google Benchmark (Release만). ASan은 Debug 구성에서 상시
- 네트워크: Winsock2 + IOCP 직접 구현 (Boost.Asio 사용 금지 — 비교용 별도 프로젝트 제외)

## 코딩 규칙
1. /W4 /permissive-, 경고는 오류로
2. new/delete 직접 호출 금지. unique_ptr 기본, shared_ptr은 소유권 공유가 필요한 경우만 주석과 함께
3. 모든 OVERLAPPED/WSABUF가 가리키는 버퍼의 수명은 완료 통지 이후까지 보장 (소유자 명시)
4. 룸 로직에 mutex 금지(Job 큐로). atomic 사용 시 memory_order 선택 근거 주석
5. UB 후보(초기화 안 된 읽기, 부호 오버플로, 댕글링)는 리뷰에서 최우선
6. 로그에 `traceId/requestId/service/instance`를 포함하고 시크릿을 기록하지 않는다. 포트와 serverId는 설정으로 분리한다

## 에이전트 행동 규칙 추가
- 새 vcpkg 의존성 추가 시 이유·대안·빌드 시간 영향을 적는다
- Win32 API를 쓸 때 실패 시 GetLastError/WSAGetLastError 처리를 빠뜨리지 않는다
```

---

## T4. 설계 문서 — `docs/DESIGN.md`

```markdown
# <서버 이름> 설계 문서

## 1. 목표와 비목표
- 목표:
- 비목표(이번에 하지 않는 것):

## 2. 제약
- 환경(Windows, 단일 프로세스/다중), 접속 수, 룸 수, 지연 목표, 사용 라이브러리 제한

## 3. 컴포넌트
(다이어그램: 네트워크·세션·로비·매칭·룸·규칙·타이머·로그·설정)
| 컴포넌트 | 책임 | 의존 |
|---|---|---|

## 4. 스레드 모델
(다이어그램: 어떤 스레드가 무엇을 실행하는가, 스레드 간 큐)
- 선택한 모델과 근거 (ADR 링크)
- 각 스레드의 역할, 개수, 생성/종료 시점

## 5. 데이터 소유
| 데이터 | 소유 컴포넌트 | 변경 가능 스레드 | 읽기 허용 | 비고 |
|---|---|---|---|---|

## 6. 상태 머신
- 세션 / 룸 / 매칭 상태 전이표 (상태, 이벤트, 다음 상태, 동작, 불법 전이 처리)

## 7. 패킷 목록
(PROTOCOL.md 링크 + 이 서버가 처리하는 패킷 표: ID, 방향, 허용 상태, 핸들러)

## 8. 주요 시퀀스
- 시퀀스 1: 
- 시퀀스 2: 
- 시퀀스 3: 

## 9. 리스크와 대응
| 리스크 | 발생 조건 | 대응 |
|---|---|---|

## 10. 관측 계획
| 신호 | 이름/필드 | 목적 | 알람/보존 |
|---|---|---|---|

## 11. 구현 후 달라진 점 (구현 완료 시 작성)
| 항목 | 설계 | 실제 | 이유 |
|---|---|---|---|
```

---

## T5. ADR — `docs/adr/NNNN-<제목>.md`

```markdown
# ADR-NNNN: <결정 제목>

- 날짜: 
- 상태: 제안 / 채택 / 폐기 / 대체됨(ADR-XXXX)

## 맥락
어떤 문제·제약 때문에 결정이 필요한가.

## 결정
무엇을 하기로 했는가 (한 문단).

## 검토한 대안
| 대안 | 장점 | 단점 | 기각 이유 |
|---|---|---|---|

## 결과
- 이 결정으로 쉬워지는 것:
- 어려워지는 것 / 감수하는 리스크:
- 되돌리려면 무엇을 바꿔야 하는가:

## AI 리뷰 기록
- AI가 지적한 취약점과 내 판단(수용/반박):
```

---

## T6. 부하 테스트·성능 개선 리포트 — `PERF-REPORT.md`

```markdown
# 성능 리포트: <대상 서버> (<날짜>)

## 1. 환경
- 서버 머신(CPU/RAM/OS), 클라이언트 머신, 동일 PC 여부·코어 배분, 네트워크(루프백/LAN), 전원 모드·백그라운드 통제, DB 백엔드, 빌드 구성(Release+PDB), 버전/커밋

## 2. 시나리오
- 도구: (5-1 더미 클라이언트) / 시나리오명 / 접속 수 / 램프업 / 유지 시간 / 반복 횟수

## 3. 기준선
| 지표 | 1회 | 2회 | 3회 | 중앙값 | 편차 |
|---|---|---|---|---|---|
| TPS | | | | | |
| p50 / p99 (ms) | | | | | |
| 에러율 | | | | | |
| CPU / 메모리 | | | | | |

## 4. 가설과 실험
| # | 가설 | 확인 방법 | 결과 | 채택/기각 |
|---|---|---|---|---|
| 1 | | | | |
| 2 | | | | |
| 3 | | | | |
> 가설 3개와 **기각 1개 이상**이 필수다.

## 5. 프로파일러 결과
- 도구/수집 시간/심볼 경로/핫스택 또는 할당 상위 5개/해석:

## 6. 적용한 변경
- 변경 1: 무엇을 / 왜 / 커밋
- 변경 2:

## 7. 최종 전/후
| 지표 | 전 | 후 | 변화 |
|---|---|---|---|

## 8. 부작용·비용
- 메모리/CPU/코드 복잡도 변화:

## 9. 용량 추정
- 접속당 CPU·메모리 / 한 대의 안전한 최대 접속 수와 여유율:

## 10. 남은 병목과 다음 후보
- 
```

---

## T7. 런북 — `RUNBOOK.md`

```markdown
# 런북: <서비스 이름>

## 서비스 구성
| 프로세스 | 포트 | PID 파일 | 로그 경로 | 헬스 체크 |
|---|---|---|---|---|

## 공통 명령
- 상태 확인: `Get-Process -Id (Get-Content <pid-file>)`; 포트: `Get-NetTCPConnection -LocalPort <포트>`
- 전체 시작/중지: `.\scripts\start-all.ps1` / `.\scripts\stop-all.ps1`
- Redis: `redis-cli ping`
- 최근 로그: `Get-Content <경로> -Tail 200`
- 대시보드: <URL>, 알람 목록: <URL>

## 장애 1: <프로세스 다운 / 부하 급증 / DB 잠금 / Redis 중단 / 네트워크 차단>
- 마지막 검증 일자: / 주입 스크립트:
- 감지: 알람 <이름> / 증상 (로그인 실패 급증, 대시보드 패널 X)
- 진단 (5분 내):
  1. PID·프로세스·포트 상태
  2. API 서버 로그에서 `DbTimeout` 카운트
  3. 대시보드 DB 지연 패널
- 대응:
  1. 해당 실행 파일 또는 `.\scripts\start-all.ps1`로 재기동
  2. 서킷 브레이커 상태 확인 (자동 half-open 대기)
- 복구 확인: 로그인 성공률 정상, 알람 해제
- 사후: 포스트모템 작성, 재발 방지 항목

## 장애 2: ...

## 연락/에스컬레이션 (혼자 학습 중이면 "미래의 나에게 남기는 메모")

> MySQL 선택자는 진단용으로만 `Get-Service MySQL80`을 사용할 수 있다.
```

---

## T8. 포스트모템 — `docs/postmortem/YYYY-MM-DD-<제목>.md`

```markdown
# 포스트모템: <제목>

- 발생: / 감지: / 복구: / 영향 시간:
- 영향: (접속 수, 실패 요청 수)

## 타임라인
| 시각 | 일어난 일 | 조치 |
|---|---|---|

## 근본 원인
## 왜 감지가 늦었/빨랐는가
## 잘된 것 / 안 된 것
## 개선 항목 (담당·기한)

## 재현
- 주입 스크립트 링크 / 실행 인자 / 기대 관찰:
```

---

## T9. AI 면접관 프롬프트 (Phase 말 설명 시험)

```
너는 온라인 게임 서버 개발자 신입 채용의 기술 면접관이다. 30분 동안 다음 주제 범위에서 심층 질문을 한다.
범위: <Phase N 설명 시험 예시 질문 목록 붙여넣기>
규칙:
- 한 번에 한 질문. 내 답변에 대해 꼬리 질문을 최대 3개까지 이어간다.
- 답이 모호하면 "구체적으로 코드/수치로 설명해 달라"고 요구한다.
- 내가 모른다고 하면 힌트 없이 다음 질문으로 넘어간다.
- 끝나면 질문별로 5점 척도(5: 원리·근거·대안·한계 모두 정확 / 4: 원리·근거 정확, 대안 또는 한계 약함 / 3: 개념은 맞으나 근거 부족 / 2: 부분 오류 / 1: 설명 불가)로 채점하고 평균과 가장 약한 주제 2개를 알려 달라.
- 채점표 형식: | 질문 | 점수 | 근거 |
```

### T9-b. 60분 확장 면접

포트폴리오 20분, CS 25분, 언어 15분으로 진행한다. 위 5점 척도를 그대로 쓰고 면접 중에는 정답을 알려주지 않는다. 종료 후 `모른다고 답한 질문`, `근거가 약한 질문`, `다음 복습 순서`를 별도 표로 출력한다.

## T10. AI 출제자 프롬프트 (매일 아침 퀴즈)

```
어제 학습 범위: <노트의 "배운 것" 3개 + 교재 장>
이 범위로 5문제를 내라: 객관식 2(4지선다, 오답도 그럴듯하게), 단답 1, 서술 1("왜"를 묻는 것), 코드 결함 찾기 1(10줄 이내 코드에 결함 1개).
한 번에 다 보여주고, 내가 답을 보내면 채점·해설하라. 틀린 문제는 관련 교재 장 또는 공식 문서 키워드를 알려 달라.
```

## T11. AI 결함 주입 프롬프트

```
다음 요구사항/코드(첨부)를 기반으로 결함 5개를 숨긴 버전을 만들어 달라.
결함 종류(각 1개): <예: 경쟁 조건, 리소스 누수, 경계 조건 오류, 에러 처리 누락, 프로토콜 규칙 위반>
규칙:
- 결함은 컴파일은 되고 대부분의 경우 정상 동작하되 특정 조건에서만 드러나는 것으로.
- 정답(위치·종류)은 내가 "정답 공개"라고 말하기 전까지 절대 언급하지 말라. 힌트 요청 시 "종류"만.
- 내가 찾은 결함을 보고하면 맞았는지만 답하고, 틀렸으면 근거 없이 "아니다"라고만 하라.
```

## T12. AI "처음 보는 개발자" README 재현 테스트 프롬프트

```
너는 이 프로젝트를 오늘 처음 보는 개발자다. 첨부한 README만으로 로컬에서 실행하는 절차를 단계별로 서술하라.
실제로 명령을 실행하지 말고 문서만으로 판단한다. 다음을 지적하라:
1) 문서에 없어서 추측해야 하는 것
2) 순서가 이상하거나 빠진 단계
3) 10분 안에 끝나지 않을 것 같은 지점
```

---

## T13. `AI-USAGE.md`

```markdown
# AI 활용 기록
| 작업 | 내가 먼저 한 것 | AI에 요청한 것 | 검증 방법 | 채택/수정/기각 |
|---|---|---|---|---|
## 대표 사례 3개
## AI가 틀린 사례 2개와 공식 근거
## AI 없이 재구현한 핵심 컴포넌트
## 공개하면 안 되는 입력 점검(시크릿·개인정보·회사 코드)
```

## T14. `SECURITY-CHECK.md`

```markdown
# 보안 점검
- [ ] 토큰 절대/슬라이딩 만료, 회전, replay 거부
- [ ] 패킷/본문 userId를 신뢰하지 않고 세션에 바인딩
- [ ] SQL 100% 파라미터화
- [ ] 비밀번호 해시 비용·salt 포함 문자열 검증
- [ ] 저장소·로그·히스토리에 시크릿 없음
- [ ] TLS 적용 또는 로컬 한정 사유와 운영 전 전환 계획
```

## T15. 저장소 `README.md`

```markdown
# 프로젝트 이름
## 문제와 범위 / 비목표
## 아키텍처(ARCHITECTURE.md 링크)
## 요구 환경과 정확한 버전
## 10분 로컬 실행(빌드→설정→시작→검증→종료)
## 포트·프로세스·serverId 표
## 테스트와 성능 결과(커밋·환경 포함)
## 장애 주입/런북 / 보안 점검
## AI 활용 범위(AI-USAGE.md 링크)
## 알려진 한계 / 라이선스
```

## T16. `ARCHITECTURE.md` + 계약 문서

```markdown
# 아키텍처
## 컴포넌트·실행 구성도
## 요청/매칭/게임 결과 시퀀스
## 세션·매칭·룸 상태 머신
## 상태 배치표(DB/Redis/프로세스 메모리와 단일 진실)
## 장애 영향 범위와 부분 기능 저하
## 범위·마일스톤·ADR 색인

# PROTOCOL.md / API.md 최소 골격
버전 / 인증 / 메시지·엔드포인트 / 필드 타입·상한 / 오류 / 멱등성 / 타임아웃 / 예제
```

## T17. 측정 계획·메트릭·장애 주입

```markdown
# 측정 계획
## 질문·가설 / 통제 변수 / 환경·커밋 / 반복·워밍업 / 성공 기준
## 부하 모델(closed/open), think time, seed, 동일 PC 여부
## JSON 리포트 스키마
{ "schemaVersion":1, "scenario":"", "seed":1, "connections":0, "sent":0, "received":0, "errors":{}, "latencyMs":{"p50":0,"p95":0,"p99":0,"max":0} }
## 메트릭 설계
| 이름 | 종류 | 라벨(상한 포함) | 목적 | SLI/SLO | 알람 |
## 장애 주입
| 장애 | 기대 동작 | 주입 명령/스크립트 | 관찰 신호 | 복구 기준 |
## alerts.yml 최소 예시
groups:
- name: game-server
  rules:
  - alert: GameServerDown
    expr: up{job="game-server"} == 0
    for: 1m
```

## T18. 테스트·면접·발표 묶음

```markdown
# TEST-PLAN.md
| 수준(단위/통합/E2E) | 시나리오 | 자동 스크립트 | 성공 기준 | 결과 링크 |
# INTERVIEW-LOG.md
| 날짜 | 회사/역할 | 질문 | 내 답 | 점수 | 보강 |
# 목표 회사 분석
| 회사 | 공고 스택 | 필수 역량 | 내 증거 | 공백·보강 |
# 프로젝트 서술
문제 → 제약 → 선택과 기각 대안 → 수치 결과 → 배운 점
# 블로그/발표/데모
블로그: 문제-실험-수치-한계 / 발표 15장 / 예상 질문 10개 / 데모 4구간 대본·자막
```
