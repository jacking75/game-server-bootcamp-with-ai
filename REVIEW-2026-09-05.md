# 커리큘럼 문서 보강 점검 보고서 (2026-09-05)

> 처리 상태: **완료** — 2026-09-05에 작업 패키지 A~G를 한·영문 문서, README 언어 정책, 템플릿, 작업 로그에 반영했다. 각 Phase 끝의 `2026-09-05 보강 사항/Revisions` 절은 기존 내용과 충돌할 때 우선한다.

> 검토 기준: `main` 커밋 `48c3ba5` (docs-working → 루트 이동, 영문화, `_kr` 보존 직후)
> 대상: `README.md`(총론), `01`~`08` 한글 원본(`*_kr.md`) 8종, 영문본 8종, `working_log.md`
> 줄 번호 표기: `파일약칭:줄` — `01`=`01-phase1-language-tools_kr.md`, `02`=`02-phase2-network_kr.md`, `03`=`03-phase3-realtime-server_kr.md`, `04`=`04-phase4-data-api_kr.md`, `05`=`05-phase5-operations_kr.md`, `06`=`06-phase6-capstone_kr.md`, `07`=`07-books-guide_kr.md`, `08`=`08-templates_kr.md`, `R`=`README.md`. 영문본은 상단 링크 삽입으로 줄 번호가 +2 전후로 어긋난다.
> 등급: 🔴 즉시 수정(명세 오류·모순·학습자가 그대로 베끼는 결함) / 🟡 보강 권장(공백·현실성) / 🟢 선택·정리

검토 방법

1. 스크립트 정합성 검사(직접 수행): 시간·주차·일차 연속성, 실습 ID 정의↔배치, 과제·ADR·템플릿 번호, 총론 §참조, 교재 키워드 출현, 정책 용어(Wireshark·CI·Docker·배포 등) grep, 포트, 줄바꿈, 영문/한글 구조(제목 계층·표·코드 펜스·체크박스 수) 대조, 영문 용어 변형 집계.
2. 전문 검토(Phase 1–2 / 3–4 / 5–6·07·08 세 갈래): 신입 게임 서버 개발자 채용 관점의 주제 공백, 일 단위 계획 현실성, DoD 측정 가능성, 과제 명세 완결성, AI 협업 가이드, 질문 은행, 막힘 포인트, 선수 조건, 트랙 균형, 총론과의 모순.
3. 표본 대조: 전문 검토가 제기한 영향 큰 주장 14건(2-2 길이 필드, 1-2 C++ API, Channel DropWrite, `Cache=Shared`, `salt`, 로그인 p99, 배치 워커, 105/106일차 순서, AI 없는 1시간 누락 일자, 알람 경로, 5-6 시간, Win32 10장, T7 서비스 명령, 07 카드 모순)을 원문에서 직접 확인 — **전부 일치**. 나머지 항목도 줄 번호가 붙어 있으나 수정 전 해당 줄을 다시 확인할 것.

---

## 0. 요약 — 먼저 고칠 것

| # | 등급 | 위치 | 문제 | 조치 요약 |
|---|---|---|---|---|
| 1 | 🔴 | `02:820, 991, 1056, 1059, 1143` | 헤더 `TotalLength`가 uint16(최대 65,535)인데 테스트 11은 70000, 테스트 14는 "정확히 64KB(65,536)"를 요구 — 인코딩 불가능 | 최대 크기를 65,535로 통일, 테스트 11을 "`maxPacketSize=1024`에서 2000 → TooLarge", 테스트 14를 "정확히 65,535"로 |
| 2 | 🔴 | `03:551-554` vs `03:992` | `BoundedChannelFullMode.DropWrite` + `TryWrite`는 가득 차도 `true`를 반환 — 재구현 시험 API "초과 시 false"와 모순 | `FullMode = Wait` + `TryWrite` 또는 드롭 카운터 방식으로 통일, §6.3 "AI가 자주 틀리는 지점"에 등재 |
| 3 | 🔴 | `04:622`, `04:862`, `04:168` | SQLite `Cache=Shared` 권장(비권장·`SQLITE_LOCKED` 유발), `account.salt` 별도 컬럼(Argon2id/bcrypt 지침과 불일치), C# 해싱 패키지 미지정 | `Cache=Shared` 삭제(WAL+busy_timeout만), `salt` 컬럼 삭제, PBKDF2(내장) 또는 BCrypt.Net-Next 지정 |
| 4 | 🔴 | `04:429-440, 996` | 로그인 p99 100ms(동시 100) 목표가 "해시 비용 낮추면 보안 후퇴" 정책과 충돌(Argon2 1회 50~100ms) | 부하 형태 재정의("총 100·동시 10, p99 ≤ 해시 1회 실측×2+30ms") + `/user/data` p99 50ms 별도 목표 |
| 5 | 🔴 | `01:1265-1292` | 1-2 C++ API가 `unique_ptr<byte[]>` 값 반환이라 테스트 4 "이중 반환 감지"가 타입상 성립 불가 | 커스텀 deleter RAII 핸들(`unique_ptr<T, PoolDeleter>`)로 재설계하거나 테스트 4를 C# 한정으로 명시 |
| 6 | 🔴 | `01:456, 833, 1163, 1297`, `02:421, 535, 627`, `05:800` | ASan에 "누수 0"·레이스 검출을 기대 — MSVC ASan은 LeakSanitizer 미지원, 데이터 레이스 검출 불가 | "잡는 것/못 잡는 것" 표 + CRT 디버그 힙(`_CrtSetDbgFlag`) 실습, 통과 기준을 카운터 정합(대여=반환, 생성=소멸)으로 교체 |
| 7 | 🔴 | `06:942, 12, 27` / `06:205` vs `06:218` | 필수 구성 "배치 워커"의 역할 정의 0줄; 105일차 DoD "4종 프로세스 기동"인데 매칭 서버 프로젝트는 106일차에 생성 | 배치 워커를 "시즌 스냅샷 1잡, 🟡"로 정의, 104일차 `L6-C-05`에 골격 4종 생성 추가 |
| 8 | 🔴 | `05:246, 934, 62` | 알람 "발동 시 로그 또는 Windows 알림" — `alerts.yml`만으로는 Alertmanager 없이 어디로도 안 감. 채점 30점·8.5 판정의 근거 부재 | Alertmanager Windows 바이너리 또는 Grafana Alerting + 로컬 webhook(PowerShell `HttpListener`) 택1 지정, 산출물에 설정 파일 추가 |
| 9 | 🔴 | `01:761-851`, `04:630-661` | 🔴 필수 실습이 어느 일차에도 없음: `L-CPP-11`(ASan), `L4-CS-03/04`, `L4-CPP-03/04`(테스트 인프라). 그 상태로 59·66일차가 통합·동시성 테스트를 요구 | 01 14일차에 L-CPP-11·12 배치, 04 58일차 오후를 "테스트 인프라 데이"로 |
| 10 | 🔴 | `R:13, 23, 131, 158, 169, 180` | 총론 정합성: 존재하지 않는 `00-overview.md`, 실제와 다른 절 구성 설명, 주간 점검 3h/4h·재구현 60/90/120분·설명 시험 30/60분 불일치, §4 교재 장 번호 5건 불일치(Win32 10장 등) | §1 참조. README 표를 07 카드 기준으로 재생성, 시간 수치는 Phase 문서 기준으로 총론 갱신 |
| 11 | 🟡 | `R:104, 106` vs 03·04·05·06 | "과제마다 CLAUDE.md"와 "매일 AI 없는 1시간(재구현)"이 후반 Phase에서 사라짐: CLAUDE.md 요구 0건(03~06), AI 없는 1시간이 03·04는 대부분 작문, 06은 121~124·126~128일차 블록 자체 없음 | 각 Phase 첫 프로젝트 착수일 DoD에 CLAUDE.md(T3/T3') 추가, AI 없는 1시간을 코드 재구현 항목으로 교체·삽입 |
| 12 | 🟡 | `02:287, 324, 929-940`, `02:687, 703` | 채팅 서버 스레드 모델(워커 N개의 Logic 동시 호출) 미명세; zero-byte recv·`WSAENOBUFS`·미해결 recv 1개·완료 순서·pinned 버퍼 미다룸 | §7.2에 "Logic 전역 락 1개, Send는 락 밖" 명시 + "대규모 접속 함정" 실습 90분 신설 |
| 13 | 🟡 | `03:282, 828, 865` / `03:227, 862` vs 04 | 재접속이 닉네임만으로 허용(세션 탈취) / Phase 4에서 3-1 로그인을 토큰으로 교체한다는 약속의 실습 없음 | `reconnectToken` 발급·검증 + 치트 테스트 6번; 69일차 `L4-C-24b` 토큰 검증 핸드오프 60분 |
| 14 | 🟡 | `04:873, 904, 1094` / `04:871, 886, 376` | 멱등성 키 미들웨어를 만드는 실습 없음(시험에는 출제); 재화 원장 부재; Redis Lua 재고 차감 후 DB 실패 시 보상 없음; 출석 날짜 비교와 배치 리셋 모순 | `L4-C-20a 멱등성 필터` 90분, `currency_ledger` 테이블, DB 조건부 UPDATE 단일 진실 + 보상·불일치 검출 |
| 15 | 🟡 | `05:93-101, 445, 979` / `06:1075-1092, 1023` | 부하 측정 타당성(동일 PC 간섭·동적 포트/TIME_WAIT 고갈·closed 모델) 미언급; 면접 준비가 캡스톤 질문 100%(CS 은행·코딩 테스트 8문제·행동 면접·회사 조사 없음) | 76일차 40분 개념+포트 튜닝 단계; §8.4-b CS·언어 30문항, 코딩 테스트 32문제, 회사 조사 60분 |
| 16 | 🟡 | `08` 전체, `07:358, 366` | 템플릿 누락(측정 계획서·메트릭 설계서·SECURITY-CHECK·저장소 README·ARCHITECTURE·AI-USAGE·면접 기록), T6와 05 §7.3 두 벌 불일치, T7 런북이 서비스 명령 기준; 07 Phase 5 지정 장 ≈54h > 개념 학습 예산 50h | T13~T18 신설, T6 통합, T7 프로세스 기준으로 교체; 07에 Phase별 장 수·예상 시간·모드(통독/참조) 표 |
| 17 | 🟢 | 저장소 | 영문 README 부재, 언어 정책 미기재, 영문 용어 불일치(`Exercise`/`Lab` 등), `.gitattributes` 없음, `working_log.md` 미기재 | §1 참조 |

---

## 1. 저장소·문서 체계 (이번 이동·영문화 이후 남은 것)

### 1.1 README(총론)의 문서 구성 표와 구조 설명

| 위치 | 현재 | 문제 | 조치 |
|---|---|---|---|
| `R:13` | `00-overview.md (이 문서)` | 그런 파일이 없다. 실제 파일은 `README.md`. Phase 문서는 이 문서를 "총론 §x.y"(01에서 7회)로 지칭 | `README.md (이 문서, 총론)`로 바꾸거나 실제로 `00-overview.md`를 두고 README는 안내로 축소. 전자 권장 |
| `R:14-21` | `01-…md` ~ `08-…md`만 나열 | `_kr.md` 8종이 표에 없고, "영문이 기본·한글은 `_kr`"이라는 언어 정책 문장이 없다 | 표에 "한국어판" 열 추가 + 표 위에 정책 1문장 |
| `R:23` | "개요 → 주차별 계획 → 학습 항목 상세 → 교재 활용 → AI 협업 → 과제 명세 → 학습 완료 판정 → 흔한 막힘 포인트" | 실제 Phase 문서는 §0 문서 사용법 → §1 개요·자가 진단 → §2 **일 단위** 계획 → §3 **실습 카탈로그** → §4 학습 항목 → §5 교재 → §6 AI 협업 → §7 과제 → §8 완료 판정(**질문 은행** 포함) → §9 막힘 포인트 → §10 다음 Phase 준비 | 실제 구조로 갱신 |
| `R:3` | 버전 0.3 | 9/4 전면 상세화(3배 확장)와 9/5 영문화 이후에도 0.3 | 0.4로 올리고 변경 요약 1줄 |

### 1.2 영문 README 부재

GitHub 첫 화면인 `README.md`만 한글이고 나머지 8개 문서는 영문이 기본이 되어 저장소 언어가 뒤섞였다. 제안: `README.md`를 영문화하고 `README_kr.md`를 같은 규칙(선두에 상대 링크)으로 보존. 이때 §1.1의 표·구조 설명·수치 모순(§2.6)을 먼저 고친 뒤 번역해야 두 번 일하지 않는다.

### 1.3 작업 로그 미기재

`working_log.md`는 9/4 항목까지만 있다. 오늘 작업(docs-working → 루트 이동, `_kr` 보존, 8개 문서 영문화, 선두 링크) 항목이 없다. 기존 형식(`## 2026-09-05 HH:MM KST — 제목` + 불릿)으로 추가할 것. 이 보고서 자체의 존재도 로그에 남기는 편이 좋다.

### 1.4 줄바꿈 설정

작업 트리는 CRLF(`core.autocrlf=true`), 인덱스는 LF, `.gitattributes` 없음. 그래서 `git add` 때마다 "LF will be replaced by CRLF" 경고가 뜨고, 다른 OS에서 클론하면 diff가 통째로 난다. `.gitattributes`에 다음 두 줄을 두면 해결된다.

```
* text=auto
*.md text eol=lf
```

(01 §3의 L-C-02 `.editorconfig` 실습이 `end_of_line=crlf`(`01:544`)를 쓰므로 학습자 저장소 규칙과 이 저장소 규칙은 별개다. 이 저장소만의 결정이다.)

### 1.5 영문본 용어 불일치 (파일마다 번역자가 달랐던 흔적)

영문/한글 쌍은 제목 계층·표 행·코드 펜스·체크박스·구분선 수가 8쌍 모두 정확히 일치한다(구조 손실 없음). 남은 문제는 같은 한글 라벨이 파일마다 다른 영어로 옮겨진 것이다.

| 한글 라벨 | 01 | 02 | 03 | 04 | 05 | 06 | 권장 통일안 |
|---|---|---|---|---|---|---|---|
| §3 실습 카탈로그 | Exercise Catalog | Exercise Catalog | Lab Catalog | Lab Catalog | Lab Catalog | Exercise Catalog | **Lab Catalog** (ID가 `L-…`이므로) |
| §3.2/3.3 트랙 실습 | C# Track Exercises | C# Track Exercises | C# Track Labs | C# Track Labs | C# Track Labs | — | **Track Labs** |
| §5 교재 활용 | Textbook Usage Guide | Textbook Usage Guide | Book Usage Guide | Book Usage Guide | Book Usage Guide | Textbook Usage Guide | **Textbook Guide** |
| §4 학습 항목 상세 | Learning Items in Detail | Learning Items in Detail | Learning Item Details | Detailed Learning Items | Detailed Learning Items | Learning Items in Detail | **Learning Items in Detail** |
| §2 주차별 상세 계획 | (by Day) | (by day) | (Day by Day) | (Day by Day) | (Day by Day) | (by day) | **(Day by Day)** |
| §6 AI 협업 가이드 | (Phase 1-specific) | (Phase 2-specific) | (Phase 3 Specific) | (Phase 4 Specific) | (Phase 5 Specific) | (Phase 6 Specific) | **(Phase N)** |
| 확인 기준 | Pass criterion (39) | Pass criterion (33) | Acceptance criteria (22) | Acceptance criteria (38) | Acceptance criteria (43) | Acceptance criteria (29) | **Acceptance criteria** |
| 기대 결과 | Expected Result | Expected | — | — | — | — | **Expected result** |
| 노트에 남길 것 / 노트 | What to note | Note | Note | Note | — | — | **Note** |
| 자주 나는 오류 | Common mistake | Common mistake | Common mistake | Common mistake | Common mistake | Common error | **Common mistake** |
| §8.3 설명 시험 | Oral Exam | Explanation Exam | Explanation Exam | Oral Exam | Oral Exam | Explanation-Test | **Oral exam** |
| §5.4/7.5 부 트랙 읽기 | Cross-Track Reading | Cross-Track Reading | Secondary Track Reading | Supplementary Track Reading | Supplementary Track Reading | — | **Secondary-track reading** |
| §8 학습 완료 판정 | (Friday, Day 15) | (Friday, Day 35) | (Day 55, Friday) | (Friday, Day 75) | (Friday, Day 100) | (Day 130, Final Course Assessment) | **(Day N, Friday)** |
| §10 다음 Phase 준비 | Preparing to Move on to | Preparing to Move on to | Preparation Before Moving to | Preparing Before Moving to | Preparing Before Moving to | After Finishing the Course | **Preparing for Phase N+1** |
| 총론 지칭 | Overview §x | — | — | — | — | — | **the master document (README.md) §x** — README가 영문화되면 파일명으로 |

권장 작업: 통일안을 `08-templates.md` 말미 또는 별도 `GLOSSARY.md`에 KR↔EN 용어표로 두고, 이후 재번역·수정 시 참조하게 한다.

### 1.6 영문본 세부 결함 (07·08 전수 대조 결과)

- `07-books-guide.md` 106·228행 "Building an API Game Server with ASP.NET Core Web API"(원제 "…API 게임 서버 **실습**")와 107·234행 "Building a Game Server with ASP.NET Core Web API"(원제 "…로 만드는 게임 서버")가 "an API" 유무로만 구분된다. §5(359·360·367행)의 약칭 "the API Game Server Lab"의 "Lab"이 어느 카드 제목에도 없다 → 카드 제목에 "Hands-On Lab"을 넣어 약칭과 맞출 것.
- `07` 341행 "Behavior Tree AI for Online Game Servers"는 원제 "…비헤이버트리 AI **시스템 구축**" 축약. 378행 폴더 접두어 "온라인게임-" 누락(정규화로 볼 수 있음).
- `07` 23·77·231행 "gacha-collection RPG" — 원문 "수집형 RPG"에 없는 "gacha"가 추가됨.
- `08-templates.md` 131행 "sign overflow" → "signed integer overflow". 92행 `redis-windows(https://…)` 괄호 앞 공백 없음(07은 있음). 71행 "Actual h out of planned 40h" → "Actual __h of planned 40h"가 채우기 칸임을 드러냄.
- `07` 영문본에 남은 한글 34줄은 전부 외부 저장소(`programming-books-with-ai`)의 실제 폴더·파일 경로와 `## 12장` 같은 인용이다(정상, 번역 대상 아님).

### 1.7 한글 원본 수정 시 영문본 동기화 절차

이 보고서의 수정 대부분은 한글 원본(`_kr`)에서 이뤄질 것이다. 영문본을 다시 통째로 번역하면 §1.5의 불일치가 재발한다. 권장: (1) 한글 수정을 커밋 단위로 모으고, (2) `git diff` 범위만 영문본에 반영하며, (3) §1.5 용어표를 번역 지시에 첨부한다. 영문본 상단 링크(`> 🇰🇷 Korean version: …`)는 유지.

---

## 2. 문서 간 정합성 (스크립트 검증 결과)

### 2.1 시간·주차·일차 — 🟢 이상 없음

- README §0.3의 Phase별 시간·공통/트랙·주차가 6개 Phase 문서 1·3행과 전부 일치(120/40/80, 160/70/90, 160/90/70, 160/100/60, 200/140/60, 240/130/110).
- 일차 번호는 1~130 연속: 01=1~15, 02=16~35, 03=36~55, 04=56~75, 05=76~100, 06=101~130. 결번·중복 없음.
- 금요일 오후 4h 주간 점검 양식은 전 Phase가 README §5.1과 같은 4단계(재구현·설명 시험·코드 리뷰·회고)다.

### 2.2 실습 ID 정의↔배치 — 🔴 미배치 9개

정의(§3 `#### L…` 헤딩)와 참조는 6개 문서 모두 1:1로 맞는다(정의 38/32/24/38/43/29, 참조되지 않은 정의 없음). 그러나 **§2 일 단위 계획에서 한 번도 호출되지 않는 실습**이 있다.

| 문서 | 실습 | 분 | 등급 | 영향 |
|---|---|---|---|---|
| 01 | `L-CS-10` ThreadPool 기아 재현 (`01:761`) | 60 | — | 14일차(`01:471`)가 같은 내용을 프로스로만 언급 |
| 01 | `L-CS-11` Task vs ValueTask (`01:766`) | 40 | 🟡 | §4.2 학습 순서(`01:913`)에는 있음 |
| 01 | `L-CS-12` CancellationToken 전파 (`01:771`) | 40 | — | |
| 01 | `L-CPP-11` ASan으로 use-after-free 잡기 (`01:846`) | 45 | 🔴 | 1-1(B)의 "ASan 통과"(`01:1163`)의 전제 |
| 01 | `L-CPP-12` 이동 의미론 복사 카운터 (`01:851`) | 45 | — | |
| 04 | `L4-CS-03` WebApplicationFactory 통합 테스트 (`04:630`) | 75 | 🔴 | 59일차 통합 테스트 6개(`04:175`) 요구 |
| 04 | `L4-CS-04` 동시성 테스트 작성법 (`04:634`) | 45 | 🔴 | 66일차 동시 100회 테스트(`04:308`) 요구 |
| 04 | `L4-CPP-03` HTTP 서버와 라우팅 (`04:657`) | 120 | 🔴 | 56일차 `L4-C-01` "45분" 뒤에 숨은 120분 |
| 04 | `L4-CPP-04` 통합 테스트 (`04:661`) | 90 | 🔴 | |

조치: 01은 14일차 트랙 블록에 L-CPP-11·12(90분)를 넣고 대신 `L-CS-08`(180분, 함정 12종)을 5종으로 축소. 04는 58일차 오후를 "테스트 인프라 데이(L4-CS-03/04 또는 L4-CPP-03/04)"로 바꾸고 `L4-CS-01/CPP-01`을 57일차 오후로 당긴다(§4.2 참조).

### 2.3 과제·ADR 번호 결번

- Phase 5 과제 번호가 `5-1, 5-2, 5-4, 5-5, 5-6` — **5-3 결번**(배포 과제 삭제 흔적, `working_log.md` 9/4 기록). 재번호 매기거나 "5-3(삭제됨: 배포)"를 각주로.
- ADR 파일 번호: `0001`(02), `0002`(03 스레드 모델; 03이 요구하는 나머지 2개 "룸-세션 참조·매칭 방식"은 번호 없음), `0005`(04), `0010~0015`(06). **0003·0004·0006~0009 공백**, 06의 0013·0014는 결정일 없이 119일차 소급 작성(`06:443`, 원칙 "ADR은 결정 즉시"와 충돌). 총론 또는 08에 "과정 전체 ADR 번호표"(번호·주제·Phase·결정 일차)를 두면 해결.

### 2.4 템플릿 참조

- 08 정의: T1~T12(+T3'). 참조됨: T1, T2, T3, T3', T4, T5, T7, T8, T11. **미참조: T6(성능 리포트), T9(면접관 프롬프트), T10(퀴즈 프롬프트), T12(README 재현 테스트)**.
- Phase 4·6 문서는 템플릿을 한 번도 참조하지 않는다. 05 §7.3(`05:977-996`)과 06 §6(`06:911-932`)은 08과 별도의 인라인 템플릿을 갖고 있고 T6와 내용이 다르다(§6.2).
- README 규칙 5 "과제마다 CLAUDE.md"(`R:106`)를 요구하는 DoD가 03·04·05·06에 없다(01만 L-C-06으로 요구).
- 06:870~875는 T12와 동일 문장 중복, 06:878~884는 T9 유사본이나 5점 척도 정의가 빠져 있다.

### 2.5 교재 장 매핑 불일치 (README §4 ↔ Phase 문서 ↔ 07 카드 ↔ 07 §5 읽기 순서)

README §4의 모든 책은 07에 카드가 있다(누락 없음). 문제는 장 번호다.

| 책 (Phase) | README §4 | Phase 문서 | 07 카드 | 07 §5 | 판정·조치 |
|---|---|---|---|---|---|
| 최신 Win32 API (P5 C++) | 7·**10**장 (`R:158`) | "10장(서비스)은 다루지 않음" (`05:831`) | 7장·부록 C, "10장 읽지 않아도" (`07:259, 261`) | 7·**10**·부록 C (`07:366`) | README·07 §5가 틀림. 카드 기준으로 10장 삭제 |
| ASP.NET Core Web API로 만드는 게임 서버 (P5) | 없음 | 14·16장, 17장 제외 (`05:814`) | '언제' "14·16·**17**장" (`07:233`) vs '어떻게' "17장 건너뛴다" (`07:235`) | 14·16·17 (`07:358`) | 07 카드 자체 모순. 17장(배포) 제거 |
| TCP/IP Windows 소켓 (P5) | 12·13장 | 12·13·14장 (`05:834`) | 12·13장 (`07:163`) | 12·13·14 | 14장 카드에 추가 |
| fluentd (P5) | 1~7·13장 | 1·2·3·4·5·13장 (`05:811`) | 1~7·13 (`07:327`) | 1~7·13 | 6·7장 의도 확정(읽기 예산상 1·2 필수, 나머지 참조 권장) |
| C# async/await 라이브러리 만들기 (P5) | 19·20장 | **14**·19·20장 (`05:823`, 14장이 5-4 "직접 교재") | 14·19·20 (`07:195`) | ✓ | README에 14장 추가 |
| C# Async/Await 완전 정복 (P5) | 부록 B | 5장·부록 B (`05:822`) | ✓ | ✓ | README에 5장 추가 |
| 〃 (P3) | 없음 (`R:156`) | 10장 (`03:704`) | | | README에 추가 |
| 가챠 확률 가이드 (P4) | 9~12장 | 9·10·11·12·**16**장 (`04:758`) | | | README에 16장 추가 |
| 네트워크 학습 로드맵 (P4) | 없음 (`R:157`) | Ch.6 인증 절 (`04:395`) | | | README에 추가 |
| C# Socket 프로그래밍 (P2) | 1~10장 (`R:155`) | 1~7·9·10장, 8장은 P5 (`02:725`) | | | README "1~7·9·10" |
| TCP/IP Windows 소켓 (P2) | 1~13장 | 1~6·8~11·13장 (`02:734`) | | | 11장·13장이 README에서는 P3·P5(`R:156, 158`)에도 배정 — 이중 배정 정리 |
| Boost.Asio (P2 선택) | 1~9장 | 1~2·4~7장 (`02:736`) | | | README "1~7" |
| Boost.Asio (P3 선택) | 11~14·17장 | 11·12·13·17장 (`03:714`) | | | README에서 14 삭제 |
| Boost.Asio (P6 선택) | 17·20장 | 17·20·**21**장 (`06:840`) | 17·20·21 (`07:289`) | ✓ | README에 21장 추가 |
| 모던 Windows 멀티스레딩 (P1) | 3~5·8장 | 2·3·4·5·8장 (`01:960`) | | | README에 2장 추가 |
| 모던 Windows 멀티스레딩 (P3) | 6·11장 | 6·11·12장 🟡 (`03:711`) | | | README에 12장(선택) |
| 프로메테우스 (P6) | 없음 | 06 §5에 없음 | "Phase 5만" (`07:321`) | 없음 | 07 매트릭스 `07:121`의 "P6 ○"만 근거 없음 → 삭제 |
| 네트워크 이론·네트워크부터 이해하기 (P6 복습) | 없음 | 124~128일차 재독 (`06:820`) | '언제'에 P6 없음 (`07:143`) | ✓ | 카드 '언제' 갱신 |
| Rust처럼 안전한 Modern C++ (P6) | 없음 | 126일차=26주 (`06:843`) | "25주" (`07:295`) | | 주차 통일 |
| C# 디자인 패턴 (P5) | 없음 | 3·16장 정규 항목 (`05:824`) | 매트릭스 △ (`07:102`) | | ○로 올리거나 05에서 내림 |
| 타깃 프레임워크 | .NET 10 (`R:76`) | net10.0 (`05:811`) | "net10.0으로 올려 빌드" (`07:23`) vs ".NET 8 코드는 9로 올려" (`07:330`), "SDK 없으면 net9.0으로 낮추고" (`07:192`) | | 07 카드 두 곳을 net10.0 기준으로 |

### 2.6 총론(README) ↔ Phase 문서 수치·규칙 모순

| 항목 | README | Phase 문서 | 조치 |
|---|---|---|---|
| 주간 점검 시간 | 4h (`R:131, 168`) | 01 주간 배분표 "3h" (`01:118`) — 같은 문서의 5·10일차는 4h | 01:118을 4h로 |
| 주간 재구현 시간 | 60분 (`R:169, 179`) | 02·03·04·05·06 대부분 90분(`02:271, 364`, `03:267, 356`, `04:380`, `06:289, 378, 463`), 06 8.2는 120분(`06:605`) | 과제 난이도상 90분이 현실적 → README를 "60~120분(Phase별 지정)"으로 |
| 설명 시험 길이 | 30분 (`R:180`) | 60분(`01:495`, `02:452`, `03`·`04`·`05`·`06` 동일), 01 8.3 "30~60분"(`01:1364`) | README 60분으로 |
| AI 없는 1시간 = 매일 빈 파일 재구현 | `R:104` | 02 16·17·19·24·27·29·31~34일차, 03 36·39·44·47·49·51·52·54일차, 04 56~74일차 **16일 전부**가 작문·메모 과제. 06은 121·122·123·124·126·127·128일차에 블록 자체 없음(120·125 금요일 재구현, 129 코딩 테스트만 예외) | §5 각 Phase E 항목의 교체안 적용 |
| CLAUDE.md 과제마다 작성 | `R:106` | 03·04·05·06 요구 0건 | 41일차(3-1 착수), 58일차(4-1), 77일차(5-1), 104일차(캡스톤) DoD에 "CLAUDE.md(T3/T3') 작성·커밋 30분" |
| 게임 루프 = Phase 3 공통 주제 | `R:141` | 03은 "오목은 이벤트 기반으로 충분"(`03:133`), 틱 루프는 🟡 3-3b(`03:959`)에만 | 38일차 "고정 틱 루프 60분" 🔴 실습 추가(§5.3) |
| 미달 시 다음 Phase 1주차 보강 8h | `R:184` | 02·03·04·05·06 첫 주가 전부 🔴 풀데이(예: `03:36~40일차`, `04:56~60`, `05:76·77`) — 8h가 들어갈 자리 없음 | 각 Phase 1주차에 반일 버퍼 1개 확보(§4.3) |
| Phase 5 C++ "ASan" | `R:143` | 05는 "새니타이저 상시"(`05:800`) 한 구절, 실습 없음 | L5-CPP에 CRT 디버그 힙·핸들 카운터 실습 45분 |
| Phase 6 시간 | 240h (`R:65`) | §7 합 160+24+32+16+16 = **248h**, 126~129일차에 6-3·6-4·6-5 중첩(`06:1003, 1012, 1019`) | §4.1 |
| 스크립트 기준 PowerShell | `R:77` | `grep -r "lock\|mutex"` (`03:444, 980`) — 대소문자 구분이라 `SRWLOCK`·`Monitor.Enter`도 놓침 | `rg -i` 또는 `Select-String -CaseSensitive:$false` 패턴으로 |
| SuperSocketLite 선택 | "(선택)" (`R:140`) | 02는 필수 실습 `L2-CS-05`(`02:437, 599`) | 어느 쪽이든 통일 |
| Phase 2 교재 통독 시점 | — | 01 §10 "Phase 2 4일차(19일차)에 통독"(`01:1445`) vs 02 "16~17일차"(`02:95, 717`) | 02 기준으로 01 수정 |
| 동시 접속 목표 | "수백~천 단위" (`R:33`) | 5-1 제출물 "1,000접속" 필수(`05:961`) vs 5-2 기준선 300, 1,000은 🟡(`05:969`) | 5-1 제출 조건을 500으로 |
| 포트폴리오 저장소 5개 | `R:206` | 06 6-2 = Phase 2~6 저장소(`06:993`) ✓. 단 05는 `phase5/` 단일 폴더(`05:57`)라 LoadTool 분리 여부 미정 | 05에 "부하 도구는 별도 저장소로" 명시 |
| 혼합 경로 첫 주 통독 20h | `R:215`, `03:1066` | 04 12주차(56~60일차)에 자리 없음(`04:108-199`) | §4.4 대체 일정표 |

### 2.7 정책 용어 — 🟢 위반 없음

Wireshark·tcpdump·VS 2022·net9/.NET 9·net8·Memurai·WSL·ZLogger·GitHub Actions·CI/CD·Docker·Kubernetes·Windows 서비스·배포·롤백·Nginx·IIS를 README·한글 8종·영문 8종 전체에서 검색한 결과, 모든 출현이 "이 과정은 쓰지 않는다"는 정책 서술이거나 교재 내용 설명(예: `05:814` "17장(배포)은 다루지 않음", `07:23` 책의 집필 시점 설명)이다. 실제 지시·요구·채점에 남은 위반은 없다. 예외 하나: 08 T7 런북의 공통 명령이 `Get-Service`/`Restart-Service` 기준이다(§3.9).

### 2.8 포트·경로

| 문서 | 등장 포트 | 용도 |
|---|---|---|
| 01 | 9000 (`01:1448` "9000~9100 방화벽") | Phase 2 준비 |
| 02 | 9000 (`02:100, 474, 575, 1077`) | 채팅 서버 |
| 03 | 5000·5001·5050 | 게임 서버·봇 |
| 04 | (API 포트 미지정) | — |
| 05 | 9101 (`05:702`) | 메트릭 |
| 06 | 8080 API·8090 매칭·9001/9002 게임 (`06:189-192`) | 다중 서버 |

과정 전체의 포트 배정표가 없어 Phase마다 다른 번호를 쓴다. 총론 §1 환경표에 "서버·포트·메트릭 포트" 표를 두고 Phase 문서가 참조하게 할 것. 참고: 작성자의 전역 개발 규칙(개발용 TCP 25001~25199, UDP 27001~27199)과 문서 예시 포트가 다르다 — 공개 커리큘럼이면 무방하나, 맞출지 결정이 필요하다.

폴더 규약은 `notes/daily/`, `notes/weekly/`, `docs/adr/`, `scripts/` 등이 01 L-C-01(`01:518-537`)과 08 템플릿 경로에서 일치한다.

---

## 3. 명세·참조 코드 오류 (🔴 즉시 수정)

학습자가 그대로 베끼거나 통과 기준으로 삼는 것들이라 가장 먼저 고쳐야 한다.

### 3.1 Phase 2 과제 2-2 — 길이 필드와 테스트의 모순 (`02:820, 828, 991, 1056, 1059, 1143`)

- 헤더 `TotalLength: uint16, 최대 65,535`(`02:820`)인데 "최대 패킷 크기 64KB"(`02:828, 1143`)는 65,536, `maxPacketSize = 65535`(`02:991`)는 65,535 — 1바이트 어긋남.
- 테스트 11 "TotalLength=70000 → TooLarge"(`02:1056`)는 uint16에 담을 수 없고, 테스트 14 "정확히 64KB → Ok"(`02:1059`)도 인코딩 불가.
- 수정: 최대 크기를 65,535로 통일(또는 길이 필드를 uint32로). 테스트 11 → "`maxPacketSize=1024` 설정에서 TotalLength=2000 → TooLarge", 테스트 14 → "정확히 65,535 → Ok". §8.2 재구현 시험(`02:1143`)도 동일.
- 같은 과제의 추가 구멍: `PooledBuffer`·`IPacketBody`(`02:1010`) 타입 미정의, `PacketView`/`span` 수명·`Feed` 복사 여부 미정, C++ `PacketReader`에 풀 인자 없음(`02:1026`), 파서 처리량 벤치 없음(할당만), 퍼징 테스트 없음(§5.2).

### 3.2 Phase 1 과제 1-2 — C++ API와 테스트의 모순 (`01:1265-1292`)

- `std::unique_ptr<std::byte[]> Rent(); void Return(std::unique_ptr<std::byte[]>)`는 소유권을 호출자에게 넘기므로 테스트 4 "이중 반환 감지"가 타입상 성립하지 않는다(두 번째 Return은 null 또는 컴파일 오류). 테스트 7 "소멸 시 미반환 경고"는 대여−반환 카운터로 가능하지만 방법이 명시돼 있지 않다.
- 수정: 커스텀 deleter가 자동 반환하는 RAII 핸들(`std::unique_ptr<T, PoolDeleter>`)로 재설계하고 "명시 Return은 없음, 이중 반환은 C# 전용 테스트"로 하거나, 테스트 4를 C# 한정으로 표기. 테스트 7 "경고 로그"는 로거가 없으므로 `Action<string>`/콜백 주입으로 캡처.
- 이 API는 Phase 2에서 IOCP 수신 버퍼로 쓰인다(`02:939`). OVERLAPPED가 완료까지 버퍼를 보유하는 모델과 "값 반환" API가 맞지 않으므로 Phase 2 요구(수신 버퍼 크기·슬라이스·워커 스레드 호출 빈도)를 1-2 명세에 미리 적을 것.

### 3.3 Phase 3 C# 골격 — `DropWrite` + `TryWrite` (`03:551-554` vs `03:992`)

`Channel.CreateBounded(... FullMode = BoundedChannelFullMode.DropWrite)`에서 `TryWrite`는 가득 차도 항목을 조용히 버리고 `true`를 반환한다. 재구현 시험 API `Room.Post(job) -> bool // 큐 상한 초과 시 false`(`03:992`)와 정면으로 어긋난다. 수정: `FullMode = Wait` + `TryWrite`(→ false) 또는 DropWrite + 드롭 카운터 중 하나로 통일하고, 이 차이를 §6.3 "AI가 자주 틀리는 지점"에 등재.

### 3.4 Phase 4 참조 코드·테이블 (`04:622, 862, 168, 644-649`)

- `Cache=Shared`(`04:622`): SQLite 공유 캐시 모드는 공식 문서가 권장하지 않으며 WAL과 함께 쓰면 테이블 단위 락으로 `SQLITE_LOCKED`가 나서 `busy_timeout`이 듣지 않는다. WAL + `busy_timeout` + 커넥션 분리만으로 충분.
- `account.salt` 컬럼(`04:862`): PBKDF2식. Argon2id/bcrypt는 해시 문자열에 솔트가 포함되므로 해싱 지침(`04:168`)과 불일치. 삭제.
- C# 해싱 패키지 미지정(`04:168, 502`): .NET에 Argon2 내장 없음. `Rfc2898DeriveBytes.Pbkdf2`(내장) 또는 `BCrypt.Net-Next`/`Konscious.Security.Cryptography` 중 하나 지정(C++는 libsodium으로 지정돼 있어 비대칭).
- C++ 골격(`04:644-649`)이 단일 `SqliteConnection`인데 cpp-httplib는 요청마다 스레드 → 동시성 테스트에서 크래시. "스레드별 커넥션(thread_local) 또는 뮤텍스 풀, `SQLITE_OPEN_FULLMUTEX` 여부"를 명시.
- SQLite는 raw `Microsoft.Data.Sqlite`, MySQL은 SqlKata/Dapper(`04:39, 627`)로 스타일이 달라 4-3c "DI 등록만 교체"(`04:1058`)가 어렵다. Dapper를 양쪽 공통으로.

### 3.5 Phase 4 로그인 p99 목표 (`04:429-440, 996`)

"동시 100요청 p99 100ms"와 "해싱 파라미터를 낮추면 보안 후퇴"(`04:434`)가 충돌한다. Argon2id/bcrypt 권장 비용은 해시 1회 50~100ms이고 동시 100이면 8코어에서도 뒤쪽 요청은 수백 ms다. DoD의 "(또는 미달 시 원인 분석)"(`04:440`) 예외로 사실상 목표가 무의미해진다. 수정: 부하 형태를 "총 100요청·동시 10"으로 정의하고 목표를 "p99 ≤ 해시 1회 실측 × 2 + 30ms(실측치 병기)"로, 로그인 외 API(`/user/data`) p99 50ms(동시 100)를 별도 목표로. 73일차 오전에 "해시 1회 비용 실측(파라미터 3단계)" 30분 추가, `PERF-4-1.md` 4절에 해시 비용 표 필수.

### 3.6 MSVC ASan에 대한 과잉 기대 (`01:456, 833, 1163, 1297`, `02:421, 535, 627`, `05:800`)

- "ASan으로 누수 0 확인"·"ASan 통과 = 레이스 없음"을 요구하지만 MSVC AddressSanitizer는 LeakSanitizer를 지원하지 않고(VS 2022 공식 문서 기준, VS 2026 재확인 필요), 데이터 레이스는 어떤 MSVC 새니타이저도 잡지 못한다(TSan 미지원).
- 수정: 01 L-CPP-11에 "ASan이 잡는 것/못 잡는 것" 표와 CRT 디버그 힙(`_CrtSetDbgFlag(_CRTDBG_LEAK_CHECK_DF)`)·VS 메모리 스냅숏 실습 15분 추가. "누수 0"은 "풀 대여/반환·세션 생성/소멸 카운터 일치"로, "레이스 없음"은 "1,000만 건 왕복 유실·중복 0을 10회 반복"으로 교체. 05 C++ 트랙에 핸들/메모리 누수 추적 실습 45분.

### 3.7 Phase 6 캡스톤 구성 (`06:942, 12, 27`, `06:205` vs `06:218`, `06:357`)

- "배치 워커 1개"가 필수 구성으로 3회 반복되지만 역할 정의가 0줄(단서는 `06:362` 시즌 스냅샷, `06:365` 배치 집계뿐). 범위 팽창 방지 원칙(`06:11`)을 문서 스스로 어긴다.
- 105일차 DoD "`start-all.ps1`로 4종 프로세스 기동"(`06:205`)인데 매칭 서버 프로젝트는 106일차 `L6-C-07`(`06:218`)에서 생성되고 배치 워커는 생성일이 없다 — 존재하지 않는 프로세스를 띄우는 DoD.
- 콘텐츠 (a)안 "관전+룸 채팅+시즌 랭킹+전적" 4종(`06:357`)에 실습은 전적(`06:688`)·시즌(`06:692`) 2종뿐. 114일차 하루에 4종.
- 5-1 부하 도구를 매칭 프로토콜·티켓·재매칭으로 확장하는 실습이 없는데 106~109일차와 데모(`06:477`)가 그 클라이언트를 전제.
- 수정: 배치 워커 = "시즌 종료 스냅샷 1잡, 🟡"로 정의하고 104일차 `L6-C-05`에 빈 골격 4종 생성(30분) 추가; 요구사항 5(a)를 "전적+시즌 랭킹 필수, 관전·룸 채팅 🟡"로; 108일차 오전을 `L6-C-10b 5-1 도구 매칭 흐름 확장(120분)`으로 바꾸고 L6-C-11~13을 하루씩 밀기; "500접속·100룸" 대체(`06:419`)를 §7.1 품질표의 정식 경로로.

### 3.8 Phase 5 알람 발송 경로 (`05:246, 934, 62`)

"발동 시 로그 또는 Windows 알림"이라 쓰였지만 `alerts.yml`은 Prometheus 룰 파일이고 Alertmanager 없이는 `/alerts` 상태만 바뀐다. 5-C 채점 30점 "실제 발동 증거"(`05:945`)와 8.5 "알람이 발동하지 않음"(`05:1137`) 판정이 불가능하고 Phase 6 알람 3개(`06:426`)도 같다. 수정: 84일차 오전 30분 — "Prometheus 룰 → Alertmanager(Windows 바이너리)" 또는 "Grafana Alerting → contact point = 로컬 webhook(PowerShell `HttpListener` 20줄, 수신 시 파일 로그+토스트)" 중 택1 지정. `L5-C-13` 확인 기준에 "webhook 수신 로그 캡처", §1.4 산출물에 `alertmanager.yml` 또는 contact point 설정 추가, 08에 `alerts.yml` 룰 예시 1개(`for: 1m` 포함). 07 프로메테우스 카드(`07:320-324`)에 "10장 알림 경로가 Docker/Linux 전제면 Grafana Alerting으로 대체" 주의.

### 3.9 08 T7 런북 — Windows 서비스 전제 (`08:266-290`)

공통 명령이 `Get-Service`/`Start-Service`/`Restart-Service MySQL80`(`08:268, 275, 279`)인데 서버는 서비스 등록 없는 프로세스(`06:12, 942`)다. "실행 계정"(`08:264`)·"롤백 절차"(`08:286`)는 배포 없는 과정에 무의미. 수정: `Get-Process`/`Get-NetTCPConnection -LocalPort`/`.\scripts\start-all.ps1`/`redis-cli ping` 기준으로 교체, 05:1030·06:429의 장애 5종 헤딩과 "검증 일자/주입 스크립트" 열 추가, 실행 계정·롤백 행 삭제(MySQL 선택자용 서비스 명령은 각주로).

### 3.10 07 카드 내부 모순 (`07:233` vs `235`, `07:366`, `07:330`, `07:192`)

§2.5 표 참조. 카드 '언제'와 '어떻게'가 서로 다른 장을 말하는 것(WebAPI 17장), 카드와 §5 읽기 순서가 다른 것(Win32 10장), 타깃 프레임워크 문장이 net10 원칙과 다른 것(`07:330, 192`) 세 가지.

### 3.11 Phase 3 §7.2 명세 구멍

| 위치 | 구멍 | 조치 |
|---|---|---|
| `03:263, 864` vs `03:282-283, 865` | 턴 30초 자동 패와 끊김 60초 유예가 동시에 걸릴 때 우선순위 없음(내 턴에 끊기면 30초 후 패 → 유예 무의미) | "끊김 중 턴 타이머 정지(권장), 유예 종료 시 패" 규칙 + 재접속 테스트 7번 |
| `03:152` vs `03:836, 838` | 세션 상태 머신에 `Spectating`이 없는데 패킷 표는 `Spectating` 허용 상태를 씀 | 상태 추가, 전이표(`03:158`)에 `Lobby→Spectating→Lobby` |
| `03:908` vs `03:215, 249, 287, 303` | 로직 테스트 "25개 = 전이 10+불법 5+치트 5+재접속 5"인데 일정에는 매칭 8+불법 5+치트 5+재접속 6+관전 5 = 29개, 전이 10개 목록은 어디에도 없음 | §7.2에 테스트 표 확정(예: 41개) |
| `03:817-835` | 모든 `*Res.result:uint16`의 값 정의 없음 | `PlaceResult` enum(`03:485`)↔네트워크 코드 매핑 표 |
| `03:139` vs `03:812-839` | 하트비트 검사 5초를 쓰지만 5000번대 표에 하트비트 패킷이 없음 | `Heartbeat/HeartbeatAck` 추가 또는 "2-C 1xxx 재사용" 명시 |
| `03:822, 824, 245` | `turnSeq`가 있으나 "불일치 → stale 거부" 검증이 44일차 목록에 없음 | 7번째 검증으로 명시 |
| `03:827, 904, 946` | `boardHash` 알고리즘 미지정 | "225바이트+다음 턴+turnSeq의 FNV-1a 64"처럼 고정 |
| `03:250, 868` vs `03:249` | "누적 3회 위반 종료"에서 초당 50회 폭주가 1회인지 50회인지 미정 | 규칙 명시 |
| `03:227, 862` | 닉네임 로그인·로비 패킷 "2-C 재사용"만, 중복 닉네임 정책(기존 세션 강제 종료 vs 거부) 없음 → 재접속(`03:287`)과 뒤섞임 | 정책 명시 |
| `03:922-930`, `03:847-855` | 3-1·3-C 통과 점수 없음(3-2·3-3만 제출물 있음) | "70/100, 구조 항목 만점 필수" 같은 게이트 |
| `03:879-885` | 비기능 조건에 CPU 코어·RAM 기준 없음 → p99 50ms가 4코어 노트북+봇 300에서 달성 가능한지 판단 불가 | "8코어 권장, 미만이면 50룸으로 축소" 스케일 규칙 |

### 3.12 Phase 2 2-C·2-1·2-3 명세 구멍

- 2-C(`02:812-897`): 문자열 인코딩·길이 접두 형식, 배열 카운트 크기, `ChatNotify.seq` 범위(방별/전역), 귓속말 대상 오프라인 에러 코드, `RateLimited` 누적 시 종료 여부, `DisconnectNotify` 송신 완료 후 close 보장 방법 없음. `Reserved` "정렬용"(`02:824`)은 바디가 정렬되지 않으므로 근거가 틀림 → "체크섬/확장용".
- 2-1(`02:899-979`): 스레드 모델 부재(§5.2 A-1), `COMPARE-2-1.md` 템플릿 없음, 5초 주기 카운터 로그(`02:416`)가 요구·채점에 없음, 최대 접속 도달 시 동작·중복 닉네임 시 기존 세션 처리·"유실 0"의 정의·실행 방법(`--config`, 샘플 설정) 없음.
- 2-3(`02:1071-1107`): 클라이언트 I/O 모델 미명시(500접속을 스레드-퍼-커넥션으로 만들면 클라이언트가 먼저 죽음), `storm`의 정상/강제 종료 비율 옵션(`02:1093`)이 인자 표(`02:1081-1089`)에 없음, 메시지 크기 인자 없음, 출력 JSON 스키마 미정, `garbage` 시나리오(`02:1094`)가 어느 일차에도 배치되지 않음.

### 3.13 Phase 1 1-1 명세 구멍 (`01:1082-1235`)

`LogQueueStats`·`PoolStats` 타입 미정의(`01:1117, 1260`); `sealed record LogEntry`(참조형)에 `Enqueue(in LogEntry)`(`01:1102, 1115`)는 무의미(`record struct`로 또는 `in` 제거); `DisposeAsync`↔`StopAsync` 멱등·순서 미정; Block 정책 대기 중 Stop 시 반환값 미정(`01:1149, 1233`); 파일 인코딩·개행·`\t`/`\n` 이스케이프 규칙 없음(테스트가 라인 수를 셈); `FlushIntervalMs`(`01:1109`)가 (B) Channel 버전에서 무엇인지 미정; C++ (B) Block 정책 대기 방식 미정(스핀 100% CPU 예상 → `_mm_pause`/`yield`/`atomic::wait` 요구); **p50/p99 측정 방법 부재**(`01:1189, 1202` — BenchmarkDotNet/Google Benchmark는 분포를 주지 않음 → `Stopwatch.GetTimestamp()` 샘플 배열 정렬 하네스 명시); "소비자가 그냥 버리는 모드"(`01:1190`)가 API에 없음(→ `ILogSink` 주입 요구); 성능 참고 기준치 없음(예: C# lock 버전 ≥ 300k msg/s, C++ SPSC ≥ 5M msg/s); `bench/run.ps1`(Release 강제·결과 저장) 요구 없음.

### 3.14 Phase 4 기타 명세

- `POST /account/token/refresh`(`04:898, 949`)의 의미 불명(세션은 슬라이딩 갱신 `04:207, 515`) → "절대 만료 24h + 슬라이딩 1h, refresh는 토큰 회전(구 토큰 즉시 폐기)"로 정의하거나 삭제.
- 로그인 시 `session:{token}`·`user_session:{userId}` 두 키 갱신(`04:208, 511`)을 Lua/MULTI로 묶으라는 지시 없음 → 동시 로그인 2건이면 유령 세션.
- 출석: `last_check_date` 비교(`04:571, 869`)인데 배치 워커에 "출석 리셋(자정)"(`04:376, 958`) → 리셋 불필요·자정 경계 레이스. 리셋 삭제, 대신 원장 정합 검사.
- `mail.status` 값 목록 미정, `idempotency_key`에 `status`·`request_hash` 없음, `notice` 테이블 없음(엔드포인트 18 `/notice`는 있음 `04:910`).
- 4-2 리플레이 시나리오(`04:1049`): 멱등성 키를 그대로 재전송하면 "이전 결과 반환"이 정상이라 공격이 성립하지 않음 → "키 없이 재전송" 또는 "키 재사용+바디 변조".
- 재구현 시험(`04:1086-1088`): "빈 프로젝트(DB 포함)" 90분에 프로젝트 생성·마이그레이션·DI·트랜잭션·멱등성·동시성 테스트는 C++(httplib+sqlite3+gtest)에서 불가능에 가까움 → 골격 제공 + 120분. 시험 요청 `{userId, …}`는 4-2가 금지한 "본문 userId 신뢰"(`04:1046`) → 주석 필요.
- L4-C-24(`04:582-584`) 로컬 큐의 영속성·최대 보관 수·타임아웃 없음. 룸 Job 안에서 HTTP 호출 금지(Phase 3 액터 원칙) → "아웃박스 큐 + 별도 송신 스레드" 명시.
- 채점 통과선 없음: 4-C(`04:942`)·4-1(`04:1020-1028`)·4-2(`04:1052`). "정합성·멱등성 25점 만점 필수" 같은 게이트.
- 테스트 목록이 영역별 개수만(`04:1003`) → 30개 전체 표와 에러 코드 매핑.
- 73일차 "동시 100요청"(`04:429`)을 무엇으로 쏘는지 미정 → "3-2 클라이언트 HTTP 시나리오 또는 `HttpClient` 병렬 스크립트, 정식 도구는 Phase 5".

### 3.15 Phase 5 기타 명세

- 5-1 제출물 "1,000접속"(`05:961`) vs 5-2 기준선 300·1,000은 🟡(`05:969`); think-time·시드·JSON 스키마·CLI 인자 표·정상 disconnect 요구 없음(서버 종료 사유 카운터 `05:928` 오염).
- 5-2(`05:965-1005`): 환경 항목(`05:979`)에 "부하 도구 동일 PC 여부·코어 배분·DB 백엔드" 없음. SQLite 단일 파일 쓰기 직렬화가 API TPS 상한이므로 "API TPS 30% 개선"(`05:972`)이 DB 선택에 따라 불가능할 수 있음을 명시.
- 5-4(`05:1007-1042`): SQLite 30초 잠금(`05:1021`)을 어떤 도구로 거는지 미정; 방화벽 규칙은 관리자 PowerShell 필요(미명시); 런북 5종 중 "프로세스 다운·부하 급증"(`05:1030`)은 주입 실험 없음 → `Stop-Process` 주입+재기동 스크립트를 Part B에; 결과 큐 내구성(메모리/파일, `05:390, 665`) 결정 ADR 요구.
- 5-5(`05:1044-1049`): C#에서 "메모리 손상" 크래시는 `unsafe`/P-Invoke 없이 재현 어려움 → C# 대체 시나리오(`FailFast`, `Marshal` AccessViolation) 명시.
- 5-6(`05:1051-1064`): "28h, 94~99일차"인데 실배정 ≈ 8h(§4.1); "테스트 3개"(`05:1055`) vs 재구현 시험 4개(`05:1098`).
- 측정 기준 충돌: "계측 오버헤드 1% 이내"(`05:228`) vs "3회 편차 10% 이내"(`05:96`) — 분산 10%로 1% 차이는 검출 불가 → "편차 범위 내(유의미한 열화 없음)". 히스토그램 버킷 목록이 `05:188`(…1000ms)·`05:700`(…1.0)·`05:925`(…500ms) 세 군데 다름.
- 5-C(`05:913-945`): `instance`/`server_id` 라벨 요구 없음(06:1168은 Phase 6에서 필요하다고 함) → 지금 표(`05:920`)에; 로그 항목 7(`05:935`)에 스키마·보존 없음; `alerts.yml` 룰 예시 없음.

---

## 4. 일정 현실성 (시간 회계)

### 4.1 과제 시간 표기 vs 실제 일자 배정

README §2.4 루틴(개념 2h·구현 2.5h·AI 없는 1h·리뷰 40분)에서 하루 구현 가용은 약 3.5h, 금요일 오전 3h를 더해 Phase당 구현 슬롯은 약 65~70h다. 그런데 §7 과제 시간 표기는 그 1.5~2배이고, 일자 블록을 실제로 세면 표기의 1/3~1/5인 과제도 있다.

| Phase | 과제 (표기) | 일자 배정 실측 | 근거 |
|---|---|---|---|
| 1 | 1-1 24h (8~12일차) | ≈ 10.5h (8일차 1h+9일차 1.5h+10일차 3h+12일차 5h; 11일차는 벤치 방법론 전용) | `01:1082, 1216-1218` |
| 1 | 1-2 16h | ≈ 7.5h (5일차 2.5h + 13일차 5h) | `01:1237, 450-465` |
| 1 | 주간 배분 "과제 14h×3 = 42h" vs 1-C 8+1-1 24+1-2 16 = 48h | — | `01:118` |
| 2 | 2-2 24h (22~23일차) | ≤ 10h (이틀 실습 시간 전부) | `02:981, 224-241` |
| 2 | 2-3 16h (31·33일차) | ≈ 3.5h (31일차 2.5h + 33일차 L2-C-21 60분); `garbage` 시나리오 미배치 | `02:1071, 375-392` |
| 2 | 합계: 과제 96h(+🟡 8h) + 실습 ≈35h + 개념 ≈40h + 점검 16h + AI 없는 1h 20h ≈ **207h > 160h** | | `R:61` |
| 3 | 3-C 16 + 3-1 80 + 3-2 16 = **112h** vs 가용 ≈ 68h; 3-2는 49일차 하루 | | `03:787, 856, 939, 332-347` |
| 4 | 4-C 16 + 4-1 88 + 4-2 12 = **116h** vs 가용 ≈ 68h; 혼합 경로 20h 통독 자리 없음 | | `04:853, 944, 1037, 742` |
| 5 | 5-6 28h (94~99일차) | ≈ 8h (94 오후 2.5+95 오전 3+99 오후 2.5) | `05:1051` |
| 5 | 5-4 48h (91~98일차) | ≈ 30h | `05:1007` |
| 5 | 5-1 24h (77~80일차) | ≈ 12h (79일차는 트랙 진단) | `05:947` |
| 5 | 트랙 60h | 일자에 존재하는 트랙 전용 시간 **7.5h**(79일차 5h + 88일차 오후) — 나머지 ~50h 소재 불명 | `05:784-800` |
| 6 | §7 합 160+24+32+16+16 = **248h > 240h**; 126~129일차에 6-3·6-4·6-5 중첩(4일에 64h 상당) | | `06:1003, 1012, 1019` |

조치: 각 §7 과제의 시간을 §2 배정 합으로 재산정하거나(예: 3-1 ≈ 55h, 4-1 ≈ 60h), "AI 없는 1h·리뷰 포함" 산식을 명시한다. 어느 쪽이든 "그대로 따라 하면 끝난다"(`01:7`)는 전제가 지켜지게.

### 4.2 과부하 일자 (실습 분 합계 vs 블록 시간, 근거 포함)

오후 블록은 2.5h = 150분, 트랙 데이는 5h = 300분이 상한이다. 아래는 카탈로그의 실습 분을 일자 블록 호출 기준으로 합산해 확인한 것이다(양 트랙이 모두 적힌 날은 한 트랙 기준).

| Phase | 일차 | 배정 | 초과 | 조치안 |
|---|---|---|---|---|
| 1 | 14 (C#) | L-CS-10 60 + L-CS-08 180 + 🟡 L-CS-09 60 = 300분, 개념 0 (`01:469-473`). L-CS-08의 함정 1번(데드락)은 `SynchronizationContext` 있는 환경이 필요해 콘솔에서 재현 불가 | 5h 전부 | L-CS-08을 12개→5개, 나머지는 교재 읽기+설명 |
| 1 | 9 | 개념 5항목 + L-CS-05 45 + L-CS-06 90 + 교재 1~4장 통독 + 테스트 5~9 작성 (`01:366-390`) | 개념 시간 소진 | 교재 통독을 8일차 저녁·주말로 |
| 1 | 13 | 오전 2.5h에 1-2 완성+테스트 8개, 오후 3자 벤치+`dotnet-counters`+REPORT (`01:450-465`) | 16h 과제를 7.5h에 | 5일차 설계 시간 확대 |
| 1 | 10 오전 | 3h에 두 버전 기능 완성 + 테스트 10~15(30초 장시간 포함) (`01:394`) | | 12일차로 분산 |
| 1 | 4 | L-C-07 60 + 트랙 2개(≈80) ≈ 140~165분 (`01:242-246`) | 소폭 | — |
| 2 | 23 | 오후 2.5h에 L2-C-11 60 + PacketWriter + **패킷 20종 정의(C++ 수동 직렬화)** + ADR (`02:224-241`) | C++ 6h+ | 2-2를 22~24일차 3일로, C++에 BinaryWriter/Reader 헬퍼 또는 FlatBuffers 기본 |
| 2 | 25 | 오전 3h에 L2-CS-04 150 / **L2-CPP-04 180** + 측정 + COMPARE (`02:263-268`); DoD "2-2 테스트 15개"는 2-2 시간이 없는 상태 | C++ 오전 초과 | L2-CPP-05를 27일차로 |
| 2 | 26 (C++) | L2-C-12 60 + L2-C-13 90 + L2-CPP-05 120 = 270분/150 (`02:291-293`) | +120 | 위와 동일 |
| 2 | 29 | L2-C-17 60 + 18 60 + 19 45 = 165/150 | +15 | — |
| 2 | 30 오전 | 3h에 프레이밍+세션+송신 큐+로직+설정 파일(C++ toml++ 미교육) 통합 (`02:357-371`) | | 설정 로딩 30분을 별도 배정 |
| 2 | 31 | L2-C-20 120 + `chat` 시나리오 + 지연 측정 + 100접속 5분 + p99 테스트 (`02:375-392`) | | 2-3에 33일차 오전 전부 |
| 3 | 45 오전 | 3h에 착수→판정→통지→턴 교대→승패→결과→룸 파괴+타임아웃 자동 패+기권+재매칭 (`03:259-274`) — 서버 핵심 흐름 전부 | 8~10h짜리 | 44일차 오후 후반으로 분할 |
| 3 | 43 | L3-C-08 90 + L3-C-09 120 = 210/150 + 불법 전이 테스트 5개 (`03:224-239`) | +60 | |
| 3 | 46 | L3-C-11 90 + L3-C-12 120 = 210/150 + 테스트 6개 (`03:278-294`) | +60 | |
| 3 | 40 오전 | DESIGN.md 10항목(다이어그램 3종) + ADR 3개 + AI 리뷰 반영 (`03:169-184`) | 5~6h | |
| 3 | 39 | L3-C-04 60 + L3-C-05 60 + 시퀀스 다이어그램 3개 ≈ 180/150 (`03:149-167`) | +30 | 다이어그램 2개로 |
| 3 | 37 (C++) | L3-CPP-01 180분을 150분 슬롯에 (`03:120, 576`) | +30 | "워커 배정"을 38일차로 |
| 3 | 48 | L3-CS/CPP-02 + L3-C-15 + L3-C-16 ≈ 180~225/150 (`03:321-323`) | +30~75 | |
| 3 | 55 오전 | 120+45+45 = 210분이 "3h"로 표기 (`03:438-441`) | +30 | |
| 4 | 59 | L4-C-05 90 + L4-C-06 60 = 150 + 통합 테스트 6개(인프라 미구축) + 해싱 라이브러리 선정 (`04:165-182`) | | 58일차를 테스트 인프라 데이로 |
| 4 | 66 | L4-C-18 90 + L4-C-19 60 = 150 + "10회 반복 안정" + L4-CS-04(미배정) (`04:296-315`) — 이 Phase의 핵심 평가 항목이 하루 | | |
| 4 | 64 | L4-C-14 60 + L4-C-15 75 + 통합 테스트 8개 ≈ 200/150 (`04:260-276`) | +50 | |
| 4 | 62 | L4-C-09 60 + 10 60 + 11 45 = 165/150, 첫 Redis 클라이언트(C++는 redis-plus-plus vcpkg 빌드 포함) (`04:222-239`) | +15+ | L4-C-10 캐시를 🟡 |
| 4 | 58 (C++) | L4-C-04 60 + L4-CPP-01 120 + 마이그레이션 러너 = 180+/150 (`04:146-164`) | +30+ | |
| 4 | 70 오전 | L4-C-26 75 + DoD "4-1 기능 10종 모두 동작"(밀린 것 전부) (`04:373-387`) | | |
| 4 | 74 | 오전 통합 30개 채우기+픽스처, 오후 4-3c MySQL 전환(§7.4 8h, L4-CPP-02 150분, MySQL 설치 시간 없음) (`04:443-458`) | | 4-3c를 Phase 5 여유 시간으로 |
| 4 | 75 오전 | 90+60+45 = 195분이 "3h" (`04:462-465`) | +15 | |
| 5 | 83 | L5-C-10 120 + L5-C-11 90 = 210/150 + 계측 오버헤드 on/off 비교 (`05:220-235`) — 게임 서버 8종·API 6종 계측을 하루에 | +60+ | L5-C-11을 84일차 오전으로 |
| 5 | 85 오전 | 3h에 롤링 구현 + 🟡 fluent-package + L5-C-14 45 + **fluentd 6개 장 + API 23장 읽기**(장당 90분이면 10h) (`05:258-262, 811`) | 3배 | fluentd 1·2장 필수, 나머지 참조 모드 |
| 5 | 77 | L5-C-03 120 + L5-C-04 90 = 210/150, full 시나리오(API 로그인→토큰→게임 접속)를 처음 만드는 날 (`05:110-125`) | +60 | |
| 5 | 95 오전 | L5-C-25 75 + 방어 4종 구현 + 재측정 + 경계표 — 방어 4종이 105분 (`05:444-447`); 포트 튜닝(§5.5 A3)까지 겹치면 하루 이상 | | "미인증 타임아웃·IP 제한 2종 필수, 나머지 🟡" |
| 5 | 84 | L5-C-12 120 + L5-C-13 60 + 알람 실제 발동 캡처(지속 1분·평가 주기 대기) (`05:237-254`) | +45 | L5-C-12를 84 오후·85 오전으로 분산 |
| 5 | 82 | L5-C-09 90 + L5-CS/CPP-04 90 = 180/150 (`05:202-218`) | +30 | |
| 5 | 79 (C++) | 75+120+60 = 255/300, WPA 첫 사용 학습 곡선 0분 | 소폭 | |
| 6 | 105 오전 | DoD "4종 기동"(§3.7) | 불가 | |
| 6 | 108 | L6-C-10 60 + L6-C-11 120 = 180/150 + traceId를 PacketLib·HTTP 헤더·로그 포맷 3곳에 + 클라이언트 확장 (`06:246-263`) | 실제 2일 | |
| 6 | 114 | 오후 150분에 콘텐츠 2종, DoD는 4종 (`06:354-369`) | | 관전·채팅 🟡 |
| 6 | 118 | L6-C-21 90분에 장애 5종 실제 주입+런북 대응(복구 대기만 5분×5종) (`06:422-437`) | 18분/종 불가 | 117~118 이틀로 |
| 6 | 123 | 오전 2.5h에 블로그 2편(각 1,500자+코드+수치), 오후 발표 15장 120분 (`06:501-513`) | 75분/편 | 블로그 1편 반나절 |
| 6 | 106 | L6-C-07 120 + L6-C-08 75 = 195/150 (`06:210-226`) | +45 | |
| 6 | 121 | 데모 대본·촬영·편집 90 + 저장소 5개 README 표준화 90(18분/저장소) (`06:474-486`) | | |

### 4.3 완충일·보강 슬롯 부재

- Phase 1: 15일차 오후 3h "미완 항목 보충"(`01:500`)이 유일. `01:25` "주말로 미뤄도"는 주 5일 과정(`R:5`)과 어긋남.
- Phase 2: 35일차 오후 3h뿐. 500접속·10분 실행이 32·34일차에 각 2회 이상이라 실행 대기만 1h+.
- Phase 3: 0일. 54일차 오후 라이브러리 분석(`03:426-427`)을 부 트랙 읽기로 옮기면 반일 확보.
- Phase 4: 74일차 오후 🟡가 유일.
- Phase 5: 🟡 오전은 99일차(`05:517`) 하나. 금요일 오전 3h도 전부 신규 내용(`05:166, 258, 351, 444`).
- Phase 6: 전 일자 🔴. 원칙 3 "24주부터 매일 1시간 문서"(`06:13`)가 116~118일차 계획(`06:389-437`)에 슬롯으로 없음.
- README:184 "다음 Phase 1주차 보강 8h"와 각 Phase의 "첫 이틀 보강"(`03:44`, `04:57`, `05:44`)이 첫 주 🔴 풀데이와 충돌.

제안: Phase마다 반일 버퍼 2개(1주차 금요일 오전 + 마지막 주 목요일 오후)를 비우고, 🟡 강등 목록(L-CS-09, L2-CPP-02, L3-C-13 관전, L4-C-10·29, L5 fluent-package, L6 관전·채팅·배치 워커)을 명시한다.

### 4.4 캡스톤 6주 달성 가능성 판정

구현 4주(101~120일차)의 실제 구현 가용 시간은 16일×5h + 4일×3h ≈ 92h(AI 없는 1h 제외), `06:108` "가용 30% 적게 가정"을 적용하면 65h. 요구 범위 = 매칭 서버 신규 + 티켓 + 하트비트 + 2대 라우팅 + traceId + 결과 큐 + 서버 다운 2종 + 콘텐츠 4종 + 배치 워커(미정의) + 클라이언트 확장(미정의) + 1,000접속 튜닝 + 대시보드/알람 3 + 런북 5 + 문서 6종. **판정: 현 범위는 불가.** §3.7의 (a) 콘텐츠 2종 필수, (b) 배치 워커 1잡 🟡, (c) 500접속 정식 경로 세 가지를 적용하면 가능. 면접 준비 2주(121~130)는 6-2 24 + 6-3 32 + 6-4 16 + 6-5 16 = 88h를 ~60h에 넣는 구조이고 CS 복습이 0h라 **불충분**(§5.6).

혼합 경로(C++ 트랙이 Phase 4를 C#으로) 12주차 대체 일정표가 없다(`04:740-744`, `R:215`). §4.4에 "56~57일차 통독(전일), 58~60일차 L4-C-01~06 압축, 4-C 완성은 62일차" 같은 표를 둘 것. (b) 학습자는 C++ 3-1에 HTTP 클라이언트(cpp-httplib client/WinHTTP)를 붙여야 하는데(`04:744`) 시간 배정이 없다.

### 4.5 교재 읽기 예산

07의 자기 기준(장당 1~2h `07:28`, 90분 `07:38`)으로 Phase 5 C# 지정 장(`07:358`)을 세면 ≈ 36장 ≈ 54h인데 Phase 5 개념 학습 시간은 25일×2h = 50h(`R:123`) → "교재 → 공식 문서 순" 대조 시간이 0. Phase 6 C#(`07:359`)도 2D MMORPG 코드 분석(2~4h×5주차분) + 네트워크 2권 복습(4,800줄) + 매칭 4장 ≈ 30h가 캡스톤 구현 주에 얹힌다. 07에 "Phase별 지정 장 수·예상 시간·개념 학습 예산" 표 1개와 §5 항목별 모드 기호(통독/참조/코드 분석, `07:26-30`)를 넣고 초과분을 참조 모드로 강등할 것. 07 §5 목록은 일자 순으로 정렬(예: P5 C# 가챠 15장이 81일차인데 목록 끝).

---

## 5. Phase별 콘텐츠 보강

각 Phase는 A 주제 공백 / C DoD 측정성 / D·E 명세·AI 협업 / F 질문 은행 / G 막힘 포인트 / H 선수 조건 / I 트랙 균형 순. D(명세)는 §3에 모았다.

### 5.1 Phase 1 — 언어·도구 심화

**A. 주제 공백**

| # | 공백 | 근거 | 추가안 |
|---|---|---|---|
| 1 | 로깅 기초·실전 로거 코드 읽기 전무 — 1-1이 "로그 큐"인데 Serilog/spdlog 미언급. `spdlog::async_logger`(`async_overflow_policy::block/overrun_oldest`), `Serilog.Sinks.Async(blockWhenFull)`에 같은 구조가 있음 | `01:1082-1236`, `R:82, 141` | 12일차 오전 "spdlog/Serilog 오버플로 정책과 내 Drop/Block 비교표" 60분, §5.1에 OSS 소스 지정, `ILogSink` 주입을 API 요구로 |
| 2 | 에러 처리 정책 없음 — 워커/소비자 예외 시 프로세스 사망(C++ `std::terminate`, C# `UnobservedTaskException`), 파일 쓰기 실패(디스크 풀) 정책·테스트 없음. R:192 루브릭 4번을 검증할 테스트 없음 | `01:1148, 1182` | 6일차 오전 "에러 처리 정책 2h" + 1-1 테스트 16 "싱크 예외(페이크) 시 큐 생존+통계 반영" |
| 3 | 메모리 레이아웃 — false sharing은 C++만(L-CPP-09). §4.1이 "L-CS-03(캐시 경합)"이라 하나 L-CS-03은 카운터 3방식 비교. `sizeof`/패딩, `[StructLayout]`, AoS vs SoA 실습 없음 | `01:885, 709-712` | C# false sharing 벤치(`[FieldOffset]`/패딩) 45분 |
| 4 | C++ 할당·누수 프로파일링 — ASan 누수 기대(§3.6). C#은 VS 진단 도구 메모리 스냅숏 없음 | `01:456, 833` | CRT 디버그 힙·VS 스냅숏 15분 |
| 5 | 디버깅: 행(hang) 덤프·Release 디버깅 — L-C-13은 널 참조 크래시만. 병렬 스택/`~*k`/`!clrstack -all`, WinDbg/`dotnet-dump analyze`, PDB·최적화·인라인, 예외 설정 창, 트레이스포인트 없음. 데이터 중단점 "C++"(`01:320`) 한정은 .NET Core 3+에서 불필요 | `01:687-690, 320` | L-C-13에 "데드락 프로세스 덤프 → 병렬 스택" 시나리오 추가 |
| 6 | Git 심화 — `bisect`(벤치 퇴행 찾기), `reflog`/`revert`, **pre-commit 훅으로 `build-and-test.ps1` 강제**(`01:32, 1064` "커밋 전 실행 습관"을 도구로 보장), `.gitattributes`(`01:143 autocrlf=true` + `01:544 end_of_line=crlf`의 표준 해법), 태그 | `01:864-868` | L-C-01/02에 훅·attributes 20분 |
| 7 | 설정 파일 — L-C-04는 문자열 파서뿐. `appsettings.json`/`IOptions<T>`, C++ JSON/TOML 읽기 없음. `global.json`이 1-C 산출물 트리(`01:1048-1058`)에 없음 | `01:577, 895` | 7일차에 설정 로딩 30분 |
| 8 | Windows 동기화 깊이(C++) — §4.3이 InitOnce·WaitOnAddress·스레드 풀 API 8h인데 실습은 L-CPP-05(SRWLock)뿐. `Event`/`WaitForMultipleObjects`(IOCP 워커 종료용), `WaitOnAddress`/`atomic::wait`로 SPSC Block 대기 없음. C#은 `ManualResetEventSlim`/`CountdownEvent`/`Barrier`가 목록(`01:347`)에 없음 | `01:927, 815` | |
| 9 | .NET 9+ `System.Threading.Lock`(C# 13 `lock` 패턴) 없음 — §6.3 "버전별 API 차이"(`01:1029`)의 좋은 예제 | `01:347` | |
| 10 | C# 메모리 모델 — C++은 메모리 오더 14h+교재 2부, C#은 L-C-10 `volatile` 한 줄(`01:673`). `Volatile.Read/Write`, `Interlocked` full fence, volatile≠원자성 | `01:673` | 🟡 C# 도전 과제 "Volatile/Interlocked 기반 SPSC" |
| 11 | 테스트 규율 — 파일 테스트 격리(임시 경로·fixture), 타임아웃 강제(`Task.WaitAsync`/`future::wait_for`), 30초 테스트 카테고리 분리(`[Trait]`/`--gtest_filter`), 테스트 더블 적용(1-1에 `ILogSink`/`IClock` 주입) 없음 | `01:181, 1183` | |
| 12 | 시계 — `Stopwatch`/`QueryPerformanceCounter`/`steady_clock` vs `system_clock` 없음(벤치 p99·Phase 3 게임 루프의 전제) | `01:1189` | |

**C. DoD 측정성** — 대체안 예: `01:389` "(B) 뼈대가 컴파일" → "(B)로 테스트 1·2 통과"; `01:405` "테스트 통과" → "§7.2 테스트 1~9(10~15는 12일차)"; `01:759` "할당 유의미 감소" → "Substring 대비 90% 이상 감소"; `01:839` "처리량 차이가 표에" → "1.5배 이상 또는 차이 없으면 코어/HT 구성 기록"; `01:1183` "메모리 증가 추세 없음" → "5초 간격 6회 샘플, 마지막 3회가 첫 3회 평균 ±10%"; `01:1178, 1180` "N초 내 종료" → 타임아웃 검증 수단 명시; `01:1226` "루브릭 AI 리뷰 4점" → README §5.3 루브릭에 점수 척도가 없으므로 §5.4 5점 척도 적용을 명시; `01:1288` "Debug에서 예외/assert" → Release 테스트 시 실패하므로 옵션 플래그로 항상 감지.

**E. AI 협업** — "맡기지 않을 것"(`01:1019-1025`)에 지침 파일 본문·`DESIGN-note.md` 추가; **에이전트가 테스트를 고쳐 통과시키는 실패 모드**를 §6.3과 1-C 지침 필수 항목(`01:1066`)에 "테스트 파일 수정 금지·`git commit` 금지"로; 3일차 AI 없는 1시간(`01:221` "프롬프트 손으로 적기")을 재구현으로 교체; OSS 코드 읽기 프롬프트 추가; .NET 10 검증 포인트에 `System.Threading.Lock`, `dotnet-counters` 카운터 이름 변경(`01:763`).

**F. 질문 은행 추가** — C# `volatile`이 보장하는 것/않는 것; `Interlocked.CompareExchange` lock-free 스택의 ABA; `Monitor.Wait`/`condition_variable`에서 `while` 재검사 이유; 데드락 덤프에서 원인 찾는 창/명령; flaky 테스트 원인 3개와 결정적 대기; 오브젝트 풀이 손해인 조건(`01:945`에서 노트로 요구하나 질문 없음); ASan이 잡는 것/못 잡는 것; rebase vs merge·bisect; Server GC vs Workstation GC; `record` vs `record struct`와 `in`.

**G. 막힘 포인트 추가** — vcpkg 첫 구성 30분+(바이너리 캐시); xUnit 병렬 실행 시 `log.txt` 공유 `IOException`; Release에서 ILSpy 상태 머신 안 보임; `clang_rt.asan_dynamic-x86_64.dll` 없음; ASan 누수 미보고(MSVC); C++ SPSC Block에서 CPU 100%; `Monitor.Wait` spurious wakeup(`01:1430`은 C++만); 조건부 중단점이 루프를 느리게; `dotnet test` 30초+(카테고리 분리); `LF will be replaced by CRLF`; 벤치에 Task/클로저 할당 혼입.

**H. 선수 조건** — 10문항이 언어 기초에 편중. 터미널 빌드·Git clone/commit/push·CMake 경험 문항과 보강 경로(CMake 반나절, Git 2시간); Phase 2 전제인 16진수·비트 연산·바이트 순서; 하드웨어(물리 코어 4개 이상 — L-C-07·L-CPP-09의 전제) 확인; 보강 경로 1~2일이 26주 밖("1주차 전")임을 명시. `01:71` "L-C-04를 1주차 화요일 오전으로"는 이미 화요일 오후(`01:188`)라 이동 의미 없음.

**I. 트랙 균형** — 시간·실습 수는 균형(80h/80h, 12/12, 분 785/720). 깊이는 C++이 메모리 오더·false sharing·UB·ASan·Win32까지, C#은 메모리 모델·레이아웃 실습 없음; 반대로 C#은 async 26h(`01:913, 918`) 편중. 과제 난이도: 1-1(B)가 C#은 `Channel<T>`, C++은 lock-free SPSC+메모리 오더+ASan. 교재 부하 C++ 5권 ≈20장 vs C# 3권 ≈11장(`01:951-963`)인데 §2 배치는 동일. 미배치 실습이 C++에 치명(L-CPP-11 🔴). §4.2 "박싱 벤치 6h"(`01:911`), §4.3 "템플릿 8h·언어 재정비 8h"(`01:930, 932`), 교재 "메모리 모델 1·2부 14일차 직전"(`01:962`)·"async/await 라이브러리 1~6장 3주차"(`01:953`)도 배치될 날이 없다.

### 5.2 Phase 2 — 네트워크 프로그래밍

**A. 주제 공백**

| # | 공백 | 근거 | 추가안 |
|---|---|---|---|
| 1 | **채팅 서버 스레드 모델 미명세** — IOCP 워커 N개(`02:267, 621`)/SAEA 콜백이 `Logic`(방·유저 딕셔너리 `02:324, 934`)을 동시에 호출할 때의 동기화(전역 락 1개 / 단일 로직 스레드+큐 / 세션별 직렬화)가 §2·§7.2에 없음. 세션 수준 방어(`02:287`)만 있어 방 딕셔너리 레이스를 그대로 만듦 | `02:287, 324, 929-940` | §7.2 구조 요구에 "Logic 전역 락 1개, Send/직렬화는 락 밖" 명시, 28일차 개념 30분 + L2-C-16 확인 기준 "워커 4개 동시 입장/퇴장 1만 회 정합성", §8.3 질문, §6.2 결함 주입 "락 없는 방 딕셔너리" |
| 2 | **IOCP/SAEA 실무 함정** — zero-byte recv는 단어만(`02:703`); `WSAENOBUFS`(10055) 문서 전체 부재(1,000접속×대기 recv 버퍼가 바로 이 오류); "소켓당 미해결 recv 1개"·완료 순서(같은 소켓 완료가 다른 워커에서 동시 처리) 없음; C#은 "버퍼 고정"(`02:687`) 한 단어, pinned 힙 단편화·`GC.AllocateArray(pinned:true)`·SAEA 대형 버퍼 슬라이스·`BufferList`(gather 송신) 없음 | `02:687, 703, 620-623, 590-597` | 33일차 오전 `L2-CPP-07/L2-CS-06 "대규모 접속 함정"(90분)`: 대기 recv 64KB×1,000으로 10055 재현 → zero-byte recv로 수정, 같은 소켓 recv 2개로 순서 붕괴 로그 |
| 3 | **TCP 의미론 실습 부재** — keep-alive(`SO_KEEPALIVE`/`SIO_KEEPALIVE_VALS` vs 앱 하트비트), half-close(`shutdown(SD_SEND)` 후 잔여 데이터 수신), backlog 초과 시 클라이언트 현상(Windows RST/10061), `SO_EXCLUSIVEADDRUSE`(`02:482`가 하이재킹을 경고하면서 해법 이름 없음), `SO_RCVBUF/SO_SNDBUF` 측정, CLOSE_WAIT 누적 진단 | `02:115, 288, 482, 584, 646, 700, 1176` | 각 30분 실습 3개 |
| 4 | **파서 퍼징 없음** — 2-2 테스트 20개 전부 정해진 입력. `Reserved` 검증 여부(`02:824`) 미정 | `02:1042-1065` | 테스트 21 "무작위 1MB를 1~100B 조각으로 Feed → 예외·크래시 0, 반환은 오류 코드/NeedMore뿐" |
| 5 | **접속 상한 정책** — "최대 접속 수" 설정(`02:917`)만 있고 도달 시 동작·IP당 상한·세션당 패킷 상한(채팅만 5회/초 `02:913`) 없음 | | 2-1 요구 "최대 접속 초과 시 `ErrorNotify` 후 종료" |
| 6 | UDP·암호화 개념 — UDP는 교재 읽기(`02:719` "설명 시험 단골")뿐, §4.1·질문 은행에 없음. 암호화는 Flags bit1 "미사용"(`02:823`)뿐 | | 1시간 개념(왜 평문 TCP가 위험한가, TLS/`SslStream`, Phase 5 예고) |
| 7 | 프로토콜 버전 진화 — `Version` 거부(`02:822`)만. 필드 추가 시 하위 호환(MemoryPack 버전 관리 `02:691` 단어만) 연습 없음 | | |
| 8 | IPv6/듀얼 스택 — `AddressFamily.InterNetwork`·`IPAddress.Any` 고정(`02:574-575`). 국내 모바일망 IPv6 비중 | | `DualMode` 한 줄 |
| 9 | 네트워크 코드 테스트 기법 — in-process 테스트(포트 0으로 리슨) 없음. 하트비트 타임아웃 테스트(`02:353`)는 시계 주입 없이는 35초 테스트 | | `IClock` 주입 |

**C. DoD 측정성** — `02:146, 583` "100접속에서 정상" → "100클라이언트×1KB×100회 에코 바이트 일치 100%, 예외 0"; 파서 테스트 수가 8/12/15/20(`02:221, 522, 278, 1042`)으로 제각각 → §7.3 번호로 일자별 누적 목표; 채팅 기능 "5종/6종/7종"(`02:334, 370, 1123`) → §7.2 요구사항 1~7로 통일; `02:277, 622` "1,000접속 유지·핸들 누수 0" → "60초 유지+전원 에코 1회, 워커 ≤ 코어×2, 접속·종료 3회 반복 후 HandleCount 편차 ±5"; `02:318, 543` "메모리 상한 유지" → "Private Bytes +50MB 이내, 큐 상한 로그 ≥1"; `02:353` "30초 내 정리" → 시계 주입으로 1초 내 검증; `02:588` "스레드 수 현저히 적음" → "동기 대비 1/10"; `02:1040, 1065` "할당 0 또는 상수" → "1만 패킷 파싱 Allocated ≤ 1KB"; `02:896` "모호 항목 전부 해소" → "재리뷰 신규 지적 0건".

**E. AI 협업** — Phase 1 형식의 "맡길 것/맡기지 않을 것" 표가 없음(맡기지 않을 것: `PacketReader` 첫 구현·세션 종료 경로·송신 큐 / 맡길 것: CLI 파싱·JSON 출력·설정 로딩·MemoryPack 특성). §6.3 추가: Linux 관습(`SO_REUSEPORT`, epoll 식 코드), `BitConverter`(머신 엔디안)를 `BinaryPrimitives` 대신 제안, `Socket.Connected`로 끊김 판단, 접속당 `Task.Run`, 레거시 APM, "워커 수 = 코어×2" 단정. AI 없는 1시간이 16·17·19·24·27·29·31~34일차에 글쓰기 → 최소 격일로 코드 재구현(에코 30분, 파서 90분, 송신 큐).

**F. 질문 은행 추가** — TCP vs UDP 장르별 선택; OS keep-alive vs 앱 하트비트; half-close 후 서버 송신 가능 여부; backlog 초과 시 클라이언트가 보는 것(Windows vs Linux); zero-byte recv와 `WSAENOBUFS`; 같은 소켓에 recv 2개를 걸면 완료 순서는; 워커 N개가 방 딕셔너리를 동시에 만지면; 퍼징 시 허용되는 결과; `Socket.Connected`가 true인데 끊긴 이유; CLOSE_WAIT 누적 원인·진단; 리틀 엔디안 고정의 근거와 위험; 필드 추가 시 구버전 클라이언트 살리기.

**G. 막힘 포인트 추가** — 1,000접속 부근 `WSAENOBUFS`; C# "An asynchronous socket operation is already in progress"(한 SAEA에 두 작업); 콜백에서 `ObjectDisposedException`; `AcceptEx` 소켓 `getpeername` 실패(`SO_UPDATE_ACCEPT_CONTEXT`); `WSARecv` `SOCKET_ERROR`+`WSA_IO_PENDING`을 오류로 처리; `GQCS` FALSE+`lpOverlapped != NULL` 미처리(`02:802` 규칙을 §9로도); MemoryPack `partial` 누락·소스 제너레이터 버전; Nagle on/off 차이 안 보임(루프백+지연 ACK — 요청-응답 왕복 패턴 필요); Ctrl+C 핸들러 미호출(`dotnet run` 아래); 램프업 중 10061(backlog); 통합 테스트 포트 충돌(포트 0); Logic 락 안 Send로 서버 멈칫.

**H. 선수 조건** — 5문항이 전부 Phase 1 산출물 확인. 16진수·비트 연산·바이트 순서, `Get-NetTCPConnection`/`netsh` 관리자 권한(`02:398`), Phase 1 §10 방화벽·동적 포트 준비, 논리 코어 8개 권장(클라이언트 2프로세스+서버 한 PC `02:502, 1209`), 미달 시 보강 경로.

**I. 트랙 균형** — 90h/90h이나 실습 분 C# 480 vs C++ 660, C++은 논블로킹→select→Overlapped→IOCP→AcceptEx→참조 카운트 6단계를 24~26일차 3일에(C#은 TAP→SAEA 2단계). 2-2 직렬화는 C# MemoryPack 특성 vs C++ "수동 또는 FlatBuffers"(`02:1039`) 20종 수동 구현이면 6~8h 차이. 설정 파일은 C# 내장 vs C++ 라이브러리 미교육(`02:917`). C# 결손: zero-byte recv·pinned·`BufferList`. 제안: L2-CPP-02(select) 🟡, L2-CPP-05를 27일차로, 2-2를 22~24일차 3일로, C++에 헬퍼 제공 또는 FlatBuffers 기본.

### 5.3 Phase 3 — 실시간 게임 서버 아키텍처

잘 덮인 것: 서버 권위·치트 5종, 룸 상태 머신, 재접속, 관전·브로드캐스트 공유, graceful shutdown, 봇 클라이언트, 매칭.

**A. 주제 공백**

| # | 공백 | 근거 | 추가안 |
|---|---|---|---|
| 1 | **틱 기반 게임 루프 결정성** — "오목은 이벤트 기반으로 충분"(`03:133`), 틱 루프는 🟡 3-3b만. README는 Phase 3 공통 주제로 명시(`R:141`). fixed timestep·accumulator·catch-up 한도·spiral of death는 면접 단골 | `03:133, 959, 615` | 38일차 오전 "고정 틱 루프 60분" 🔴(accumulator + 예산 초과 카운터 + 드리프트 측정) |
| 2 | **시간 동기화·지연 처리** — `PlaceNotify.remainMs`(`03:824`)는 있으나 RTT 측정 패킷·클라 표시 보정·"턴 마감 = 서버 기준 + RTT 여유" 결정 없음 | `03:633-637, 824` | 5000번대에 `Ping/Pong(clientTs)`, 3-2 통계에 RTT p50/p99, "타임아웃 판정은 서버 시계만" 원칙 |
| 3 | **룸 간 메시지 실습 부재** — 재구현 시험 4번 `룸간메시지`·`Room.SendTo`(`03:995, 1002`)가 평가 항목인데 실습·오목 서버 어디에도 없음. 동기 요청-응답 교착 위험을 가르치지 않음 | `03:543-588, 995, 1002` | 37일차 L3-CS-01/CPP-01에 "룸 A→B 요청, B 처리 후 A에 응답 Job(continuation) 왕복" 30분; 로비를 액터로 두고 "게임 종료 → 로비 통지"를 실제 룸 간 메시지로 |
| 4 | **재접속 인증(세션 탈취)** — 재접속이 "같은 닉네임"(`03:282, 865`, `ReconnectReq{name, roomId}` `03:828`)만으로 허용 | | 로그인 응답에 `reconnectToken`(16B) 발급, 치트 테스트 6번 "타인 닉네임/틀린 토큰 거부" |
| 5 | 세션 단위 패킷 속도 제한 일반화 — 착수 초당 10회(`03:245`)뿐. 채팅·MatchReq·SpectateListReq 폭주 시 큐 상한 정책(`03:117`)이 진짜 유저 Job을 버림 | | L3-C-08에 세션별 토큰 버킷(패킷 종류별 한도 표) |
| 6 | 리플레이/재현 로그 — 착수 순서 기록·시드 로그(`03:903, 246`)는 있으나 "moves+seed → 재현" 도구 없음 | | 3-2 `replay` 시나리오(moves JSON → 규칙 라이브러리 재생 → boardHash 일치) 30분 |
| 7 | 채팅 위생 — `text ≤100`(`03:838`)만. UTF-8 유효성·제어문자·바이트 vs 문자 길이·뮤트 | | 43일차 2줄 |
| 8 | 하트비트 패킷 미정의(§3.11) | | |

**C. DoD 측정성** — `03:444, 980` `grep -r "lock\|mutex"` → 대소문자·`SRWLOCK`·`Monitor.Enter`·`SemaphoreSlim`·`SpinLock`·`ReaderWriterLockSlim`·`CRITICAL_SECTION`·`Interlocked` 포함 패턴으로, PowerShell 기준(`R:77`); `03:147` "룸 파괴 시 타이머 정리 확인" → "파괴 후 예약 타이머 100개 발화 → 실행 Job 0"; `03:238, 503` "클라이언트로 성공" → 페이크 세션 통합 테스트 1개; `03:273` "게임 1판 완주" → "봇 2개 자동 대국 → GameEndNotify(5목) 수신"; `03:311, 523` "할당 측정으로 증명" → "관전 10명×착수 100회: 직렬화 카운터 = 100, `GC.GetAllocatedBytesForCurrentThread` 전/후"; `03:330, 531` "종료 5초 내" → `shutdown_total_ms` 로그+단계별 타임스탬프 5개; `03:362` "무사고" → "크래시 0, Error 로그 0, 게임 완료 수 = 기대치, 미반환 풀 버퍼 0"; `03:881` "p99 50ms" → 측정 지점(봇 송신 직전~PlaceNotify 파싱 직후)·동일 PC·코어 수·봇 CPU% 기록; `03:882` "메모리 10%" → Private Bytes + `GC.GetTotalMemory` 병기; `03:592` "로그 때문에 p99가 튀지 않음" → "on/off 차이 ≤ 5ms"; `03:535` "결과 파일 생성" → "통계 필드 8종 비어 있지 않음 + 3회 편차".

**E. AI 협업** — CLAUDE.md 언급 0회(§2.6). AI 없는 1시간이 36·39·44·47·49·51·52·54일차 작문 → 44일차 토큰 버킷, 47일차 참조 카운트 공유 버퍼, 51일차 히스토그램 p99 계산기로 교체. "맡기지 않는 목록"(`03:767-772`)에 6가지 검증 로직·상태 전이표 셀 값·테스트 기대값 추가("케이스 목록은 AI, 기대값과 assert는 내가"). §6.3 추가: `timeBeginPeriod`가 Win10 2004+에서 프로세스 단위로 바뀐 점, `DropWrite`에서 `TryWrite`가 true인 점, `Task.Delay` 타이머 1만 개 권장.

**F. 질문 은행 추가** — 틱 예산 초과 시 처리(catch-up, spiral of death); TCP가 순서를 보장하는데 `turnSeq`가 필요한 이유; 룸 A가 룸 B에 동기 요청-응답을 기다리면 안 되는 이유·continuation 설계; 닉네임 재접속 허용 시 가능한 공격·토큰 저장/폐기; 느린 관전자 1명이 룸 전체를 느리게 하는가·송신 큐 상한; C#에서 룸마다 Task를 두면 1만 룸일 때; 클라 남은 시간과 서버 타이머 어긋남과 보정; graceful shutdown 중 매칭 성사/재접속 거부 단계; 프로세스 사망 시 진행 중 대국(상태 외부화, Phase 4·6 연결); 버그 재현 최소 로그(시드·moves·turnSeq·타이머 세대).

**G. 막힘 포인트 추가** — 부하 시 모든 룸 동시 멈춤(C# Job 안 동기 Send/`Wait()` → ThreadPool 고갈); 종료 5초 초과(`ReadAllAsync(ct)` 대기 갇힘·`Writer.Complete()` 미호출); p99 히스토그램 15~16ms 계단(타이머 병합); 서버 CPU 낮은데 p99 큼(봇 300개 동거); 재접속 직후 판이 한 수 어긋남(스냅샷 vs 착수 통지 순서 — `03:287` 테스트만 있고 원인·대처 없음); 관전자 많은 룸 메모리 급증(느린 소비자 송신 큐 상한); C++ ASan Job 실행 중 use-after-free(버퍼 소유권을 Job으로 이동); C# Job 박싱 폭주(구조체 Job을 `IJob`로); Serilog Async 싱크 버퍼 초과 시 드롭; 규칙 테스트 1초 초과(러너 기동 시간 포함 `03:203`).

**H. 선수 조건** — 도구 준비 없음: `dotnet-counters`(`dotnet tool install -g`), ASan 빌드 1회 성공(`/fsanitize=address`+디버그 런타임); Phase 1 Channel·PeriodicTimer 숙련("Bounded Channel 옵션 4종 설명"); 보강 8h 슬롯(36~40일차 전부 🔴).

**I. 트랙 균형** — 70h 동일이나 C++ 부담 10~15h 큼(타이머 스레드 직접 구현 `03:675`, 객체 수명 10h `03:677`, ASan, vcpkg GoogleTest·toml++/nlohmann `03:678-679`; C#은 `PeriodicTimer`·Generic Host). 참조 구현 비대칭: C#은 SuperSocketLite 7장 오목 서버+WinForms 클라이언트(`03:703`), C++은 오목 참조 없음(`03:711-714`는 채팅·strand), 3-3a GUI도 C# 기준(`03:958`) → "C++ 트랙은 SuperSocketLite WinForms 클라이언트를 프로토콜만 맞춰 사용 허용" 명시. C++ 골격(`03:579-585`)에 `Job` 타입 정의 없음(C# 골격은 완전) → `struct Job { uint32_t type; std::function<void(Room&)> fn; }` 예시. L3-CPP-01 180분 vs 슬롯 150분.

### 5.4 Phase 4 — 데이터·API 서버

잘 덮인 것: 이중 지출 3패턴, 멱등성 개념, keyset 페이징, 레이트 리밋, 에러 모델, 격리 수준·재시도, 캐시 무효화, 해싱, 로그 마스킹, 우편 만료, 서버 간 인증.

**A. 주제 공백**

| # | 공백 | 근거 | 추가안 |
|---|---|---|---|
| 1 | **멱등성 키 미들웨어 실습 없음** — 테이블(`04:873`)·"키 필요" 엔드포인트(`04:904, 907`)·재구현 시험 4번(`04:1094`)은 있으나 67일차는 개념뿐, L4-C-20/21은 조건부 UPDATE 방식. "처리 중 재시도"·"같은 키·다른 바디" 정의 없음 | `04:319-322` | 67일차 `L4-C-20a 멱등성 필터(90분)`: 키 유니크 삽입 → 처리 중 409 → 완료 후 `response_json` 저장·재생 → `request_hash` 불일치 422. 테이블에 `status, request_hash` |
| 2 | **재화 원장 부재** — `purchase_log`만 gold_before/after. 우편 첨부·출석 보상·관리자 지급(`04:325, 326, 1056`) 재화 변동은 원장 없음 | `04:871` | `currency_ledger(user_id, delta, reason, ref_id, balance_after, created_at)` 추가, 모든 변동을 같은 트랜잭션에서; L4-C-26 배치에 "원장 합 = 잔액" 검사 30분 |
| 3 | **재고 이중 쓰기 보상 없음** — "상점 재고 DB + Redis(Lua 차감)"(`04:886`), 66일차 "Lua 또는 조건부 UPDATE"(`04:307`). Lua로 깎은 뒤 DB 실패 시 재고 누수 | | DB 조건부 UPDATE 단일 진실, L4-C-11 Lua는 "원자성 데모 + 실패 시 보상 INCR + 배치 불일치 검출" |
| 4 | 출석 설계 모순(§3.14) | `04:571, 869` vs `04:376, 958` | |
| 5 | **주입 가능한 시계·UTC 저장** — "자정(KST)"·"23:59:59/00:00:01 경계 테스트"(`04:326, 334, 572`)를 요구하나 `TimeProvider`/`Clock` 주입·`created_at` UTC 규칙 없음 → 경계 테스트를 결정적으로 못 돌림 | | 58일차 DI에 `IClock`, "저장은 UTC, 판정만 KST 오프셋"을 SCHEMA.md 필수 항목으로 |
| 6 | 스키마 마이그레이션 — `001_init.sql`(`04:156`)뿐. 데이터 있는 상태의 002·버전 테이블·되돌리기 없음(4-3c의 전제) | | 68일차 `002_add_season.sql` + `schema_version` 30분 |
| 7 | 낙관적 락 실습 없음 — 패턴 3으로 소개만(`04:303`) | | 64일차 닉네임 변경에 `version` 컬럼 + 409 30분 |
| 8 | **3-1 로그인을 토큰으로 교체하는 일정 없음** — Phase 3이 "Phase 4에서 인증으로 교체"(`03:227, 862`)라 했으나 `/internal/token/verify`(`04:912`)와 논의(`04:359`)만 | | 69일차 `L4-C-24b`(60분): `LoginReq{token}` → verify → userId, 만료 토큰 거부 테스트 |
| 9 | `token/refresh` 의미, 세션 2키 원자성(§3.14) | | |

**C. DoD 측정성** — `04:182` "평문/약한 해시 아님" → "해시 접두사(`$argon2id$`/`$2b$`) 확인, 같은 비밀번호 2계정 해시 상이"; `04:199, 942` AI 리뷰 모호 항목 해소/명확성 30점 → "AI가 API.md만 보고 쓴 `.http` 20건 중 18건 성공"; `04:334, 572` 경계 테스트 → 가짜 시계; `04:352` 랭킹 복구 절차 → "`FLUSHDB` 후 복구 → 상위 100 일치 테스트"; `04:387` "기능 10종 동작" → 통합 테스트 영역별 전부 통과; `04:424, 998, 1079` "문자열 결합 검색 0건" → 검색 패턴 명시; `04:538` "재시도로 회복" → "≥ 99%"; `04:604` "정상 사용자 영향 없음" → "초당 5요청 429 = 0, 초당 50요청 429 ≥ 90%"; `04:525` "캐시 히트율 로그" → "100회 조회 시 DB 쿼리 1회".

**E. AI 협업** — CLAUDE.md 0회. AI 없는 1시간이 56~74일차 **16일 전부 작문**(`04:120-454`) → 59일차 PBKDF2 래퍼, 63일차 지수 백오프, 66일차 조건부 UPDATE 리포지토리+테스트, 67일차 멱등성 저장소, 72일차 토큰 버킷. "맡기지 않는 목록"(`04:832-837`)에 동시성 테스트 코드·핵심 쿼리 5개 SQL 본문. §6.3 추가: `Cache=Shared`, `Microsoft.Data.Sqlite`의 `*Async`가 실제로는 동기(100 동시 요청이 스레드풀을 막음), `DateTime.Now`로 KST, `ConnectionMultiplexer` 요청마다 생성, EF Core 마이그레이션 권유.

**F. 질문 은행 추가** — 같은 멱등성 키의 두 번째 요청이 처리 중에 도착하면; Redis 재고 차감 후 DB 실패 시; 커서 페이징 중 삽입·삭제 시 offset vs keyset; 게임 결과가 30분 늦게 도착(랭킹·시즌 경계); UTC 저장·KST 판정 분리; 원장이 있으면 가능해지는 것·어긋날 때 무엇을 믿나; 데이터 있는 테이블 컬럼 추가 마이그레이션·되돌리기; Argon2 비용과 p99·처리량; 슬라이딩 TTL에서 refresh가 필요한가; 낙관적 락 vs 조건부 UPDATE; (C#) `Microsoft.Data.Sqlite` async는 진짜 비동기인가; (C++) httplib 스레드 여러 개가 SQLite 커넥션 하나를 공유하면.

**G. 막힘 포인트 추가** — `SQLITE_LOCKED`+`busy_timeout` 무력(`Cache=Shared`); (C#) 동시 100요청 p99 튐+CPU 낮음(async가 동기 → 스레드풀 고갈); 로그인만 p99 1초(해시 비용); 출석 하루 2번/안 됨(`DateTime.Now`); 같은 키·다른 바디가 이전 응답; Redis 소켓 증가(연결 재생성); 통합 테스트 병렬 `database is locked`; 동시 로그인 2건 뒤 두 토큰 유효(2키 비원자); 배치 워커 2회 실행(락 TTL < 작업 시간); 룸 Job 안 동기 HTTP로 룸 멈춤; (MySQL) 닉네임 유니크 대소문자(콜레이션); (C++) 커넥션 공유 크래시.

**H. 선수 조건** — 도구 준비: C++ (a)는 vcpkg 7종(cpp-httplib, sqlite3, redis-plus-plus/hiredis, nlohmann-json, libsodium, gtest, spdlog) 사전 빌드(redis-plus-plus 첫 빌드 수십 분)가 Phase 3 §10에도 Phase 4 §1.3에도 없음; C# "최소 API 띄워 본 적"; PC 시간대·UTC 변환; 보강 이틀(`04:57`)이 🔴 실습과 충돌.

**I. 트랙 균형** — C++ (a) 경로: vcpkg 패키지는 전부 Windows/MSVC 빌드 가능하나, C#이 프레임워크로 받는 DI·미들웨어·모델 바인딩·검증·`WebApplicationFactory`를 C++는 손으로 만들어야 하는데 시간은 동일 60h(`04:729-738`) → 현실 75~80h. 조정: (a) 엔드포인트 20 → 14(notice, rank/me 주변, token/refresh, health/deep 제외), 통합 30 → 20, L4-C-10·29 🟡. cpp-httplib는 요청당 블로킹 스레드라 SQLite 커넥션 공유(§3.4) 필수. Drogon 🟡 유지 적절. 혼합 경로 (b) 명세 부족(`04:740-744`, §4.4). 부 트랙 읽기(`04:779-782`) C# 항목이 "이미 공통 과제"라 실질 없음.

### 5.5 Phase 5 — 운영 품질

잘 덮인 것: 시나리오·램프업·p50/95/99, 3회 편차 10%, USE/RED, 카디널리티, 알람 규칙, 타임아웃 계층, 재시도·서킷·백프레셔, 레이트 리밋·플러딩, 의존성 장애 3종, GC pause, ThreadPool 기아, 크래시 덤프.

**A. 주제 공백**

| # | 공백 | 근거 | 추가안 |
|---|---|---|---|
| 1 | **Closed vs Open 부하 모델, coordinated omission** — 시나리오(`05:93-96`)는 closed 모델인데 용어·한계 없음. 서버 stall 시 p99 과소평가 → "p99 30% 개선"(`05:329`)이 측정 왜곡일 수 있음 | | 76일차 오전 40분 |
| 2 | **부하 클라이언트와 서버가 같은 PC** — `L5-C-02`(`05:101`)는 클라이언트 자체 한계만. 환경 항목(`05:979`)에 "동일 PC 여부" 없음. 게임서버+API+DB+Redis+더미+Prometheus가 CPU를 나눔 | | 코어 분리(`ProcessorAffinity`)·클라이언트 CPU% 병기 |
| 3 | **Windows 동적 포트/TIME_WAIT 고갈** — "동적 포트 범위 확대" 한 줄(`05:1157`). "초당 1,000 접속 시도"(`05:445`)는 기본 16,384 포트·TIME_WAIT 240s에서 4분이면 고갈 → 플러딩 테스트 자체가 안 돌아감 | | `L5-C-02` 단계에 `netsh int ipv4 set dynamicport tcp start=10000 num=50000` + `TcpTimedWaitDelay=30` |
| 4 | SLO/SLI·에러 버짓 어휘 — "목표치 50ms"(`05:188`)·알람 임계(`05:246`)만, 관계 설명 없음 | | 30분 개념 |
| 5 | 알람 발송 경로(§3.8) | | |
| 6 | **상관 ID(requestId) 게임↔API 전파** — 선수조건에 존재 확인만(`05:40`), "분산 추적 개념"(`05:753`)은 항목명뿐 → 런북 진단(`05:1029`)이 공허 | | 85일차 🟡 fluent-package 대신 `L5-C-14b 로그 스키마 표 + requestId HTTP 헤더 전파(60분)` |
| 7 | 구조화 로그 스키마·보존 — "롤링"(`05:259, 935`)만. 필드 표준·보존 일수·용량 상한 없음(디스크 풀 장애 `05:1024`와 직결) | | 5-C 요구 7에 "스키마·보존 7일·용량 상한" |
| 8 | **Graceful shutdown/drain** — 결함 찾기(`05:1129`)와 "종료 후 정리"(`05:954`)만. 진행 중 게임·결과 큐를 종료 시 어떻게 하는지 없음("유실 0" `05:1023`은 프로세스 생존 전제) | | 98일차 `L5-C-29b Graceful shutdown(45분)`: 신규 접속 거부 → 룸 통지 → 결과 큐 flush → 종료 |
| 9 | 결과 저장 큐 내구성 — "로컬 큐"(`05:390, 665`)가 메모리/파일 미결 | | 5-4 Part A에 ADR 1건 |
| 10 | 멱등성 키 — "중복 저장 0"(`05:666`)을 요구하나 Phase 4 API의 멱등 키 전제·확인 없음 | | 선수조건에 추가 |
| 11 | Soak 테스트·누수 사냥 — 5분 부하(`05:124`)로는 누수가 안 보임. 30분~2시간 유지 후 힙/핸들 추세 실습 없음 | | 97일차 오후 soak 1회 |
| 12 | 용량 추정 — "접속당 CPU%·메모리 → 1대 최대 접속 수" 없음(Phase 6 스케일 아웃의 근거) | | 5-2 리포트에 1절 |
| 13 | 보안 체크리스트 범위(`05:1053-1060`) — 토큰 만료/재사용(replay), 패킷 내 userId 신뢰 금지(세션 바인딩), SQL 파라미터화 감사, 비밀번호 해시 확인, TLS 실제 적용(`05:428` "권장") 없음 | | 5-6 항목 확장 |
| 14 | 설정 관리 — 24장 읽기(`05:813`)만. 환경별 설정·포트/서버ID 분리는 `05:1166`에서야 | | 0.5h 실습 |
| 15 | 방화벽 차단 실험(`05:483, 665`) — keep-alive 연결이 살아남으면 재현 안 됨 | | "차단 후 신규 연결 실패 확인" 절차 |

**C. DoD 측정성** — `05:228` 오버헤드 1% vs `05:96` 편차 10%(§3.15); `05:420, 638` "메모리 상한 유지·완전히 굶지 않음" → 수치·성공률 하한(≥ 95%); `05:456, 654` "유의미하게 감소" → "방어 후 정상 유저 p99 ≤ 기준선×1.5"; `05:495` "설계대로 동작" → 설계 문서명(ADR); `05:512, 670` "RUNBOOK 5개·AI 리뷰 통과" → "AI가 명령만 보고 5분 내 복구 절차 재현"; `05:557, 1005` "남이 재현 가능" → "AI에게 문서만 주고 실행 순서 복원"; `05:1026` "수동 개입 없이 정상화" → "≤ 30s"; `05:1100` 재구현 테스트 "1초 후 5회 성공" → 주입 가능 clock을 시그니처(`05:1093`)에; `05:271` "80점 이상" 채점표가 주관 항목("5초 안에 판단") → 스크린샷을 AI에 주고 "무엇이 비정상인가" 5초 컷.

**E. AI 협업** — 강점: "결론 금지"(`05:854`)·"내 해석 먼저"(`05:861`)·최악 지연 계산(`05:886`). 추가: §6.3에 **PromQL 오답**(`histogram_quantile`에 `sum by (le)` 누락, `rate()` 윈도우 < 스크레이프×2, 카운터 리셋), **환경 불일치 오답**(Docker 이미지·Linux `tc`/`iptables`·Alertmanager 컨테이너 제안, Polly v7 `Policy` vs v8 `ResiliencePipeline`, prometheus-net `KestrelMetricServer`가 별도 패키지); "새벽 3시 운영자 리뷰"(`05:878`) 실제 프롬프트 본문 없음; 프로파일러 결과 해석(`05:158`) 후 AI 대조 결과 노트 지시 없음.

**F. 질문 은행 추가** — closed 모델 p99 과소평가와 완화; 같은 PC 부하 도구의 왜곡과 통제; 카운터를 `rate()`로 보는 이유·재시작 시 그래프; 알람 임계·지속 시간 결정(오탐/미탐)·SLO와의 관계; 게임↔API 로그를 잇는 상관 ID; 안전 종료 순서; 누수 vs GC 지연 구분; TIME_WAIT·동적 포트 고갈; 재시도 시 중복 저장 방지; 1대 감당 접속 수와 근거. 결함 찾기(`05:1129`)에 트랙별 1개("동기 대기 ThreadPool 기아"/"락 순서 역전").

**G. 막힘 포인트 추가** — 부하 중 `SocketException 10048/10055`(포트·TIME_WAIT 고갈); `histogram_quantile` NaN; 알람 firing인데 아무 데도 안 옴; 대시보드 import "datasource not found"(UID); PerfView/WPA에 주소만(PDB·심볼 경로); `dotnet-trace`로 서버 느려지고 파일 GB; 방화벽 차단해도 호출 성공(keep-alive); SQLite 잠금 주입 시 즉시 `database is locked`(`busy_timeout`과 상위 타임아웃 관계); 서킷 브레이커 테스트 flaky(실제 시계); 환경 변수 시크릿을 VS 디버그가 못 읽음; 크래시했는데 덤프 없음(LocalDumps 키·권한); 재시작 후 `rate()` 급락/급등(카운터 리셋).

**H. 선수 조건** — 4-1 토큰을 게임 서버가 검증 가능한가(full 시나리오 `05:113` 전제); 3-2 클라이언트에 HTTP 클라이언트가 있는가(C++이면 cpp-httplib/WinHTTP 결정); Release+PDB 빌드; 관리자 PowerShell(방화벽·레지스트리·procdump); 결과 저장 API 멱등 키; 디스크 여유 ≥ 20GB(트레이스·덤프).

**I. 트랙 균형** — 60h/60h 표기이나 실습 C# 435분 vs C++ 495분, 일자에 존재하는 트랙 시간 7.5h(§4.1). C++ 공백: ASan 실습 없음(README와 모순), IOCP 워커 부족(=C# ThreadPool 기아 대응물) 실습 없음, 핸들/메모리 누수 추적 없음, **C++ 게임 서버가 API를 호출할 HTTP 클라이언트가 어디에도 지정되지 않음**(`05:617 L5-C-16` 전제; 혼합 경로 학습자는 여기서 처음 짬). C# 🟡 Polly 비교(`05:413, 788`)에 대응하는 C++ 항목 없음.

### 5.6 Phase 6 — 캡스톤·취업 준비

잘 덮인 것: 서버 간 통신 선택·ADR, Redis 하트비트·TTL, 1회용 티켓 원자 검증, traceId 전파, 서버 다운 감지·재매칭, 결과 큐 멱등, 스모크 테스트, 서버별 대시보드, README 재현 테스트, 데모 영상, 이력서, `AI-USAGE.md`(이 과정의 차별점).

**A. 주제 공백**

| # | 공백 | 근거 | 추가안 |
|---|---|---|---|
| 1 | 배치 워커 미정의, 클라이언트 확장 실습 부재, 콘텐츠 4종(§3.7) | | |
| 2 | **서버 간 프로토콜·내부 API 명세 시점** — `PROTOCOL.md/API.md` 최종본은 120일차(`06:460`)에만. 매칭↔클라이언트 패킷, 게임→API 결과 저장 내부 API(`06:273`), 매칭→클라이언트 배정 통지 계약을 설계하는 날 없음. "내부 API 인증"은 학습 항목(`06:747`)에만 있고 요구사항(`06:940-948`)에 없음 → 5-6에서 배운 시크릿·인증이 캡스톤에서 퇴행, 티켓 설계(`06:233`) 무의미 | | 103일차에 계약 설계 반나절, 요구사항에 내부 API 인증 |
| 3 | **재접속(네트워크 순단) 시 진행 중 게임 복귀** — 문서 전체에 없음(`06:304`는 서버 사망만). 모바일 실무·면접 최빈 | | 🟡 설계 또는 §10.1 취업 후 목록 |
| 4 | Redis Streams 선택지 부재 — 비교표(`06:144, 632`)는 Pub/Sub/gRPC/자체 TCP만, 큐 영속화(`06:328`)는 List만 | | 비교표 1행 |
| 5 | 게이트웨이/릴레이 개념 — 취업 후 목록(`06:1130-1137`)에도 없음 | | 질문 1개 |
| 6 | E2E 테스트 전략 — "단위·통합 통과"(`06:958`)만. 서버 다운 5회(`06:678`)는 수동 관찰 | | `L6-C-14` 스크립트 자동화(종료→재매칭 성공 시간 출력) |
| 7 | **CS 면접 복습·질문 은행** — "CS 일반 25분"(`06:518`)인데 복습은 "재독(속독) 124~128일차"(`06:820`) 한 줄. OS·DB 질문 은행 없음. 8.4(`06:1075-1092`) 16문항 100% 캡스톤 | | §8.4-b CS·언어 30문항(네트워크 10·OS 8·DB 7·언어 5) + 행동 5 |
| 8 | 행동 면접·회사 조사 — 지원 동기·협업/실패 경험·"왜 서버 개발자인가", 목표 회사 스택·공고 분석 과제 없음(`R:49`는 회사군 스택으로 트랙을 고르라 함) | | 125일차 오전 "목표 회사 3곳 공고·스택 분석표" 60분 |
| 9 | 코딩 테스트 "총 8문제"(`06:1023`) — 국내 게임사 1차 관문(프로그래머스 Lv2~3, 2~4문제)에 유형 노출 불가 | | 21주부터 AI 없는 1시간 주 2회를 알고리즘으로(12일×2 = 24 + 8 = 32문제), 유형별 대표 문제 목록 |
| 10 | 저장소 위생 — README·라이선스(`06:481-482`)만. git 히스토리 시크릿 제거, `.gitignore`, 커밋 메시지 정리, 개인정보 점검 없음(5-6은 현재 파일만 스캔 `05:645`) | | 6-2에 체크리스트 |

**B. 일정** — §4.2·§4.4. AI 없는 1시간 블록이 121·122·123·124·126·127·128일차에 없음(§2.6). ADR-0013·0014 결정일 없음(§2.3).

**C. DoD 측정성** — `06:413, 955` "착수 p99 Phase 5 수준 유지" → "≤ Phase 5 값×1.2"; `06:956` "복구 시간 기록" → "종료→재매칭 성공 ≤ 30초"; `06:225, 262, 295` 테스트 개수·자동 검증 미지정 → `06:792` "테스트 10개"를 §7.1 품질표로; `06:368` "택1 기능 동작" → 관전·채팅 확인 기준 없음; `06:436` "알람 3개 발동" → 하트비트 끊김 알람 쿼리(`absent()`/`up`) 정의; `06:469` "문서 6종 완성" → ADR "대안 2개 이상·기각 이유"; `06:730, 1006` "평균 4.0" → 모의 면접 프롬프트(`06:878-884`)에 README §5.4 척도가 **없음**; `06:714` "소리 없이 봐도 이해" → "자막 4구간 각 1문장"; `06:1033` "낮춘 기준+원인 분석"이 정식 통과인지 감점인지(`06:977`) 불명.

**D. 명세** — 6-1: 매칭 서버↔클라이언트 전송 방식(TCP 재사용? HTTP 롱폴?) 미지정(`06:218` "접속"만); "버전 일치"(`06:215, 944`)에 필요한 버전 임베드가 C++ 표(`06:802`)에만; Redis 1벌을 4프로세스+테스트가 공유 → README:216 키 prefix 규칙 재명시; 마일스톤 "21주 금 4종 기동"(`06:986`)은 §3.7 문제. 6-2: 위생, 블로그 플랫폼·공개 범위, 각 README의 AI 활용 표기. 6-3: 발표 자료에 "예상 질문 10개와 답" 없음. 6-4: 국내 게임사 자기소개서 정형 문항(지원 동기·성장 과정·협업/갈등·포부), 포트폴리오 링크 배치, 트랙별 키워드. 6-5: §5.6 A-9.

**E. AI 협업** — 강점: PM 리뷰(`06:851`), 시니어 리뷰에 수정 비용 요구(`06:867`), 확장성 "내 답 먼저"(`06:890`), 이력서 "삭제 권고 명시"(`06:898`), `AI-USAGE.md`(`06:915-932`). 추가: 모의 면접 프롬프트에 §5.4 척도·"정답 알려주지 말고 채점만"·"국내 신입 빈출(IOCP 워커 수·프레이밍·TIME_WAIT·데드락 조건·인덱스·격리 수준) 혼합"; Phase 6에 §6.3 "AI가 자주 틀리는 지점" 절 자체가 없음 → `GET`+`DEL` 비원자 티켓 검증(`06:232` 원칙 반대 코드), 서버 목록에 `KEYS`(블로킹), Pub/Sub을 "신뢰 전달"로 설명, `Start-Process dotnet run`(`06:189-193` 예시가 그렇다 — 자식 프로세스가 남아 `stop-all` 실패), 하트비트 TTL = 주기; ADR 리뷰 프롬프트·블로그 기술 검증 프롬프트.

**F. 면접 질문 추가(국내 게임사 신입 서버 빈출)** — 서버 쪽 TIME_WAIT 문제와 감소법; Nagle/`TCP_NODELAY` 설정 근거; IOCP/SAEA 워커 수 결정·완료 순서 보장; 프레이밍·부분 수신·바이트 오더; 데드락 4조건·캡스톤 락 순서(매칭 큐 vs 취소); 랭킹 Sorted Set 이유·B+tree 인덱스; 격리 수준과 상점 동시성 버그(블로그 소재 `06:504`); 서버 권위 — 어디까지 검증하나; 순단 재접속에 필요한 것; (행동) 실패 경험·왜 서버 개발자·리뷰 수용 사례; 게이트웨이를 두지 않은 이유.

**G. 막힘 포인트 추가** — `stop-all.ps1` 후 서버 생존/포트 점유(`dotnet run`의 부모-자식 — `dotnet <dll>` 또는 exe 직접, PID 파일); 4종 동시 기동 시 빌드 잠금(기동 전 1회 빌드); `GETDEL` unknown command(redis-windows < 6.2 — Lua 대체); 전원 차단·행 시 클라이언트가 끊김을 못 느낌(`Stop-Process`는 RST를 보냄 — 앱 ping 필요, 면접 포인트); 1,000접속에서 Redis 연결 폭증(멀티플렉서 1개); 같은 유저 대기열 2회(유저 상태 머신); 티켓 발급 후 미접속으로 룸 슬롯 누수(입장 타임아웃); Grafana에서 게임 서버 2대가 한 시계열(`server_id` 라벨); 매칭 서버 재기동 후 예전 티켓 유효(TTL·세대 번호); 데모 영상 도구(Game Bar/OBS); 시즌 전환 시 랭킹 소실(스냅샷 → 키 전환 순서).

**H. 선수 조건** — 5-1 도구의 매칭 흐름 확장 가능성; redis-windows ≥ 6.2(`GETDEL`); 로그 포맷에 traceId 자리(Phase 5 미실습); PC 자원(5프로세스+Redis+Prometheus/Grafana+클라이언트 2프로세스+1,000접속 → RAM 16GB·8코어 권장); 4-1 API 내부 호출 인증 수단; `LOAD-3-1.md`(`06:38`)가 Phase 3 산출물명과 일치하는지 확인.

**I. 트랙 균형** — 110h/110h. 실습 29개가 모두 "트랙 공통"(`06:18`)이라 C++ 지침 없음: C# 표는 `PriorityQueue`·`GETDEL`·`Channel`까지, C++ 표(`06:801-806`)는 "자체 TCP 또는 gRPC(vcpkg)"뿐 — gRPC/protobuf vcpkg 빌드가 반나절, 매칭 서버가 HTTP를 받으려면 서버 라이브러리 결정 필요. 참조 아키텍처: C#은 2D MMORPG 코드 전체(`06:831`), C++은 🟡 Boost.Asio 3개 장(`06:840`)뿐. 혼합 경로 학습자가 배치 워커·매칭 서버를 C#으로 써도 되는지 명시 없음(실무 흔한 조합 — 허용 명시 권장).

---

## 6. 교재 가이드(07)·템플릿(08)

### 6.1 07-books-guide

- 장 매핑 모순 §2.5, 읽기 예산 §4.5.
- 카드 경고 누락: 프로메테우스 카드(`07:320-324`) 알림 경로(Alertmanager Docker 전제 여부); Redis 카드(`07:314-318`) "Redis 6+"만 — 캡스톤 `GETDEL`은 6.2+; FPS 매칭 카드(`07:238-242`) 6장 분산 큐·4장 샤딩이 다중 Redis/컨테이너 전제일 가능성("로컬 1벌·서버 2대로 축소" 경고가 06:816에는 있고 카드에는 없음); 2D MMORPG 카드(`07:214-218`) C++ 트랙도 참조(`06:817`)하게 하면서 "코드 전부 C#·SuperSocketLite·MySQL(SqlKata)"과 SQLite 전환 시 `.sql` 방언 손질 경고 없음; 네트워크 이론 카드(`07:145`) Wireshark 없이 Nagle 비교를 관찰하는 방법 없음; `07:15` 코드 폴더 목록에 프로메테우스 책이 없는데 `07:322`는 "예제 계측"; 48,000줄 책(`07:234, 284`)을 AI 컨텍스트에 통째로 넣지 말라는 경고 없음("장 파일 단위로만").
- 매트릭스 "디자인 패턴 P5 △"(`07:102`) vs 05:824 정규 항목.
- §5 목록에 모드 기호(통독/참조/코드 분석) 부여, 일자 순 정렬.

### 6.2 08-templates

**누락 템플릿(Phase 문서가 요구하는 산출물 기준)**

| 요구 산출물 | 근거 | 08 현황 | 제안 |
|---|---|---|---|
| 측정 계획서(조건·반복·지표·통제) | `05:100, 555-557` | 없음 | T17 측정 계획서 + 메트릭 설계서 |
| 메트릭 설계서(이름/종류/라벨/목적/알람) + 장애→선행 지표 | `05:192, 584, 189` (5-C 채점 40점 `05:943`) | 없음 | 〃 |
| `PERF-REPORT.md` | `05:977-996`, `06:63` | T6 존재하나 **두 벌 불일치**: T6에 p95·전원/백그라운드 통제·가설 근거·"기각 1개 필수"·**프로파일 결과 절**·커밋 해시가 없음, T6에만 "클라이언트 머신" | T6를 05 §7.3의 상위집합으로 통합, 05는 "T6 사용" |
| 장애 주입 시나리오(기대 동작 선기술·재현·관찰) | `05:464, 1019-1024` | 없음 | T17에 절 추가 |
| 타임아웃 값·근거 표, 재시도 금지 목록, 부분 기능 저하 정책 | `05:383, 401, 416` | 없음 | T5 ADR로 대체 지시 |
| `RUNBOOK.md` | `05:500`, `06:64` | T7 존재하나 서비스 기준(§3.9) | 교체 |
| `SECURITY-CHECK.md`(6항목) | `05:1053-1062` | 없음 | T14 |
| `alerts.yml` 예시·대시보드 패널 설명표 | `05:62, 934, 602` | 없음 | 룰 1개 예시(`for:`) |
| 부하 도구 JSON 리포트 스키마 | `05:956` | 없음 | 5-1 명세에 |
| 캡스톤·저장소 표준 README | `06:59, 963, 717` (6-2 채점 40점 `06:1001`) | 없음(T12는 검증 프롬프트) | T15 |
| `ARCHITECTURE.md`(컴포넌트·시퀀스·상태·실행 구성도·상태 배치표·장애 영향표) | `06:60, 964, 162, 352` | 없음(T4는 단일 서버용) | T16 |
| `PROTOCOL.md`·`API.md` | `06:62, 966` | 없음(07:229 책 부록 B로 대체) | 최소 골격 |
| `AI-USAGE.md` | `06:911-932` 인라인 | 없음 | T13으로 이관 |
| 범위 표·마일스톤, 장애 영향 범위 표 | `06:112, 624, 352` | 없음 | T16에 |
| 테스트 계획(단위/통합/E2E 목록·스크립트) | `06:958, 384` | 없음 | T18 |
| 모의 면접 기록, 면접 준비 시트(트랙 30문항·CS), 이력서 프로젝트 서술("문제→선택→결과") | `06:519, 1006, 523, 795, 806, 554, 1014` | 없음 | T18 |
| 블로그 구조·발표 15장 구성·데모 대본 | `06:495, 508, 478` | 없음 | 각 10줄 골격 |

**기존 템플릿 수정** — T3/T3'(CLAUDE.md)를 Phase 5·6가 요구하지 않음(§2.6); T3에 traceId 로그 필드·시크릿 파일 기록 금지·포트/ID 인자화(`06:196`) 규칙 3줄. T4 DESIGN.md에 "관측 계획(지표·로그)" 절 없음(Phase 5 원칙 "관측은 설계다" `05:12`와 불일치); 매칭 서버 신규 설계(`06:213`)에 T4 요구 없음. T9에 5점 척도는 있으나 06:878은 빠짐 → "T9-b 60분 확장판(포트폴리오 20/CS 25/언어 15 + 척도 + '모른다고 한 질문 목록 출력')". T2 "AI 없이 재구현 (60분)"(`08:53`) → "(N분)". T1에 하루 실제 시간(h) 칸(README:213 "8시간 미달 → 회고에 미이행"의 근거)과 코딩 테스트 문제 수 칸. T8 "재현 스크립트 링크" 1줄. 06:870~875는 "T12 사용"으로 대체.

---

## 7. 실행 순서 제안 (작업 패키지)

| 패키지 | 내용 | 대상 | 예상 공수 |
|---|---|---|---|
| **A. 명세 오류 수정** | §3.1~3.10 (2-2 길이, 1-2 API, DropWrite, `Cache=Shared`/`salt`/해싱 패키지, 로그인 p99, ASan, 배치 워커·105일차, 알람 경로, T7, 07 카드 모순) | 01·02·03·04·05·06·07·08 | 1일 |
| **B. 총론·정합성 일괄** | §1.1·§2.3·§2.5·§2.6·§2.8 (README 표·구조·수치·교재 장, 5-3 결번, ADR 번호표, 포트표, CLAUDE.md DoD 4건, AI 없는 1시간 블록 삽입) + `working_log.md` + `.gitattributes` | README·전 문서 | 0.5~1일 |
| **C. 시간 회계·일정 재배치** | §4.1~4.4 (과제 시간 재산정, 미배치 실습 9개 배치, 과부하 일자 분산, 🟡 강등 목록, 버퍼 반일×2/Phase, 캡스톤 축소 3건, 혼합 경로 12주차 표) | 01~06 | 2일 |
| **D. 명세 구멍 보강** | §3.11~3.15 | 01~05 | 1일 |
| **E. 콘텐츠 보강** | §5 A(공백 실습 신설)·C(DoD 대체)·E(AI 협업)·F(질문)·G(막힘)·H(선수)·I(트랙) — Phase당 반일~1일 | 01~06 | 4~5일 |
| **F. 교재·템플릿** | §6 (07 예산표·모드 기호·카드 경고, 08 T13~T18 신설·T6/T7/T9/T2/T1 수정) | 07·08 | 1일 |
| **G. 영문본 동기화** | A~F 완료 후 §1.7 절차로 diff 단위 반영 + §1.5 용어 통일 + README 영문화(`README_kr.md` 보존) | 영문 8종 + README | 1~2일 |

A·B는 학습자에게 즉시 영향이 있고 서로 독립이므로 먼저, C는 B의 수치 결정(재구현 60/90/120분 등)에 의존, G는 마지막에 한 번.

---

## 부록 A. 검증 방법·확인 범위

- 구조 대조: 8쌍 모두 제목 계층 서명(md5)·h1~h4 수·코드 펜스 수·표 행 수·체크박스 수·구분선 수 일치. 펜스 홀수 없음. 표 열 수 경고 4건(02:1085, 05:984, 06:922·929)은 `\|` 이스케이프 또는 코드 펜스 안 리포트 스케치로 실제 결함 아님.
- 실습 ID: `^#### L…` 정의 vs 본문 참조 vs §2 구간 참조를 집합 비교. 일차별 실습 분 합산(양 트랙 표기 시 한 트랙 기준으로 재확인).
- 번호: `과제 N-M`, `adr/NNNN`, `T#` 토큰 추출·비교. `총론 §x.y` 7건 전부 README 실제 절 존재.
- 교재: README §4의 33개 책 키워드가 07·각 Phase 문서에 출현하는지 집계, 장 번호는 §5 표 원문 대조.
- 정책 용어: 20개 키워드 grep, 출현 문맥 전수 확인.
- 표본 대조 14건(본문 서두) 전부 원문 일치. 그 외 전문 검토 항목은 검토자가 줄 번호를 붙였으며, 수정 시 해당 줄 재확인 권장.

## 부록 B. 문제 없음으로 확인된 항목

- 시간·주차·일차 연속성(1~130), 금요일 점검 양식, Phase 헤더 시간.
- 실습 ID 정의↔참조 1:1(미배치 9개 제외), 참조되지 않은 정의 없음.
- 템플릿 T3' 존재. `_kr` 링크 8종 형식 동일·대상 존재.
- 정책 용어 위반 0건. README §4의 모든 책에 07 카드 존재.
- 영문본 한글 잔존은 07의 외부 저장소 경로 34줄뿐(정상). 07·08 영문본은 원문과 줄 단위 1:1, 숫자·ID·URL 보존.
- 최종 판정 수치(저장소 5개·학습 노트 100일·주간 회고 24개)는 README §5.5와 06 §8이 일치.
